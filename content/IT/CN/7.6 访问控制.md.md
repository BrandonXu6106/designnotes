---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=105"
bvid: "BV18g411D7Fm"
cid: "817210763"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817210763&page=105&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:09` 本节课我们介绍访问控制的相关内容
`00:14` 首先介绍访问控制的基本概念
`00:17` 如图所示
`00:19` 用户要访问某信息系统
`00:22` 通常需要经过身份鉴别
`00:24` 这是第一道安全防线
`00:27` 可将未授权用户隔离在信息系统之外
`00:32` 当用户经过身份鉴别
`00:34` 通过第一道安全防线进入系统后
`00:38` 并不应该让用户毫无限制地访问系统中的任意资源
`00:43` 而应让用户访问授权范围内的资源
`00:47` 这就是访问控制
`00:50` 访问控制可被看作信息系统的第二道安全防线
`00:54` 对进入系统的合法用户进行访问权限
`00:58` 控制对合法用户访问权限的授予一般遵循最小特权原则
`01:07` 最小特权是指能够满足用户完成工作所需的权限
`01:13` 用户不会被赋予超出其实际需求的权限
`01:18` 最小特权原则可以有效防范合法用户滥用权限所带来的安全风险
`01:27` 访问控制包含以下基本要素
`01:30` 主体客体访问访问策略
`01:40` 主体是访问活动的发起者
`01:44` 可以是某个用户
`01:46` 也可以是代表用户执行操作的进程
`01:49` 服务和设备等课题
`01:53` 是访问活动中被访问的对象
`01:57` 凡是可以被操作的信息
`01:59` 文件
`02:00` 设备服务等资源
`02:02` 都可以认为是客体
`02:05` 访问是指主体对客体的各种操作类型
`02:10` 例如创建读取修改删除执行发送接收等操作
`02:19` 不同的系统有不同的访问类型
`02:24` 访问策略是访问控制的核心
`02:27` 访问控制根据访问策略限制主体对客体的访问
`02:33` 访问控制策略可以用三元组s o p来描述
`02:38` 其中s表示主体
`02:40` o表示客体
`02:42` p表示许可许可p明确了允许主体对客体o所进行的访问类型
`02:52` 访问策略通常存储在系统的授权服务器中
`02:57` 如图所示
`02:58` 这是著名的访问监控器模型
`03:02` 当系统中出现访问请求时
`03:06` 访问监控器对访问请求进行裁决
`03:10` 他向授权服务器进行查询
`03:14` 根据授权服务器中存储的访问策略
`03:17` 决定主体对客体的访问是否被允许
`03:22` 在访问监控器模型中有一个负责审计的模块
`03:27` 它是访问控制的必要补充
`03:31` 审计模块会记录与访问有关的各类信息
`03:36` 包括主体客体访问类型
`03:40` 访问时间以及访问是否被允许等信息
`03:46` 系统管理员通过查看审计记录
`03:49` 就可以详细了解系统中访问活动的具体情况
`03:55` 主要包括以下三方面的情况
`03:59` 哪些主体对哪些资源的访问请求被拒绝
`04:04` 主体发出大量违规的访问请求
`04:07` 这往往是攻击和破坏活动的征兆
`04:11` 需要引起特别的关注
`04:15` 访问策略是否得到了严格执行
`04:19` 如果访问规则在配置或执行过程中存在失误
`04:24` 一些违反访问策略的访问请求可能被许可
`04:30` 通过查看审计记录
`04:31` 可以发现此类情况
`04:33` 以便及时修正
`04:38` 提供访问活动的证据
`04:40` 为事后追查和追责提供依据
`04:46` 接下来我们介绍三类典型的访问控制策略
`04:51` 分别是自主访问控制策略
`04:56` 强制访问控制策略
`04:58` 基于角色的访问控制策略
`05:02` 首先来看自主访问控制策略
`05:07` 自主访问控制策略中自主的意思是
`05:10` 客体的拥有者可以自主地决定
`05:13` 其他主体对其拥有的客体所进行访问的权限
`05:19` 这种访问控制策略的优点是具有很强的灵活性
`05:24` 而缺点是权限管理过于分散
`05:28` 容易出现漏洞
`05:30` 并且无法有效地控制被攻击主体
`05:33` 破坏系统安全性的行为
`05:38` 木马程序利用自主访问控制策略的上述缺点
`05:42` 可以很容易地破坏系统的安全性
`05:46` 例如用户a对文件f一具有足权限
`05:52` 攻击者必为了非法获取该文件
`05:56` 编写了一个木马程序
`05:58` 并诱骗用户a运行了该木马程序
`06:03` 木马程序获得用户a的访问权限
`06:06` 能够读取文件f一的内容
`06:09` 并将这些内容写入到新创建的文件f2 中
`06:14` 然后用户a将文件f2 的读取权限授予攻击者b
`06:20` 这样攻击者b就可以非法读取到文件f一的内容了
`06:28` 自主访问控制策略的最大特点是自主
`06:31` 也就是资源的拥有者
`06:33` 对资源的访问策略具有决策权
`06:37` 因此是一种限制比较弱的访问控制策略
`06:42` 这种访问控制策略给用户带来灵活性的同时也带来了安全隐患
`06:49` 再来看强制访问控制策略
`06:53` 强制访问控制策略与自主访问控制策略不同
`06:58` 它不允许一般的主体进行访问权限的设置
`07:03` 在强制访问控制策略中
`07:05` 主体和客体被赋予一定的安全级别
`07:10` 通常只有系统的安全管理员可以进行安全级别的设置
`07:16` 而普通用户不能改变自己或任何客体的安全级别
`07:23` 系
`07:23` 统通过比较主体和客体的安全级别来决定某个主体是否能够访问
`07:30` 某个客体
`07:32` 在强制访问控制策略中广泛使用以下两项原则
`07:38` 分别是下毒原则
`07:43` 上写原则
`07:46` 对于下毒原则
`07:47` 主体的安全级别必须高于或等于被读客体的安全级别
`07:54` 主体读取客体的访问活动才能被允许
`07:59` 对于上写原则
`08:01` 主体的安全级别必须低于或等于被写课题的安全级别
`08:07` 主题写课题的访问活动才能被允许
`08:12` 下毒和上写原则
`08:14` 限制了信息只能由低级别的对象流向高级别或同级别的对象
`08:21` 这样能够有效防止木马等恶意程序的窃密攻击
`08:27` 我们来举例说明
`08:30` 如图所示
`08:32` 用户a的安全级别高于文件f1
`08:37` 而用户b的安全级别低于文件f1
`08:41` 因此根据下毒原则
`08:44` 用户a可以读取文件f1
`08:48` 而用户b却不能读取文件f1
`08:52` 即使用户a运行了用户b编写的木马程序
`08:57` 但由于该木马程序具有与用户a同样的安全级别
`09:03` 虽然木马程序可以读取文件f1
`09:07` 却不能将文件f一的安全级别修改成用户b可读的安全级别
`09:14` 这是遵循了圣血原则
`09:17` 也无法将其内容写入到安全级别比用户d的文件中
`09:23` 这同样是遵循了上写原则
`09:27` 因此用户b无法读取到文件f一的内容
`09:34` 再来看基于角色的访问控制策略
`09:38` 基于角色的访问控制策略的核心思想是根据安全策略划分不同的角色
`09:46` 用户不再直接与徐策关联
`09:50` 而是通过角色与许可关联
`09:54` 如图所示
`09:56` 系统中可以有多个用户
`09:59` 多个角色
`10:00` 多个许可
`10:02` 一个用户可以拥有多个角色
`10:05` 一个角色也可以赋予多个用户
`10:10` 一个角色可以拥有多种许可
`10:13` 一种许可也可以分配给多个角色
`10:18` 许可指明了对某课题可以进行的访问类型
`10:23` 例如读写删除等
`10:29` 角色与许可之间的关系比角色与用户的关系要更加稳定
`10:37` 当一个用户的职责发生变化
`10:40` 或需要为一个新的用户授权时
`10:43` 只要修改或设置用户的角色即可
`10:48` 很显然
`10:49` 将用户和访问权限通过角色进行逻辑隔离
`10:54` 能够减少授权的复杂性
`10:57` 增强权限的可管理性
`11:01` 减少因授权失误导致安全漏洞的风险
`11:06` 本节课到这里就结束了
`11:09` 请同学们快速回忆一下本节课的主要内容
`11:39` 同学们
`11:40` 我们下节课再见