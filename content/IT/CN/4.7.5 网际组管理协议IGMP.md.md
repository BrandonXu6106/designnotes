---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=69"
bvid: "BV18g411D7Fm"
cid: "817197359"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817197359&page=69&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍网计组管理协议i g m p的相关知识
`00:16` 首先介绍往期组管理协议i g m p的三种报文类型
`00:22` 网计组管理协议i gmp
`00:24` 目前的最新版本是2002年10月公布的i g m p v3
`00:31` i g m p有三种报文类型
`00:34` 分别是成员报告报文
`00:38` 成员查询报文
`00:41` 离开祖报文
`00:44` i gmp报文被封装在ip数据报中传送
`00:49` 如图所示
`00:50` 这是i gmp报文tcp ip的网际层会为其添加一个ip首部
`00:58` 使之成为ip数据报
`01:01` ip首部中的协议字段的值为二
`01:04` 表示其数据载荷部分是i gmp报文目的地址字段的值
`01:11` 根据其所封装i gmp报文类型各有不同
`01:15` 但都属于ip多播地址
`01:18` 换句话说
`01:20` 封装有ig mp报文的ip数据报都是ip多播数据报
`01:26` 生存时间gdl字段的值为一
`01:30` 这样就可避免封装有gmp报文的ip多播数据报被路由器转发到其他网络
`01:39` 接下来我们举例说明网计组管理协议i g m t的基本工作
`01:45` 原理首先来看加入多播组的情况
`01:50` 如图所示
`01:52` 以太网中由主机abc和d共四台主机
`01:58` 还有一台多播路由器r1 r一被设置为接收所有的ip多播数据报
`02:06` 主机c属于多模组
`02:08` 28.1.1.1
`02:12` 在r一的多播组列表中
`02:14` 已经记录有该多播组主基地
`02:18` 不属于任何多播组
`02:21` 主机a和b都希望加入多播组
`02:24` 226点点9.26
`02:28` 假设主机b首先向以太网发送ig mp成员报告报文
`02:39` 而ip多播数据报被封装在以太网多波真中
`02:44` ig mp成员报告报文的内容主要是所希望加入的多播组的ip多播地址
`02:53` ip多播数据报的目的地址也是所希望加入的多播组的ip多播地址
`03:00` 以太网多波真的目的地址是由所希望加入的多播组的ip多播地址
`03:07` 映射而来的
`03:08` 多波mark地址
`03:10` 以太网中的主机a c和d以及多波路由器r一都会收到该多不真
`03:18` 由于d不属于任何多不足
`03:21` 因此直接将收到的多波真丢弃
`03:26` c发现该多波真的目的地址与自己所在多播组的多波mac地址不相同
`03:33` 因此在麦克层丢弃该多波真
`03:37` a
`03:38` 发现该多波真的目的地址与自己希望加入的ip
`03:42` 多播组的多波mac地址相同
`03:45` 因此在mark层接受该多不真
`03:49` 并将其所封装的ip多播数据报交付上层的网基层处理
`03:56` 往期曾发现该多波数据报的目的地址
`03:59` 与自己所希望加入的多播组的ip多播地址是相同的
`04:05` 因此接受该多播数据报
`04:08` 并将其所封装的i g m p成员报告报文交付i g m p进行解析
`04:16` 这样a就知道这是来自同一ip多播组的另一个成员的
`04:22` ig mp成员报告报文
`04:25` 于是取消自己准备发送的i gmp成员报告报文
`04:32` 多播路由器r一收到该多波真号
`04:35` 提取出其中的i g m p成员报告报文
`04:40` 解析该报文后
`04:42` 就知道了自己所直连网络中有一个新的ip多播组
`04:47` 二六点点9.26
`04:50` 因此将ip多播组地址226点点9.26
`04:55` 添加到自己的多波组列表中
`04:59` 再来看监视多播组成员变化的情况
`05:04` 多播路由器默认每隔125秒
`05:07` 就向其直连网络发送一个i g m p成员查询报文
`05:13` 该报文被封装在ip多播数据暴增
`05:17` 而ip多播数据报被封装在以太网多波真中
`05:23` i g m p成员查询报文中的内容
`05:26` 主要是要查询的特定多播组的ip多播地址
`05:31` 我们以ip多播组二六点点9.26为例
`05:36` i g m p成员查询报文中的内容
`05:39` 也可以是ip地址
`05:41` 零点点零点
`05:43` 这表示全部读博组ip多播数据报的目的地址为24点点0.1
`05:51` 这是一个特殊的ip多播地址
`05:55` 在本网络中的所有参加多播的主机和路由器的网际层
`06:00` 都会接受该多播数据报
`06:04` 以太网多波真的目的地址是由ip多播数据报的目的地址
`06:09` 24点点0.1应试而来的多播mark地址
`06:15` 以太网中的主机a b c和d都会收到该多波真
`06:21` 由于地不属于任何多播组
`06:24` 因此直接将收到的多波真丢弃
`06:28` 主机a b和c发现该多波真的目的地址是01005e000001
`06:38` 其可能对应的ip多播地址为24点点0.1
`06:42` 这个特殊的ip多播地址
`06:45` 于是在马克曾接受该多不真
`06:48` 在网际层接受该多波真所封装的ip多播数据报
`06:54` 并将ip多播数据报所封装的ig mp成员查询报文交付i g m p进行解析
`07:03` 这样主机b就知道这是来自多播路由器的i g m p成员查询报文
`07:10` 由于查询的多播组与自己所属的多播组是同一个多播组
`07:16` 于是决定两秒后进行响应
`07:21` 同理主机a就知道这是来自多播路由器i g m p成员查询报文
`07:28` 由于查询的多播组与自己所属的多播组是同一个多播组
`07:33` 于是决定八秒后进行响应
`07:37` 而主机c知道这是来自多播路由器的i g m p成员查询报文
`07:44` 但是查询的多播组与自己所属的多播组不是同一个多播组
`07:50` 因此不响应
`07:52` 需要说明的是
`07:54` 收到gm p成员查询报文的被查询多播组的任何成员
`07:59` 将会发送ig mp成员报告报文作为应答
`08:05` 为了减少不必要的重复应答
`08:08` 每个多国组只需要有一个成员应答就可以了
`08:12` 因此采用了一种延迟响应的策略
`08:16` 那就是收到i g m p成员查询报
`08:20` 我的主机并不是立即响应
`08:23` 而是在一到十秒的范围内等待一段随机的时间后再进行响应
`08:30` 如果在这段随机的时间内
`08:32` 收到了同组其他成员发送的i g m p成员报告报文
`08:38` 就取消响应
`08:40` 在本例中
`08:41` 主机b决定在两秒后进行响应
`08:45` 而主机a决定在八秒后进行响应
`08:49` 大约两秒钟后
`08:51` 主机b发送i g m p成员报告报文
`08:55` 这是该报文的封装细节
`08:58` 我们在之前介绍加入多波组的情况时已经介绍过了
`09:02` 这里就不再赘述了
`09:05` 以太网中的主机a c和d以及多波路由器r一都会收到
`09:18` 该多不真
`09:19` 究其原因
`09:20` 之前已经介绍过了
`09:22` 这里就不再赘述了
`09:25` 主机a的马克层接受该多波真网际层
`09:29` 接受该多波帧所封装的ip多播数据报
`09:33` 并将ip多播数据报所封装的ig mp成员报告报文交付i g m p进行解析
`09:42` 这样主机a就知道这是来自同一ip多播组的另一个成员的
`09:48` i g m p成员报告报文
`09:51` 于是取消对多波路由器r一的响应
`09:55` 多播路由器r一收到该多波真后
`09:59` 提取出其中的i g m p成员报告报文
`10:03` 解析该报文后
`10:05` 就知道了自己所直连网络中还有ip多播组二六点点9.26的成员
`10:13` 因此在自己的多播组列表中更新对该多播组的计时
`10:19` 假设主机c不再是多播组28.1.1.1的成员
`10:25` 多播路由器r1
`10:27` 默认每隔125秒就向其直连网络
`10:31` 发送一个i g m p成员查询报文
`10:34` 这是该报文的封装细节就不再赘述了
`10:40` 以太网中的主机a b c和d都会收到封装该报文的萝卜真
`10:48` 主机c和d的mark层丢弃
`10:50` 该多不真
`10:52` 而主机a和b的mark层接受该多波真网际层
`10:57` 接受该多波帧所封装的ip多播数据报
`11:01` 并将ip多播数据报所封装的i g m p成员查询报文
`11:06` 交付i g m p进行解析
`11:10` 根据g mp成员查询报文的内容是ip多播组28.1.1.1
`11:17` 就知道这不是查询a和b所在的多模组
`11:21` 因此不响应多播路由器r一由于长时间收不到该多播组成员的响应
`11:29` 则将其从多播组列表中删除
`11:34` 需要说明的是
`11:35` 同一网络中的多播路由器可能不止一个
`11:40` 但没有必要
`11:41` 每个多播路由器都周期性的发送i g m p成员查询报文
`11:47` 只要在这些多播路由器中选择一个作为查询路由器
`11:52` 由查询路由器发送i g m p成员查询报文
`11:56` 而其他的多播路由器仅被动接收响应
`12:01` 并更新自己的多播组列表即可选择查询路由器的方法很简单
`12:08` 每个多播路由器若监听到原ip地址比自己的ip地址小的i g m p成员
`12:15` 查询报文
`12:19` 最后网络中只有ip地址最小的多播路由器成为查询路由器
`12:26` 再来看退出多播组的情况
`12:30` i g m p v2 在ig mp v一的基础上增加了一个可选项
`12:36` 当主机要退出某个多波组织和主动发送一个离开组报文
`12:42` 而不必等待多播路由器的查询
`12:45` 这样可使多波路由器能够更快地发现某个组有成员离开
`12:52` 我们来举例说明
`12:54` 如图所示
`12:57` 假设主机c要退出该多播组
`13:00` 可以主动发送一个i g m t离开主报文
`13:05` 而不必等待多播路由器r一的查询
`13:10` 这是gmp离开主报文的封装细节
`13:14` 离开组报文的内容
`13:20` 封装离开主报文的ip多播数据报的目的地址为24点点0.2
`13:27` 这是一个特殊的ip多播地址
`13:30` 在本网络中的所有多播路由器的网际层都会接受该多波数据报
`13:38` 以太网中的主机ab和d以及多播路由器r一都会收到封装有i g m p
`13:54` 接受该多不真
`13:56` 但根据其所封装的ip多播数据报的目的地址是二四点点0.2
`14:03` 这个特殊的ip多播地址
`14:06` 于是在网际层将其丢弃
`14:09` 多播路由器r一收到该多波真后
`14:13` 就知道有成员离开了该多播组
`14:16` 但暂时不删除该多博主
`14:19` 而是立即发送针对该多播组的特殊的i g m p成员查询报文
`14:26` 探测该多播组是否还有其他成员封装这个特殊的i g m p成员
`14:34` 查询报文的ip多播数据
`14:36` 报的目的地址为该多播组的地址28.1.1.1
`14:42` 而不是特殊的多播地址
`14:44` 224点点0.1r一发送这个特殊的i g m p成员查询报文
`14:53` 此后的过程之前已经介绍过了
`14:57` 这里就不再赘述了
`15:00` 本节课到这里就结束了
`15:03` 请同学们快速回忆一下本节课的主要内容
`15:14` 同学们
`15:15` 我们下节课再见