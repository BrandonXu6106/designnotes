---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=88"
bvid: "BV18g411D7Fm"
cid: "817206175"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817206175&page=88&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍tcp超时重传时间的选择
`00:15` 超时重传时间的选择是tcp最复杂的问题之一
`00:19` 我们来举例说明
`00:22` 假设主机a和b是英特网上的两台主机
`00:26` 它们之间已经建立了tcp连接
`00:29` 纵坐标为时间
`00:31` 现在主机a给主机b发送tcp数据报文段零
`00:36` 并记录下当前的时间
`00:39` 主机b收到后给主机a发送相应的确认报文段
`00:44` 主机a收到确认报文段后
`00:47` 记录下当前的时间
`00:49` 那么主机a记录下的这两个时间
`00:51` 它们的差值就是报文段的往返时间
`00:55` rtt
`00:56` 由于这是第零个报文段的rtt
`00:59` 我们就用rtt 0来表示
`01:02` 试想一下
`01:03` 如果我们将超时重传时间rt的值设置的比r tt 0的值小
`01:09` 会出现怎样的情况呢
`01:12` 很显然这会引起报文段不必要的重传时
`01:17` 网络负荷增大
`01:19` 那么如果将超时重传时间rt的值设置的远大于rtt 0的值呢
`01:26` 又会出现怎样的情况呢
`01:29` 很显然
`01:30` 这会使重传推迟的时间太长
`01:33` 使网络的空闲时间增大
`01:36` 降低了传输效率
`01:38` 综合上述两种情况
`01:40` 我们可以得出这样的结论
`01:43` 超时重传时间rt的值应该设置为略大于报文段往返时间rtt的值
`01:51` 至此同学们可能会觉得超时重传时间的选择也并不是很复杂嘛
`01:59` 然而tcp下层是复杂的互联网环境
`02:04` 主机a所发送的报文段可能只经过一个高速率的局域网
`02:09` 也有可能经过多个低速率的网络
`02:12` 并且每个ip数据报的转发路由还可能不同
`02:17` 例如现在主机a给主机b发送tcp数据报文段
`02:22` 一
`02:23` 主机b收到后给主机a发送相应的确认报文段
`02:29` 主机a这次测得的报文段往返时间rtt 1
`02:33` 如图所示
`02:35` 显然rtt一远大于rtt 0
`02:39` 如果超时重传时间
`02:41` rt o还是我们之前所确定的
`02:44` 略大于rtt 0的话
`02:46` 这对于数据报文段一是不合适的
`02:49` 会造成该报文段不必要的重传
`02:53` 这样看来
`02:54` 超时重传时间的选择确实不那么简单了
`02:59` 我们不能直接使用某次测量得到的rtt样本来计算超时重装时间
`03:05` rt但是我们可以利用每次测量得到的rtt样本计算加权平均往返时间
`03:13` rtt
`03:15` 这样可以得到比较平滑的往返时间
`03:19` 当测量到第一个rtt样本时
`03:22` rtt的值直接取为第一个rtt样本的值以后
`03:28` 每测量到一个rtt样本时
`03:31` 都按该公式来计算新的rtt值
`03:36` 在上市中
`03:37` 阿尔法的取值大于等于零且小于一
`03:41` 若阿尔法很接近于零
`03:44` 则新rtt样本对rtt的影响不大
`03:48` 若阿尔法很接近于一
`03:50` 则新rtt样本对rtt的影响较大
`03:55` 已成为建议标准的rfc 6298推荐的阿尔法值为1/8
`04:01` 即0.125
`04:03` 用这种方法得出的加权平均往返时间
`04:06` rtt的值就比测量出的rtt的值更加平滑
`04:12` 显然超时重传时间rt的值应略大于加权平均往返时间rt t s的值
`04:21` 下面我们给出fc 6298建议使用的超时重传时间rt的计算公式
`04:30` 该公式中的rtt是加权平均往返时间
`04:34` 我们刚刚介绍过它的计算方法
`04:38` rttd是rtt偏差的加权平均
`04:42` 计算方法如下
`04:44` 当测量到第一个rtt样本时
`04:47` rttd的值取为该样本值的一半以后
`04:53` 每测量到一个rtt样本时
`04:55` 都按该公式来计算新的rttd的值
`04:59` 在上市中
`05:01` 贝塔的取值大于等于零
`05:03` 且小于一
`05:04` 已成为建议标准的rfc 6298推荐的贝塔值为1/4
`05:10` 即0.25
`05:12` 我们可以发现
`05:13` 不管是rtt还是rttd
`05:17` 都是基于所测量到的rtt样本进行计算的
`05:21` 如果所测量到的rtt样本不正确
`05:25` 那么所计算出的rtt和rt t d自然就不正确
`05:30` 进而所计算出的超时重传时间rt也就不正确
`05:36` 然而往返时间rtt的测量确实是比较复杂的
`05:41` 我们来举例说明
`05:43` 主机a给主机b发送tcp数据报文段
`05:47` 但该报文段在传输过程中丢失了
`05:51` 当超时重传计时器超时后
`05:54` 主机a就重传该报文段
`05:57` 主机b收到后给主机a发送确认报文段
`06:02` 现在问题来了
`06:04` 主机a收到该确认报文段后
`06:07` 无法判断该报文段是对原报文段的确认还是对重传报文段的确认
`06:14` 该报文段实际上是对重传报文段的确认
`06:18` 也就是说正确的rtt应该是这一段时间
`06:23` 但是如果主机a误将该确认当做是对原报文段的确认
`06:29` 也就是误认为这段时间是rtt
`06:33` 则所计算出的rtt和rt就会偏大
`06:38` 降低了传输效率
`06:41` 再来看另一种情况
`06:44` 主机a给主机b发送tcp数据报文段
`06:47` 主机b收到后给主机a发送确认报文段
`06:52` 由于某种原因
`06:54` 该确认报文段没有在正常时间内到达主机a
`06:58` 这必然会导致主机a对之前所发送的数据报文段的超时重传
`07:05` 现在问题又来了
`07:07` 主机a收到迟到的确认报文段后
`07:10` 无法判断该报文段是对原报文段的确认还是对重传报文段的确认
`07:17` 该报文段实际上是对原报文段的确认
`07:21` 也就是说正确的rtt应该是这一段时间
`07:27` 但是如果主机a误将该确认当作是对重传报文段的确认
`07:33` 也就是误认为这段时间是rtt
`07:38` 则所计算出的rtt和rt就会偏小
`07:43` 这会导致报文段没有必要的重传
`07:46` 增大网络负荷
`07:49` 通过这两个例子可以看出
`07:51` 当发送方出现超时重传后
`07:54` 收到确认报文段时
`07:56` 是无法判断出该确认到底是对原报文段的确认
`08:01` 还是对重传报文段的确认
`08:04` 也就是无法准确测量出rtt
`08:07` 进而无法正确计算超时重传时间
`08:11` rt因此针对出现超时重传时无法测准往返时间rtt的问题
`08:19` 凯恩提出了一个算法
`08:21` 在计算加权平均往返时间rtt s时
`08:26` 只要报文段重传了
`08:28` 就不采用其往返时间
`08:30` rtt样本也就是出现重传时不重新计算rtt
`08:37` 进而超时重传时间
`08:38` rt也不会重新计算
`08:42` 然而这又引起了新的问题
`08:45` 设想出现这样的情况
`08:48` 报文段的食盐突然增大了很多
`08:51` 并且之后很长一段时间内都会保持这种食盐
`08:56` 因此在原来得出的重传时间内不会收到确认报文段
`09:02` 于是就重传报文段
`09:05` 根据盘算法不考虑重传的报文段的往返时间样本
`09:10` 这样超时重传时间就无法更新
`09:15` 这会导致报文段反复被重传
`09:22` 方法是报文段每重传一次
`09:26` 就把超时重传时间rt增大一些
`09:30` 典型的做法是将新rt的值取为就rt值的两倍
`09:37` 接下来我们举例说明tcp超时重传时间的计算
`09:43` 这是测量得到的第一个rtt样本
`09:46` rtt 1
`09:48` 根据rt t s一的计算公式可知rtt s一的值
`09:53` 根据rt t d e的计算公式可知rtt d e的值
`09:58` 再根据rt的计算公式可计算出rt一的值
`10:04` 这是测量得到的第二个rtt样本
`10:07` rtt 2
`10:08` 根据rtt的计算公式和阿尔法的值和写出计算rtt s2 的表达式
`10:16` 将之前计算出的rtt s一的值和本次测量得到的rtt 2的值代入该市
`10:24` 可计算出rtt s2 的值
`10:28` 根据rtt d的计算公式和贝塔的值可写出计算rtt第二的表达式
`10:36` 将之前计算出的rtt第一rtt s一以及本次测量得到的rtt 2的值
`10:44` 代入干式可计算出rtt第二的值
`10:49` 再根据rt o的计算公式可计算出rt o2 的值
`10:55` 假设这是测量得到的第三个和第四个rtt样本
`11:00` 现在请同学们暂停播放视频
`11:04` 计算一下rt 3和t4 的值分别是多少
`11:12` 答案如图所示
`11:14` 相信大家都能正确解答
`11:18` 假设这是测量得到的第五个rtt样本
`11:22` 但是根据rt 4的值可知
`11:25` 在收到确认之前就会产生超时重传
`11:30` 我们之前介绍过
`11:32` 若出现超时重传
`11:34` 则不采用上述公式计算rt而是将新rt的值取为旧rt值的两倍
`11:43` 因此rt o5 的值取为两倍的rt o4 的值
`11:49` 本节课到这里就结束了
`11:51` 我们将本节课的内容小结如下
`11:58` 同学们
`11:59` 我们下节课再见