---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=103"
bvid: "BV18g411D7Fm"
cid: "817210175"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817210175&page=103&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍实体鉴别的相关内容
`00:17` 实体鉴别就是通信双方的一方验证另一方身份的技术
`00:22` 常简称为鉴别实体
`00:26` 可以是人客户进程或服务器进程
`00:30` 下面我们仅介绍如何鉴别通信对端实体的身份
`00:35` 也就是验证通信的对方确实是索要通信的实体
`00:40` 而不是其他伪装者
`00:44` 通信实体的鉴别通常是两个通信实体之间传输实际数据之前
`00:50` 或进行访问控制之前的必要过程
`00:53` 这是很多安全协议的重要组成部分
`00:58` 实体鉴别的最简单方法就是使用用户名和口令
`01:04` 如图所示
`01:06` 用户a将自己的用户名和口令发送给用户b
`01:11` 以便b对a进行实体鉴别
`01:16` 直接在网络中传输用户名和口令
`01:20` 面临着被攻击者截获的安全威胁
`01:24` 为了应对用户名和口令被攻击者截获的安全威胁
`01:29` 需要对用户名和口令进行加密
`01:34` 如图所示
`01:35` 用户a向b发送包含有自己用户名和口令的报文
`01:41` 并且使用双方共享的对称密钥k a b进行加密
`01:47` 然而上述这种简单的实体鉴别方法存在明显的漏洞
`01:53` 例如攻击者c可以从网络中截获a发送给b的加密报文
`02:01` c并不需要对该报文进行解密
`02:04` 而是以后把该报文发送给b使b误认为c就是a之后
`02:12` b就像伪装成a的c发送许多本来应当发送给a的报文
`02:18` 这就是重放攻击
`02:22` 为了应对重放攻击
`02:24` 可以使用不重数
`02:27` 所谓不重数就是指一个不重复使用的大随机数
`02:33` 在实体鉴别过程中
`02:36` 不重数可以使用户把重复的实体鉴别请求和新的实体鉴别请求区分开
`02:44` 我们来举例说明
`02:47` 如图所示
`02:49` 纵坐标为时间
`02:52` 用户a首先挑选了一个随机的不重数ra
`02:57` 然后将自己的用户名和不重数ra封装在一个报文中发送给用户
`03:04` b b收到后也挑选了一个随机的不重数
`03:09` 而b然后用a和b共享的密钥kb加密a发来的不成数
`03:16` 而a并将自己选择的不重数
`03:19` rb和加密后的不重数ra封装在一个报文中发送给a作为响应
`03:27` 由于kab是a和b共享的密钥
`03:31` 因此这里使用的是对称密钥密码体制
`03:36` a收到后使用共享密钥k ab解密出不重数ra
`03:42` 这样a就验证了b的身份
`03:46` a是用共享密钥k ab加密b发过来的不重数
`03:50` 而b然后将其封装在一个报文中发送给b
`03:57` b收到后是用共享密钥kb解密出不重数b这样b就验证了a的身份
`04:07` 在双方互相验证了对方的身份后
`04:11` 他们就可以进行本次绘画的数据传输了
`04:15` 由于每次绘画必须使用不同的补充数
`04:20` 所以攻击者在进行重放攻击时
`04:23` 无法重复使用所截获的不重数
`04:27` 因而也就无法伪装成a或b
`04:32` 本例使用的是不重数和对称密钥密码体制来实现实体鉴别
`04:40` 实际上使用公钥密码体制和不重数也可以实现实体鉴别
`04:47` 我们来举例说明
`04:50` 如图所示
`04:52` 纵坐标为时间
`04:55` 用户a首先挑选了一个随机的不重数ra
`05:00` 然后将自己的用户名和不重数ra封装在一个报文中发送给用户
`05:06` b b收到后也挑选了一个随机的不重数
`05:12` 而b然后用b自己的私钥s k b对不重数ra进行签名
`05:20` 并将自己选择的不重数rb和签名后的不重数ra封装在一个报文中
`05:26` 发送给a作为响应
`05:30` 由于使用b的私钥s k b对不重数ra进行签名
`05:36` 因此这里使用的是公钥密码体制
`05:40` a收到后使用b的公钥pcb核实出不重数ra
`05:47` 这样a就验证了b的身份
`05:51` a是用自己的私钥s k a签名b发来的不重数
`05:56` 而b然后将其封装在一个报文中发送给b
`06:02` b收到后是用a的公钥p k a核实出不重数
`06:07` 而b这样b就验证了a的身份
`06:12` 在双方互相验证了对方的身份后
`06:16` 他们就可以进行本次绘画的数据传输了
`06:21` 使用公钥密码体制
`06:23` 在实现实体鉴别时仍有受到攻击的可能
`06:28` 我们来举例说明
`06:30` 如图所示
`06:32` 纵坐标为时间
`06:35` 用户a和b之间的通信需要经过c的转发
`06:39` 但实际上c是一个攻击者
`06:43` c伪装成a给b发送
`06:46` 我是a的报文
`06:48` b选择了一个不重数
`06:50` 而b发送给a但被c截获并丢弃了
`06:56` c用自己的私钥sk c冒充是a的私钥
`07:00` 对rb加密并发送给b
`07:05` b向a发送报文
`07:07` 要求a把解密用的公钥发过来
`07:11` 但该报文也被c截获并丢弃了
`07:16` c把自己的公钥pk c冒充是a的公钥发送给b
`07:22` 必有收到的pk c对之前收到的被加密的不重数
`07:27` 而b进行解密
`07:29` 很显然结果是正确的
`07:32` 于是b相信通信的对方是a
`07:37` 接着就像a发售是要pk c加密的许多敏感信息
`07:42` 但都被c截获了
`07:45` c是用自己的私钥sk c即可解密出这些敏感信息
`07:52` 上述这种欺骗手段的欺骗能力是很低的
`07:56` 因为只要b给a打个电话
`07:59` 就能戳穿骗局
`08:02` 这是因为a根本没有和b进行通信
`08:07` 再来看另一种情况
`08:10` a项与b通信
`08:11` 于是向b发送我是a的报文
`08:16` 该报文被攻击者c截获
`08:18` 并原封不动的转发给b b选择一个不重复rb发送给a
`08:26` 但同样被c截获
`08:28` 并原封不动的转发给a
`08:31` c用自己的私钥sk c对rb加密后发送给b史b误认为这是a发来的
`08:41` a收到rb后
`08:43` 也用自己的私钥s k a对rb加密后发送给b
`08:48` 但被c截获并丢弃
`08:52` b向a发送报文
`08:54` 要求a把解密用的公钥p k a发送过来
`08:58` 但该报文被c截获并转发给a
`09:03` 谁把自己的公钥pk c冒充是a的公钥发送给b
`09:09` 而c也截获到a发送给b的公钥p k a
`09:13` 但不会将其转发给b b收到公钥pk c
`09:19` 但却误认为是公钥p k a以后就用pk c对数据进行加密后
`09:27` 发送给a c截货币发送的加密数据号
`09:32` 用自己的私钥s k c对其解密
`09:36` 这样c就得到了b发送给a的数据
`09:40` c还需要再用a的公钥p k a对该数据加密后发送给a
`09:47` a收到c发送的加密数据后
`09:50` 用自己的私钥s k a对其解密
`09:54` 这样a也得到了b发送的数据
`09:58` 就这样a以为和b进行了保密通信
`10:03` 但实际上b发送给a的加密数据已被中间人c截获并解密
`10:10` 然而a和b却都不知道上述这种更具欺骗性的攻击称为中间人攻击
`10:20` 通过本地可以看出
`10:22` 公钥的分发以及公要真实性的认证也是一个非常重要的问题
`10:29` 下节课我们就介绍公钥分发的有关内容
`10:34` 本节课到这里就结束了
`10:37` 请同学们快速回忆一下本节课的主要内容
`11:01` 同学们
`11:02` 我们下节课再见