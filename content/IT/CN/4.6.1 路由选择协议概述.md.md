---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=59"
bvid: "BV18g411D7Fm"
cid: "817194990"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817194990&page=59&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课
`00:11` 我们介绍路由选择协议的相关基本概念
`00:16` 路由选择可分为静态路由选择
`00:20` 和动态路由选择两类
`00:23` 静态路由选择是指采用人工配置的方式
`00:28` 给路由器添加网络路由
`00:30` 默认路由特定主机路由
`00:33` 黑洞路由等路由信息
`00:36` 而动态路由选择是指
`00:38` 路由器通过路由选择协议
`00:41` 自动获取路由信息
`00:44` 对于路由器自身而言
`00:46` 静态路由选择简单
`00:48` 开销小
`00:49` 但不能及时适应网络状态的变化
`00:53` 而动态路由选择比较复杂
`00:56` 开销比较大
`00:58` 但能较好地适应网络状态的变化
`01:02` 因此静态路由选择
`01:04` 一般只在小规模网络中采用
`01:07` 而动态路由选择适用于大规模网络
`01:12` 英特网是全球最大的互联网络
`01:15` 它所采用的路由选择协议
`01:18` 具有以下三个主要特点
`01:21` 分别是自适应分布式分层次
`01:28` 自适应是指英特网采用的是动态路由选择
`01:32` 能较好地适应网络状态的变化
`01:36` 分布式是指英特网中的各路由器
`01:40` 通过相互间的信息交互
`01:42` 共同完成路由信息的获取和更新
`01:47` 分层次是指
`01:48` 将整个英特网划分为许多较小的自制系统
`01:53` as例如一个较大的英特网服务提供商
`01:58` 就可划分为一个自制系统
`02:01` 在自制系统内部和自制系统外部
`02:05` 采用不同类别的路由选择协议
`02:08` 分别进行路由选择
`02:11` 下面我们来举例说明
`02:14` 英特网采用的分层次路由选择协议
`02:18` 假设这是英特网中的很小的一部分
`02:22` 我们可以将这些网络和路由器
`02:25` 划归到一个自制系统
`02:28` 而将这些网络和路由器
`02:31` 划归到另一个自制系统
`02:34` 自制系统之间的路由选择简称为预间路由
`02:39` 选择自制系统内部的路由选择
`02:42` 简称为域内路由
`02:44` 选择预监路由
`02:47` 选择使用外部网关协议
`02:49` 一gp这个类别的路由选择协议
`02:54` 域内路由选择使用内部网关协议gp
`02:58` 这个类别的路由选择协议
`03:01` 需要说明的是
`03:03` 外部网关协议一gp和内部网关协议i gp
`03:07` 只是路由选择协议的分类名称
`03:11` 而不是具体的路由选择协议
`03:14` 另外名称中使用的是网关这个名词
`03:18` 是因为在英特网早期的rfc文档中
`03:22` 没有使用路由器
`03:24` 而使用的是网关这一名词
`03:27` 现在新的rfc文档中
`03:30` 又改用路由器这一名词
`03:33` 因此外部网关协议一gp
`03:36` 可改称为外部路由协议e r p
`03:41` 内部网关协议i g p
`03:43` 可改称为内部路由协议i r p
`03:48` 本课程仍然采用rfc原先使用的名词
`03:52` 以方便大家查阅rfc文档
`03:58` 使用的具体的内部网关协议
`04:01` 与英特网中
`04:02` 其他自制系统中
`04:04` 选用何种内部网关协议无关
`04:10` 使用的内部网关协议为路由信息协议rip
`04:17` 使用的内部网关协议为开放式
`04:20` 最短路径优先
`04:21` os pf协议
`04:24` 自制系统之间使用的外部网关协议
`04:27` 为边界网关协议bgp
`04:31` 接下来我们给出常见的路由选择协议
`04:36` 分为内部网关协议和外部网关协议两大类
`04:45` 内部网关路由协议i g r p
`04:49` 增强型内部网关路由协议e i g r p
`04:54` 开放式最短路径优先
`04:55` os spf中间系统到中间系统i s i s
`05:02` 而外部网关协议中
`05:04` 最常见的仅有边界网关协议bgp
`05:09` rap和ig rp是基于距离向量的
`05:14` rap
`05:14` 是最早应用在英特网上的路由选择协议
`05:22` 现在已被e i g r p取代
`05:26` ospf和s是基于链路状态的
`05:32` ospf在各种网络中得到了广泛的使用
`05:37` 而集成化的s是sp骨干网上最常用的
`05:42` 内部网关协议
`05:48` 用来取代ig rp的混合型路由选择协议
`05:53` 它结合了距离向量和链路状态
`05:58` 路由选择协议是在路由器上运行的
`06:02` 下面我们就来看看路由器的基本结构
`06:07` 如图所示
`06:08` 路由器是一种
`06:10` 具有多个输入端口和输出端口
`06:12` 的专用计算机
`06:14` 其任务是转发分组
`06:18` 整个路由器结构可划分为两大部分
`06:22` 一个是路由选择部分
`06:26` 另一个是分组转发部分
`06:30` 路由选择部分的核心构件是路由
`06:33` 选择处理机
`06:35` 他的任务是根据所使用的路由选择协议
`06:39` 周期性的与其他路由器进行
`06:42` 路由信息的交互来更新路由表
`06:47` 分组转发部分由三部分构成
`06:51` 分别是交换结构
`06:54` 一组输入端口
`06:57` 一组输出端口
`07:00` 信号从某个输入端口进入路由器
`07:04` 物理层将信号转换成比特流
`07:08` 送交数据链路层处理
`07:11` 数据链路层从比特流中识别出真
`07:15` 去掉针头和真伪后
`07:17` 送交网络层处理
`07:20` 如果送交网络层的分组
`07:22` 是普通带转发的数据分组
`07:25` 则根据分组首部中的目的地址
`07:28` 进行查表转发
`07:31` 若找不到
`07:32` 匹配的转发条目则丢弃该分组
`07:35` 否则按照匹配条目中所指示的端口
`07:40` 进行转发
`07:42` 网络层更新数据分组首部中某些字段的值
`07:46` 例如将数据分组的生存时间减一
`07:50` 然后送交数据链路层进行封装
`07:55` 数据链路层将数据分组封装成真
`07:58` 送交物理层处理
`08:01` 物理层将真看作是比特流
`08:04` 将企业变换成相应的电信号进行发送
`08:09` 如果送交网络层的分组是路由器之间
`08:12` 交换路由信息的路由报文
`08:16` 则把这种分组送交路由
`08:18` 选择处理机
`08:20` 路由选择处理器
`08:22` 根据分组的内容来更新自己的路由表
`08:30` 从目的网络到吓一跳的映射
`08:34` 路由表需要对网络拓扑变化的计算最优化
`08:40` 而转发表是从路由表得出的
`08:47` 转发表的结构应当使查找过程最优化
`08:52` 需要说明的是
`08:54` 我们在之前的静态路由配置的相关课程中
`08:58` 并没有严格区分
`09:00` 路由器中的路由表和转发表
`09:03` 这样有助于简化问题的分析
`09:07` 因此在后续有关路由选择协议的课程中
`09:11` 我们仍然不严格区分路由表和转发表
`09:15` 还是以路由表来表述问题
`09:20` 路由选择处理机
`09:22` 除了处理收到的路由报文外
`09:24` 还会周期性的给其他路由器发送
`09:28` 自己所知道的路由信息
`09:38` 路由器的各端口
`09:40` 还应具有输入缓冲区和输出缓冲区
`09:45` 输入缓冲区用来暂存新进入路由器
`09:49` 但还来不及处理的分组
`09:52` 输出缓冲区用来暂存已经处理完毕
`09:56` 但还来不及发送的分组
`10:00` 需要说明的是
`10:01` 路由器的端口
`10:03` 一般都具有输入和输出的功能
`10:07` 我们图中分别给出输入端口和输出端口
`10:11` 目的在于更好地演示
`10:14` 路由器的基本工作过程
`10:16` 使同学们更容易理解
`10:20` 本节课到这里就结束了
`10:22` 我们将本节课的内容小结如下
`10:28` 同学们
`10:29` 我们下节课再见