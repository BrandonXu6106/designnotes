---
title: 3.4.2 可靠传输的实现机制 —— 停止-等待协议
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=22
bvid: BV18g411D7Fm
cid: "817184045"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817184045&page=22&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 从本节课开始
`00:12` 我们将用几次课的时间来介绍三种可靠传输的实现机制
`00:18` 他们是停止等待协议
`00:21` 回退n帧协议
`00:23` 选择重传协议
`00:26` 需要提醒大家注意的是
`00:28` 这三种可靠传输实现机制的基本原理并不仅限于数据链路层
`00:34` 可以应用到计算机网络体系结构的各层协议中
`00:39` 希望同学们在学习时不要把思维局限在数据链路层
`00:44` 而应放眼于整个网络体系结构
`00:48` 本节课我们介绍停止等待协议
`00:52` 如图所示
`00:54` 收发双方基于互联网进行通信
`00:57` 而不是局限在一条点对点的数据链路
`01:02` 纵坐标为时间
`01:04` 发送方给接收方发送数据分组
`01:08` 接收方收到后对其进行差错检测
`01:12` 若没有密码
`01:13` 则接受该数据分组
`01:16` 并给发送方发送确认分组
`01:19` 简称为a c k
`01:21` 发送方收到对所发送数据分组的确认分组后
`01:25` 才能发送下一个数据分组
`01:28` 假设这个数据分组在传输过程中出现了误码
`01:32` 接收方收到后对其进行差错检测
`01:35` 发现了密码则丢弃该数据分组
`01:39` 并给发送方发送否认分组
`01:42` 简称为n a k发送方收到对所发送数据分组的否认分组后
`01:48` 就知道了之前自己所发送的数据分组出现了差错
`01:53` 而被接收方拒绝
`01:55` 于是立刻重传该数据分组
`01:59` 因此发送方每发送完一个数据分组后
`02:03` 并不能立刻将该数据分组从缓存中删除
`02:07` 只有在收到针对该数据分组的确认分组后
`02:11` 才能将其从缓存中删除
`02:14` 看来发送方每发送完一个数据分组后
`02:18` 就停止发送下一个数据分组
`02:21` 等待来自接收方的确认分组或否认分组
`02:25` 若收到确认分组
`02:27` 则可继续发送下一个数据分组
`02:30` 若收到否认分组
`02:32` 则重发之前发送的那个数据分组
`02:35` 这样就实现了发送方发送什么
`02:38` 接收方最终都能收到什么
`02:41` 也就是所谓的可靠传输
`02:44` 但实际情况远比我们想象的要复杂
`02:47` 来看这种情况
`02:50` 发送方给接收方发送数据分组
`02:53` 然而该数据分组在传输过程中丢失了
`02:58` 需要说明的是
`02:59` 对于数据链路层点对点信道而言
`03:03` 不太容易出现这种情况
`03:06` 但对于多个网络
`03:07` 通过多个路由器互联的复杂互联网环境而言
`03:12` 这种情况是会经常出现的
`03:15` 对于这种情况
`03:16` 接收方既然收不到数据分组
`03:19` 那么也就不会无缘无故地发送确认或否认分组
`03:24` 如果不采取其他措施
`03:27` 发送方就会一直处于等待接收方确认或否认分组的状态
`03:33` 为了解决该问题
`03:35` 可以在发送方发送完一个数据分组时
`03:38` 启动一个超时计时器
`03:41` 若到了超时计时器所设置的重传时间
`03:44` 而发送方仍收不到接收方的确认或否认分组
`03:49` 则重传原来的数据分组
`03:52` 这就叫做超时重传
`03:55` 一般可将重传时间选为略大于从发送方到接收方的平均往返时间
`04:03` 如图所示
`04:04` 发送方超时重传之前所发送的数据分组
`04:10` 接收方正确接收重传的数据分组后给发送方发送确认分组
`04:17` 发送方收到确认分组后
`04:19` 发送下一个数据分组
`04:22` 接收方正确接收该数据分组后给发送方发送确认分组
`04:28` 到目前为止
`04:30` 貌似基于停止等待使用确认或否认分组
`04:35` 再加上超时重传的手段
`04:37` 就可以实现可靠传输了
`04:41` 但请大家再深入地思考一下
`04:44` 是否还会出现目前这些手段不足以应对实现可靠传输的其他情况呢
`04:52` 来看这种情况
`04:54` 既然发送方发送的数据分组可能丢失
`04:58` 那么接收方发送的确认或否认分组就也有可能丢失
`05:04` 例如发送方发送了一个数据分组
`05:08` 接收方正确接收该数据分组后
`05:11` 给发送方发送确认分组
`05:14` 但该确认分组在传输过程中丢失了
`05:18` 这必然会造成发送方对之前所发送数据分组的超时重传
`05:25` 假设这个重传的数据分组也正确到达了接收方
`05:30` 那么现在问题来了
`05:31` 接收方如何判断该数据分组是否是一个重复的分组呢
`05:38` 为了避免分组重复这种传输错误
`05:41` 必须给每个数据分组带上序号
`05:45` 例如该数据分组的序号为零
`05:49` 对于停止等待协议
`05:51` 由于每发送一个数据分组就进行停止等待
`05:55` 只要保证每发送一个新的数据分组
`05:58` 其序号与上次发送的数据分组的序号不同
`06:02` 就可以了
`06:03` 因此用一个比特来编号就够了
`06:07` 记序号零和一
`06:10` 这样根据数据分组的序号
`06:13` 接收方就可以判断出该数据分组是否是重复的
`06:19` 接收方丢弃重复的数据分组
`06:22` 并给发送方发送针对该数据分组的确认分组
`06:26` 以免发送方对该数据分组的再次超时重传
`06:32` 发送方收到针对0号数据分组的确认分组
`06:35` 就可以发送下一个数据分组了
`06:38` 其序号为一
`06:41` 接收方正确收到1号数据分组后
`06:44` 给发送方发送确认分组
`06:47` 我们通过确认分组丢失的情况
`06:50` 引出了给数据分组编号的问题
`06:54` 请大家思考一下
`06:56` 既然数据分组需要编号
`06:59` 那么确认分组是否也需要编号呢
`07:03` 来看这种情况
`07:06` 发送方发送0号数据分组
`07:09` 接收方正确接收后给发送方发送确认分组
`07:14` 由于某些原因
`07:16` 该确认分组迟到了
`07:18` 这必然会导致发送方对0号数据分组的超时重传
`07:24` 在重传的0号数据分组的传输过程中
`07:28` 发送方收到了迟到的确认分组
`07:31` 于是发送1号数据分组
`07:34` 接收方收到重传的0号数据分组后
`07:38` 发现这是一个重复的数据分组
`07:41` 将其丢弃
`07:43` 并针对该数据分组给发送方发送确认分组
`07:48` 以免发送方再次超时重传该数据分组
`07:53` 现在问题来了
`07:54` 我们可以非常清楚地看到
`07:57` 这是一个对0号数据分组的重复确认
`08:01` 但是发送方又如何知道呢
`08:05` 如果不采取其他措施的话
`08:07` 发送方会误认为这是对1号数据分组的确认
`08:13` 如果对确认分组也进行编号
`08:16` 就可以使发送方避免这种误判
`08:19` 如图所示
`08:21` 该确认分组的序号为零
`08:24` 发送方通过确认分组的序号知道这是一个重复的确认分组
`08:30` 忽略即可
`08:32` 接收方正确接受1号数据分组后
`08:35` 给发送方发送针对该数据分组的确认分组
`08:39` 其序号为一
`08:42` 发送方收到该确认分组后
`08:44` 发送下一个数据分组序号为零
`08:48` 请注意
`08:49` 该数据分组与之前序号为零的那个数据分组不是同一个数据分组
`08:57` 我们用给确认分组编号的方法
`09:00` 解决了确认迟到所导致的重复确认的问题
`09:04` 需要说明的是
`09:06` 对于数据链路层的点对点信道往返时间比较固定
`09:12` 不会出现确认迟到的情况
`09:15` 因此如果只在数据链路层实现停止等待协议
`09:20` 可以不用给确认分组编号
`09:24` 接下来我们对停止等待协议的一些注意事项进行一下小结
`09:30` 接收端检测到数据分组有误码时
`09:34` 将其丢弃
`09:35` 并等待发送方的超时重传
`09:38` 但对于误码率较高的点对点链路
`09:41` 为使发送方尽早重传
`09:44` 也可给发送方发送否认分组
`09:48` 为了让接收方能够判断所收到的数据分组是否是重复的
`09:54` 需要给数据分组编号
`09:57` 由于停止等待协议的停等特性
`10:00` 只需一个比特编号就够了
`10:03` 记序号零和一
`10:06` 为了让发送方能够判断所收到的确认分组是否是重复的
`10:12` 需要给确认分组编号
`10:15` 所用比特数量与数据分组编号所用比特数量一样
`10:21` 数据链路层一般不会出现确认分组迟到的情况
`10:25` 因此在数据链路层实现停止等待协议
`10:29` 可以不用给确认分组编号
`10:33` 超时计时器设置的重传时间应仔细选择
`10:38` 一般可将重装时间选为略大于从发送方到接收方的平均往返时间
`10:46` 在数据链路层点对点的往返时间比较确定
`10:50` 重传时间比较好设定
`10:53` 然而在运输层
`10:55` 由于端到端往返时间非常不确定
`10:59` 设置合适的重传时间有时并不容易
`11:03` 我们将在第五章有关tcp协议的课程中详细讨论该问题
`11:10` 接下来我们来看看停止等待协议的信道利用率
`11:15` 如图所示
`11:17` 横坐标为时间
`11:20` 为了简单起见
`11:22` 假设收发双方之间是一条直通的信道
`11:27` 发送方发送完一个数据分组后就停止发送
`11:31` 并等待接收方对该数据分组的确认
`11:35` 当收到确认分组后
`11:37` 可以发送下一个数据分组
`11:39` 如此反复进行
`11:42` 这一段时间是发送方发送数据分组所耗费的发送时延
`11:48` td这一段时间是收发双方之间的往返时间
`11:54` rtt这一段时间是接收方发送确认分组所耗费的发送时延
`12:01` ta图中忽略了接收方对数据分组的处理时间
`12:07` 以及发送方对确认分组的处理实验
`12:11` 这是使用停止等待协议的发送方从发送一个数据分组开始
`12:17` 到可以发送下一个数据分组为止所经历的总时间
`12:22` 因为仅仅是在时间td内才用来传送有用的数据
`12:28` 也就是数据分组
`12:30` 因此信道的利用率u可以用下式来计算
`12:37` ta一般都远小于tt
`12:39` 可以忽略
`12:41` 当rtt远大于td时
`12:44` 信道利用率会非常低
`12:46` 我们来举例说明
`12:49` 假设信道长度为2000km
`12:52` 数据分组长度为1500字节
`12:55` 发送速率为十兆比特每秒
`12:58` 忽略ta
`13:00` 因为ta一般都远小于t
`13:03` 可以计算出信道利用率约为5.66%
`13:09` 这是数据分组的发送实验td这是往返时间
`13:15` rtt如果将发送速率提高到100兆比特每秒
`13:21` 则可计算出信道利用率仅为0.6%
`13:26` 这是数据分组的发送时延td这是往返时间rtt可以看出
`13:35` 当往返时延rtt远大于数据帧发送时延td时
`13:40` 例如使用卫星链路
`13:43` 信道利用率非常低
`13:46` 若出现重传
`13:48` 则对于传送有用的数据信息来说
`13:51` 信道利用率还要降低
`13:54` 为了克服停止等待协议
`13:56` 信道利用率很低的缺点
`13:59` 就产生了另外两种协议
`14:01` 即后退n帧协议gbn和选择重传协议sr
`14:09` 最后我们来做一道有关停止等待协议的练习题
`14:15` 这是计算机专业考研全国统考计算机网络部分
`14:20` 2018年的题
`14:22` 36
`14:24` 请大家暂停播放视频
`14:26` 思考一下
`14:28` 给出你的答案
`14:33` 答案是d选项
`14:35` 大家都选对了吗
`14:38` 我们一起来分析该题
`14:41` 根据题意可以画出停止等待协议的示意图
`14:48` 这一段是数据帧的发送时延
`14:52` 这一段是数据帧的最后一比特
`14:54` 信号从主机甲传播到主机已所耗费的传播实验
`15:00` 也就是信号在两主机之间的单程传播实验
`15:05` 同理这一段也是单程传播实验
`15:11` 停止等待协议的信道利用率等于数据帧的发送时延
`15:17` 除以数据帧发送时延
`15:20` 加端到端往返时延
`15:23` 也就是两倍的单程传播实验
`15:28` 设数据帧长度为x个比特
`15:32` 将其与题目所给的相关已知量带入上式可得
`15:40` 解得x等于800比特
`15:44` 本节课到这里就结束了
`15:47` 我们将本节课的内容小结如下
`15:51` 顺便提一下
`15:53` 像停止等待协议这种通过确认和重传机制实现的可靠传输协议
`15:59` 常称为自动请求重传协议
`16:02` a r q意思是重传的请求是自动进行的
`16:08` 因为不需要接收方显示的请求
`16:11` 发送方重传某个出错的分组
`16:15` 同学们
`16:16` 我们下节课再见