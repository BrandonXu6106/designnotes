---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=84"
bvid: "BV18g411D7Fm"
cid: "817204886"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817204886&page=84&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:04` 同学们好
`00:05` 欢迎大家学习计算机网络这门课程
`00:09` 本节课我们介绍tcp的连接释放tcp通过四报文挥手来释放连接
`00:16` 我们来举例说明
`00:19` 数据传输结束后
`00:20` tcp通信双方都可以释放连接
`00:24` 现在tcp客户进程和tcp服务器进程都处于连接已建立状态
`00:32` 假设是用tcp客户进程的应用进程通知其主动关闭tcp连接
`00:38` tcp客户进程会发送tcp连接
`00:42` 释放报文段
`00:43` 并进入终止等待一状态
`00:46` 该报文段首部中的中指位fn和确认为a c k的值都被设置为一
`00:53` 表明这是一个tcp连接释放报文段
`00:57` 同时也对之前收到的报文段进行确认
`01:01` 序号s1 q字段的值设置为u
`01:05` 它等于tcp客户进程之前已传送过的数据的最后一个字节的序号加一
`01:12` 请注意tcp规定终止为f i n等于一的报文段
`01:18` 即使不携带数据
`01:19` 也要消耗掉一个序号确认号a c k字段的值设置为v
`01:25` 它等于tcp客户进程之前已收到的数据的最后一个字节的序号加一
`01:33` tcp服务器进程收到tcp连接释放报文段后
`01:37` 会发送一个普通的tcp确认报文段
`01:41` 并进入关闭等待状态
`01:44` 该报文段首部中的确认为a c k的值被设置为一
`01:49` 表明这是一个普通的tcp确认报文段
`01:54` 序号s1 q字段的值设置为v
`01:57` 它等于tcp服务器进程之前以传送过的数据的最后一个字节的序号加一
`02:05` 这也与之前收到的tcp连接释放报文段中的渲染号匹配
`02:11` 确认号ack字段的值设置为u加一
`02:15` 这是对tcp连接释放报文段的确认
`02:20` tcp服务器进程这时应通知高层应用进程
`02:25` tcp客户进程要断开与自己的tcp连接
`02:30` 此时从tcp客户进程到tcp服务器进程这个方向的连接就释放了
`02:38` 这时的tcp连接属于半关闭状态
`02:42` 也就是tcp客户进程已经没有数据要发送了
`02:46` 但tcp服务器进程如果还有数据要发送
`02:51` tcp客户进程仍要接收
`02:54` 也就是说
`02:55` 从tcp服务器进程到tcp客户进程这个方向的连接并未关闭
`03:02` 这个状态可能会持续一段时间
`03:06` tcp客户进程收到tcp确认报文段后就进入终止等待二状态
`03:12` 等待tcp服务器进程发出的tcp连接释放报文段
`03:19` 若使用tcp服务器进程的应用进程已经没有数据要发送了
`03:24` 应用进程就通知其tcp服务器进程释放连接
`03:30` 由于tcp连接释放是由tcp客户进程主动发起的
`03:35` 因此tcp服务器进程对tcp连接的释放称为被动关闭连接
`03:43` tcp服务器进程发送tcp连接释放报文段
`03:47` 并进入最后确认状态
`03:50` 该报文段首部中的中指位fn和确认为a c k的值都被设置为一
`03:58` 表明这是一个tcp连接释放报文段
`04:02` 同时也对之前收到的报文段进行确认
`04:06` 现在假定序号s1 q字段的值为w
`04:11` 这是因为在半关闭状态下
`04:13` tcp服务器进程可能又发送了一些数据确认号
`04:20` ack字段的值为u加一
`04:23` 这是对之前收到的tcp连接释放报文段的重复确认
`04:30` tcp客户进程收到tcp连接释放报文段后
`04:34` 必须针对该报文段发送普通的tcp确认报文段
`04:39` 只好进入时间等待状态
`04:43` 该报文段首部中的确认为ak的值被设置为一
`04:48` 表明这是一个普通的tcp确认报文段
`04:52` 序号s e q字段的值设置为u加一
`04:56` 这是因为tcp客户进程之前发送的tcp连接释放报文段
`05:02` 虽然不携带数据
`05:04` 但要消耗掉一个序号确认号
`05:08` ack字段的值设置为w加一
`05:11` 这是对所收到的tcp连接释放报文段的确认
`05:17` tcp服务器进程收到该报文段后就进入关闭状态
`05:23` 而tcp客户进程还要经过两倍的msl后才能进入关闭状态
`05:30` msl的意思是最长报文段寿命
`05:34` rfc 793文档建议为两分钟
`05:38` 也就是说tcp客户进程进入时间等待状态后
`05:43` 还要经过四分钟才能进入关闭状态
`05:48` 这完全是从工程上来考虑的
`05:51` 对于现在的网络msl取为两分钟可能太长了
`05:56` 因此tcp允许不同的实现
`06:00` 可根据具体情况使用更小的msl值
`06:06` 那么tcp客户进程在发送完最后一个确认报文段后
`06:12` 为什么不直接进入关闭状态
`06:15` 而是要进入时间等待状态两倍msl号才进入关闭状态
`06:21` 这是否有必要呢
`06:24` 来看这种情况
`06:26` tcp服务器进程发送tcp连接释放报文段后进入最后确认状态
`06:33` tcp客户进程收到该报文段后
`06:36` 发送普通的tcp确认报文段
`06:39` 并进入关闭状态
`06:41` 而不是时间等待状态
`06:44` 然而该tcp确认报文段丢失了
`06:48` 这必然会造成tcp服务器进程对之前所发送的tcp连接
`06:54` 释放报文段的超时重传
`06:57` 并仍处于最后确认状态
`07:00` 重传的tcp连接释放报文段
`07:03` 到达tcp客户进程
`07:06` 由于tcp客户进程处于关闭状态
`07:09` 因此不理睬该报文段
`07:12` 这必然会造成tcp服务器进程反复重传tcp连接
`07:18` 释放报文段
`07:20` 并一直处于最后确认状态
`07:22` 而无法进入关闭状态
`07:26` 因此时间等待状态以及处于该状态两倍msl的时长
`07:32` 可以确保tcp服务器进程可以收到最后一个tcp确认报文段
`07:38` 而进入关闭状态
`07:41` 另外tcp客户进程在发送完最后一个tcp确认报文段后
`07:47` 再经过两倍msl时长
`07:50` 就可以使本次连接持续时间内所产生的所有报文段都从网络中消失
`07:58` 这样就可以使下一个新的tcp连接中不会出现就连接中的报文段
`08:06` 以上就是tcp通过四道文挥手释放连接的过程
`08:12` 最后我们再来看看tcp中饱和计时器的作用
`08:18` 设想这样一种情况
`08:20` tcp双方已经建立了连接
`08:23` 后来tcp客户进程所在的主机突然出现了故障
`08:29` 显然tcp服务器进程以后就不能再收到tcp客户进程发来的数据
`08:37` 因此应当有措施使tcp服务器进程不要再白白等待下去
`08:44` 换句话说
`08:45` tcp服务器进程应该如何发现这种情况呢
`08:51` 方法就是使用饱和计时器
`08:54` tcp服务器进程每收到一次tcp客户进程的数据
`08:59` 就重新设置并启动饱和计时器
`09:03` 若饱和计时器定时周期内未收到tcp客户进程发来的数据
`09:10` 则当保和计时器
`09:11` 到时候tcp服务器进程就像tcp客户进程发送一个探测报文段以后
`09:20` 每隔75秒钟发送一次
`09:23` 若一连发送十个探测报文段后
`09:26` 仍无tcp客户进程的响应
`09:29` tcp服务器进程就认为tcp客户进程所在主机出了故障
`09:36` 接着就要关闭这个链接
`09:39` 本节课到这里就结束了
`09:42` 我们将本节课的内容小结如下
`09:49` 同学们
`09:50` 我们下节课再见