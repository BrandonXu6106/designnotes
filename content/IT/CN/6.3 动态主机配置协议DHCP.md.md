---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=92"
bvid: "BV18g411D7Fm"
cid: "817207116"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817207116&page=92&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍动态主机配置协议
`00:13` d h c p
`00:15` 我们首先来举例说明dh cp的作用
`00:18` 如图所示
`00:20` 有这样一个网络拓扑
`00:22` 请同学们思考一下
`00:24` 我们应该给网络中的各主机设置
`00:27` 怎样的网络相关配置信息
`00:29` 才能使他们可以正常访问
`00:32` 网络中的web服务器
`00:36` 根据我们之前课程所介绍过的相关知识
`00:39` 可知
`00:40` 需要给网络中的各主机正确配置ip地址
`00:44` 子网掩码
`00:46` 默认网关dns服务器的网络相关配置信息
`00:51` 例如这是我们给该主机手工配置的网络
`00:55` 相关配置信息
`00:57` 这是我们给另一台主机手工配置的
`01:01` 网络相关配置信息
`01:03` 试想一下
`01:04` 如果网络中的主机数量比较多
`01:08` 则这种手工配置的工作量就比较大
`01:11` 并且容易出错
`01:14` 如果我们给网络中添加一台dh cp服务器
`01:18` 在该服务器中设置好
`01:20` 可谓网络中
`01:21` 其他各主机配置的网络配置信息
`01:25` 网络中各主机开机后自动启动dh cp程序
`01:30` 像第一支cp服务器
`01:32` 请求自己的网络配置信息
`01:35` 这样
`01:36` 网络中的各主机就都可以从dh cp服务器
`01:41` 自动获取网络配置信息
`01:43` 而不用手工参与
`01:48` 接下来我们举例说明dh cp的工作过程
`01:53` 假设网络中有两台dh cp服务器
`01:57` 和多台用户主机
`01:59` 为了简单而有效地描述dh cp的工作过程
`02:04` 我们画出网络中的这两台dh cp服务器
`02:08` 和一台用户主机
`02:11` d h c p使用客户服务方式
`02:14` 在d h c p服务器上运行
`02:17` dh cp服务器进程也可简称为dh cp
`02:21` 服务器
`02:23` 在用户主机上运行
`02:25` d h c p客户进程
`02:27` 也可简称为d h c p客户
`02:31` d h c p是tcp ip协议体系应用程中的协议
`02:36` 它使用运输层的udp所提供的服务
`02:41` 也就是说
`02:42` dh cp报文在运输层会被封装成为udp
`02:47` 用户数据报
`02:49` d h c p服务器使用的udp端口是67
`02:53` dh cp客户使用的udp端口是68
`02:58` 这两个usb端口都是熟知端口
`03:02` 封装有dh cp报文的udp
`03:05` 用户数据报在网络层会被封装成ip数据报
`03:10` 然后再根据所使用的网络接口
`03:14` 封装成相应的数据链路层的帧进行发送
`03:18` 例如封装成以太网帧
`03:22` 为了简单起见
`03:23` 在后续描述过程中
`03:26` 除非有特别需要
`03:28` 否则我们将不再每次描述d h c p报文
`03:32` 逐层封装的过程
`03:35` 下面我们来看看
`03:36` dh cp客户与dh cp服务器的交互过程
`03:42` 当启用主机的dh cp号
`03:44` d h c p客户将广播发
`03:47` 送dh cp发现报文封装该报文的ip数据
`03:52` 报的原ip地址为零点点零点
`03:56` 这是因为主机目前还未分配的ip地址
`04:01` 因此使用该地址来代替
`04:05` 目的ip地址为广播地址
`04:07` 255.255.255.255
`04:11` 之所以进行广播发送
`04:13` 是因为主机现在并不知道
`04:16` 网络中有哪几个dh cp服务器
`04:19` 他们的ip地址各是什么
`04:23` 由于是广播的ip数据报
`04:26` 因此网络中的所有设备
`04:28` 都会收到该ip数据报
`04:31` 并对其层层解封
`04:34` 解封出封装
`04:35` 有dh c p发现报文的udp用户数据报
`04:40` 对于dh c p客户
`04:42` 其应用层没有监听该udp用户数据
`04:45` 报的目的端口67的进程
`04:48` 也就是dh cp服务器进程
`04:51` 因此无法交付
`04:53` dh cp发现报文只能丢弃
`04:56` 而对于dh cp服务器
`04:59` 其应用层始终运行着dh cp服务器进程
`05:04` 因此会接受该dh cp发现报文并作出响应
`05:11` d h c p报文的格式比较复杂
`05:14` 对于dh c p发现报文
`05:17` 我们只需要知道其内部封装
`05:20` 有是5d和dh cp客户端的mac地址即可
`05:25` dh cp服务器收到dh cp发现报文号
`05:29` 根据其中封装的dh cp客户端的mac地址
`05:34` 来查找自己的数据库
`05:36` 看是否有针对该mac地址的配置信息
`05:41` 如果有
`05:42` 则是用这些配置信息来构建并发送
`05:46` dh cp提供报文
`05:48` 如果没有
`05:49` 则采用默认配置信息来构建并发送
`05:54` dh cp提供报文
`05:56` 封装该报文的ip数据
`05:58` 报的原ip地址为dh cp服务器的ip地址
`06:03` 目的ip地址仍为广播地址
`06:07` 仍然使用广播地址的原因
`06:09` 是主机目前还没有配置ip地址
`06:13` 为了使主机可以收到
`06:15` 只能发送广播
`06:17` 这样一来
`06:18` 网络中的所有设备都会收到该ip数据报
`06:22` 并对其层层解封
`06:25` 解封出封装
`06:26` 有dh cp提供报文的udp用户数据报
`06:30` 对于dh c p服务器
`06:33` 其应用层没有监听该udp用户数据报
`06:36` 目的端口68的进程
`06:39` 也就是d h c p客户进程
`06:42` 因此无法交付dh cp提供报文
`06:46` 只能丢弃
`06:48` 而对于dh c p客户
`06:50` 其应用层运行着d h c p客户进程
`06:54` 因此会接受该d h c p提供报文
`06:58` 并作出相应处理
`07:01` d h c p客户会根据d h c p
`07:05` 提供报文中的事务
`07:06` id来判断该报文是否是自己所请求的报文
`07:12` 换句话说
`07:14` 如果该是5d与自己之前发送的第一支cp
`07:18` 发现报文装封装的是id相等
`07:21` 就表明这是自己所请求的报文
`07:25` 就可以接受该报文
`07:28` 否则就要丢弃该报文
`07:31` d h c p提供报文中还封装有配置信息
`07:36` 例如ip地址
`07:38` 子网掩码地址租期
`07:43` 默认网关dns服务器等
`07:47` 需要注意的是
`07:49` 第一支cp服务器从自己的ip地址池中挑选
`07:54` 在租用给主机的ip地址时
`07:56` 会使用arp
`07:58` 来
`07:58` 确保所选ip地址为被网络中其他主机占用
`08:04` 在本例中
`08:05` d h c p客户会收到两个dh cp服务器发
`08:10` 来的dh cp提供报文
`08:13` dh cp客户从中选择一个
`08:17` 一般来说选择先到的那个
`08:21` 并向所选择的dh cp服务器发送dh cp
`08:25` 请求报文
`08:27` 封装该报文的ip数据
`08:29` 报的原地址仍为零点点零点
`08:33` 因为此时
`08:35` dh cp客户才从多个dh cp服务器中
`08:39` 挑选一个作为自己的dh cp服务器
`08:43` 他首先需要征得该服气的同意之后
`08:48` 才能正式使用
`08:49` 向该dh cp服务器租用的ip地址
`08:54` 目的ip地址仍为广播地址
`08:57` 这样做的目的是
`08:58` 不用像网络中的每一个dh c p服务器
`09:02` 单播发送d h c p请求报文
`09:05` 来告知他们是否请求他们作为自己的dh
`09:09` cp服务器第一支cp请求
`09:13` 报文装封装有事务id
`09:17` dh cp客户端的mac地址
`09:21` 接受的租约中的ip地址
`09:24` 提供此租约的第一支cp服务器
`09:27` 端的ip地址等信息
`09:30` 在本例中
`09:32` 假设第一支cp客户选择第一支cp服务器一
`09:36` 作为自己的第一支cp服务器
`09:40` 并且第一支cp服务器一接受该请求
`09:44` 于是d h c p服务器一
`09:47` 给dh c p客户发送d h c p确认报文
`09:53` 封装该报文的ip数据
`09:54` 报的原ip地址为d h c p服务器的ip地址
`09:59` 目的ip地址仍为广播地址
`10:03` dh cp客户收到该确认报文号
`10:06` 就可以使用所租用到的ip地址了
`10:11` 需要
`10:11` 注意的是
`10:12` 在使用所用到的ip地址之前
`10:16` 主机还会使用a r p
`10:18` 检测该ip地址
`10:20` 是否已被网络中其他主机占用
`10:23` 若被占用
`10:25` 第一支cp客户会给第一支cp服务器发送
`10:29` 第一支cp谢绝报文来谢绝ip地址租约
`10:33` 并重新发送dh cp发现报文若未被占用
`10:39` 则可以使用足月中的ip地址
`10:42` 与网络中的其他主机通信了
`10:45` 当租用期过了一半时
`10:48` 第一支cp客户会向第一支cp服务器发送
`10:53` 第一支cp请求报文来请求更新租用期
`10:58` 封装该报文的ip数据
`11:00` 报的原ip地址为dh c p
`11:02` 客户之前租用到的ip地址
`11:05` 目的ip地址为dh
`11:08` cp服务器一的地址
`11:10` d h c p服务器若同意
`11:13` 则发回d h c p确认报文
`11:17` 这样dh cp客户就得到了新的租用期
`11:23` d h c p服务器若不同意
`11:26` 则发回d h c p否认报文
`11:30` 这时dh cp客户必须立即停止使用
`11:34` 之前租用的ip地址
`11:36` 并重新发送dh cp发现报文
`11:39` 来重新申请ip地址
`11:42` d h c p服务器若未做出响应
`11:46` 则在租用期过了87.50%
`11:50` d h c p客户必须重新发送dh cp
`11:53` 请求报文
`11:55` 然后继续等待
`11:56` dh cp服务器可能做出的反应
`12:00` 若dh cp服务器未作出反应
`12:04` 则当租用期到期后
`12:07` d h c p客户必须立
`12:09` 即停止使用之前租用的ip地址
`12:12` 并重新发送dh cp发现报文
`12:15` 来重新申请ip地址
`12:19` d h c p客户可以随时提前终止
`12:22` dh cp服务器所提供的租用期
`12:26` 这时只需要向dh cp服务器发送
`12:30` dh cp释放报文段即可
`12:34` 综上所述
`12:35` 这部分是dh cp客户寻找dh cp服务器
`12:41` 这部分是dh cp服务器
`12:43` 向d h c p客户提供ip地址租用
`12:48` 这部分是第一次cp客户接受ip地址租约
`12:54` 这部分是dh cp服务器确认ip地址租约
`13:05` 这部分是第一次
`13:07` cp客户可以随时解除ip地址租约
`13:12` 需要注意的是
`13:14` d
`13:14` h c p服务器
`13:15` 在给dh c p客户挑选ip地址时
`13:19` 使用a r p来确保所挑选的ip地址
`13:23` 未被网络中其他主机占用
`13:26` 而dh cp客户在使用所租用的ip地址之前
`13:31` 也会使用arp
`13:32` 来检测单ip地址
`13:35` 是否已被网络中其他主机占用
`13:38` 最后我们再来看看dh cp中继代理的概念
`13:43` 如图所示
`13:45` 有这样一个网络拓扑
`13:47` 请大家思考一下
`13:50` 该网络中的各主机
`13:52` 是否可以通过dh cp来自动获取的
`13:56` 网络配置信息呢
`13:59` 答案是否定的
`14:01` 原因很简单
`14:04` 该网络中的主机广播发送dh cp发现报文
`14:08` 但该广播报文不会被路由器转发
`14:12` 而是丢弃解
`14:15` 决方法是
`14:16` 给该路由器配置dh cp服务器的ip地址
`14:20` 并使之成为dh cp中继代理
`14:24` 这样该网络中的各主机
`14:27` 就可以通过dh cp来自动获取的
`14:30` 网络配置信息了
`14:32` 当该路由器收到广播的dh cp发现报文后
`14:37` 会将其单播转发给dh cp服务器
`14:42` d h c p客户和d h c p服务器
`14:45` 通过该路由器的后续交互过程
`14:48` 我们就不再赘述了
`14:50` 使用dh cp终极代理的主要原因是
`14:54` 我们并不愿意在每一个网络上都设置一个
`14:58` dh cp服务器
`15:00` 因为这样会使第一支cp服务器的数量太多
`15:04` 本节课到这里就结束了
`15:07` 我们将本节课的内容
`15:09` 小结如下
`15:14` 同学们
`15:14` 我们下节课再见