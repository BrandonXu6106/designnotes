---
title: 3.4.2 （1）CSMA_CD协议的基本原理
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=28
bvid: BV18g411D7Fm
cid: "817186518"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817186518&page=28&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:09` 本节课我们介绍c s m a
`00:12` c d协议的基本原理
`00:15` 在以太网的发展初期
`00:17` 人们普遍认为
`00:19` 无缘的电缆线比有源器件可靠
`00:23` 因此将多个站点连接在一条总线上
`00:27` 来构建共享总线
`00:29` 以太网如图所示
`00:31` 这是一条使用同轴电缆
`00:33` 作为传输介质的总线
`00:35` 各主机或称为站点都连接在这条总线上
`00:41` 共享总线
`00:42` 以太网具有天然的广播特性
`00:44` 即使总线上某个站点
`00:47` 给另一个站点发送单波
`00:49` 真表示真的信号
`00:51` 也会沿着总线传播到总线上的其他各站点
`00:56` 例如站点b给c发送一个单波针
`01:01` 表示该针的信号
`01:03` 会沿着总线传播到总线上的其他各站点
`01:08` 当某个站点在总线上发送帧时
`01:12` 中线资源会被该站点独占
`01:15` 此时如果总线上的其他站点
`01:18` 也要在总线上发送帧
`01:20` 就会产生信号碰撞
`01:23` 当两个或多个站点同时使用总线发送帧时
`01:28` 就会产生信号碰撞
`01:31` 例如站点b和c同时在总线上发送帧
`01:36` 则表示这两个真的两路信号
`01:38` 被同时发送到总线上
`01:41` 他们必然会产生碰撞
`01:44` 碰撞后的信号还会沿着总线传播
`01:47` 这里就不再演示了
`01:50` 共享总线以太网的一个重要问题
`01:53` 就是如何协调总线上的各站点征用总线
`01:58` 为了解决各站点征用总线的问题
`02:02` 共享总线以太网使用了一种专用协议
`02:05` c s m a c d
`02:07` 它是在播监听
`02:09` 多指接入碰撞检测的英文缩写词
`02:13` 多指接入的英文缩写词为ma
`02:16` 它是指多个站点连接在一条总线上
`02:20` 竞争使用总线在播间听的英文缩写词为cs
`02:26` 它是指每个站点在发送帧之前
`02:29` 先要检测一下
`02:31` 总线上是否有其他站点在发送帧
`02:35` 可比喻为先听后说
`02:38` 若检测到总线空闲96比特时间
`02:42` 即发送96比特所耗费的时间
`02:46` 也称为针尖最小间隔
`02:49` 则发送这个真若检测到总线盲
`02:53` 则继续检测
`02:54` 并等待总线转为空弦96比特时间
`02:58` 然后发送这个针
`03:01` 碰撞检测的英文缩写词为cd
`03:04` 它是指每个正在发送帧的站点
`03:07` 边发送帧边检测碰撞
`03:10` 可比喻为边说边听
`03:13` 一旦发现总线上出现碰撞
`03:16` 立即停止发送
`03:18` 退避一段随机时间后
`03:21` 再次从赛博监听开始进行发送
`03:24` 可比喻为一旦冲突立即停说
`03:28` 等待时机重新再说
`03:31` 接下来我们举例说明
`03:34` 多指接入赛博监听以及碰撞检测
`03:38` 多指介入的概念比较简单
`03:41` 及多个站点连接到一根总线上
`03:44` 各主机随机发送针
`03:47` 再来看在播监听
`03:50` 假设站点c要发送帧
`03:53` 他首先进行赛博监听
`03:56` 在检测到总线空闲96比特时间后
`04:00` 就可以发送帧了
`04:02` 表示真的信号会沿着总线传播
`04:06` 假设在站点c使用总线发送针的过程中
`04:11` 站点b也要发送针
`04:14` 站点b进行载波监听
`04:16` 发现总线盲
`04:19` 于是持续检测总线
`04:23` 一旦发现总线空闲96比特时间
`04:27` 则立即发动针
`04:30` 编发动针还要边检测碰撞
`04:33` 这就是所谓的碰撞检测
`04:36` 只要没检测到碰撞
`04:38` 就可继续发动针的剩余部分
`04:42` 假设在站点b发送帧的过程中
`04:45` 站点c也要发送针
`04:51` 检测到总线空闲
`04:54` 但总线实际上并不空闲
`04:57` 这是因为
`04:58` 站点地发送的信号还没有传播到站点c
`05:02` 站点c检测不到而已
`05:05` 站点c检测到总线空闲
`05:07` 96比特时间后就立即发送帧
`05:11` 这必然会产生碰撞
`05:14` 在产生碰撞的时刻
`05:16` 站点b和c都在边发送帧边检测碰撞
`05:21` 但都检测不到碰撞
`05:23` 碰撞信号沿总线传播
`05:26` 就本地而言
`05:32` 因此站点c比b更早检测到碰撞
`05:36` 并停止发送帧退避一段随机时间后
`05:41` 重新从载波监听开始
`05:43` 以便重发之前遭遇到碰撞的针
`05:47` 当站点b检测到碰撞后
`05:50` 立即停止发送
`05:52` 退避一段随机时间后
`05:54` 重新从赛博监听开始
`05:57` 以便重发之前遭遇到碰撞的针
`06:01` 以太网还采取了一种叫做强化碰撞的措施
`06:06` 这就是发送针的站点
`06:08` 一旦检测到碰撞
`06:10` 除了立即停止发送之外
`06:12` 还要再继续发送
`06:14` 32比特或48比特的人为干扰信号
`06:19` 以便有足够多的碰撞信号
`06:21` 使所有站点都能检测出碰撞
`06:25` 由于赛博监听检测到总线空闲
`06:29` 但总线并不一定空闲
`06:32` 因此使用cs ma cd协议的共享总线
`06:36` 以太网上的各站点只是尽量避免碰撞
`06:40` 并在出现碰撞时做出退避后重发的处理
`06:45` 但不能完全避免碰撞
`06:48` 在使用cs ma cd协议时
`06:51` 由于正在发送针的站点
`06:54` 必须边发送针边检测碰撞
`06:57` 因此站点不可能同时进行发送和接收
`07:02` 也就是不可能进行全双工通信
`07:06` 而只能进行半双工通信
`07:09` 解双向交替通信
`07:16` 知识点相关的408考研真题
`07:20` 请同学们暂停播放视频思考一下
`07:24` 然后给出你的答案
`07:32` 本题的答案是选项b大家都选对了吗
`07:36` 我们来一起分析一下选项
`07:40` a中的cdma是码分多址的英文缩写词
`07:45` cdma的每个用户可以在相同的时间
`07:49` 使用相同的平台进行通信
`07:52` 如图所示
`07:54` 这是我们曾介绍过的有关cdma的一个例子
`07:58` 不知同学们是否还记得
`08:02` 选项c中的tdma是十分多指的英文缩写词
`08:07` 如图所示
`08:09` 十分复用的
`08:10` 所有用户
`08:11` 在不同的时间占用同样的平台进行通信
`08:17` 选项d中的fdma是评分多指的
`08:21` 英文缩写词
`08:22` 如图所示
`08:24` 频分复用的所有用户
`08:26` 同时占用不同的平台资源进行通信
`08:32` 选项b中的c s m a是在播监听
`08:35` 多指接入的英文缩写词
`08:38` 这是我们本节课所学的内容
`08:42` c s m a属于征用型的媒体接入控制协议
`08:46` 连接在同一媒体上的多个站点
`08:49` 使用该协议以竞争方式发送数据帧
`08:54` 可能出现冲突
`08:56` 也称为碰撞
`08:58` 而t d m a
`09:00` f d mcd ma是常见的物理层信道复用技术
`09:06` 属于静态划分性的
`09:09` 用于多用户共享信道
`09:11` 不会发生冲突
`09:14` 本节课到这里就结束了
`09:17` 请同学们快速回忆一下本节课的主要内容
`09:32` 同学们
`09:33` 我们下节课再见