---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=51"
bvid: "BV18g411D7Fm"
cid: "817192610"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817192610&page=51&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍ipv 4地址
`00:13` 编制方法的第二个历史阶段
`00:15` 也就是划分子网的ipv 4地址
`00:18` 我们首先来举例说明
`00:21` 为什么会出现划分子网这样的需求
`00:24` 如图所示
`00:26` 某单位有一个大型的局域网
`00:28` 需要连接到因特网
`00:30` 如果申请一个c类网络地址
`00:33` 即可分配的ip地址数量只有254个
`00:37` 不够实用
`00:38` 因此该单位申请了一个b类网络地址
`00:42` 即可分配的ip地址数量
`00:44` 达到了65534个
`00:47` 给每台计算机和路由器的接口
`00:50` 分配一个ip地址后
`00:52` 还有大量
`00:53` 的ip地址
`00:54` 剩余这些剩余的ip地址
`00:57` 只能由该单位的同一个网络使用
`01:00` 而其他单位的网络不能使用
`01:03` 随着该单位计算机网络的发展和建设
`01:07` 该单位又新增了一些计算机
`01:10` 并且需要将原来的网络划分成
`01:13` 三个独立的网络
`01:15` 我们称其为子网一
`01:17` 子网二和子网三
`01:20` 假设子网一仍然使用
`01:22` 原先申请到的b类网络地址
`01:25` 那么就需要为子网二和子网三
`01:28` 各自申请一个网络地址
`01:30` 但这样会存在一些弊端
`01:34` 申请新的网络地址需要等待很长的时间
`01:38` 并且要花费更多的费用
`01:40` 即便申请到了两个新的网络地址
`01:43` 这也
`01:44` 会使其他路由器的路由表新增
`01:47` 针对这两个新的网络的路由记录
`01:50` 另外这样还会浪费原有网络中
`01:53` 剩余的大量ip地址
`01:56` 如果可以从ip地址的主机号部分
`01:59` 借用一些way作为子网号来区分不同的子网
`02:04` 就可以利用原有网络中剩余的大量ip地址
`02:08` 而不用申请新的网络地址
`02:11` 例如对于本例
`02:13` 我们可以借用16位主机号中的八位
`02:16` 作为子网号
`02:18` 假设我们给子网一分配的子网号为零
`02:21` 给子网二分配的子网号为一
`02:25` 给子网三分配的子网号为二
`02:28` 之后
`02:29` 就可以给各子网中的主机和路由器接口
`02:33` 分配ip地址
`02:34` 了现在请大家思考这样一个问题
`02:40` 如果我们未在图中标记子网号部分
`02:43` 那么我们和计算机又是如何知道
`02:46` 分类地址中
`02:47` 主机号有多少位被用作了子网号呢
`02:52` 这样我们就引出了一个划分子网的工具
`02:56` 它就是子网掩码
`03:00` 32比特的子网掩码
`03:02` 可以表明
`03:02` 分类ip地址的主机号部分被借用了几位
`03:06` 作为子网号
`03:08` 如图所示
`03:09` 这是32比特的分类ipv 4地址
`03:13` 它由网络号和主机号两部分构成
`03:17` 当我们从主机号部分借用一些way
`03:20` 作为子网号时
`03:22` ip地址从两级结构的分类ip地址
`03:25` 变成了三级结构的划分子网的ip地址
`03:29` 我们用连续的比特一
`03:31` 来对应网络号和子网号
`03:37` 用连续的比特零来对应主机号
`03:43` 这样就构成了这个划分子网的ip地址的
`03:47` 32比特的子网掩码
`03:50` 将划分子网的ip地址与相应的子网掩码
`03:54` 进行逻辑与运算
`03:56` 也就是主比特相遇
`03:58` 就可以得到ip地址所在子网的网络地址
`04:02` 如图所示
`04:06` 这样网络号和子网号被保留
`04:10` 而主机号被清零
`04:12` 结果为ip地址所在子网的网络地址
`04:17` 接下来我们来举例说明划分子网的细节
`04:22` 已知某个网络的地址为218.7
`04:25` 5.230点
`04:27` 使用子网掩码255.255.25
`04:30` 5.128
`04:31` 对其进行子网划分
`04:33` 请给出划分细节
`04:37` 我们一起来分析
`04:40` 这是题目给定的网络地址
`04:43` 从它的左起第一个十进制数可知
`04:47` 这是一个c类网络地址
`04:49` 因此网络号占三个字节
`04:52` 主机号占一个字节
`04:56` 这是题目给定的子网掩码
`05:03` 这三个十进制数255
`05:06` 其二进制为24个连续的比特一
`05:10` 他们对应ip地址中的网络号部分
`05:13` 而该子网掩码中的十进制数128
`05:17` 用来表示从ip地址的主机号部分
`05:20` 借用多少比特来作为子网号
`05:24` 我们将其转换为八个二进制比特
`05:28` 其中只有一个比特一
`05:30` 这就表明从主机号部分
`05:33` 借用一个比特作为子网号
`05:36` 因此可划分出的子网数量为二的一次
`05:40` 也就是两个
`05:42` 每个子网可分配的地址
`05:44` 数量为二的8-1次
`05:47` 然后再减去二
`05:48` 也就是126个
`05:51` 由于原来的八位主机号被借走一位作为
`05:55` 子网号
`05:56` 因此主机号还剩七位
`05:59` 这就是表达式中8-1的原因
`06:03` 可有二的七次方个组合
`06:06` 但是我们还要去掉
`06:08` 主机号为全名的网络地址
`06:10` 和全一的广播地址
`06:12` 这就是表达式中减二的原因
`06:16` 我们先来看看题目所给的c类网地址
`06:20` 218.75.230点的细节
`06:24` 28.75.230点
`06:26` 是该网络上的第一个地址
`06:30` 由于这是一个c类网地址
`06:32` 因此网络号占三个字节
`06:35` 主机号占一个字节
`06:38` 将网络号保持不变
`06:40` 而主机号取最大值255
`06:43` 这就是该网络上的最后一个地址
`06:47` 该c类网共有250
`06:49` 六个地址
`06:50` 其中第一个地址作为该网络的网络地址
`06:54` 也就是主机号全名的地址
`06:58` 最后一个地址作为该网络的广播地址
`07:01` 也就是主机号全一的地址
`07:05` 这是比网络地址大一的地址
`07:07` 也就是该网络可分配给主机或路由器接口
`07:12` 的最小地址
`07:14` 这是比广播地址小一的地址
`07:17` 也就是该网络可分配给主机或路由器接口
`07:21` 的最大地址
`07:23` 因此该网络可分配给主机或路由器接口的
`07:27` 地址数量为254个
`07:31` 之前我们已经分析过了
`07:33` 题目所给的子网掩码
`07:35` 表明
`07:36` 从主机号部分借用一个比特作为子网号
`07:40` 也就是将
`07:41` dc类网均分为两个子网
`07:45` 我们将主机号写成巴比特的形式
`07:49` 从主机号借用一个比特作为子网号
`07:53` 子网号只能是零或一
`07:56` 这就是子网龄的网络地址
`07:59` 我们将其写成点分十进制形式
`08:04` 保持网络号和子网号不变
`08:07` 主机号从七个比特零增加到七个比特一
`08:12` 这就是子网林的广播地址
`08:15` 我们将其写成点分十进制形式
`08:20` 这是比子网龄的网络地址大一的地址
`08:24` 也就是子网零
`08:25` 可分配给主机或路由器接口的最小地址
`08:30` 我们将其写成点分十进制形式
`08:35` 这是比子网林的广播地址小e的地址
`08:39` 也就是子网零
`08:40` 可分配给主机或路由器接口的最大地址
`08:45` 我们将其写成点分十进制形式
`08:51` 这是比子网林的广播地址
`08:53` 大一的地址
`08:54` 请注意它的子网号部分为一个比特一
`08:58` 主机号部分为七个比特零
`09:02` 也就是说这是子网易的网络地址
`09:06` 我们将其写成点分十进制形式
`09:11` 保持网络号和子网号不变
`09:13` 主机号从七个比特零增加到七个比特一
`09:18` 这就是子网一的广播地址
`09:21` 我们将其写成点分十进制形式
`09:26` 这是比子网易的网络地址大一的地址
`09:29` 也就是子网一
`09:31` 可分配给主机或路由器接口的最小地址
`09:35` 我们将其写成点分十进制形式
`09:40` 这是比子网一的广播地址小一的地址
`09:44` 也就是子网一可分配给主机或路由器接口
`09:49` 的最大地址
`09:50` 我们将其写成点分十进制形式
`09:55` 可见使用子网掩码255.25
`09:59` 5.255.128
`10:01` 可将c类网218.75.230点
`10:05` 均分为两个子网
`10:08` 接下来请同学们参照我们所举的例子
`10:12` 完成这道练习题
`10:19` 本题的解答如下所示
`10:21` 希望同学们都能正确完成
`10:26` 我们再来练习一道考研题
`10:29` 这是计算机专业考研全国统考
`10:33` 计算机网络部分
`10:35` 2012年的题
`10:36` 39
`10:38` 请大家暂停播放视频
`10:40` 思考一下
`10:42` 给出你的答案
`10:46` 答案是选项d
`10:48` 大家都选对了吗
`10:51` 我们来一起分析一下
`10:54` 这是题目所给的ip地址
`10:58` 从它的左起第一个十进制数可知
`11:01` 这是一个b类网络地址
`11:04` 因此网络号占两个字节
`11:07` 主机号也占两个字节
`11:10` 这是题目所给定的子网掩码
`11:18` 这两个十进制数255
`11:21` 其二进制为16个连续的比特一
`11:24` 他们对应ip地址中的网络号部分
`11:29` 而该子网掩码桩后面两个十进制数
`11:32` 252和零
`11:33` 用来表示从ip地址的主机号部分
`11:37` 借用多少个比特来作为子网号
`11:41` 我们将其转换为16个二进制比特
`11:46` 其中有连续六个比特一
`11:48` 这就表明从主机号部分
`11:51` 借用六个比特作为子网号
`11:55` 我们将ip地址的主机号部分
`11:58` 转换成二进制形式
`12:01` 然后将子网掩码中连续六个比特一
`12:05` 所对应的主机号部分作为子网号
`12:09` 将网络号和子网号保持不变
`12:13` 而将主机号的十个比克全部取零
`12:17` 就可以得到该主机所在子网的网络地址
`12:22` 写成点分十进制
`12:23` 形式为180.80.七六点
`12:28` 将网络号和子网号保持不变
`12:31` 而将主机号的十个比特全部取一
`12:35` 就可以得到该主机所在子网的广播地址
`12:40` 写成点分十进制形式为180.8
`12:43` 0.79.255
`12:46` 因此本题的答案为选项d
`12:51` 最后我们介绍默认子网掩码的概念
`12:55` 默认子网掩码是指在未划分子网的情况下
`12:59` 使用的子网掩码
`13:02` 这是a类地址
`13:04` 由八位网络号和24位主机号构成
`13:08` 根据子网掩码的构成规则
`13:11` 我们用八个连续的比特一
`13:13` 对应ip地址中的八位网络号
`13:16` 用24个连续的比特零
`13:18` 对应ip地址中的24位主机号
`13:22` 这样就构成了a类地址的默认子网掩码
`13:26` 其点分十进制形式为255点点零点
`13:31` 这是b类地址
`13:33` 由16位网络号和16位主机号构成
`13:38` 根据子网掩码的构成规则
`13:40` 我们用16个连续的比特一
`13:43` 对应ip地址中的16位网络号
`13:46` 用16个连续的比特零
`13:48` 对应ip地址中的16位主机号
`13:52` 这样就构成了b类地址的默认子网掩码
`13:56` 其点分十进制形式为255点
`13:59` 255点点零
`14:02` 这是c类地址
`14:03` 由24位网络号和八位主机号构成
`14:08` 根据子网掩码的构成规则
`14:10` 我们用24个连续的比特一
`14:13` 对应ip地址中的24位网络号
`14:16` 用八个连续的比特零
`14:19` 对应ip地址中的八位主机号
`14:22` 这样就构成了c类地址的默认子网掩码
`14:26` 写点分十进制形式为255.255点
`14:30` 255点
`14:33` 本节课到这里就结束了
`14:35` 我们将本节课的内容小结如下
`14:42` 希望大家能重点掌握这部分内容
`14:47` 同学们
`14:48` 我们下节课再见