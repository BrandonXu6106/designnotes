---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=82"
bvid: "BV18g411D7Fm"
cid: "817204326"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817204326&page=82&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍tcp报文段的首部格式
`00:14` 在之前的课程中
`00:16` 我们曾介绍过
`00:18` 为了实现可靠传输
`00:20` tcp采用了面向字节流的方式
`00:23` 如图所示
`00:25` tcp将应用进程交付下来的应用报文看作是字节流存入tcp发送缓存中
`00:33` 但tcp在发送数据时是从发送缓存取出一部分或全部字节
`00:41` 并给其添加一个手部
`00:43` 使之成为tcp报文段后进行发送
`00:52` 一个tcp报文段由首部和数据载荷两部分构成
`01:01` tcp的全部功能都体现在他手部中各字段的作用
`01:06` 接下来我们就来看看tcp报文段的首部格式
`01:11` tcp报文段的首部格式与ip数据报的首部格式类似
`01:16` 都是由20字节的固定手部和最大40字节的扩展手部构成
`01:22` 我们首先来看源端口和目的端口字段
`01:27` 源端口字段占16比特
`01:30` 用来写入原端口号
`01:32` 而原端口号用来标识发送该tcp报文段的应用进程
`01:38` 目的端口号字段占16比特
`01:41` 用来写入目的端口号
`01:44` 而目的端口号用来标识接收该tcp报文段的应用进程
`01:50` 我们来举例说明原端口和目的端口的作用
`01:55` 假设主机中的浏览器进程要访问web服务器中的web服务器进程
`02:01` 为了简单起见
`02:03` 我们仅从运输层端口号这个角度来举例说明
`02:07` 而不考虑其他细节
`02:10` 例如arp域名解析
`02:13` tcp连接建立等
`02:16` 当在浏览器地址栏中输入了web服务器的域名号
`02:20` 浏览器进程会构建一个封装有http请求报文的tcp报文段
`02:28` 该报文段首部中的原端口字段会填写一个短暂端口号
`02:33` 例如49152
`02:36` 用来标识发送该报文段的浏览器进程
`02:41` 目的端口字段会填写熟知端口号80
`02:45` 因为是由http协议的web服务器进程默认监听该端口
`02:51` web服务器收到该tcp报文段后
`02:54` 从中解封出http请求报文
`02:58` 并根据tcp报文段首部中目的端口字段的值80
`03:03` 将http请求报文上交给web服务器进程
`03:08` web服务器进程根据http请求报文的内容进行相应处理
`03:14` 并构建一个http响应报文
`03:18` http响应报文需要封装成tcp报文段进行发送
`03:24` 该报文段首部中的原端口字段会填写熟知端口号
`03:29` 80
`03:30` 用来标识发送该tcp报文段的web服务器进程
`03:35` 而目的端口字段会填写49152
`03:39` 这是主机中需要接收该tcp报文段的浏览器进程所对应的端口号
`03:47` 主机收到该tcp报文段后
`03:49` 从中解封出http响应报文
`03:53` 并根据tcp报文段首部中目的端口字段的值
`03:57` 49152
`03:59` 将http响应报文上交给浏览器进程
`04:04` 浏览器进程对http响应报文的内容进行解析并显示
`04:13` 接下来我们再来看看与tcp实现可靠传输相关的序号字段
`04:19` 确认号字段以及确认标志位
`04:25` 序号字段占32比特
`04:27` 因此取值范围是0~2的32次减一
`04:32` 当序号增加到最后一个号
`04:35` 下一个序号就又回到零
`04:38` 序号字段的值用来指出本tcp报文段数据载荷的第一个字节的序号
`04:45` 例如这是一个tcp报文段
`04:49` 它有手部数据载荷两部分构成
`04:54` 数据载荷中的每个字节数据都有序号
`04:57` 如图所示
`05:01` 请注意它们是字节数据的序号
`05:04` 而不是内容
`05:05` 对于本例首部中序号字段应填入的十进制值为166
`05:12` 用来指出数据载荷的第一个字节的序号为166
`05:18` 确认号字段占32比特
`05:21` 因此取值范围是0~2的32次减一
`05:26` 当确认号增加到最后一个号
`05:28` 下一个确认号就又回到零
`05:32` 确认号字段的值
`05:33` 用来指出希望收到对方下一个tcp报文段的
`05:38` 数据载荷的第一个字节的序号
`05:41` 同时也是对之前收到的所有数据的确认
`05:46` 可以这样理解
`05:48` 若确认号等于n则表明到序号n减一为止的所有数据都已正确接收
`05:56` 期望接收序号为n的数据
`06:00` 只有当确认标志为a c k取值为一时
`06:03` 确认号字段才有效
`06:06` 取值为零时
`06:07` 确认号字段无效
`06:10` tcp规定
`06:11` 在连接建立后
`06:13` 所有传送的tcp报文段都必须把a c k至一
`06:18` 我们来举例说明这三个字段的作用
`06:23` tcp客户进程发送一个tcp报文段
`06:27` 该报文段首部中序号字段的取值为201
`06:31` 这表示该tcp报文段数据载荷的第一个字节的序号为201
`06:38` 假设数据载荷的长度为100字节
`06:42` 首部中确认号字段的取值为800
`06:46` 这表示tcp客户进程收到了tcp服务器进程发来的序号
`06:52` 到799为止的全部数据
`06:55` 现在期望收到序号从800开始的数据
`06:59` 为了使确认号字段有效
`07:01` 手部中的确认标志为a c k的值必须设置为一
`07:07` tcp服务器进程收到该报文段后
`07:11` 也给tcp客户进程发送tcp报文段
`07:15` 该报文段首部中序号字段的取值为800
`07:19` 这表示该tcp报文段数据载荷的第一个字节的序号为800
`07:26` 这正好与tcp客户进程的确认相匹配
`07:31` 假设数据载荷的长度为200字节
`07:34` 首部中确认号字段的取值为301
`07:38` 这表示tcp服务器进程收到了tcp客户进程发来的序号
`07:44` 到300为止的全部数据
`07:47` 现在期望收到序号从301开始的数据
`07:52` 为了使确认号字段有效
`07:54` 手部中的确认标志为a c k的值必须设置为一
`08:00` 我们再来看数据偏移字段
`08:03` 该字段占四比特
`08:05` 并以四字节为单位
`08:07` 用来指出tcp报文段的数据载荷部分的起始处
`08:12` 距离tcp报文段的起始处有多远
`08:16` 该字段实际上指出了tcp报文段的首部长度
`08:22` 手部固定长度为20字节
`08:24` 因此数据偏移字段的最小值为二进制的0101
`08:30` 加上最大扩展手部40字节
`08:33` 手部最大长度为60字节
`08:36` 因此数据偏移字段的最大值为二进制的1111
`08:42` 我们来举例说明
`08:44` 假设这个tcp报文段首部中的数据偏移字段的取值为二进制的0101
`08:51` 那么首部长度就为20字节
`08:54` 因为二进制的0101的十进制值是五
`08:59` 而该字段以四字节为单位
`09:01` 因此5x4字节等于20字节
`09:06` 假设这个tcp报文段首部中的数据偏移字段的取值为二进制的1111
`09:13` 那么首部长度就为60字节
`09:17` 因为二进制1111的十进制值为15
`09:22` 而该字段以四字节为单位
`09:25` 因此15x4字节等于60字节
`09:29` tcp报文段首部中的保留字段占六比特
`09:34` 保留为今后使用
`09:36` 目前应置为零
`09:38` 我们再来看看窗口字段
`09:41` 该字段占16比特
`09:43` 以字节为单位
`09:45` 该字段指出的是发送本报文段的一方的接收窗口
`09:51` 窗口值作为接收方
`09:53` 让发送方设置其发送窗口的依据
`09:56` 这是以接收方的接收能力来控制发送方的发送能力
`10:02` 也就是所谓的流量控制
`10:05` 需要注意的是
`10:07` 发送窗口的大小还取决于拥塞窗口的大小
`10:11` 也就是应该从接触窗口和拥塞窗口中取小者
`10:17` tcp报文段首部中的校验和字段占16比特
`10:22` 用来检查整个tcp报文段在传输过程中是否出现了雾
`10:28` 与udp类似
`10:30` 在计算校验核时
`10:32` 要在tcp报文段的前面加上12字节的为首部
`10:37` 具体的校验算法就不再赘述了
`10:40` 因为它仅仅是一种检测算法
`10:43` 与tcp的其他重要功能相比
`10:46` 检测算法并不是重点
`10:48` 接下来我们来看同步标志为sn
`10:53` 该标志位在tcp连接建立时用来同步序号
`10:58` 如图所示
`10:59` 这是我们之前课程中曾介绍过的
`11:03` tcp通过三报文握手建立连接的过程
`11:08` tcp客户进程发送的tcp连接请求
`11:11` 报文段首部中的同步标志为s yn被制一
`11:16` 表明这是一个tcp连接请求报文段
`11:21` tcp服务器进程发送的tcp连接请求
`11:25` 确认报文段首部中的同步标志为s y n被制一确认为a c k也被质疑
`11:32` 表明这是一个tcp连接请求确认报文段
`11:37` 再来看看中止标志位fn
`11:40` 该标志位用来释放tcp连接
`11:44` 如图所示
`11:46` 这是我们之前课程中曾介绍过的
`11:49` tcp通过四报文挥手释放连接的过程
`11:54` 不管是tcp客户进程还是tcp服务器进程
`11:59` 他们所发送的tcp连接释放报文段首部中的中指标志位fn都被制一
`12:07` 表明这是tcp连接释放报文段
`12:11` 手部中的复位标志为rst
`12:14` 用来复位tcp连接
`12:17` 当rst等于一时
`12:20` 表明tcp连接出现了异常
`12:23` 必须释放连接
`12:25` 然后再重新建立连接
`12:28` rs t g一还可以用来拒绝一个非法的报文段
`12:33` 或拒绝打开一个tcp连接
`12:37` 首部中的推送标志为ph用来实现推送操作
`12:44` 当接收方的tcp收到该标志位为一的报文段
`12:48` 会尽快上交应用进程
`12:51` 而不必等到接收缓存都填满后再向上交付
`12:57` 手部中的紧急标志为u r g和紧急指针字段
`13:02` 用来实现紧急操作
`13:06` 紧急标志为ur g取值为一时
`13:09` 紧急指针字段有效取值为零时
`13:13` 紧急指针字段无效
`13:16` 紧急指针字段占16比特
`13:19` 以字节为单位
`13:21` 用来指明紧急数据的长度
`13:25` 当发送方有紧急数据时
`13:28` 可将紧急数据插队到发送缓存的最前面
`13:32` 并立刻封装到一个tcp报文段中进行发送
`13:37` 紧急指针会指出本报文段数据载荷部分包含了多长的紧急数据
`13:45` 紧急数据之后是普通数据
`13:48` 接收方收到紧急标志
`13:50` 唯一的报文段
`13:52` 会按照紧急指针字段的值从报文段数据载荷部分取出紧急数据
`13:59` 并直接上交应用进程
`14:02` 而不必在接收缓存中排队
`14:06` tcp报文段首部除了20字节的固定部分
`14:10` 还有最大40字节的选项部分
`14:14` 增加选项可以增加tcp的功能
`14:18` 目前有以下选项
`14:21` 最大报文段长度选项
`14:23` 它用来指出tcp报文段数据载荷部分的最大长度
`14:30` 窗口扩大选项
`14:31` 用来扩大窗口
`14:33` 提高吞吐率
`14:36` 时间戳选项有以下两个功能
`14:40` 一个是用来计算往返时间
`14:43` 另一个是用来处理序号超范围的情况
`14:47` 又称为防止序号绕回
`14:51` 选择确认选项
`14:54` 用来实现选择确认功能
`14:58` 由于选项的长度可变
`15:00` 因此还需要使用填充字段来确保报文段首部能被四整除
`15:08` 这是因为数据偏移字段
`15:10` 也就是首部长度
`15:12` 字段是以四字节为单位的
`15:15` 如果选项的长度加上20字节
`15:19` 固定手部的长度不能被四整除
`15:22` 则需要使用填充字段来确保手部能被四整除
`15:28` 本节课到这里就结束了
`15:31` 我们将本节课的内容小结如下
`15:37` 同学们
`15:38` 我们下节课再见