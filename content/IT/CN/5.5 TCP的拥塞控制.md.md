---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=86"
bvid: "BV18g411D7Fm"
cid: "817205262"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817205262&page=86&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍tcp的拥塞控制
`00:14` 首先来看拥塞控制的基本概念
`00:17` 在某段时间
`00:19` 若对网络中某一资源的需求超过了该资源所能提供的可用部分
`00:25` 网络性能就要变坏
`00:27` 这种情况就叫做拥塞
`00:30` 在计算机网络中的链路容量
`00:37` 若出现拥塞而不进行控制
`00:40` 整个网络的存储量将随输入负载的增大而下降
`00:45` 我们使用下图来说明拥塞控制的作用
`00:48` 横坐标是输入负载
`00:51` 代表单位时间内输入给网络的分组数量
`00:54` 纵坐
`00:55` 标是吞吐量代表单位时间内从网络输出的分组数量
`01:01` 具有理想拥塞控制的网络
`01:03` 在吞吐量达到饱和之前
`01:06` 网络吞吐量应等于所输入的负载
`01:09` 故吞吐量曲线是45度的斜线
`01:13` 但当输入负载超过某一限度时
`01:16` 由于网络资源受限
`01:18` 吞吐量就不再增长
`01:20` 而保持水平线
`01:22` 也就是存储量达到饱和
`01:25` 这就表明输入的负载中有一部分损失掉了
`01:30` 例如输入到网络中的某些分组被某个节点丢弃了
`01:35` 虽然如此
`01:37` 在这种理想的拥塞控制作用下
`01:39` 网络的吞吐量仍然维持在其所能达到的最大值
`01:45` 然而实际的网络情
`01:47` 况就很不同了
`01:48` 我们再来看这条吞吐量曲线
`01:51` 随着输入负载的增大
`01:53` 网络吞吐量的增长率逐渐减小
`01:56` 也就是在网络吞吐量还未达到饱和时
`02:00` 就已经有一部分的输入分组被丢弃了
`02:03` 当网络的吞吐量明显的小于理想的吞吐量时
`02:08` 网络就进入了轻度拥塞的状态
`02:11` 更值得注意的是
`02:13` 当输入负载到达某一数值时
`02:16` 网络的吞吐量反而随输入负载的增大而减小
`02:20` 这时网络就进入了拥塞状态
`02:24` 当输入负载继续增大到某一数值时
`02:27` 网络的吞吐量就减小为零
`02:30` 此时网络就无法工作了
`02:33` 这就是所谓的思索
`02:35` 因此
`02:36` 进行拥塞控制是非常有必要的
`02:39` 实际的拥塞控制曲线应该尽量接近理想的拥塞控制曲线
`02:45` 接下来我们介绍tcp的四种拥塞控制算法
`02:50` 它们分别是慢开始拥塞
`02:53` 避免快重传快恢复
`02:58` 我们来举例说明tcp这四种用塞控制算法的基本原理
`03:04` 为了集中精力讨论拥塞控制
`03:07` 我们假定如下条件
`03:09` 一数据是单方向传送的
`03:12` 而另一个方向只传送
`03:14` 确认接收方总是有足够大的缓存空间
`03:20` 因而发送方发送窗口的大小仅由网络的拥塞程度来决定
`03:27` 以tcp最大报文段的个数为讨论问题的单位
`03:31` 而不是以字节为单位
`03:34` 假设这是tcp的发送方和接收方
`03:38` 发送方给接收方发送tcp数据报文段
`03:42` 接收方收到后给发送方发送tcp确认报文段
`03:48` 发送方要维护一个叫做拥塞窗口的状态变量
`03:52` 其值取决于网络的拥塞程度
`03:55` 并且动态变化
`03:58` 拥塞窗口的维护原则是只要网络没有出现拥塞
`04:02` 拥塞窗口就再增大一些
`04:05` 但只要网络出现拥塞
`04:07` 拥塞窗口就减少一些
`04:10` 判断出现网络拥塞的依据是没有按时收到应当到达的确认报文段
`04:16` 也就是发生了超时重传
`04:20` 发送方将拥塞窗口作为发送窗口
`04:23` 也就是发送窗口等于拥塞窗口
`04:28` 发送方还需要维护一个叫做慢开始门限的状态变量
`04:33` 当拥塞窗口小于慢开始门限时
`04:36` 使用慢开始算法
`04:38` 当拥塞窗口大于慢开始门限时
`04:42` 停止使用慢开始算法
`04:44` 而改用拥塞避免算法
`04:47` 当拥塞窗口等于慢开始门限时
`04:50` 既可以使用慢开始算法
`04:52` 也可以使用usb免算法
`04:55` 首先来看慢开始算法
`04:58` 为了更清楚地显示出拥塞控制过程
`05:01` 我们还可以绘制这样一幅拥塞窗口随传输轮次变化的图
`05:07` 横坐标为传输轮次
`05:10` 传输轮次是指发送方给接收方发送数据报文段后
`05:16` 接收方给发送方发回相应的确认报文段
`05:20` 一个传输轮次所经历的时间其实就是往返时间
`05:26` 请注意往返时间并非是恒定的数值
`05:31` 使用传输轮次是为了强调
`05:34` 把u塞窗口所允许发送的报文段都连续发送出去
`05:39` 并收到了对已发送的最后一个报文段的确认
`05:44` 纵坐标是拥塞窗口
`05:46` 它会随网络拥塞程度以及所使用的拥塞控制算法动态变化
`05:53` 在tcp双方建立逻辑连接关系时
`05:56` 拥塞窗口的值被设置为一
`06:00` 我们在图上标出传输轮次零时的拥塞窗口值为一
`06:05` 另外还需设置慢开始门限的初始值
`06:09` 本例采用16
`06:11` 我们也将它在图中标出
`06:14` 在执行慢开始算法时
`06:17` 发送方每收到一个对申报文段的确认时
`06:21` 就把uc窗口值加一
`06:23` 然后开始下一轮的传说
`06:27` 当拥塞窗口值增长到慢开始门限值时
`06:30` 就改为执行拥塞
`06:32` 避免算法
`06:34` 由于发送方当前的拥塞窗口值是一
`06:37` 而发送窗口值等于拥塞窗口值
`06:41` 因此发送方当前只能发送一个tcp数据报文段
`06:46` 换句话说
`06:48` 用塞窗口指示器就能发送几个数据报文段
`06:53` 如图所示
`06:54` 发送方发
`06:55` 送0号数据报文段
`06:57` 接收方收到后给发送方发回对0号报文段的确认报文段
`07:04` 发送方收到该确认报文段后
`07:06` 将拥塞窗口值加一增大到二
`07:10` 我们在图中标出该值
`07:12` 这意味着发送方现在可以发送1~2号共两个数据报文段
`07:19` 接收方收到后给发送方发回对1~2号报文段的确认报文段
`07:25` 发送方收到后将拥塞窗口值加二增大到四
`07:30` 我们在图中标出该值
`07:34` 发送方现在可以发送3~6号共四个数据报文段
`07:39` 接收方收到后给发送方发回对3~6号报文段的确认报文段
`07:46` 发送方收到后将拥塞窗
`07:49` 口值加四增大到八
`07:51` 我们在图中标出该值
`07:54` 发送方现在可以发送七到14号共八个数据报文段
`08:00` 接收方收到后给发送方发回对七到14号报文段的确认报文段
`08:07` 发送方收到后将拥塞窗口值加八增大到16
`08:12` 我们在图中标出该值
`08:15` 发送方当前的拥塞窗口值已经增大到了慢开始门限值之后
`08:22` 我们要改用拥塞避免算法
`08:24` 也就是每个传输轮次结束后
`08:27` 拥塞窗口值只能线性加一
`08:31` 而不像慢开始算法那样
`08:33` 每个传输轮次结束后
`08:35` 拥塞窗口值按指数规律增长
`08:39` 发送方现在可以发送15~30号
`08:42` 共
`08:43` 16个数据报文段
`08:45` 接收方收到后给发送方发回对15~32报文段的确认
`08:51` 报文段发送方收到后将拥塞窗口值加一增大到17
`08:57` 我们在图中标出该值
`09:01` 发送方现在可以发送31~47号共17个数据报文段
`09:07` 接收方收到后给发送方发回对31~47号报文段的确认
`09:14` 报文段发送方收到后
`09:17` 将拥塞窗口值加一增大到18
`09:20` 我们在图中标出该值
`09:24` 随着传输轮次的增加
`09:26` 拥塞窗口值每轮次都线性加一
`09:30` 例如当前拥塞窗口值增加到了24
`09:35` 发送方现在可以发送171
`09:38` 到194号共24个数据报文段
`09:42` 假设这24个数据报文段在传输过程中丢失了几个
`09:48` 这必然会造成发送方对这些丢失报文段的超时重传
`09:54` 发送方以此判断网络很可能出现了拥塞
`09:58` 需要进行以下工作
`10:00` 一将慢开始门限值更新为发生拥塞时拥塞窗口值的一半
`10:08` 网络发生拥塞时的拥塞窗口值是24
`10:12` 因此更新慢开始门限值为该值的一半
`10:16` 解12
`10:18` 如图所示
`10:26` 将拥塞窗口值减小为一
`10:29` 并重新开始执行慢开始算法如图所示
`10:45` 当慢开始执行到拥塞窗口值增大到新的慢开始门限值时
`10:50` 就停止使用慢开始算法
`10:53` 转而执行用塞d点算法
`10:56` 如图所示
`11:05` 通过本地可以看出tcp发送方一开始使用慢开始算法
`11:10` 让拥塞窗口值从一开始按指数规律增大
`11:15` 当拥塞窗口值增大到慢开始门限值时
`11:19` 停止使用慢开始算法
`11:22` 转而执行拥塞
`11:23` 避免算法让拥塞窗口值按线性加一的规律增大
`11:29` 当发生超时重传时
`11:31` 就判断网络很可能出现了拥塞
`11:35` 采取相应的措施
`11:37` 一方面将慢开始门限值更新为发生拥塞时拥塞窗口值的一半
`11:44` 另一方面将拥塞窗口值减小为一
`11:48` 并重新开始执行慢开始算法
`11:51` 拥塞窗口值又从一开始按指数规律增大
`11:56` 当增大到了新的慢开始门限值
`11:59` 时停止使用慢开始算法
`12:02` 转而执行用塞
`12:03` 避免算法让拥塞窗口值按线性加一的规律增大
`12:09` 需要注意的是
`12:10` 慢开始是指一开始向网络注入的报文段少
`12:15` 而并不是指拥塞窗口值增长速度慢
`12:20` 拥塞避免也并非指完全能够避免用塞
`12:24` 而是指在拥塞避免阶段将拥塞窗口值控制为按线性规律增长
`12:31` 使网络比较不容易出现拥塞
`12:36` 那么开始和usb面是1988年就提出的tcp拥塞控制算法
`12:42` 也就是tcp的他后版本
`12:45` 1990年又增加了两个新的拥塞控制算法
`12:52` 这就是快重传和快恢复被
`12:56` 称为tcp的reo版本
`12:59` 有时个别报文段会在网络中丢失
`13:03` 但实际上网络并未发生拥塞
`13:07` 这将导致发送方超时重传
`13:10` 并误认为网络发生了拥塞
`13:14` 例如在之前的例子中
`13:16` 当拥塞窗口值增大到24时
`13:20` 发生了超时重传
`13:22` 而网络此时并没有发生拥塞
`13:25` 但是发送方却误认为网络发生了云塞
`13:30` 于是发送方把拥塞窗口值减小为一并
`13:35` 错误的启动慢开始算法
`13:37` 因而降低了传输效率
`13:40` 采用快充传算法
`13:42` 可以让发送方尽早知道发生了个别报文段的丢失
`13:48` 所谓快重传
`13:49` 就是使发送方尽快进行重传
`13:52` 而不是等超时重传
`13:54` 计时器超时再重传
`13:57` 这就要求接收方不要等待自己发送数据时才进行捎带确认
`14:03` 而是要立即发送确认
`14:05` 即使收到了失去的报文段
`14:07` 也要立即发送
`14:09` 对已收到的报文段的重复确认
`14:12` 发送方一旦收到三个连续的重复确认
`14:16` 就将相应的报文段立即重传
`14:19` 而不是等该报文段的重传
`14:21` 计时器超时再重传
`14:24` 我们来举例说明快重传算法
`14:29` 发送方发送1号数据报文段
`14:31` 接收方收到后给发送方发回对1号报文段的确认
`14:37` 在该确认报文段到达发送方之前
`14:40` 发送方还可以将发送窗口内的2号数据报文段发送出去
`14:46` 接收方收到后给发送方发回对2号报文段的确认
`14:51` 在该确认报文段到达发送方之前
`14:55` 发送方还可以将发送窗口内的3号数据报文段发送出去
`15:00` 但该报文段丢失了
`15:03` 接收方自然不会给发送方发回
`15:06` 针对该报文段的确认
`15:09` 发送方还可以将发送窗口内的4号数据报文段发送出去
`15:14` 接收方收到后发现
`15:17` 这不是按序到达的报文段
`15:19` 因此给发送方发回
`15:22` 针对2号报文段的重复确认
`15:25` 表明我现在希望收到的是3号报文段
`15:29` 但是我没有收到3号报文段
`15:32` 而是收到了未按序到达的报文段
`15:36` 发送方还可以将发送窗口内的5号数据报文段发送出去
`15:42` 接收方收到后发现
`15:44` 这不是按序到达的报文段
`15:46` 因此给发送方发回
`15:49` 针对2号报文段的重复确认
`15:52` 发送方还可以将发送窗口内的6号数据报文段发送出去
`15:58` 接收方收到后发现
`16:00` 这不是按序到达的报文段
`16:02` 因此给发送方发回
`16:05` 针对2号报文段的重复确认
`16:09` 至此发送方会收到三个连续的对2号报文段的重复确认
`16:15` 就立即重传3号报文段
`16:19` 接收方收到后给发送方发回
`16:22` 针对6号报文段的确认
`16:24` 表明序号到六为止的报文段都正确接收了
`16:29` 这样就不会造成对3号报文段的超时重传
`16:34` 而是提早进行了重传
`16:37` 对于个别丢失的报文段
`16:39` 发送方不会出现超时重传
`16:42` 也就不会误认为出现了拥塞
`16:45` 而错误的降低拥塞窗口值为最小之一
`16:49` 使用快充船可以使整个网络的吞吐量提高约20%
`16:56` 再来看快恢复算法
`16:58` 发送方一旦收到三个重复确认
`17:01` 就知道现在只是丢失了个别的报文段
`17:05` 于是不提动漫开始算法
`17:08` 而是执行快恢复算法
`17:11` 发送方将慢开始门限值和拥塞窗口值调整为当前窗口值的一半
`17:17` 开始执行拥塞
`17:19` 避免算法也有的快恢复实现是吧
`17:23` 快恢复开始时的拥塞
`17:25` 窗口值再增大一些
`17:27` 也就是等于新的慢开始门限值加三
`17:31` 这样做的理由是
`17:33` 既然发送方收到三个重复的确认
`17:36` 就表明有三个数据报文段已经离开了网络
`17:40` 这三个报文段不再消耗网络资源
`17:44` 而是停留在接收方的接收缓存中
`17:48` 可见现在网络中不是堆积的报文段
`17:51` 而是减少了三个报文段
`17:54` 因此可以适当把拥塞窗口值扩大一些
`17:59` 接下来我们给出tcp拥塞窗口值在拥塞控制时的变化情况
`18:11` tcp发送方一开始需要慢开始算法
`18:14` 让拥塞窗口值从一开始按指数规律增大
`18:19` 当增大到慢开始门限初始值时
`18:23` 停止使用慢开始算法
`18:25` 转而执行拥塞
`18:26` 避免算法让拥塞窗口值按线性加一的规律增大
`18:32` 当发生超时重传时
`18:35` 就判断网络可能出现了拥塞
`18:37` 采取相应的措施
`18:40` 一方面将慢开始门限值更新为发生拥塞时拥塞窗口值的一半
`18:47` 另一方面将拥塞窗口值减小为一
`18:50` 并重新开始执行慢开始算法
`18:54` 拥塞窗口值又从一开始按指数规律增大
`18:59` 当增大到了新的慢开始门限值时
`19:02` 停止使用慢开始算法
`19:04` 转而执行拥塞
`19:06` 避免算法
`19:07` 让拥塞窗口值按线性加一的规律增大
`19:12` 当发送方收到三个重复的确认时
`19:15` 就要进行快重传和快恢复
`19:19` 也就是更新慢开始门限值为当前拥塞窗口值的一半
`19:24` 并将拥塞窗口值也取为新的慢开始门限值
`19:29` 转而执行拥塞
`19:31` 避免算法
`19:32` 让拥塞窗口值按线性加一的规律增大
`19:41` 这是计算机专业考研全国统考计算机网络部分2009年的题
`19:48` 39
`19:49` 请同学们暂停播放视频
`19:52` 思考一下
`19:53` 给出你的答案
`19:57` 答案是选项c
`19:59` 大家都选对了吗
`20:01` 我们来一起分析一下
`20:04` 根据题意
`20:05` 我们可以画出发送方拥塞窗口值随传输轮次的变化图
`20:12` 这是慢开始部分
`20:14` 当拥塞窗口值增大到慢开始门限的初始值时停止时
`20:19` 要慢开始算法转而执行拥塞
`20:22` 避免算法
`20:24` 根据题意
`20:26` 当拥塞窗口值增大到16k字节时
`20:29` 发生了超时重传
`20:31` 于是将慢开始门限值更新为当前拥塞窗口值的一半
`20:37` 并将拥塞窗口值减小为1k字节
`20:41` 并重新开始执行慢开始算法
`20:45` 当拥塞窗口值增大到新的慢开始门限值时
`20:49` 停止使用慢开始算法
`20:52` 转而执行拥塞
`20:53` 避免算法
`20:55` 可以看到该处就是题目所问的
`20:58` 在超时后的四个往返时间后
`21:02` 也就是在超时后的四个传输轮次结束后
`21:06` 拥塞窗口值增长到9k字节
`21:10` 需要说明的是
`21:11` 题目并未给出慢开始门限值的初始值
`21:16` 因此这部分传输轮次只是示意
`21:20` 目的是为了让大家能够更好地理解本节课到这里就结束了
`21:27` 我们将本节课的内容小结如下
`21:34` 同学们
`21:35` 我们下节课再见