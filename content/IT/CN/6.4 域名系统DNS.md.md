---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=93"
bvid: "BV18g411D7Fm"
cid: "817214091"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817214091&page=93&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍域名系统dns
`00:14` 我们首先来举例说明域名系统的作用
`00:18` 如图所示
`00:19` 英特网中的某台主机要访问某台web服务器
`00:24` 我们只需在用户主机中运行某个浏览器软件
`00:29` 在其地址栏中输入要访问的web服务器的域名
`00:34` 并按下回车键即可访问到web服务器所提供的内容
`00:39` 这是大家最常用的一种网络应用
`00:43` 接下来我们在用户主机中使用拼命令来测试一下
`00:49` 用户主机与web服务器的连通性
`01:01` 可以看到我们拼的是web服务器的域名
`01:05` 但拼命令实际上拼的是web服务器的ip地址
`01:10` 这与我们之前课程中所介绍的tcp ip体系
`01:15` 采用ip地址进行寻址的知识是一致的
`01:19` 也就是说
`01:20` 即使不使用域名
`01:22` 也可以通过ip地址来寻址目的主机
`01:26` 但域名与ip地址相比便于人们记忆
`01:31` 因此对于大多数网络应用
`01:34` 我们一般使用域名来访问目的主机
`01:37` 而不是直接使用ip地址来访问
`01:41` 对于本例
`01:42` 简单来说
`01:44` 当我们在浏览器地址栏中输入某个web服务器的域名时
`01:49` 用户主机会首先在自己的dns高速缓存中查找该域名所对应的ip地址
`01:57` 如果没有找到
`01:59` 则会向网络中的某台dns服务器查询
`02:03` dns服务器中有域名和ip地址映射关系的数据库
`02:09` 当dns服务器收到dns查询报文号
`02:13` 在其数据库中进行查询之后
`02:16` 将查询结果发送给用户主机
`02:20` 现在用户主机中的浏览器可以通过web服务器的ip地址对其进行访问了
`02:29` 请同学们思考一下
`02:31` 英特网是否可以只使用一台dns服务器呢
`02:37` 尽管理论上是可行的
`02:39` 但在实践中这种做法并不可取
`02:43` 因为英特网的规模很大
`02:46` 这样的域名服务器肯定会因为超负荷而无法正常工作
`02:51` 而且一旦域名服务器出现故障
`02:54` 整个英特网就会瘫痪
`02:58` 早在1983年
`03:00` 因特网就开始采用层次结构的命名术作为主机的名字
`03:05` 也就是我们所谓的域名
`03:07` 并使用分布式的域名系统
`03:11` 域名系统是大多数域名都在本地解析
`03:15` 仅少量解析需要在英特网上通信
`03:20` 因此系统效率很高
`03:22` 由于域名系统是分布式系统
`03:26` 即使单个域名服务器出现了故障
`03:29` 也不会妨碍整个系统的正常运行
`03:33` 接下来我们就来看看英特网域名系统所采用的层次
`03:38` 树状结构的域名结构
`03:41` 域名的结构由若干个分量组成
`03:45` 各分量之间用点隔开
`03:48` 分别代表不同级别的域名
`03:50` 如图所示
`03:53` 每一级的域名都由英文字母和数字组成
`03:58` 不超过63个字符
`04:00` 不区分大小写字母
`04:03` 级别最低的域名写在最左边
`04:06` 而级别最高的顶级域名写在最右边
`04:11` 完整的域名不超过255个字符
`04:20` 也不规定每一级的域名代表什么意思
`04:24` 各级域名尤其上一级的域名管理机构管理
`04:28` 而最高的顶级域名则由英特网名称与数字地址分配机构进行管理
`04:36` 这是我校网络信息中心的域名
`04:40` cn是顶级域名
`04:55` 表示湖南科技大学nsa是由我校自行管理的
`05:01` 四级域名表示网络信息中心顶级域名分为以下三类
`05:09` 一类是国家顶级域名
`05:12` 采用iso 3166的规定
`05:15` 如cn表示中国us表示美国uk表示因果
`05:24` 另一类是通用顶级域名
`05:27` 最常见的通用顶级域名有七个
`05:30` 即com表示公司企业
`05:33` net表示网络服务机构o r g表示非盈利性组织
`05:40` int表示国际组织edu表示美国教育机构ga表示美国政府部门
`05:49` ml表示美国军事部门
`05:53` 还有一类是反向域
`05:56` 用于反向域名解析解ip地址
`06:00` 反向解析为域名
`06:12` 将企业教育和企业机构的二级域名定为ac和co
`06:18` 而不用e d o和com
`06:21` 我国则将二级域名划分为以下两类
`06:26` 一类是类别域名共七个
`06:30` ac表示科研机构
`06:33` com表示工商金融等企业
`06:37` edu表示教育机构
`06:41` goov表示政府部门表示提供网络服务的机构
`06:47` ml表示军事机构
`06:50` o r g表示非盈利性组织
`06:54` 另一类是行政区域名共34个
`07:03` 例如bj为北京市
`07:07` 为上海市
`07:09` js为江苏省等等
`07:12` 需要注意的是
`07:14` 名称相同的域名且等级未必相同
`07:18` 例如com是通用顶级域名
`07:22` 但我国顶级域名cn下也有一个名称为com的二级域名
`07:28` 接下来我们举例说明英特网的域名空间
`07:32` 它实际上是一棵倒着生长的树
`07:36` 在最上面的是根
`07:38` 但没有对应的域名
`07:40` 跟下面一集的节点就是顶级域名
`07:44` 顶级域名和往下划分出二级域名
`07:47` 例如这是表示公司企业的顶级域名框下面划分有cctv ibm ti等二级域名
`08:03` 这是表示中国的顶级域名cn下面划分的二级域名
`08:09` 图中所示的域名分别表示上海北京教育机构
`08:15` 政府部门等二级域名
`08:18` 再往下划分就是三级域名
`08:25` cctv下划分的三级域名ma表示邮件系统
`08:31` 这是表示我国教育机构的二级域名
`08:35` edu下划分的三级域名
`08:38` 图中所示的域名分别表示清华大学
`08:42` 湖南科技大学
`08:44` 北京大学
`08:47` 三级域名
`08:48` 再往下划分就是四级域名
`08:51` 例如这是表示湖南科技大学的三级域名
`08:55` hn u s t下划分的一些四级域名
`08:59` 分别表示湖南科技大学的网络信息中心
`09:03` 图书馆
`09:05` 邮件系统
`09:06` 教务处
`09:08` 上述这种按等级管理的命名方法
`09:11` 便于维护域名的唯一性
`09:14` 并且也容易设计出一种高效的域名查询机制
`09:19` 需要注意的是
`09:21` 域名只是个逻辑概念
`09:24` 并不代表计算机所在的物理地点
`09:28` 域名和ip地址的映射关系必须保存在域名服务器中
`09:33` 供所有其他应用查询
`09:37` 显然不能将所有信息都储存在一台域名服务器中
`09:43` dns使用分布在各地的域名服务器来实现域名到ip地址的转换
`09:51` 域名服务器可以划分为以下四种不同类型
`09:55` 一类是根域名服务器
`10:03` 每个根域名服务器都知道所有的顶级域名服务器的域名及其ip地址
`10:11` 英特网上共有13个不同ip地址的根域名服务器
`10:17` 尽管我们将这13个根域名服务器中的每一个都视为单个的服务器
`10:24` 但每台服务器实际上是由许多分布在世界各地的计算机构成的
`10:30` 服务器群集
`10:32` 当本地域名服务器向根域名服务器发出查询请求时
`10:38` 路由器就把查询请求报文
`10:41` 转发到离这个dns客户最近的一个根域名服务器
`10:46` 这就加快了dns的查询过程
`10:50` 同时也更合理地利用了英特网的资源
`10:54` 根域名服务器通常并不直接对域名进行解析
`10:59` 而是返回该域名所属顶级域名的顶级域名服务器的ip地址
`11:07` 另一位是顶级域名服务器
`11:10` 这些域名服务器负责管理在该顶级域名服务器注册的所有二级域名
`11:17` 当收到dns查询请求时
`11:20` 就给出相应的回答
`11:22` 可能是最后的结果
`11:24` 也可能是下一级权限域名服务器的ip地址
`11:29` 还有一类是权限域名服务器
`11:33` 这些域名服务器负责管理某个区的域名
`11:37` 每一个主机的域名都必须在某个权限域名服务器处注册登记
`11:43` 因此权限域名服务器知道其管辖的域名与ip地址的映射关系
`11:51` 另外权限域名服务器还知道其下级域名服务器的地址
`11:57` 最后一类是本地域名服务器
`12:01` 本地域名服务器不属于上述的域名服务器的等级结构
`12:06` 当一个主机发出dns请求报文时
`12:10` 这个报文就首先被送往该主机的本地域名服务器
`12:16` 本地域名服务器起着代理的作用
`12:20` 会将该报文转发到上述的域名服务器的等级结构中
`12:26` 每一个因特网服务提供者
`12:29` 一个大学甚至一个大学里面的学院都可以拥有一个本地域名服务器
`12:36` 它有时也称为默认域名服务器
`12:40` 本地域名服务器离用户较近
`12:43` 一般不会超过几个路由器的距离
`12:47` 也有可能就在同一个局域网中
`12:51` 本地域名服务器的ip地址需要直接配置在需要域名解析的主机中
`12:58` 接下来我们介绍域名解析的过程
`13:06` 一种是递归查询
`13:08` 另一种是迭代查询
`13:11` 首先来看递归查询
`13:14` 假设图中的主机想知道域名y.abc.com的ip地址
`13:21` 主机首先向其本地域名服务器进行递归查询
`13:27` 本地域名服务器收到递归查询的委托后
`13:31` 也采用递归查询的方式向某个根域名服务器查询
`13:36` 根域名服务器收到递归查询的委托后
`13:40` 也采用递归查询的方式向某个顶级域名服务器查询
`13:46` 顶级域名服务器收到递归查询的委托后
`13:50` 也采用递归查询的方式向某个权限域名服务器查询
`13:56` 当查询到域名所对应的ip地址后
`13:59` 查询结果会在之前受委托的各域名服务器之间传递
`14:05` 最终传回给用户主机
`14:10` 再来看迭代查询主机首先向企业本地域名服务器进行递归查询
`14:19` 本地域名服务器采用迭代查询
`14:22` 它先向某个根域名服务器查询
`14:26` 根域名服务器告诉我
`14:28` 本地域名服务器下一次应查询的顶级域名服务器的ip地址
`14:35` 本地域名服务器向顶级域名服务器进行迭代查询
`14:41` 顶级域名服务器告诉本地域名服务器下一次应查询的权限
`14:46` 域名服务器的ip地址
`14:50` 本地域名服务器向全县域名服务器进行迭代查询
`14:56` 权限域名服务器告诉本地域名服务器所查询的域名的ip地址
`15:03` 本地域名服务器最后把查询结果告诉主机
`15:09` 由于递归查询对于被查询的域名服务器负担太大
`15:14` 通常采用以下模式
`15:17` 从请求主机到本地域名服务器的查询采用递归查询方式
`15:24` 而其余的查询是迭代查询方式
`15:29` 为了提高dns的查询效率
`15:31` 并减轻根域名服务器的负担和减少英特网上的dns查询报文数量
`15:38` 在域名服务器中广泛地使用了高速缓存
`15:43` 高速缓存用来存放最近查询过的域名
`15:46` 以及从何处获得域名映射信息的记录
`15:51` 如图所示
`15:52` 如果不久前已经有用户查询过域名为y.abc.com的ip地址
`15:59` 则本地域名服务器的高速缓存中应该存有该域名对应的ip地址
`16:06` 当主机向本地域名服务器递归查询该域名时
`16:11` 本地域名服务器就没有必要再向某个根域名服务器进行迭代查询了
`16:18` 而是直接把高速缓存中存放的上次查询结果
`16:23` 借该域名的ip地址告诉用户
`16:26` 主机需要注意的是
`16:29` 由于域名的ip地址的映射关系并不是永久不变
`16:34` 为保持高速缓存中的内容正确域名服务器因为每项内容设置计时器
`16:42` 并删除超过合理时间的项
`16:45` 例如每个项目只存放两天
`16:50` 不但在本地域名服务器中需要高速缓存
`16:54` 在用户主机中也很需要
`16:57` 许多用户主机在启动时
`17:00` 从本地域名服务器下载域名和ip地址的全部数据库维护
`17:06` 存放自己最近使用的域名的高速缓存
`17:10` 并且只在从缓存中找不到域名时才向域名服务器查询
`17:17` 同理主机也需要保持高速缓存中内容的正确性
`17:27` 这是计算机专业考研全国统考计算机网络部分2010年的题
`17:35` 40
`17:36` 请同学们暂停播放视频
`17:39` 思考一下
`17:40` 给出你的答案
`17:45` 答案是选项a
`17:47` 大家都选对了吗
`17:49` 我们来一起分析一下本题考查的内容
`17:54` 就是我们刚刚才介绍过的
`17:57` 采用递归查询方式进行域名解析的过程
`18:02` 这是主机给其本地域名服务器发送的一条dns查询
`18:08` 由于题目给定本地域名服务器无缓存
`18:12` 因此本地域名服务器收到主机的委托查询后
`18:17` 会向某个根域名服务器发送一条dns查询
`18:23` 再来看2016年的题
`18:25` 40
`18:27` 请同学们暂停播放视频
`18:29` 思考一下
`18:31` 给出你的答案
`18:35` 答案是选项c
`18:37` 大家都选对了吗
`18:40` 我们来一起分析一下
`18:43` 若主机h4 的dns高速缓存中有该域名的dns信息时
`18:50` 不需要查询任何域名服务器
`18:53` 这样h4 无需向图中的默认域名服务器发出dns查询
`19:00` 该域名服务器也无需发出dns查询
`19:05` 题目给定
`19:06` 采用迭代查询方式进行域名解析
`19:10` 在最坏的情况下
`19:13` 需要图中的本地域名服务器依次向以下域名服务器发出共四次dns查询
`19:21` 根域名服务器
`19:24` 顶级域名服务器点com权限域名服务器xyz.com
`19:30` 权限域名服务器a b c.x y z.com
`19:36` 本节课到这里就结束了
`19:39` 我们将本节课的内容小结如下
`19:46` 需要注意的是
`19:47` dns报文是用运输层的udp协议进行封装
`19:53` 运输层端口号为53
`19:56` 至于dn报文的格式就不再赘述了
`20:00` 另外dns污染等安全问题已超出了本系列课程的教学大纲
`20:07` 有兴趣的同学可自行查阅相关资料
`20:12` 同学们
`20:13` 我们下节课再见