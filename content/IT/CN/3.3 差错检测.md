---
title: 3.3 差错检测
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=20
bvid: BV18g411D7Fm
cid: "817183687"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817183687&page=20&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍差错检测的相关知识
`00:14` 我们知道实际的通信链路都不是理想的
`00:18` 比特在传输过程中可能会产生差错
`00:22` 一可能变成零
`00:24` 而零也可能变成一
`00:26` 这称为比特差错
`00:28` 如图所示
`00:30` 比特流在传输过程中
`00:32` 由于受到各种干扰
`00:34` 就可能会出现比特差错
`00:37` 或称为误码
`00:39` 在一段时间内传输错误的比特占所传输比特总数的比率
`00:44` 称为误码率b r大家可以通过动画演示
`00:49` 清楚地看到比特在传输过程中出现了错误
`00:53` 但是接收方的数据链路层又是通过怎样的方法才能知道呢
`01:00` 使用差错检测码来检测数据在传输过程中是否产生了比特差错
`01:06` 是数据链路层所要解决的重要问题之一
`01:10` 例如这是以太网版本二的mark真格式
`01:15` 其真伪中包含了一个长度为四字节的帧检验序列
`01:19` fcs字段
`01:21` 其作用就是让接收方的数据链路层检查
`01:25` 真在传输过程中是否产生了误码
`01:29` 这是点对点协议ppp的真格式
`01:33` 其真伪中也包含了一个长度为两字节的帧检验序列
`01:38` fcs字段
`01:40` 其作用也是让接收方的数据链路层检查正在传输过程中
`01:45` 是否产生了误码
`01:48` 接下来我们介绍基有校验这种比较简单的检测方法
`01:53` 在待发送的数据后面添加一位基友校验位
`01:58` 使整个数据包括所添加的胶原位在内中的一的个数为基数
`02:04` 这称为鸡叫宴或为偶数
`02:08` 则称为偶校验
`02:10` 我们来举例说明
`02:12` 这是在发送的数据
`02:15` 假设收发双方约定采用g校验
`02:19` 那么在数据后面添加的校验位应该为比特一
`02:24` 使得比特一的总数为奇数
`02:28` 假设传输过程中产生了一位密码
`02:31` 我们用红色表示接收方对收到的比特流进行激校验
`02:38` 发现比特一的总数为偶数
`02:41` 而不是奇数
`02:42` 就知道传输过程中发生了误码
`02:46` 换句话说
`02:47` 采用机校验
`02:48` 若比特一的数量的急性改变
`02:51` 可以检查出错误
`02:53` 但是如果传输过程中产生了两位密码
`02:58` 接收方对收到的比特流进行激校验
`03:02` 发现比特一的总数为奇数
`03:05` 就误认为传输过程中没有发生误码
`03:09` 换句话说
`03:10` 采用机校验
`03:12` 若比特一的数量的机性不改变
`03:15` 是无法检查出错误的
`03:19` 假设收发双方约定采用偶校验
`03:23` 那么在数据后面添加的校验位应该为比特零
`03:28` 使得比特一的总数为偶数
`03:31` 假设传输过程中产生了一位密码
`03:35` 接收方对收到的比特流进行偶校验
`03:39` 发现比特一的总数为奇数
`03:42` 而不是偶数
`03:43` 就知道传输过程中发生了误码
`03:47` 换句话说
`03:48` 采用偶校验
`03:50` 若比特一的数量的偶性改变
`03:52` 可以检查出错误
`03:55` 但是如果传输过程中产生了两位密码
`04:00` 接收方对收到的比特流进行偶校验
`04:03` 发现比特一的总数为偶数
`04:06` 就误认为传输过程中没有发生误码
`04:10` 换句话说
`04:12` 采用偶校验
`04:13` 若比特一的数量的偶性不改变
`04:16` 是无法检查出错误的
`04:19` 通过本地可以看出
`04:21` 采用奇偶校验
`04:23` 如果有奇数个未发生物吗
`04:26` 则极偶性会发生变化
`04:29` 也就是畸变偶或者是偶变机
`04:33` 这样就可以检查出物吗
`04:36` 而如果有偶数个位发生物嘛
`04:39` 则基偶性不会发生变化
`04:42` 这是因为每个密码对奇偶性的影响相互抵消了
`04:47` 这样就无法检查出木马
`04:49` 也称为漏检
`04:52` 由于奇偶校验的漏检率比较高
`04:55` 因此计算机网络的数据链路层一般不会采用这种检测方法
`05:01` 接下来我们介绍循环冗余校验cr c
`05:06` 这是一种具有很强检测能力的检测方法
`05:09` 漏检率极低
`05:12` 收发双方约定好一个生成多项式
`05:16` 发送方
`05:17` 基于待发送的数据和生成多项式
`05:20` 计算出差错检测码称为冗余码
`05:24` 并将其添加到带传输数据的后面一起传输
`05:30` 接收方通过生成多项式来计算收到的数据是否产生了误码
`05:36` 我们来看发送方的操作是用除法来计算冗余码
`05:43` 但发送的数据作为被除数的一部分
`05:47` 后面添加生成多项式
`05:49` 最高次数和零已构成被除数
`05:53` 生成多项式
`05:54` 各项系数构成的比特串作为除数进行除法
`05:59` 得到商和余数
`06:03` 余数就是所计算出的冗余码
`06:07` 将其添加到待发送数据的后面一起发送
`06:12` 需要注意的是
`06:13` 冗余码的长度与生成多项式最高次数相同
`06:18` 而熵仅作为标记
`06:20` 防止计算过程中对错位
`06:23` 再来看接收方的处理
`06:26` 也是做除法
`06:28` 被除数就是接收到的数据
`06:32` 除数仍是生成多项式各项系数构成的比特串进行除法
`06:38` 得到商和余数
`06:41` 如果余数为零
`06:43` 可判定传输过程没有产生误码
`06:46` 否则可判定传输过程产生了误码
`06:50` 下面我们对生成多项式举例说明
`06:55` 这是一个生成多项式
`06:59` 这是该生成多项式的完整形式
`07:06` 生成多项式各项系数构成的比特串
`07:09` 就是我们做除法时所用到的除数
`07:14` 这是常用的生成多项式
`07:17` 使用这些生成多项式进行cr c校验
`07:20` 可以达到较好的检测效果
`07:23` 也就是漏检率比较低
`07:27` 需要注意的是
`07:28` cr c算法要求生成多项式必须包含最低次项
`07:34` 接下来我们来做一个练习
`07:38` 该题给定了待发送的信息和一个生成多项式
`07:42` 要求计算余数
`07:44` 我们使用之前介绍的除法来计算
`07:48` 第一步构造被除数
`07:53` 在在发送信息后面添加生成多项式最高次数个零
`07:57` 就可以构造出被除数
`08:01` 这是在发送信息
`08:04` 这是添加生成多项式最高次数的零
`08:09` 第二步构造除数
`08:14` 生成多项式各项系数构成的比特串就是除数
`08:21` 第三步做除法
`08:25` 在单位做标记
`08:28` 将除数移过来
`08:31` 然后进行异或运算
`08:36` 将该位落下来
`08:39` 在该位做标记
`08:41` 一表示构除
`08:44` 将除数移过来
`08:46` 然后进行异或运算
`08:50` 将该位落下来
`08:53` 在单位做标记
`08:55` 零表示不够除
`08:58` 再将单位落下来
`09:01` 在单位做标记
`09:03` 一表示构除
`09:05` 将除数移过来
`09:08` 然后进行异或运算
`09:13` 将单位落下来
`09:15` 在单位做标记
`09:18` 零表示不够除再将单位落下来
`09:23` 在单位做标记
`09:25` 一表示构除
`09:29` 将除数移过来
`09:33` 然后进行异或运算
`09:37` 得到余数为一
`09:41` 第四步检查余数
`09:46` 余数的位数应与生成多项式最高次数相同
`09:50` 如果位数不够
`09:52` 则在余数前补零来凑足位数
`09:57` 将余数添加到待发送信息的后面就可以发送了
`10:03` 我们再来做一个练习
`10:08` 该题给定了接收方收到的信息和一个生成多项式
`10:13` 要求判断传输过程是否产生了误码
`10:18` 我们给出以下步骤
`10:20` 请同学们参照之前的除法过程计算余数
`10:24` 然后根据余数是否为零来判断传输过程是否产生了误码
`10:34` 现在给出答案
`10:35` 大家都做对了吗
`10:39` 需要说明的是
`10:40` 检错码只能检测出真在传输过程中出现了差错
`10:45` 但并不能定位错误
`10:47` 因此无法纠正错误
`10:50` 要想纠正传输中的差错
`10:53` 可以使用荣誉信息更多的纠错码进行前向纠错
`10:58` 但纠错码的开销比较大
`11:00` 在计算机网络中较少使用循环冗余校验
`11:06` cr c有很好的检测能力
`11:09` 漏检率非常低
`11:12` 虽然计算比较复杂
`11:14` 但非常易于用硬件来实现
`11:18` 因此被广泛应用于计算机网络的数据链路层
`11:23` 在计算机网络中
`11:25` 通常采用我们后续课程中将要讨论的检错重传方式来纠正传输
`11:31` 中的差错或者仅仅是丢弃检测到差错的帧
`11:37` 这取决于数据链路层向其上层提供的是可靠传输服务
`11:42` 还是不可靠传输服务
`11:45` 本节课到这里就结束了
`11:48` 同学们
`11:49` 我们下节课再见