---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=57"
bvid: "BV18g411D7Fm"
cid: "817194082"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817194082&page=57&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍ipv 4数据报的首部格式
`00:15` 为了简单起见之后
`00:17` 我们将ipv 4数据报简称为ip数据报
`00:20` 而不指出版本号
`00:23` it数据报的首部格式及其内容
`00:26` 是实现it协议主要功能的基础
`00:29` 因此我们有必要搞清楚这部分内容
`00:34` 如图所示
`00:35` 这是ip数据报的首部格式
`00:38` 它由20字节的固定部分
`00:40` 和最大40字节的可变部分组成
`00:44` 所谓固定部分是指每个ip数据报
`00:51` 而某些ip数据报的首部
`00:57` 可选的字段来增加ip数据报的功能
`01:01` ip数据报的首部
`01:03` 常以32个比特为单位进行描述
`01:06` 图中的每一行都有32个比特
`01:09` 也就是四个字节构成
`01:12` 每个小格子称为字段或者玉
`01:16` 每个字段或某些字段的组合
`01:19` 用来表达ip协议的相关功能
`01:22` 这是版本字段
`01:27` 该字段占四个比特
`01:30` 用来表示ip协议的版本
`01:32` 通信双方使用的ip协议的版本必须一致
`01:36` 目前广泛使用的ip协议的版本号为四
`01:40` 即i p v4
`01:43` 这是首部长度字段
`01:47` 该字段占四个比特
`01:49` 用来表示ip数据报首部的长度
`01:53` 该字段的取值以四字节为单位
`01:59` 表示ip数据报
`02:00` 首部只有20字节的固定部分
`02:06` 表示ip数据报首部
`02:10` 和最大44节的可变部分
`02:14` 这是可选字段
`02:19` 该字段的长度
`02:21` 从一个字节到40个字节不等
`02:24` 用来支持排错测量以及安全措施
`02:28` 可选字段增加了ip数据报的功能
`02:31` 但这同时也使得ip数据报的首部长度
`02:34` 成为可变的
`02:36` 这就增加了每一个路由器
`02:38` 处理ip数据报的开销
`02:41` 实际上可选字段很少被使用
`02:45` 这是填充字段
`02:50` 该字段
`02:50` 用来确保首部长度为四字节的整数倍
`02:54` 使用全零进行填充
`02:57` 我们之前刚刚介绍过首部长度
`03:00` 字段是以四字节为单位的
`03:03` 换句话说
`03:04` ip数据报的首部长度
`03:06` 一定是四字节的整数倍
`03:09` 由于手部中的可选字段的长度
`03:12` 从一个字节到40个字节不等
`03:15` 那么当20字节的固定部分
`03:18` 加上1~40个字节长度不等的可变部分
`03:21` 会造成手部长度不是四字节的整数倍时
`03:25` 就用取值为全零的填充字段
`03:28` 填充相应的字节
`03:30` 以确保ip数据报的首部长度
`03:32` 是四字节的整数倍
`03:36` 这是区分服务字段
`03:40` 该字段占八个比特
`03:43` 用来获得更好的服务
`03:46` 该字段在旧标准中叫做服务类型
`03:49` 但实际上一直没有被使用过
`03:53` 1998年
`03:54` 英特网工程任务组把这个字段改名为
`03:58` 区分服务
`04:00` 利用该字段的不同数值
`04:02` 可提供不同等级的服务质量
`04:05` 只有在使用区分服务时
`04:07` 该字段才起作用
`04:10` 一般情况下都不使用该字段
`04:14` 这是总长度字段
`04:19` 该字段占16比特
`04:21` 用来表示ip数据报的总长度
`04:24` 也就是手部和数据载荷的长度总和
`04:29` 最大取值为十进制的65535
`04:32` 以字节为单位
`04:34` 需要注意的是
`04:36` 在实际应用中很少传输这么长的ip数据报
`04:41` 接下来我们来举例说明
`04:44` 首部长度字段和总长度字段的区别与联系
`04:49` 这是一个ip数据报
`04:51` 它由首部和数据载荷两部分构成
`04:56` 假设手部中的首部长度字段的二进制
`04:59` 取值为0101
`05:01` 则可以计算出ip数据报的首部长度
`05:06` 也就是用首部长度字段的取值
`05:09` 乘以四字节单位共20字节
`05:14` 假设手部中的总长度
`05:18` 000001111111100
`05:23` 则可以计算出ip数据报的总长度
`05:26` 也就是在总长度字段的取值后
`05:29` 添加字节单位共1020个字节
`05:34` 进而可以计算出数据载荷的长度
`05:38` 也就是用ip数据报的总长度
`05:41` 减去ip数据报的首部长度
`05:43` 结果为1000个字节
`05:46` 接下来我们介绍标识标志片偏移
`05:50` 这三个字段
`05:56` 这是网际层封装出的ip数据报
`06:00` 它将在数据链路层封装成真
`06:04` 每一种数据链路层协议
`06:06` 都规定了真的数据载荷的最
`06:08` 大程度称为最大传输单元mt u
`06:13` 例如以太网的数据链路层
`06:15` 规定mt u的值为1500个字节
`06:19` 如果某个ip数据报的总长度超过mt u时
`06:24` 将无法封装成真
`06:26` 需要将原ip数据报分片为更小的ip数据报
`06:31` 再将各分片ip数据报封装成真
`06:35` 标识字段占16个比特
`06:39` 属于同一个数据报的各分片数据报
`06:42` 应该具有相同的标识
`06:44` ip软件维持一个计数器
`06:47` 每产生一个ip数据报
`06:49` 计数器的值就加一
`06:52` 并将此值赋给标识字段
`06:55` 标志字段占三个比特
`06:59` 各比特含义如下
`07:01` df比特表示是否允许分片
`07:06` 这一表示不允许分片
`07:08` 清零表示允许分片
`07:12` mf比特表示本分片后面是否还有分片
`07:17` 这一表示本分片后面还有分片
`07:20` 清明表示本分片就是最后一个分片
`07:25` 还有一个保留位
`07:27` 必须设置为零
`07:29` 偏偏移字段占13个
`07:32` 比特指出分片数据报的数据载荷部分偏移
`07:37` 其在原数据报的位置有多远
`07:41` 偏偏以以八个字节为单位
`07:45` 接下来我们举例说明
`07:47` ip数据报如何进行分片
`07:50` 假设有这样一个ip数据报
`07:53` 它有20字节的固定手部
`07:55` 和3800字节的数据载荷两部分构成
`08:00` 也就是说
`08:01` 该ip数据报的总长为3820个字节
`08:06` 假设是用以
`08:07` 太网传送该ip数据报
`08:11` 以太网的最大传送单元
`08:13` mt u为1500个字节
`08:16` 也就是以太网的数据载荷部分
`08:18` 最大为1500个字节
`08:21` 无法封装3820字节长的ip数据报
`08:26` 因此需要把该ip数据报
`08:29` 分片成几个更小的ip数据报
`08:32` 每个长度不能大于1500个字节
`08:37` 然后再将每个分片ip数据报
`08:39` 封装成一个以太网的帧进行传输
`08:43` 为了更好的描述后续的分片工作
`08:46` 我们将原ip数据报
`08:49` 数据载荷部分的每一个字节都编上号
`08:53` 第一个字节编号为零
`08:55` 最后一个字节编号为3799
`08:59` 我们可将
`09:00` 原ip数据报的数据载荷
`09:02` 分成三个更小的分片
`09:05` 第一个分片
`09:06` 从0号字节到1399号字节
`09:10` 共1400个字节
`09:13` 第二个分片
`09:14` 从1400号字节到2799号字
`09:18` 节
`09:19` 共1400个字节
`09:21` 第三个分片
`09:23` 从2800号字节到
`09:25` 3799号字节
`09:27` 共1000个字节
`09:30` 分片结束后
`09:31` 给每个分片重新添加一个手部
`09:34` 使之成为ip数据报
`09:38` 请同学们思考这样一个问题
`09:41` 给各分片添加的手部是否完全相同呢
`09:47` 我们来一起填一下下面这张表格
`09:51` 原ip数据报首部中的总长度
`09:56` 千800+20
`10:03` mf比特的取值为零
`10:05` 表示后面没有分片数据报
`10:08` 这就是最后一个分片数据报
`10:11` df比特的取值为零
`10:13` 表示该ip数据报允许被分片
`10:17` 由于这是未分片的原ip数据报
`10:20` 因此片偏移为零
`10:24` 我们来填写分片一ip数据报
`10:26` 首部中相关字段的值
`10:29` 总长度
`10:34` 其中1400字节是分片的长度
`10:38` 20字节是固定手部的长度
`10:42` 标识字段的十进制
`10:43` 取值与原ip数据报的相同
`10:47` 由于该分片不是最后一个分片
`10:51` 因此mf比特的取值为
`10:53` 一表示该分片后还有分片
`10:58` 该分片id数据报允许被再次分片
`11:02` 因此df比特的取值为零
`11:06` 该分片ip数据报
`11:08` 数据载荷部分的第一个字节
`11:10` 就是原ip数据报
`11:12` 数据载荷部分的第一个字节
`11:14` 因此片偏移字段的取值为零
`11:18` 除以8÷8的原因是
`11:21` 偏偏移字段以八字节为单位
`11:25` 接下来请同学们填写分片二
`11:28` ip数据报首部中相关字段的值
`11:35` 相信大家都可以正确完成分片二ip数据报
`11:40` 首部中这几个相关字段的值
`11:42` 只有片偏移字段的值
`11:44` 与分片一ip数据报的不同
`11:48` 我们再来填写分片三ip数据报
`11:50` 首部中相关字段的值
`11:54` 总长度
`11:59` 其中1000字节是分片的长度
`12:02` 20字节是固定手部的长度
`12:06` 标识字段的十进制
`12:07` 取值与原ip数据报的相同
`12:11` 由于该分片就是最后一个分片
`12:14` 因此mf比特的取值为零
`12:17` 表示该分片后没有其他分辨
`12:21` 该分片ip数据报允许被再次分片
`12:25` 因此df比特的取值为零
`12:29` 该分配ip数据报数据
`12:31` 载荷的第一个字节是原ip数据报
`12:34` 数据载荷中编号为2800个字节
`12:38` 因此片偏移字段的取值为2800÷8
`12:44` 现在假定分片二的ip数据报
`12:46` 经过某个网络时
`12:48` 还需要再进行分片
`12:51` 其中一个分辨长度为800字节
`12:54` 另一个分辨长度为600字节
`12:57` 分片结束后
`12:59` 给每个分片重新添加一个手部
`13:02` 使之成为ip数据报
`13:05` 接下来请同学们填写该表格
`13:13` 相信大家都可以正确完成
`13:17` 再来看生存时间字段
`13:22` 该字段占八个比特
`13:24` 最初以秒为单位
`13:27` 最大生存周期为255秒
`13:31` 路由器转发ip数据报时
`13:33` 将ip数据报首部中的该字段的值减去
`13:37` ip数据报在本路由器上所耗费的时间
`13:41` 若不为零就转发
`13:43` 否则就丢弃
`13:46` 现在以票数为单位
`13:48` 路由器转发ip数据报时
`13:51` 将ip数据报首部中的该字段的值减一
`13:55` 若不为零就转发
`13:57` 否则就丢弃
`14:00` 接下来我们来举例说明生存时间
`14:04` 字段的作用
`14:06` 假设采用如下图所示的网络拓扑路由器
`14:11` r1 r2 r3 各自的路由表如图所示
`14:16` 为了简单起见
`14:18` 我们只给出了各路由器路由表中
`14:20` 的由人工配置的静态路由条目
`14:24` 例如路由器r2 中的这条静态路由条目
`14:29` 他指出了ip数据报要到达网络n2
`14:33` 吓一跳
`14:34` 需要转发给路由器r3
`14:37` 假设我们在人工配置这条静态路由条目时
`14:41` 错将吓一跳配置成了r1
`14:44` 这会导致将去往网络n2 的ip数据报
`14:48` 错误的转发给路由器r1
`14:51` 我们来看看会出现什么问题
`14:55` 假设某个ip数据报
`14:57` 从网络n一发往网络n2
`15:06` 发现了匹配的路由条目
`15:08` 其吓一跳
`15:10` 只是转发给r2
`15:12` 于是r一将该ip数据报转发给了二
`15:17` 而
`15:17` 收到该ip数据报后进行查表转发
`15:21` 发现了匹配的路由条目
`15:24` 其吓一跳
`15:25` 只是转发给r1
`15:28` 于是r将该ip数据报又转发回了r1
`15:33` 很显然形成了路由环路
`15:36` 如果没有其他措施
`15:39` ip数据报将在路由环路中永久兜圈
`15:43` 现在相信大家能够更好地理解ip数据报
`15:48` 首部中生存时间字段的作用了
`15:51` 那就是确保ip数据报
`15:53` 不会在网络中永久兜圈
`15:57` ip数据报
`15:58` 每经过一个路由器及生存时间的值被减一
`16:03` 当减少到零时
`16:05` ip数据报将被路由器丢弃
`16:09` 再来看协议字段
`16:13` 该字段占八个比特
`16:15` 用来指明ip数据报的数据部分是何种协议
`16:19` 数据单元
`16:21` 常用的一些协议和相应的协议字段值
`16:25` 如下表所示
`16:27` 我们来举例说明
`16:30` 这是一个ip数据报
`16:32` 当手部中协议字段的取值为一时
`16:35` 表明数据部分是sm p报文
`16:39` 也就是有s m p协议封装的协议数据单元
`16:44` 当手部中协议字段的取值为60
`16:48` 表明数据部分是tcp报文段
`16:51` 也就是用tcp协议封装的协议数据单元
`16:56` 这是首部检验合字段
`17:01` 该字段占16个比特
`17:04` 用来检测ip数据报
`17:06` 首部在传输过程中是否出现差错
`17:11` 所采用的检错码比循环冗余检错码
`17:14` 简单称为英特网检验盒
`17:18` 我们就不再赘述了
`17:20` 有兴趣的同学可自行查阅相关资料
`17:25` ip数据报每经过一个路由器
`17:28` 路由器都要重新计算首部检验盒
`17:31` 因为某些字段例如生存时间
`17:35` 标志片偏移等的取值可能会发生变化
`17:40` 由于ip层本身并不提供可靠传输的服务
`17:44` 并且计算首部检验盒是一项耗时的操作
`17:49` 因此在ipv 6中
`17:52` 路由器不再计算首部检验盒
`17:55` 从而更快转发ip数据报
`17:58` 这是原ip地址和目的ip地址字段
`18:04` 他们各占32个比特
`18:07` 用来填写
`18:08` 发送该ip数据报的原主机的ip地址
`18:12` 和接收该ip数据报的目的主机的ip地址
`18:22` 这是计算机专业考研全国统考
`18:25` 计算机网络部分
`18:27` 2018年提47的第二问
`18:31` 请大家暂停播放视频
`18:34` 思考一下
`18:35` 给出你的答案
`18:40` 我们来一起分析一下
`18:43` 这是题目所给的销售部中
`18:46` 某个主机发送的ip分组
`18:49` 总长度为1500个字节
`18:53` 题目给出ip分组的头部长度为24节
`18:57` 因此数据载荷部分
`18:59` 因为1480个字节
`19:02` 为了方便描述
`19:04` 我们将数据载荷中的每一个字节都编上号
`19:08` 第一个字节的编号为零
`19:10` 最后一个字节的编号为1479
`19:15` 题目给出
`19:16` 由于该ip数据报
`19:18` 通过路由器的f一接口转发时
`19:21` 受到所在网络的最大传送单元
`19:24` mt u为800字节的限制
`19:27` 需要进行分片
`19:30` 换句话说
`19:31` 分片后的每一个ip数据报的最大长
`19:34` 度可以是800字节
`19:37` 假设这是分辨一的ip数据报
`19:41` 这是他的20字节固定手部
`19:44` 为了符合题目的要求
`19:46` 尽可能分为最大片
`19:48` 我们将分辨长度取为780个字节
`19:52` 其第一个字节的编号为零
`19:55` 最后一个字节的编号为779
`19:59` 偏偏移量等于0÷8
`20:01` 结果是零
`20:04` 将剩余的700字节作为分片二
`20:08` 其第一个字节的编号为780
`20:11` 最后一个字节的编号为1479
`20:15` 给其添加相应的20字节
`20:17` 首部使之成为分片二的ip数据报
`20:21` 片偏移量等于780÷8
`20:25` 结果为97.5
`20:28` 由于片偏移
`20:30` 量必须为整数
`20:31` 因此这种分配方案不行
`20:34` 我们可将分片的最大长度取为
`20:37` 小于780
`20:39` 且能整除八的最大整数
`20:42` 具体方法是将780÷8的结果
`20:46` 向下取整
`20:47` 然后再乘以八
`20:50` 结果为776
`20:53` 这样分配一的长度为776个字节
`20:57` 剩余704个字节作为分片二
`21:02` 分片一的最后一个字节编号为775
`21:06` 分片二的第一个字节编号为776
`21:10` 这样分片二的片偏移量等于776
`21:15` 除以八
`21:15` 结果为97
`21:19` 再来看2011年的题
`21:21` 47的第一问和第四问
`21:24` 请大家暂停播放视
`21:26` 频思考一下
`21:28` 给出你的答案
`21:33` 我们来一起分析一下
`21:36` 尽管到目前为止
`21:40` web请求是用什么应用层协议封装的
`21:43` 也不知道在运输层
`21:45` 还会用什么协议进行封装
`21:48` 但是我们知道
`21:50` 在网际层一定会用ip协议对其进行封装
`21:54` 使之成为ip分组外部服务器的ip地址
`22:00` 就填写在ip分组头中的目的ip地址字段
`22:05` ip分组还要封装成以太网帧
`22:08` 才能发送
`22:10` 在ip分组投中
`22:12` 目的ip地址字段前还有16个字节的内容
`22:17` 在以太网帧中
`22:19` 数据载荷前还有14字节的内容
`22:23` 因此可知
`22:24` 在以太网帧中
`22:26` 从第31个字节开始的四个字节
`22:29` 实际上
`22:30` 就是目的ip地址的内容
`22:33` 题目所给的以太网数据帧的第一列数据
`22:37` 实际上并不是以太网帧的内容
`22:40` 仅仅作为行号
`22:42` 其增量为16
`22:44` 表示每行有16个字节的内容
`22:48` 这些是以太网帧内容的16进制形式
`22:53` 这些是以太网帧内容的阿斯克码
`22:57` 这些是以太网真的前30个字节
`23:02` 接下来的四个字节
`23:04` 就是外部服务器的ip地址
`23:07` 起点分十进制形式为64.17
`23:11` 0.98.32
`23:14` 从图中可知
`23:16` 主机的默认网关就是路由器
`23:19` r主机会将web请求发送给默认网关
`23:24` 由默认网关将web请求转发到英特网
`23:29` 因此以太
`23:30` 网帧头中的目的mac地址
`23:32` 字段封装的就是默认网关的mac地址
`23:36` 如图所示
`23:43` 而目的mac地址字段
`23:45` 就是以太网帧的前六个字节
`23:47` 其内容就是默认网关的mac地址
`23:51` 写成mac地址的形式为零零杠二幺杠
`23:56` 二七杠
`23:57` 二幺杠
`23:58` 五一杠一一
`24:01` 再来解答第四问
`24:04` ip分组经过路由器r时
`24:06` 生存时间字段的值被减一
`24:12` 手部检验盒会被重新计算
`24:17` 若ip分组总长度大于最大传送单元
`24:22` mt u的值
`24:23` 则需要进行分片
`24:25` 此时总长度
`24:27` 字段标志字段偏偏移字段都需要修改
`24:34` 本节课到这里就结束了
`24:37` 我们将本节课的内容小结如下
`24:43` 同学们
`24:44` 我们下节课再见