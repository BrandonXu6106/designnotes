---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=83"
bvid: "BV18g411D7Fm"
cid: "817204545"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817204545&page=83&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 从本节课开始
`00:12` 我们将分两次课来介绍tcp的运输连接管理
`00:17` tcp是面向连接的协议
`00:20` 它基于运输连接来传送tcp报文段
`00:25` tcp运输连接的建立和释放
`00:27` 是每一次面向连接的通信中
`00:30` 必不可少的过程
`00:33` tcp运输连接有以下三个阶段
`00:38` 第一个阶段是建立tcp连接
`00:41` 也就是通过三报文握手来建立tcp连接
`00:47` 第二个阶段是数据传送
`00:50` 也就是基于已建立的tcp连接
`00:53` 进行可靠的数据传输
`00:57` 第三个阶段是释放连接
`01:00` 也就是在数据传输结束后
`01:03` 还要通过四报文挥手来释放tcp连接
`01:08` tcp的运输连接管理
`01:10` 就是使运输连接的建立和释放
`01:13` 都能正常的进行
`01:16` 本节课我们介绍tcp连接的建立过程
`01:21` tcp的连接建立要解决以下三个问题
`01:25` 第一是tcp双方能够确知对方的存在
`01:32` 使tcb双方能够协商一些参数
`01:35` 例如最大窗口值
`01:37` 是否使用窗口扩大选项和时间戳选项
`01:42` 以及服务质量等
`01:44` 第三是tcb双方能够对运输实体资源
`01:49` 例如缓存大小
`01:51` 连接表中的项目等进行分配
`01:54` 接下来我们就来看看tcp使用三报文握手
`01:59` 建立连接的具体过程
`02:02` 这是两台要基于tcp进行通信的主机
`02:06` 其中一台主题中的某个应用进程
`02:09` 主动发起tcp连接建立
`02:12` 称为tcp客户
`02:14` 另一台主机中被动等待tcp连接
`02:17` 建立的应用进程称为tcp服务器
`02:21` 我们可以将tcp建立连接的过程比喻为握手
`02:27` 握手
`02:27` 需要在tcp客户和服务器之间交换
`02:31` 三个tcp报文段
`02:34` 最初两端的tcp进程都处于关闭状态
`02:40` 一开始tcp服务器进程首先创建传输控制块
`02:45` 用来存储tcp连接中的一些重要信息
`02:50` 例如tcp连接表指向发送和接收缓存的指针
`02:57` 指向重传队列的指针
`03:00` 当前发送和接收序号等
`03:03` 之后就准备接受tcp客户进程的连接请求
`03:09` 此时tcp服务器进程就要进入监听状态
`03:14` 等待tcp客户进程的连接请求
`03:19` tcp服务器进程是被动等待
`03:22` 来自tcp客户进程的连接请求
`03:24` 而不是主动发起
`03:27` 因此称为被动打开连接
`03:31` tcp客户进程也是首先创建传输控制块
`03:35` 然后在打算建立tcp连接时
`03:39` 向tcp服务器进程发送tcp连接请求报文段
`03:44` 并进入同步已发送状态
`03:48` tcp连接请求报文段
`03:50` 首部中的同步位s yn被设置为一
`03:54` 表明这是一个tcp连接请求报文段
`03:58` 序号字段s e q被设置了一个初始值
`04:02` x作为tcp客户进程所选择的初始序号
`04:08` 请注意tcp规定xyn被设置为一的报文段
`04:13` 不能携带数据
`04:15` 但要消耗掉一个序号
`04:18` 由于tcp连接建立
`04:20` 是由tcp客户进程主动发起的
`04:24` 因此称为主动打开连接
`04:28` tcp服务器进程收到tcp连接请求报文段后
`04:32` 如果同意建立连接
`04:34` 则向tcp客户进程发送tcp连接请求
`04:38` 确认报文段
`04:39` 并进入同步已接收状态
`04:43` 该报文段首部中的同部位
`04:45` s yn和确认为a c k都设置为一
`04:50` 表明这是一个tcp连接请求确认报文段
`04:55` 序号字段s e q被设置了一个初始之外
`04:59` 作为tcp服务器进程所选择的初始序号
`05:04` 确认号字段a c k的值被设置成了x加一
`05:09` 这是对tcp客户进程所选择的
`05:12` 初始序号的确认
`05:15` 请注意
`05:16` 这个报文段也不能携带数据
`05:19` 因为它是xyn被设置为一的报文段
`05:23` 但同样要消耗掉一个序号
`05:27` tcp客户进程收到tcp连接请求确认报文段后
`05:32` 还要向tcp服务器进程发送一个普通的tcp
`05:37` 确认报文段
`05:38` 并进入连接已建立状态
`05:42` 该报文段首部中的确认问
`05:44` a c k被设置为一
`05:46` 表明这是一个普通的tcp确认报文段
`05:51` 序号字段s1 q被设置为x加一
`05:55` 这是因为tcp客户进程发送的第一个tcp
`05:59` 报文段的序号为x
`06:02` 并且不携带数据
`06:04` 因此第二个报文段的序号为x加一
`06:09` 请注意tcp规定
`06:12` 普通的tcp确认报文段可以携带数据
`06:16` 但如果不携带数据
`06:18` 则不消耗序号
`06:20` 在这种情况下
`06:22` 所发送的下一个数据
`06:24` 报文段的序号仍是x加一
`06:28` 确认号字段a c k被设置为y加一
`06:32` 这是对tcp服务器进程所选择的初始序号
`06:36` 的确认
`06:38` tcp服务器进程收到该确认报文段后
`06:42` 也进入连接已建立状态
`06:45` 现在tcp双方都进入了连接已建立状态
`06:50` 他们可以基于已建立好的tcp连接
`06:53` 进行可靠的数据传输了
`06:57` 请同学们思考这样一个问题
`07:00` 为什么tcp客户进程最后还要发送一个
`07:05` 普通的tcp确认报文段呢
`07:07` 这是否多余
`07:10` 换句话说
`07:11` 能否使用两报文握手建立连接呢
`07:16` 答案是并不多余
`07:18` 不能简化为两报文握手
`07:21` 我们来举例说明
`07:24` 考虑这样一种情况
`07:27` tcp客户进程发出一个tcp连接请求报文段
`07:32` 但该报文段在某些网络节点长时间滞留了
`07:37` 这必然会造成该报文段的超时重传
`07:41` 假设重传的报文段
`07:43` 被tcp服务器进程正常接收
`07:47` tcp服务器进程给tcp客户进程发送一个tcp
`07:52` 连接请求确认报文段
`07:54` 并进入连接已建立状态
`07:58` 请注意
`07:59` 由于我们改为两道文握手
`08:02` 因此tcp服务器进程发送完tcp连接请求
`08:07` 确认报文段后
`08:08` 进入的是连接已建立状态
`08:11` 而不像三报文握手那样
`08:14` 进入同步已接收状态
`08:17` 并等待tcp客户进程发来
`08:20` 针对tcp连接请求
`08:21` 确认报文段的普通确认报文段
`08:25` tcp客户进程收到tcp连接请求确认报文段后
`08:30` 进入tcp连接已建立状态
`08:33` 但不会给tcp服务器进程发送
`08:36` 针对该报文段的普通确认报文段
`08:40` 现在tcp双方都处于连接已建立状态
`08:45` 他们可以相互传输数据
`08:49` 之后可以通过四报文挥手来释放连接
`08:54` tcp双方都进入了关闭状态
`08:58` 一段时间后
`09:00` 之前滞留在网络中的那个失效的tcp连接请
`09:04` 求报文段到达了tcp服务器进程
`09:08` tcp服务器进程会误认为这是tcp客户进程
`09:13` 又发起了一个新的tcp连接请求
`09:17` 于是给tcp客户进程发送tcp连接请求
`09:21` 确认报文段并进入连接已建立状态
`09:26` 该报文段到达tcp客户进程
`09:30` 由于tcp客户进程并没有发起新的tcp
`09:34` 连接请求
`09:35` 并且处于关闭状态
`09:38` 因此不会理会该报文段
`09:41` 但tcp服务器进程已进入了连接已建立状态
`09:46` 他认为新的tcp连接已建立好了
`09:50` 并一直等待tcp客户进程发来数据
`09:54` 这将白白浪费tcp服务器进程
`09:57` 所在主机的很多资源
`10:01` 综上所述
`10:02` 采用三报文握手
`10:04` 而不是两报文握手来建立tcp连接
`10:08` 是为了防止以失效的连接请求
`10:11` 报文段突然又传送到了tcp服务器进程
`10:16` 因而导致错误
`10:19` 最后我们来做一个相关的练习题
`10:23` 这是计算机专业考研全国统考
`10:27` 计算机网络部分2011年的题
`10:30` 39
`10:32` 请同学们暂停播放视频
`10:34` 思考一下
`10:36` 给出你的答案
`10:40` 答案是选项c
`10:42` 大家都选对了吗
`10:44` 我们来一起分析一下
`10:48` 如图所示
`10:49` 这是本节课所介绍的
`10:51` tcp通过三报文握手建立连接的过程
`10:56` 根据提议
`10:57` 主机甲中的是tcp客户进程
`11:01` 主机已中的是tcp服务器进程
`11:06` 这是主机甲中tcp客户进程
`11:09` 向主机已中tcp服务器进程
`11:12` 发送的第一个tcp段
`11:15` 其首部中的同部位s yn的值为一
`11:19` 序号字段
`11:20` s e q的值就是题目所给的11220
`11:25` 这是主机乙中tcp服务器进程
`11:29` 给主机甲中tcp客户进程发送的tcp连接请求
`11:34` 确认报文段
`11:36` 其首部中的同步位s y n和
`11:40` 确认为a c k的值都被设置为一
`11:44` 表明这是一个tcp连接请求确认报文段
`11:49` 确认号字段
`11:50` a c k的值是对主机甲中
`11:53` tcp客户进程所选择初始序号
`11:56` 11220的确认
`11:58` 因此为11221
`12:02` 至此
`12:03` 我们就已经可以选出正确答案为选项c了
`12:08` 序号字段s e q的值是主机已中
`12:12` tcp服务器进程所选择的初始序号
`12:16` 可由tcp服务器进程随意指定
`12:20` 与其他报文段中的值无关
`12:24` 在本题的正确选项c中
`12:27` 序号字段s e q的值
`12:29` 恰好与确认号字段a c k的值
`12:32` 同为11221
`12:34` 这正是本题迷惑大家的地方
`12:37` 使很多同学认为该选项有点别扭
`12:42` 本节课到这里就结束了
`12:45` 我们将本节课的内容小结如下
`12:51` 同学们
`12:52` 我们下节课再见