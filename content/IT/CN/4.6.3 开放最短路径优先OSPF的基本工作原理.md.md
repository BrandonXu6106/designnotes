---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=61"
bvid: "BV18g411D7Fm"
cid: "817195469"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817195469&page=61&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍开放最短路径优先
`00:13` os spf协议的基本工作原理
`00:17` 开放最短路径优先
`00:18` os spf协议是为克服瑞普协议的缺点
`00:22` 在1989年开发出来的
`00:25` 开放表明
`00:26` os spf不是受某一厂商控制的
`00:30` 而是公开发表的最短路径优先
`00:33` 是因为使用了迪杰斯特拉提出的
`00:36` 最短路径算法
`00:38` ospf是基于链路状态的
`00:42` 而不像rap那样是基于距离向量的
`00:46` os p f采用最短路径优先算法计算路由
`00:50` 从算法上保证了不会产生路由环路
`00:55` ospf不限制网络规模
`00:58` 更新效率高
`00:59` 收敛速度
`01:00` 快链路状态是指
`01:03` 本路由器都和哪些路由器相邻
`01:07` 以及相应链路的代价
`01:10` 代价用来表示费用距离
`01:13` 时延带宽等等
`01:15` 这些都是由网络管理人员来决定的
`01:19` 我们来举例说明
`01:25` 在思科路由器中
`01:27` ospf计算代价的方法是
`01:29` 用100兆比特每秒除以链路带宽
`01:34` 计算结果小于一的值仍记为一
`01:37` 大于一
`01:38` 且有小数的舍去小数
`01:41` 我们来看看图中各路由器的链路状态
`01:45` r一的邻居路由器有r2 相应的链路代价
`01:50` 用100兆比特每秒除以链路带宽
`01:53` 100兆比特每秒
`01:55` 结果是一
`01:57` r一的邻居路由器
`01:59` 还有r4
`02:00` 相应的链路代价
`02:02` 用100兆比特每秒除以链路带宽
`02:05` 1g比特每秒计算结果小于一
`02:09` 但仍即为一
`02:11` 相信同学们可以很容易地得出
`02:14` 其他各路由器的链路状态
`02:16` 我们就不再赘述了
`02:18` os p f相邻路由器之间
`02:21` 通过交互问候分组来建立和维护邻居关系
`02:25` 如图所示
`02:27` 问候分组需要封装在ip数据报中
`02:30` 发送发网主播地址
`02:33` 224点点0.5
`02:36` ip数据报首部中的协议号字段的取值
`02:45` os pf分组问候分组的发送周期为十秒
`02:50` 若40秒仍未收到来自邻居路由器的问候
`02:54` 分组
`02:54` 则认为该邻居路由器不可达
`02:58` 因此每个路由器都会建立一张邻居表
`03:03` 例如这是路由器r一的邻居表
`03:06` 其中的每一个条目对应记录其各邻居
`03:10` 路由器的相关信息
`03:17` 例如r2 是r一的一个邻居路由器
`03:21` 为简单起见
`03:23` 邻居id就记为r2
`03:26` 实践中应填写相应的路由器
`03:29` 该邻居路由器与自己的接口一相连
`03:33` 将接口号记为一
`03:42` 再次收到了来自r2 的问候分组
`03:45` 则重新启动针对该邻居条目的40秒
`03:55` 则判定该邻居路由器不可达
`03:58` r4 是r一的另一个邻居路由器
`04:02` 邻居id就即为二次
`04:04` 该邻居路由器与自己的接口零相连
`04:14` 使用ospf的每个路由器
`04:17` 都会产生链路状态通告
`04:23` 一类是直连网络的链路状态信息
`04:26` 另一类是邻居路由器的链路状态信息
`04:30` 假设n一是路由器r4 的直连网络
`04:34` 则r4 的链路状态通告
`04:44` r一的链路状态信息
`04:47` 以及其邻居路由器r3 的链路状态信息
`04:52` 链路状态通告
`04:53` 被封装在链路状态更新分组中
`04:56` 采用红犯法发送
`04:59` 收到链路状态更新分组的路由器
`05:02` 将从自己其他所有接口转发该分组
`05:06` 也就是进行红泛转发
`05:09` 如图所示
`05:17` 这样自制系统中每个路由器所发送的
`05:21` 封装有链路状态通道的链路状态更新分组
`05:25` 会传递给系统中其他所有路由器
`05:29` 使用ospf的
`05:31` 每个路由器都有一个链路状态数据库
`05:34` 用于存储链路状态通告
`05:37` 通过各路由器红泛发送
`05:40` 封装有自己链路状态通道的
`05:42` 链路状态更新分组
`05:44` 各路由器的链路状态数据库
`05:46` 最终将达到一致
`05:49` 例如这是路由器r的链路状态数据库
`05:53` 其中记录有系统中各路由器的链路状态
`05:57` 通道
`06:06` 使用os pf的各路由器
`06:08` 基于链路状态数据库
`06:10` 进行最短路径优先计算
`06:13` 就可构建出
`06:14` 各自到达其他各路由器的最短路径
`06:18` 也就是构建出各自的路由表
`06:22` 例如有这样一个网络拓扑
`06:25` 各链路旁的数字表示代价
`06:28` 通过各路由器红泛发送
`06:31` 封装有自己链路状态通告的链路状态更新
`06:34` 分组
`06:35` 各路由器最终会得出相同的链路状态
`06:39` 数据库
`06:40` 有链路状态数据库可以得出带权有向图
`06:45` 对该图进行
`06:47` 基于迪杰斯特拉的最短路径优先算法
`06:50` 就可以得出以各路由器为根的最短路径
`06:54` 如图所示
`07:02` 对于这样一个比较简单的网络拓扑
`07:05` 即使大家不懂得最短路径优先算法
`07:08` 也可以很快找出每个路由器
`07:11` 到达其他各路由器的最短路径
`07:15` 但是如果网络拓扑比较复杂
`07:18` 该项工作对人类而言就比较复杂了
`07:22` 因此可以按照狄杰斯特拉提出的最短路径
`07:26` 优先算法编制程序
`07:28` 让路由器执行该程序
`07:32` 如果大家对该算法感兴趣
`07:34` 可以自行查阅相关资料
`07:37` 我们就不再赘述了
`07:39` 对于一般的网络工程师
`07:41` 即便不熟悉该算法
`07:43` 也不影响对os spf协议的配置和使用
`07:53` 一是问候分组
`07:55` 用来发现和维护邻居路由器的可达性类型
`08:00` 二是数据库描述分组
`08:03` 用来向邻居路由器给出自己的链路状态
`08:07` 数据库中的所有链路状态
`08:09` 项目的摘要信息类型
`08:12` 三是链路状态请求分组
`08:14` 用来向邻居路由器请求发送
`08:18` 某些链路状态项目的详细信息类型
`08:22` 四是链路状态更新分组
`08:25` 路由器使用这种分组
`08:27` 将其链路状态进行红范发送
`08:30` 既有红犯法对全网更新链路状态类型
`08:35` 五是链路状态确认分组
`08:38` 这是对链路状态更新分组的确认分组
`08:43` 接下来我们来举例说明
`08:45` ospf协议的基本工作过程
`08:49` 相邻路由器之
`08:50` 间周期性发送问候分组
`08:53` 以便建立和维护邻居关系
`08:56` 建立邻居关系号
`08:58` 给邻居路由器发送数据库描述分组
`09:02` 也就是将自己的链路状态
`09:05` 数据库中的所有链路状态项目的摘要信息
`09:08` 发送给邻居路由器
`09:11` 例如r一收到r的数据库描述分组后
`09:15` 发现自己缺少其中的某些链路状态项目
`09:19` 于是就给r2 发送链路状态请求分组
`09:24` r收到后
`09:25` 将r一所缺少的链路状态项目的详细信息
`09:29` 封装在链路状态更新分组中
`09:32` 发送给r一而一收到后
`09:36` 将这些所缺少的链路状态项目的详细信息
`09:39` 添加到自己的链路状
`09:41` 态数据库中
`09:42` 并给r2 发送链路状态确认分组
`09:47` 需要说明的是
`09:49` 二
`09:49` 也可以像r一请求自己所缺少的链路状态
`09:53` 项目的详细信息
`09:55` 这里我们就不再演示该过程了
`09:59` 最终r一和r2 的链路状态
`10:02` 数据库将达到一致
`10:04` 也就是链路状态数据库达到同步
`10:08` 每30分钟或链路状态发生变化时
`10:12` 路由器都会发送链路状态更新分组
`10:16` 收到该分组的其他路由器
`10:18` 将红泛转发该分组
`10:21` 并给该路由器发回链路状态确认分组
`10:25` 这又称为新情况下的链路状态数据库同步
`10:30` 当os spf路由器在多点接入网络中
`10:34` 建立邻居关
`10:35` 系时如果不采用其他机制
`10:39` 将会产生大量的多波分组
`10:42` 例如这五台路由器
`10:45` 连接在同一个多点接入网络中
`10:48` 他们周期性地发送问候分组
`10:50` 以建立和维护邻居关系
`10:54` 这些路由器中的任意两个路由器
`10:57` 都互为邻居关系
`10:59` 如图所示
`11:10` 邻居关系的数量为n乘以n减一
`11:13` 然后再除以二
`11:15` 其中n是路由器的数量
`11:18` 这样每个路由器要向其他n减一个
`11:22` 路由器发送问候分组和链路状态更新分组
`11:27` 为了减少所发送分组的数量
`11:30` ospf采用全局指定路由器
`11:33` dr和备用的指定路由器bdr的方法
`11:37` 如图所示
`11:45` 所有的非dr bdr只与dr bdr建立邻居关系
`11:51` 如图所示
`11:57` 因此之前的邻居关系数量降低为二乘以
`12:02` n减二
`12:02` 再加一
`12:05` 非dr bdr之间不能直接交换信息
`12:09` 而必须通过dr bdr进行交换
`12:13` 若dr出现问题
`12:15` 则由bdr顶替dr实现
`12:27` 例如路由器优先级
`12:29` 路由器接口ip地址等
`12:38` 这与交换机生成树协议
`12:42` 我们就不再赘述了
`12:45` 为了使ospf协议
`12:47` 能够用于规模很大的网络
`12:50` os spf把一个自制系统再划分为若干个
`12:55` 更小的范围
`12:56` 称为区域
`12:58` 如图所示
`12:59` 这是一个规模很大的网络
`13:02` 我们将其划分成一个自制系统
`13:07` 所有路由器都使用ospf协议
`13:12` ospf
`13:23` 每个区域都有一个32比特的区域
`13:26` 标识符
`13:27` 可以用点分十进制表示
`13:30` 例如主干区域的标识符必须为零
`13:34` 也可表示成点分十进制形式的零点
`13:37` 零点点零
`13:39` 主干区域用于联通其他区域
`13:43` 其他区域的标识符不能为零
`13:45` 且互不相同
`13:48` 每个区域的规模不应太大
`13:55` 划分区域的好处
`13:56` 就是把利用红犯法交换链路状态
`14:00` 信息的范围局限于每一个区域
`14:03` 而不是整个自制系统
`14:05` 这样就减少了整个网络上的通信量
`14:10` 如果路由器的所有接口都在同一个区域内
`14:14` 则该路由器
`14:15` 称为区域内路由器
`14:19` 为了本区域
`14:24` 每个区域都会有一个区域边界路由器
`14:28` 它的一个接口用于连接自身所在区域
`14:32` 另一个接口用于连接主干区域
`14:36` 主干区域内的路由器称为主干路由器
`14:41` 我们也可以把区域边界路由器
`14:43` 看作是主干路由器
`14:46` 在主干区域内还要有一个路由器
`14:52` 交换路由信息
`14:54` 这样的路由器称为自制系统边界路由器
`15:00` 在本例中
`15:02` 区域边界路由器r3
`15:04` 向主干区域发送自己所在区域
`15:07` 一的链路状态通告
`15:09` 向自己所在区域发射区域
`15:12` 023的链路状态通告
`15:15` 区域边界路由器r4
`15:17` 向主干区域发送自己所在区域
`15:21` 二的链路状态通告
`15:23` 向自己所在区域发送区域
`15:25` 013的链路状态通告
`15:30` 区域边界路由器r7
`15:32` 向主干区域发送自己所在区域
`15:35` 三的链路状态通告
`15:37` 向自己所在区域发送区域
`15:39` 012的链路状态通告
`15:44` 采用分层次划分区域的方法
`15:47` 虽然使交换信息的种类增多了
`15:50` 同时也使os spf协议更加复杂了
`15:54` 但这样做却
`15:56` 能使每一个区域内部
`16:01` 因而是ospf协议
`16:08` 本节课到这里就结束了
`16:10` 我们将本节课的内容小结如下
`16:18` 同学们
`16:19` 我们下节课再见