---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=107"
bvid: "BV18g411D7Fm"
cid: "817211665"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817211665&page=107&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍网络层的安全实力
`00:14` 首先介绍p c的协议族
`00:17` 目前在英特网的网际层
`00:20` 运输层和应用层都有相应的网络安全协议
`00:25` epizc是ip security的缩写词
`00:30` 他是为英特网的网际层
`00:32` 提供安全服务的协议族
`00:35` 也有人不太严格地称其为ip赛的协议
`00:40` ab赛克
`00:41` 并不限定
`00:42` 用户使用何种特定的加密和鉴别算法
`00:46` 通信双方可以选择合适的算法和参数
`00:51` 例如密钥长度
`00:53` 为了保证互操作性
`00:59` 所有ip的实现都必须实现这套加密算法
`01:08` 运输方式和隧道方式
`01:11` 运输方式下的ip安全数据报的封装方式为
`01:16` ipsec
`01:17` 将运输层向下交付给网际层的运输层报文
`01:21` 作为ip sg数据载荷
`01:24` 为其添加ip
`01:25` 在手部和尾部之后
`01:29` 网际层再为其添加一个ip首部
`01:32` 使之成为ip安全数据报
`01:39` 为网际层
`01:41` 给运输层向下交付的报文添加一个ip首部
`01:46` 使之成为ip数据报
`01:49` ap赛将该i p数据报作为ip sx数据载荷
`01:53` 为其添加i p赛克首部和尾部之后
`01:58` 网际层再为其添加一个ip首部
`02:02` 使之成为ip安全数据报
`02:06` 无论是用上述哪种方式所封装出的
`02:10` ip安全数据报的手部都是不加密的
`02:14` 这是为了使英特网中的各路由器
`02:17` 能够识别ip安全数据报首部中的相关信息
`02:22` 进而可以将ip安全数据报
`02:24` 在不安全的英特网中
`02:26` 从原点安全地转发到终点
`02:30` 所谓ip安全数据报是指数据报的数据载荷
`02:35` 是经过加密并能够被鉴别的
`02:39` 目前使用最多的是隧道方式
`02:44` 接下来我们介绍安全关联的相关概念
`02:49` 在使用隧道方式传送ip安全数据报之前
`02:53` 应当首先为通信双方
`02:56` 建立一条网际层的逻辑连接
`02:59` 接安全隧道称为安全关联
`03:04` 其英文缩写词为sa
`03:07` 这样传统英特网无连接的网际层
`03:11` 就变成了具有逻辑连接的一个层
`03:15` 提供安全服务的安全关联
`03:17` sa是从原点到终点的单向连接
`03:22` 如果需要进行双向安全通信
`03:25` 则两个方向都需要建立sa ip安全数据报
`03:30` 就在这些sa中传送
`03:33` 我们来举例说明
`03:35` 在路由器之间建立安全关联
`03:38` 如图所示
`03:40` 路由器r一和r2
`03:42` 分别是公司总部和分公司的防火墙
`03:46` 中的路由器
`03:47` 他们各自负责为其所在部门收发ip数据报
`03:53` 因此公司总部与分公司之间的安全关联sa
`03:58` 可以建立在r一和r2 之间
`04:02` 假设公司总部中的主机a
`04:04` 要给分公司中的主机b
`04:06` 通过因特网中已建立好的安全关联sa
`04:10` 发送ip数据报
`04:13` 主机a必须把该数据报发送给路由器r1
`04:18` 该数据报
`04:19` 首部中的原地址为主机a的ip地址
`04:22` 目的地址为主机b的ip地址
`04:27` r一对该数据报进行ip c的加密
`04:30` 并为其添加一个新的ip首部
`04:34` 使之成为ip安全数据报
`04:37` 在新的ip首部中的原地址为r一的ip地址
`04:42` 目的地址为r2 的ip地址
`04:46` ip安全数据报
`04:47` 经过英特网中多个路由器的转发后到达r
`04:56` 还原出原始的ip数据报
`05:00` 并根据原始ip数据报首部中的目的地址
`05:04` 将其直接交付给主机b
`05:11` 从逻辑上看
`05:13` ip安全数据报在安全关联sa中的传送
`05:25` 由于都在公司内部
`05:28` 而不需要经过不安全的因特网
`05:31` 因此不需要建立sa
`05:35` 主机a发送的ip数据报
`05:37` 只需通过路由器r一转发一次
`05:40` 就可传送给主机c
`05:43` 如果主机a想要上网浏览一些新闻
`05:47` 也不需要建立sa
`05:49` 只需通过路由器r1
`05:51` 收发普通的ip数据报即可
`05:55` 我们再来举例说明
`05:56` 在路由器和主机之间建立安全关联
`06:01` 如图所示
`06:04` 假设公司总部中的主机
`06:06` a要和正在外地出差的
`06:08` 某个员工的主机b进行安全通信
`06:12` 在这种情况下
`06:14` 公司总部中的路由器r1
`06:16` 与外地员工的主机b之间
`06:19` 需要建立安全关联
`06:21` sa
`06:22` 主机a必须把ip数据报发送给路由器r1
`06:27` 该数据报
`06:28` 首部中的原地址为主机a的ip地址
`06:32` 目的地址为主机b的ip地址
`06:39` 并为其添加一个新的ip首部
`06:43` 使之成为ip安全数据报
`06:46` 在新的ip首部中的原地址为r一的ip地址
`06:52` 目的地址为b的ip地址
`06:55` ip安全数据报
`06:56` 经过英特网中多个路由器的转发
`06:59` 最终到达主机b主机b中的ip sc
`07:03` 对ip安全数据报进行鉴别和解密
`07:07` 还原出主机a发送的原始ip数据报
`07:11` 从逻辑上看
`07:13` ip安全数据报
`07:15` 是在路由器r1
`07:16` 和员工的主机b之间的安全隧道中传送的
`07:21` 建立安全关联sa的路由器或主机
`07:25` 需要维护这条sa的状态信息
`07:33` 称为安全参数索引
`07:35` 其英文缩写词为s p i
`07:39` sa的原点和终点的ip地址
`07:42` 对于本地
`07:43` 就是路由器r一和主机b的ip地址
`07:49` 所使用的加密类型
`07:51` 例如ds或a加密的密钥
`07:58` 完整性检查的类型
`08:00` 例如使用报文摘要
`08:02` md 5或上衣的豹纹鉴别码
`08:07` 鉴别使用的密钥
`08:10` 当路由器r一要通过安全关联sa
`08:13` 传送ip安全数据报时
`08:16` 首先需要读取sa的状态信息
`08:20` 以便知道应当如何加密和鉴别ip数据报
`08:25` 接下来我们介绍ip安全数据报的格式
`08:30` 在i p c的协议族中有两个主要的协议
`08:35` 鉴别手部协议和封装安全有效载荷协议
`08:41` 鉴别手部的英文缩写词为a h
`08:45` 封装安全有效载荷的英文缩写词为esp
`08:59` e s p协议比h协议复杂得多
`09:09` ap sx既支持ipv 4
`09:12` 也支持ipv 6
`09:14` 在ipv 6中
`09:16` h协议和esp协议都是扩展手部的一部分
`09:27` 因此使用esp协议就无需使用h协议
`09:33` 下面不再介绍aa制协议
`09:36` 而只介绍esp协议的要点
`09:40` 使用e s p协议或a h协议的ip数据报
`09:43` 称为ip安全数据报或ip数据报
`09:48` 它可以在两台主机之间
`09:51` 两台路由器之间或一台主机和一台路由
`09:55` 器之间的安全关联
`09:56` sa也就是安全隧道中传送
`10:01` 在隧道方式下
`10:03` 构建ip安全数据报的过程如下
`10:07` 这是原始的ip数据报
`10:10` 它作为esp的有效载荷
`10:21` 第一个字段是填充字段
`10:23` 用全民进行填充
`10:26` 第二个字段是填充长度
`10:29` 字段用来指出填充字段的长度
`10:32` 以字节为单位进行填充的原因如下
`10:38` 在进行数据加密时
`10:41` 通常都要求数据块的长度
`10:43` 是若干字节的整数倍
`10:46` 例如四字节的整数倍
`10:49` 当原始ip数据报的长度不满足此条件时
`10:53` 就必须使用全零字节进行填充
`10:58` 尽管
`10:59` 填充长度字段占八比克
`11:01` 其最大值为255
`11:04` 但实际上很少填充255个字节
`11:09` 第三个字段是下一个首部字段
`11:12` 其实用来指明在接收端
`11:16` 应将esp的有效载荷交给什么协议来处理
`11:21` 如果采用的是隧道方式
`11:23` 则esp的有效载荷为原始的ip数据报
`11:28` 因此esp尾部中的下一个
`11:31` 首部字段的取值为四
`11:33` 指明的就是原始ip数据报的首部
`11:38` 如果采用的是运输方式
`11:40` 则esp的有效载荷就是tcp报文段或udp
`11:45` 用户数据报
`11:47` 则usb尾部中的下一个首部字段的取值
`11:51` 也要改为相应的数值
`11:54` 按照安全关联sa指明的加
`11:57` 密算法和密钥
`11:59` 对esp的有效载荷以及esp尾部进行加密
`12:14` 第一个字段用来存放安全参数索引spi
`12:19` 在同一个安全关联sa中
`12:21` 传送的ip安全数据报都使用同样的spi值
`12:27` 第二个字段是序号字段
`12:30` 用于鉴别时
`12:31` 防止重放攻击
`12:34` 请注意当分组重传时
`12:37` 序号并不重复
`12:40` 这样就构建出了需要鉴别的部分
`12:45` 按照安全关联sa指明的算法和密钥
`12:49` 对需要鉴别的部分生成豹纹鉴别码
`12:52` mac将其添加在esp尾部的后面
`12:57` 这样就构建出了ip安全数据报的数据载荷
`13:03` 给ip安全数据报的数据载荷
`13:06` 添加一个新的ip首部
`13:08` 使之成为ip安全数据报
`13:12` 新的ip首部中协议字段的取值为50
`13:16` 表明在接收端
`13:18` 应将ip安全数据报的数据载荷
`13:21` 交给esp协议来处理
`13:25` 在隧道方式下
`13:27` 接收方处理ip安全数据报的过程如下
`13:31` 这是接收方收到的ip安全数据报
`13:35` 这是其数据载荷的构成细节
`13:39` 根据ip安全数据报
`13:41` 首部中协议字段的取值为50
`13:43` 可知
`13:45` 应将该数据报的数据载荷
`13:47` 用esp协议进行处理
`13:51` esp检查esp首部中的安全参数索引
`13:55` spi字段的值
`13:57` 已确定收到的ip安全数据报
`13:59` 属于哪一个安全关联
`14:01` sa对鉴别部分计算报文鉴别码
`14:06` 并与尾部的豹纹鉴别码比较
`14:10` 如果相符
`14:11` 就可以确定收到的数据报确实来自发送方
`14:17` 根据usp手部中的序号
`14:19` 可判断出是否被攻击者进行了重放攻击
`14:24` 若没有遭到重放攻击
`14:27` 则用之前确定的安全关联
`14:29` sa所指明的加密算法和密钥
`14:32` 对加密部分进行解密
`14:36` 完成解密后
`14:37` 根据esp尾部中的填充长度删除发送端
`14:42` 填充到所有权林字节
`14:45` 还原出加密前的esp有效载荷
`14:48` 也就是原始的ip数据报
`14:52` 根据解密后得到的esp尾部中
`14:55` 下一个首部字段的取值四
`14:57` 可知
`14:59` 应将esp有效载荷交付给ip来处理
`15:04` 通过上述介绍可知
`15:06` 若某个ip安全数据报
`15:08` 在英特网中被某个攻击者截获
`15:12` 只要攻击者不知道该ip安全数据报的密码
`15:16` 攻击者就仅能知道该ip安全数据报
`15:20` 的首部信息
`15:21` 例如原ip地址和目的ip地址
`15:25` 但无法看懂其数据载荷的含义
`15:29` 即使攻击者故意删除了该ip安全数据报的
`15:33` 数据载荷中的一些字节
`15:36` 由于接收端能够进行完整性验证
`15:40` 因此不会接受这种含有差错的数据报
`15:45` 如果攻击者尝试重放攻击
`15:48` 但由于ip安全数据报使用了有效的序号
`15:53` 因此使得重放攻击也不能成功
`15:58` 最后我们简单介绍一下ip set的其他构建
`16:03` 分别是安全关联数据库
`16:07` 安全策略数据库
`16:11` 因特网密钥交换协议
`16:15` 发送ip安全数据报的实体
`16:17` 也就是路由器或主机
`16:19` 使用安全关联数据库sd来存储
`16:24` 可能要用到的很多条安全关联sa
`16:28` 当主机要发送ip安全数据报时
`16:31` 会在sd中查找相应的sa
`16:35` 以便获取对该ip安全数据报
`16:38` 实施安全保护的必要信息
`16:42` 同理当主机接收ip安全数据报时
`16:46` 也要在s a d中查找相应的sa
`16:50` 以便获取检查该ip安全数据报
`16:53` 的安全性的必要信息
`16:56` 安全策略数据库sp d
`16:58` 指明了什么样的ip数据报
`17:01` 需要进行ip sk处理
`17:03` 这取决于原地址
`17:06` 原端口
`17:07` 目的地址
`17:08` 目的端口以及协议的类型等
`17:12` 根据应用需求
`17:13` 主机所发送的ip数据报
`17:16` 并非全部都需要进行加密
`17:19` 很多信息使用普通的ip数据报
`17:22` 用明文发送即可
`17:24` 因此对于一个ip数据报
`17:27` sp d指出是否需要使用ip安全数据报
`17:32` 如果需要
`17:33` 则sad指出应当使用哪一个安全关联sa
`17:39` 有的同学可能会有这样的疑问
`17:42` 安全关联数据库
`17:44` sd中存放的许多安全关联sa
`17:48` 是怎样建立起来的呢
`17:51` 如果一个使用ipsec的虚拟专用网
`17:58` 则
`17:58` 用人工配置的方法
`18:00` 就可以建立其所需的s a d
`18:08` 则人工配置的方法几乎是不可能的
`18:12` 因此对于大型的地理位置分散的系统
`18:17` 为了创建sad需要使用自动生成的机制
`18:22` 而英特网密钥交换协议
`18:25` i k e就提供了这样的机制
`18:28` 他为ip安全数据报自动创建安全关联sa
`18:32` 而无需人工配置
`18:36` 本节课到这里就结束了
`18:39` 请同学们快速回忆一下本节课的主要内容
`19:17` 同学们
`19:18` 我们下节课再见