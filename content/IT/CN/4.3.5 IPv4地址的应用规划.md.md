---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=54"
bvid: "BV18g411D7Fm"
cid: "817193503"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817193503&page=54&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍ipv 4地址的应用规划
`00:14` 也就是给定一个ipv 4地址块
`00:17` 如何将其划分成几个更小的地址块
`00:21` 并将这些地址块分配给互联网中的不同网络
`00:26` 进而可以给各网络中的主机和路由器接口分配
`00:30` ipv 4地址一般有以下两种方法
`00:35` 一种是采用定长的子网掩码进行划分
`00:39` 另一种是采用变长子网掩码进行划分
`00:44` 所谓正常的子网掩码就是使用同一个子网掩码来划分子网
`00:50` 所谓变长的子网掩码就是使用不同的子网掩码来划分子网
`00:56` 采用定长的子网掩码来划分子网
`00:59` 每个子网所分配的ip地址数量相同
`01:03` 容易造成ip地址浪费
`01:06` 而采用变长的子网掩码来划分子网
`01:10` 每个子网所分配的ip地址数量可以不相同
`01:13` 尽可能减少了对ip地址的浪费
`01:17` 接下来我们举例说明
`01:20` 采用定长的子网掩码划分子网的方法
`01:24` 假设我们申请到了一个c类网络
`01:27` 218.75.230点
`01:31` 要使用定长的子网掩码
`01:33` 给图中所示的小型互联网中的各设备分配ip地址
`01:39` 我们首先来统计一下图中各网络所需的ip地址数量
`01:44` 来看网络n一的ip地址需求
`01:48` n一中有六台主机
`01:50` 因此主机需要的地址数量为六个
`01:54` 有一个路由器接口
`01:57` 因此路由器接口需要的地址数量为一个
`02:01` 再加上n一自身的网络地址
`02:04` 一个和广播地址一个
`02:07` 总共需要九个ip地址
`02:10` 再来看网络n2 的ip地址需求
`02:14` n2 中有25台主机
`02:17` 因此主机需要的地址数量为25个
`02:21` 有一个路由器接口
`02:23` 因此路由器接口需要的地址数量为一个
`02:28` 再加上n2 自身的网络地址和广播地址
`02:33` 一个总共需要28个ip地址
`02:37` 相信同学们可以很容易地统计出网络n3 和n4
`02:42` 各自所需的ip地址数量
`02:45` 我们就不再赘述了
`02:48` 请注意我们可将图中两个路由器之间的这段链路看成是网络n5
`02:55` 其中没有主机
`02:57` 因此主机需要的地址数量为零
`03:01` 有两个路由器接口
`03:03` 因此路由器接口需要的地址数量为两个
`03:08` 再加上n5 自身的网络地址和广播地址
`03:12` 一个总共需要四个ip地址
`03:16` 这样我们就可得到本地的应用需求
`03:20` 也就是将c类网218.75.230点划分成五个子网
`03:27` 每个子网上可分配的ip地址数量不得少于各自的需求
`03:33` 这是所申请到的c类网地址
`03:36` 218.75.2
`03:38` 三零点
`03:40` 这是它的三字节网络号
`03:44` 这是他的一字节主机号
`03:47` 我们需要从主机号部分借用三个比特作为子网号
`03:53` 这样可划分出的子网数量为二的三次方个
`03:57` 也就是八个
`03:59` 可以满足我们对子网数量的要求
`04:03` 而每个子网上的地址数量为2~8减三次个
`04:08` 也就是32个
`04:10` 可以满足我们对子网上ip地址数量的要求
`04:15` 这样我们就可以得出划分子网所需的子网掩码
`04:22` 用三个十进制数255来对应网络号部分
`04:27` 也就是用24个连续的比特一来对应网络号
`04:32` 而最后八个比特为1110万
`04:37` 其中三个连续的比特一表示从主机号部分借用三个比特作为子网号
`04:45` 将这巴比特写成十进制
`04:47` 数为224
`04:49` 接下来我们来看划分子网的细节
`04:55` 这是24比特网络号部分
`05:00` 这是从主机号部分借用的三比特作为子网号部分
`05:07` 这是剩余的五比特主机号部分
`05:13` 这是子网易的网络地址
`05:15` 这是它的点分十进制形式
`05:19` 将网络号和子网号保持不变
`05:23` 主机号从五个比特零增长到五个比特一
`05:27` 就可得到子网易的广播地址
`05:30` 这是它的点分十进制形式
`05:34` 这是比子网易的网络地址大一的地址
`05:38` 它是子网仪上可分配给主机或路由器接口的最小地址
`05:44` 这是他的点分十进制形式
`05:48` 这是比子网易的广播地址小一的地址
`05:52` 它是子网仪上可分配给主机或路由器接口的最大地址
`05:58` 这是它的点分十进制形式
`06:02` 这是比子网易的广播地址大一的地址
`06:06` 它是子网二的网络地址
`06:10` 这
`06:10` 是它的点分十进制形式
`06:14` 将网络号和子网号保持不变
`06:17` 主机号从五个比特零增长到五个比特一
`06:21` 就可得到子网二的广播地址
`06:24` 这是它的点分十进制形式
`06:29` 这是比子网二的网络地址大一的地址
`06:33` 它是子网二上可分配给主机或路由器接口的最小地址
`06:39` 这是它的点分十进制形式
`06:43` 这是比子网二的广播地址小一的地址
`06:47` 它是子网二上可分配给主机或路由器接口的最大地址
`06:53` 这是它的点分十进制形式
`06:57` 这是比子网二的广播地址大一的地址
`07:01` 它是子网三的网络地址
`07:04` 这是它的点分十进制形式
`07:09` 此类推我们可以得到划分子网的全部细节
`07:14` 现在我们就可以从子网1~8周任选五个分配给图中的网络
`07:21` n一到n5
`07:25` 通过本地可以看出
`07:27` 采用定长的子网掩码进行子网划分
`07:31` 只能划分出二的n次方个子网
`07:34` 其中n是从主机号部分借用的
`07:38` 用来作为子网号的比特数量
`07:41` 每个子网所分配的ip地址数量相同
`07:45` 容易造成ip地址的浪费
`07:48` 例如图中的网络五只需要四个ip地址
`07:53` 但是我们只能给它分配32个ip地址
`07:57` 这样就造成了ip地址的严重浪费
`08:02` 接下来我们举例说明
`08:04` 采用变长的子网掩码划分子网的方法
`08:09` 本例与之前的例子基本一样
`08:12` 只是划分子网的方法
`08:14` 改为采用变长的子网掩码
`08:18` 这是我们之前就统计过
`08:20` 的各网络所需ip地址的数量
`08:24` 既然网络n一需要九个地址
`08:27` 那么分配给网络n一的地址的主机号
`08:31` 因为四个比特
`08:33` 因为二的四次方等于16
`08:36` 这样网络n一就可以有16个地址
`08:41` 由于使用四个比特作为主机号
`08:44` 因此剩余28个比特可作为网络前缀
`08:50` 既然网络n2 需要28个地址
`08:53` 那么分配给网络n2 的地址的主机号应该为五个比特
`08:59` 因为二的五次方等于32
`09:02` 这样网络n2 就可以有32个地址
`09:06` 由于使用五个比特作为主机号
`09:10` 因此剩余27个比特可作为网络前缀
`09:15` 相信同
`09:16` 学们可以很容易地得出
`09:18` 分配给网络n3 n4 n5 的地址中的
`09:22` 主机号所占比特数量和网络前缀所占比特数量
`09:27` 我们就不再赘述了
`09:31` 我们将所得出的各网络所需地址块的大小标注在他们各自的旁边
`09:39` 这样我们就可得到本地的应用需求
`09:43` 也就是从地址块218.75.230点斜线24周
`09:49` 取出五个地址块
`09:52` 按需分配给图中所示的五个网络
`10:03` 如下所示
`10:07` 我们需要在该地址块中给图中所示的网络n一到n5 分配子块
`10:14` 分配的原则是
`10:16` 每个字块的起点位置不能随意选取
`10:19` 只能选取块大小整数倍的地址作为起点
`10:24` 建议大家先给大的紫块进行分配
`10:28` 例如将这32个地址作为一个地址块分配给网络n2 地址块中
`10:36` 最小的地址就是n2 的网络地址
`10:40` 最大的地址就是n2 的广播地址
`10:44` 网络地址和广播地址之间的
`10:47` 这些地址可分配给n2 中的主机或路由器接口
`10:52` 我们将网络n2 所分配到的地址块标注在它的旁边
`10:58` 将这16个地址作为一个
`11:01` 地址块分配给网络n一地址块中最小的地址就是n一的网络地址
`11:08` 最大的地址就是n一的广播地址
`11:12` 网络地址和广播地址之间的这些地址
`11:15` 可分配给n一中的主机或路由器接口
`11:20` 我们将网络n一所分配到的地址块标注在它的旁边
`11:26` 将这16个地址作为一个地址块分配给网络
`11:30` n3 地址块中最小的地址就是n3 的网络地址
`11:36` 最大的地址就是n3 的广播地址
`11:41` 网络地址和广播地址之间的这些地址
`11:44` 可分配给n3 中的主机或路由器接口
`11:48` 我们将网络n3 所分配到的地址块标注在它
`11:52` 的旁边
`11:55` 将这16个地址作为一个地址块分配给网络
`11:59` n4 地址块中最小的地址就是n4 的网络地址
`12:05` 最大的地址就是n4 的广播地址
`12:09` 网络地址和广播地址之间的这些地址
`12:13` 可分配给n4 中的主机或路由器接口
`12:17` 我们将网络n4 所分配到的地址块标注在它的旁边
`12:23` 将这四个地址作为一个地址块分配给网络
`12:27` n5 地址块中最小的地址就是n5 的网络地址
`12:33` 最大的地址就是n5 的广播地址
`12:37` 网络地址和广播地址之间的这些地址
`12:40` 可分配给n5 中的主机或路由器接口
`12:44` 我们将网络n
`12:46` 五所分配到的地址块标注在它的旁边
`12:51` 原地址块中还剩余这些地址可以留作今后分配
`12:57` 通过本例可以看出
`12:59` 采用变长的子网掩码进行子网划分
`13:02` 可以按需划分出相应数量的子网
`13:06` 每个子网所分配到的ip地址数量可以不相同
`13:11` 尽可能减少了对ip地址的浪费
`13:14` 例如图中的网络五只需要四个ip地址
`13:19` 我们也非常精确的给他分配了四个ip地址
`13:23` 没有造成ip地址的浪费
`13:27` 最后请同学们参照本节课所介绍的两个例子
`13:36` 本题的答案并不唯一
`13:39` 相信同学们都能正确完成
`13:42` 我们就不再赘述了
`13:45` 本节课到这里就结束了
`13:47` 我们将本节课的内容小结如下
`13:53` 同学们
`13:54` 我们下节课再见