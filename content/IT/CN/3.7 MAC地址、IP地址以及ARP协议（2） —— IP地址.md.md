---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=52"
bvid: "BV18g411D7Fm"
cid: "817192879"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817192879&page=52&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家来到我的计算机网络微课堂
`00:09` 在上节课中我们介绍了mac地址的相关知识
`00:14` 本节课我们介绍ip地址的相关知识
`00:19` 需要说明的是
`00:20` ip地址属于网络层的范畴
`00:24` 而非数据链路层的范畴
`00:26` 之所以在数据链路层这一章的讲解中
`00:29` 引入ip地址
`00:31` 是因为在我们日常的大多数网络应用中
`00:35` 属于数据链路层的mac地址
`00:38` 和属于网络层的ip地址
`00:40` 都在使用
`00:41` 它们之间存在一定的关系
`00:45` ip地址的相关内容比较多
`00:47` 例如分类的ip地址
`00:50` 划分子网的ip地址
`00:52` 构造超网的ip地址等
`00:54` 这些内容我们将在网络层这一章详细介绍
`00:58` 本节课我们主要介绍ip地址的作用
`01:03` ip地址是英特网上的主机和路由器
`01:07` 所使用的地址
`01:08` 用于标识两部分信息
`01:11` 一部分是网络编号
`01:13` 用来标识英特网上数以百万计的网络
`01:18` 另一部分是主机编号
`01:20` 用来标识同一网络上
`01:23` 不同主机或路由器各接口
`01:26` 如图所示
`01:28` 假设这是英特网的一部分
`01:31` 我们给网络n8 上的两台主机
`01:33` 各分配了一个ip地址
`01:36` 给路由器r4 连接该网络的接口
`01:39` 也分配了一个ip地址
`01:42` 这三个ip地址的前三个十进制数是相同的
`01:46` 也就是网络n8 的编号
`01:49` 而最后一个十进制数各不相同
`01:53` 是网络n8 上各主机和路由器接口的编号
`01:58` 换句话说
`01:59` 同一个网络上的各主机和路由器的各接口
`02:02` 的ip地址的网络号部分应该相同
`02:06` 而主机号部分应该互不相同
`02:10` 又例如
`02:11` 我们给网络n9 上的
`02:12` 各主机和路由器的接口
`02:15` 各分配了一个ip地址
`02:17` 这三个ip地址的前三个十进制数是相同的
`02:21` 也就是网络n9 的编号
`02:24` 而最后一个十进制数各不相同
`02:27` 是网络n9 上各主机和路由器接口的编号
`02:33` 英特网中
`02:34` 不同网络的网络编号必须各不相同
`02:37` 例如在本例中
`02:39` 网络n8 的编号为1921680
`02:44` 而网络n9 的编号为1921681
`02:49` 需要提醒大家注意的是
`02:52` 在一个ip地址中
`02:53` 哪部分是网络编号
`02:56` 哪部分是主机编号
`02:58` 并不都和本地相同
`03:00` 我们将在后续网络层这一章进行详细介绍
`03:05` 很显然
`03:07` 之前介绍的mac地址
`03:09` 不具备区分不同网络的功能
`03:11` 而ip地址具备这样的功能
`03:15` 如果只是一个单独的网络
`03:17` 不接入英特网
`03:19` 可以直使用mac地址
`03:21` 但这并不是一般用户的常见应用方式
`03:26` 如果主机所在的网络要接入因特网
`03:29` 则ip地址和mac地址都需要使用
`03:34` 接下来我们从网络体系结构的角度
`03:37` 看看ip地址与mac地址的封装位置
`03:41` 我们以五层原理体系结构为例
`03:45` 这是应用层封装好的应用层报文
`03:48` 将其向下交付给运输层
`03:52` 运输层
`03:53` 看不懂也无需看懂
`03:55` 应用层报文的结构与内容
`03:57` 我们用黑色表示
`03:59` 仅仅给其添加一个运输层
`04:02` 首部运输层将封装好的协议数据单元
`04:06` 向下交付给网络层
`04:09` 网络层
`04:10` 看不懂也无需看懂运输层
`04:12` 协议数据单元的结构与内容
`04:15` 仅仅给其添加一个网络层
`04:17` 首部网络层
`04:20` 将封装好的协议数据单元
`04:22` 向下交付给数据链路层
`04:25` 数据链路层看不懂也无需看懂
`04:28` 网络层协议数据单元的结构与内容
`04:32` 仅仅给其添加一个数据链路层
`04:34` 首部和一个数据链路层
`04:36` 尾部
`04:38` 数据链路层
`04:39` 将封装好的协议数据单元
`04:41` 向下交付给物理层
`04:44` 物理层看不懂也无需看懂数据链路层
`04:48` 协议数据单元的结构与内容
`04:51` 仅仅将他们看作是比特流
`04:53` 以便将它们转换为相应的电信号
`04:56` 发送到传输媒体
`05:00` 由于ip地址属于网络体系结构中
`05:02` 网络层的范畴
`05:04` 因此在网络层首部中
`05:07` 应该封装有原ip地址和目的ip地址
`05:11` 相应的
`05:12` 由于market值属于网络体系结构中
`05:15` 数据链路层的范畴
`05:17` 因此在数据链路层首部中
`05:20` 应该封装有原mac地址和目的mac地址
`05:29` ip地址与mac地址的变化情况
`05:33` 如图所示
`05:34` 为了简单起见
`05:36` 图中各主机和路由器
`05:39` 各接口的ip地址和mac地址
`05:41` 用比较简单的标识符来表示
`05:44` 而并未使用实际的ip地址和mac地址
`05:49` 假设主机h一要给主机h2
`05:54` 我们从网络体系结构的角度来看看
`05:59` ip地址与mac地址的变化情况
`06:03` 需要注意的是
`06:04` 主机中有完整的网络体系结构
`06:10` 它没有网络体系结构中的运输层和应用层
`06:15` 我们所关注的重点是网络层
`06:18` 封装ip数据报时
`06:20` 原ip地址和目的ip地址应该填写什么
`06:25` 数据链路层
`06:26` 封装真实原mac地址和目的mac地址
`06:30` 应填写什么
`06:32` 因此我们忽略网络体系结构中
`06:35` 除网络层和数据链路层外的其他各层
`06:39` 可以想象成
`06:55` 在网络层封装的ip数据报
`06:57` 首部中原ip地址应填写主机h一的ip地址
`07:02` ip一目的ip地址应填写主机h2 的ip地址
`07:08` ip 2也就是从ip发送给ip 2
`07:13` 而在数据链路层封装的真守不中原
`07:17` mark地址应填写主机h一的mac地址
`07:20` mark一目的mac地址
`07:23` 应填写路由器r一的mac地址
`07:25` mk 3
`07:27` 也就是从mark一发送给max 3
`07:35` r2
`07:36` 在网络层封装的ip数据报
`07:38` 首部中
`07:39` 原ip地址仍然填写主机h一的ip地址
`07:43` ip 1
`07:45` 目的ip地址仍然填写主机h2 的ip地址
`07:49` ip 2也就是从ip发送给ip 2
`07:54` 而在数据链路层封装的真首部中
`07:57` 原mac地址应填写路由器r一的mac地址
`08:01` mac 4
`08:02` 目的mac地址应填写路由器r2 的mac地址
`08:06` mark 5也就是从mark 4发送给mark 5
`08:16` h2
`08:17` 在网络层封装的ip数据报首部中
`08:20` 原ip地址仍然填写主机h一的ip地址
`08:24` ip一目的ip地址
`08:27` 仍然填写主机h2 的ip地址
`08:29` ip 2也就是从ip一发送给ip 2
`08:35` 而在数据链路层封装
`08:37` 真守不实
`08:38` 原mac地址应填写路由器r2 的mac地址
`08:42` mk 6目的mac地址应填写主机h2 的mac地址
`08:46` mark 2也就是从mark 6发送给mark 2
`08:52` 通过本例可以看出
`08:57` 原ip地址和目的ip地址始终保持不变
`09:02` 而原mac地址和目的mac地址
`09:05` 逐个链路或逐个网络改变主机
`09:18` h一知道r一相应接口的ip地址为ip 3
`09:22` 但不知道企业对应的marketage是什么
`09:30` 而一知道r相应接口的ip地址为ip 5
`09:34` 但不知道其对应的mac地址是什么
`09:38` 路由器r2
`09:43` r2 知道h2 的ip地址为ip 2
`09:46` 但不知道其对应的market址是什么
`09:51` 有的同学可能会对这部分内容产生疑问
`09:55` 请大家目前先全且这么认可
`09:58` 我们将在后续的网络层这一章详细介绍
`10:06` 路由器r一和r2 都存在一个共同的问题
`10:10` 那就是知道ip地址
`10:13` 但不知道其相应的mac地址
`10:17` 如何通过ip地址找出其对应的mac地址
`10:21` 这是我们下节课将要介绍的地址解析协议
`10:25` arp所要实现的功能
`10:28` 最后我们来做一道与mac地址和ip地址
`10:35` 这是计算机专业考研全国统考
`10:39` 计算机网络部分
`10:41` 2018年的题
`10:42` 37
`10:44` 请大家暂停播放视频
`10:46` 思考一下
`10:48` 给出你的答案
`10:52` 答案是选项d
`10:53` 大家都选对了吗
`10:56` 我们来一起分析一下
`10:59` 我们之前刚刚介绍过
`11:04` 原ip地址和目的ip地址始终保持不变
`11:09` 而原mac地址和目的mac地址
`11:12` 逐段链路或逐个网络改变
`11:15` 针对本题
`11:17` 我们来一起填写下表
`11:23` r在网络层封装ip数据报时
`11:27` 在其首部的原ip地址字段
`11:30` 填入主机h一的ip地址
`11:34` 目的ip地址字段填入主机h2 的ip地址
`11:41` 在数据链路层封装mark真时
`11:44` 在其首部的原mac地址字段
`11:47` 填入主机h一的mac地址
`11:52` 目的mac地址字段填入路由器r
`11:55` 相应接口的mac地址
`12:03` h2 在网络层封装ip数据报时
`12:07` 在其首部的原ip地址字段
`12:10` 仍然填入主机h一的ip地址
`12:16` 目的ip地址字段
`12:17` 仍然填入主机h2 的ip地址
`12:23` 在数据链路层封装mark真时
`12:26` 在其首部的原mac地址字段
`12:29` 填入路由器r相应接口的mac地址
`12:35` 目的mac地址字段填入主机h2 的mac地址
`12:42` 本节课到这里就结束了
`12:45` 我们将本节课的内容小结如下
`12:51` 同学们
`12:52` 我们下节课再见