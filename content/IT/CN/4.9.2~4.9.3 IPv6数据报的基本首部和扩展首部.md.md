---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=73"
bvid: "BV18g411D7Fm"
cid: "817198982"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817198982&page=73&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:09` 本节课我们介绍ipv 6数据报的基本手部和扩展
`00:14` 首播
`00:15` 首先介绍ipv 6数据报的基本手部
`00:19` 如图所示
`00:21` ipv 6数据报由两部分组成
`00:24` 一部分是长度为40字节的基本手部
`00:28` 另一部分是长度可变
`00:31` 但最大不超过65535字节的有效载荷
`00:35` 有效载荷由零个或多个扩展手部及其后面的数据部分构成
`00:42` 请同学们注意
`00:43` 所有的扩展手部并不属于ipv 6数据报的首部
`00:48` 他们与其后面的数据部分合起来构成有效载荷
`00:53` 也称为净负荷
`00:55` ipv 6数据报的基本手部的格式如图所示
`01:01` 我们将其与ipv 4数据报的首部进行对比
`01:06` ipv 6将ipv 4数据包首部中不必要的功能取消了
`01:12` 这使得ipv 6数据报基本手部中的字段数量减少到只有八个
`01:18` 但由于ipv 6地址的长度扩展到了128比特
`01:23` 因此使得ipv 6数据报基本手部的长度反而增大到了40字节
`01:29` 比ipv 4数据报首部固定部分的长度增大了20字节
`01:35` ipv 6取消了首部长度字段
`01:39` 因为ipv 6数据报的首部长度是固定的40字节
`01:44` ipv 6取消了区分服务字段
`01:47` 因为ipv 6数据报首部中的通信量类和流标号字段
`01:52` 实现了区分服务字段的功能
`01:56` ipv 6取消了总长度字段
`01:59` 改用有效载荷长度字段
`02:02` 这是因为ipv 6数据报的首部长度是固定的
`02:06` 40字节
`02:07` 只有其后面的有效载荷长度是可变的
`02:12` ipv 6取消了标识标志和片片一字段
`02:17` 因为这些功能已包含在ipv 6数据报的分片扩展首部中
`02:24` ipv 6把生存时间ttl字段改称为票数限制字段
`02:30` 这样名称与作用更加一致
`02:33` ipv 6取消了协议字段
`02:36` 改用下一个首部字段
`02:40` ipv 6取消了首部检验和字段
`02:43` 这样可以加快路由器处理
`02:46` ipv 6数据报的速度
`02:49` ipv 6取消了选项字段
`02:51` 改用扩展手部来实现选项功能
`02:56` 下面我们介绍ipv 6数据报基本首部中各字段的含义
`03:03` 版本字段长度为四比特
`03:06` 用来表示ip协议的版本
`03:08` 对于ipv 6
`03:10` 该字段的值是六
`03:12` 通信量类
`03:13` 字段长度为八比特
`03:16` 该字段用来区分不同的ipv 6数据报的类别或优先级
`03:21` 目前正在进行不同的通信量类性能的实验
`03:27` 流标号字段长度为20比特
`03:31` ipv 6提出了流的抽象概念
`03:34` 留就是英特网上从特定原点到特定终点
`03:40` 单播或多播的一系列i p v6 数据报
`03:44` 如实时音视频数据的
`03:46` 传送
`03:47` 而在这个流所经过的路径上的所有路由器都保证指明的服务质量
`03:55` 所有属于同一个流的ipv 6数据报都具有同样的流标号
`04:00` 换句话说
`04:01` 流标号用于资源分配
`04:05` 流标号对于实时音视频数据的传送特别有用
`04:10` 但对于传统的非实时数据流标号则没有用处
`04:15` 把流编号字段的值置为零即可
`04:20` 有效载荷长度
`04:21` 字段长度为16比特
`04:24` 它指明ipv 6数据报基本首部后面的有效载荷的字节数量
`04:30` 有效载荷包括扩展手部和数据部分
`04:35` 该字段以字节为单位
`04:37` 最大取值为65535
`04:40` 因此ipv 6数据报基本首
`04:43` 部后面的有效载荷的最大长度为65535字节
`04:48` 下一个首部字段长度为八比特
`04:52` 该字段相当于ipv 4数据报首部中的协议字段或可选字段
`04:59` 当ipv 6数据报没有扩展首部时
`05:02` 该字段的作用与ipv 4的协议字段一样
`05:06` 它的值指出了ipv 6数据包基本首部后面的数据是何种协议数据单元
`05:13` pdu
`05:15` 例如该ipv 6数据报
`05:17` 其基本手部中的下一个首部字段的取值若为六
`05:22` 则有效载荷部分是tcp报文段
`05:27` 若取值为17
`05:29` 则有效载荷部分是udp用户数据报
`05:34` 当ipv 6数据报基本首部后面带有扩展首部时
`05:39` 该字段的
`05:40` 值就标识后面第一个扩展手部的类型
`05:44` 如图所示
`05:46` 假设该ipv 6数据报的有效载荷部分包含有扩展手部一和扩展首部二
`05:52` 共两个扩展手部
`05:55` 在扩展首部后面是数据部分
`05:59` ipv 6基本首部中下一个首部字段的取值
`06:03` 就指明了7号第一个扩展手部及扩展手部一的类型
`06:09` 扩展手部一中的下一个首部字段的取值就指明了其后的扩展手部
`06:15` 即扩展手部二的类型
`06:18` 跳数限制字段长度为八比特
`06:22` 该字段用来防止ipv 6数据报在英特网中永久兜圈
`06:28` 原点在每个ipv 6数据报发出时
`06:31` 即设定某个跳数限制
`06:34` 最
`06:34` 大255跳
`06:37` 每个路由器在转发ipv 6数据报时
`06:41` 要先把票数限制字段中的值减一
`06:45` 当跳出限制的值为零时
`06:47` 就把这个ipv 6数据包丢弃
`06:50` 即不转发
`06:52` 该字段的作用与ipv 4数据报首部中的生存时间ttl字段完全一样
`06:59` ipv 6将名称改为跳出限制后
`07:02` 可使名称与作用更加一致
`07:07` 原地址字段和目的地址字段长度都为128比特
`07:12` 分别用来填写ipv 6数据报的发送端的ipv 6地址
`07:17` 和接收端的ipv 6地址
`07:20` 接下来我们简单介绍一下ipv 6数据报的扩展手部
`07:27` ipv 4数据报
`07:28` 如果在其首部中使用了选项
`07:31` 字段则在数据报的整个传送路径中的全部路由器
`07:35` 都要对选项字段进行检查
`07:39` 这就降低了路由器处理数据包的速度
`07:43` 实际上在路径中的路由器对很多选项是不需要检查的
`07:49` 因此为了提高路由器对数据报的处理效率
`07:53` ipv 6把原来i p v4 首部中的选项字段都放在了扩展手部中
`08:00` 由路径两端的原点和终点的主机来处理
`08:05` 而数据报传送路径中的所有路由器都不处理这些扩展手部主教选项
`08:12` 扩展首部除外
`08:14` 在rfc 2460文档中
`08:17` 定义了以下六种扩展手部
`08:22` 逐跳选项
`08:24` 二路由选择分片四鉴别
`08:32` 五封装安全有效载荷
`08:36` 六目的弹选项
`08:39` 每一个扩展手部都由若干个字段组成
`08:43` 它们的长度也各不相同
`08:46` 所有扩展首部中的第一个字段都是巴比特的下一个首部字段
`08:52` 该字段的值指出在该扩展手部后面是何种扩展手部
`08:58` 当使用多个扩展手部时
`09:00` 应按以上的先后顺序出现
`09:05` 本节课到这里就结束了
`09:08` 请同学们快速回忆一下本节课的主要内容
`09:37` 同学们
`09:38` 我们下节课再见