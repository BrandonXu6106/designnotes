---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=81"
bvid: "BV18g411D7Fm"
cid: "817204019"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817204019&page=81&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们将从几个方面对比
`00:13` udp和tcp
`00:16` udp和tcp是tcp ip体系结构运输层中的两个重要协议
`00:22` 如图所示
`00:24` 这是我们之前课程中介绍过的tcp ip体系结构
`00:29` 它的运输层有两个非常重要的协议
`00:32` udp和tcp
`00:34` 在使用tcp ip体系结构的网络通信中
`00:38` 这两个协议的使用频率仅次于网际层的ip协议
`00:43` tcp ip体系结构应用层中的某些协议需要使用运输层的tcp提供的服务
`00:50` 而另一些协议需要使用运输层的udp提供的服务
`00:56` udp是用户数据报协议的英文缩写词
`01:00` tcp是传输控制协议的英文缩写词
`01:05` 接下来我们将从几个方面对这两个协议进行比较
`01:10` 如图所示
`01:11` 这是英特网上的两台主机
`01:15` 他们在运输层使用udp协议进行通信
`01:18` 纵坐标为时间
`01:21` 使用udp协议的通信
`01:23` 双方可以随时发送数据
`01:26` 再来看使用tcp协议的情况
`01:30` 使用tcp协议的通信
`01:31` 双方在进行数据传输之前
`01:34` 必须使用三报文握手来建立tcp连接
`01:39` tcp连接建立成功后才能进行数据传输
`01:45` 数据传输结束后
`01:46` 必须使用四报文挥手来释放tcp连接
`01:52` 三报文握手和四报文挥手
`01:54` 属于tcp的连接管理
`01:57` 其过程比较复杂
`01:59` 我们将在后续课程中专门介绍
`02:03` 需要注意的是
`02:04` 这里所谓的连接是指逻辑连接关系
`02:08` 而不是物理连接
`02:11` 综上所述
`02:12` udp是无连接的
`02:14` 而tcp是面向连接的
`02:18` 来看这个对比项
`02:20` 这是某个局域网上的使用udp协议进行通信的四台主机
`02:26` 其中任何一台主机都可向其他三台主机发送广播
`02:33` 也可以向某个多播组发送多播
`02:38` 还可以向某台主机发送单播
`02:42` 也就是说udp支持单播多播以及广播
`02:48` 换句话说
`02:49` udp支持一对一
`02:51` 一对多以及一对全的通信
`02:55` 再来看使用tcp协议的情况
`02:58` 使用tcp协议的通信
`03:00` 双方在进行数据传输之前
`03:03` 必须使用三报文握手来建立tcp连接
`03:08` tcp连接建立成功后
`03:10` 通信双方之间就好像有一条可靠的通信信道
`03:15` 通信双方使用这条基于tcp连接的可靠信道进行通信
`03:24` 也就是一对一的通信
`03:28` 接下来我们来对比这两个协议对应用报文的处理
`03:33` 先来看使用udp协议的情况
`03:37` 发送方的应用进程将应用层报文交付给运输层的udp
`03:42` udp直接给应用层报文添加一个udp
`03:46` 首部
`03:47` 使之成为udp用户数据报
`03:50` 然后进行发送
`03:52` 需要说明的是
`03:54` 为了简单起见
`03:55` 我们忽略运输层下面的各层处理
`03:59` 接收方的udp收到该udp用户数据报后
`04:03` 去掉udp
`04:04` 首部将应用层报文交付给应用进程
`04:09` 也就是说
`04:10` udp对应用进程交下来的报文既不合并
`04:14` 也不拆分
`04:16` 而是保留这些报文的边界
`04:19` 换句话说
`04:20` udp是面向应用报文的
`04:23` 再来看使用tcp协议的情况
`04:27` 发送方的tcp把应用进程交付下来的数据块
`04:31` 仅仅看作是一连串的无结构的字节流
`04:36` tcp并不知道这些带传送的字节流的含义
`04:40` 仅将它们编号并存储在自己的发送缓存中
`04:45` tcp根据发送策略
`04:47` 从发送缓存中提取一定数量的字节
`04:51` 构建tcp报文段
`04:52` 并发送
`04:56` 接收方的tcp一方面从所接收到的tcp报文段中取出数据载荷部分
`05:02` 并存储在接收缓存中
`05:05` 一方面将接收缓存中的一些字节交付给应用进程
`05:11` tcp不保证接收方应用进程所收到的数据块
`05:16` 与发送方应用进程所发出的数据块具有对应大小的关系
`05:22` 例如发送方应用进程交给发送方的tcp共十个数据块
`05:28` 但接收方的tcp可能只用了四个数据块
`05:32` 就把收到的字节流交付给了上层的应用进程
`05:36` 但接收方应用进程收到的字节流
`05:39` 必须和发送方应用进程发出的字节流完全一样
`05:44` 当然接收方的应用进程必须有能力识别收到的字节流
`05:50` 把它还原成有意义的应用层数据
`05:54` 也就是说tcp是面向字节流的
`05:59` 这正是tcp实现可靠传输流量控制以及拥塞控制的基础
`06:05` 需要说明的是
`06:07` 为了突出示意图的要点
`06:09` 我们只画出了一个方向的数据流
`06:13` 在实际网络中
`06:15` 基于tcp连接的两端可以同时进行tcp报文段的发送和接收
`06:21` 也就是全双工通信
`06:36` 再来看下一个对比项
`06:39` 在之前的课程中
`06:41` 我们曾介绍过tcp ip体系结构的网际层
`06:45` 向其上层提供的是无连接不可靠的传输服务
`06:50` 当运输层使用udp协议时
`06:53` 向其上层提供的也是无连接不可靠的传输服务
`06:59` 发送方给接收方发送udp用户数据报
`07:03` 若传输过程中用户数据报受到干扰而产生误码
`07:09` 接收方udp可以通过该数据报首部中的校验和字段的值
`07:14` 检查出产生误码的情况
`07:17` 但仅仅丢弃该数据报
`07:19` 其他什么也不做
`07:22` 发送方给接收方发送udp用户数据报
`07:26` 如果该数据报被英特网中的某个路由器丢弃了
`07:30` 发送方udp不做任何处理
`07:34` 因为udp向上层提供的是无连接不可靠的传输服务
`07:40` 因此对于udp用户数据报出现的雾霾和丢失等问题
`07:46` udp并不关心
`07:48` 基于udp的这个特点
`07:50` udp适用于实时应用
`07:53` 例如ip电话
`07:55` 视频会议等
`07:57` 再来看使用tcp协议的情况
`08:01` 尽管网际层中的ip协议向上层提供的是无连接不可靠的传输服务
`08:08` 也就是说ip数据报可能在传输过程中出现丢失或物
`08:14` 但只要运输层使用tcp协议
`08:17` 就可向其上层提供面向连接的可靠传输服务
`08:23` 我们可将其想象成使用tcp协议的收发双方
`08:27` 基于tcp连接的可靠信道进行数据传输
`08:32` 不会出现误码丢失
`08:35` 乱序以及重复等传输差错
`08:39` tcp适用于要求可靠传输的应用
`08:42` 例如文件传输
`08:46` 最后我们再来对比一下udp用户数据报的首部与tcp报文段的首部
`08:53` 一个udp用户数据报由首部和数据载荷两部分构成
`08:59` 其首部格式如图所示
`09:02` 仅有四个字段
`09:04` 每个字段长度为两个字节
`09:07` 由于udp不提供可靠传输服务
`09:11` 它仅仅在网际层的基础上添加了用于七分应用进程的端口
`09:17` 因此它的手部非常简单
`09:20` 仅有八个字节
`09:23` 一个tcp报文段由手部和数据载荷两部分构成
`09:29` 其首部格式如图所示
`09:32` 这比udp用户数据报的首部复杂得多
`09:37` 其最小长度为20字节
`09:39` 最大长度为60字节
`09:43` 这是因为tcp要实现可靠传输
`09:46` 流量控制
`09:47` 拥塞控制等服务
`09:49` 其手部自然会比较复杂
`09:52` 手部中的字段比较多
`09:54` 手部长度也比较长
`09:58` 本节课到这里就结束了
`10:00` 我们将本节课的内容小结如下
`10:07` 需要说明的是
`10:09` tcp的流量控制和拥塞控制比较复杂
`10:12` 我们并未在本节课中介绍他们
`10:16` 有关他们的介绍
`10:17` 我们将在后续课程中进行
`10:20` 同学们
`10:21` 我们下节课再见