---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=106"
bvid: "BV18g411D7Fm"
cid: "817211211"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817211211&page=106&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 从本节课开始
`00:11` 我们将用四次课的时间
`00:14` 分别介绍网络体系结构的各层
`00:17` 所采取的安全措施
`00:19` 本节课
`00:21` 我们介绍物理层和数据链路层的安全实力
`00:25` 首先介绍物理层安全实力
`00:29` 在物理层实现通信数据的保密性和完整性
`00:33` 的方法是对信道进行加密
`00:37` 对信道进行加密
`00:39` 需要使用信道加密机
`00:41` 如图所示
`00:42` 这两个路由器之间是一段点对点链路
`00:47` 左侧的路由器给右侧的路由器发送数据
`00:52` 为了实现通信数据的保密性和完整性
`00:56` 需要给左侧路由器的输出端
`00:58` 和右侧路由器的输入端
`01:00` 分别添加一个信道加密机
`01:04` 左侧路由器发送的明文数据
`01:07` 经过信道加密机的加密后成为秘闻密文
`01:12` 通过点对点链路的传送
`01:14` 到达链路右端的信道加密机
`01:17` 新的加密机将其解密后成为铭文
`01:22` 使用新的加密技术
`01:24` 可谓通过链路的所有数据提供安全保护
`01:29` 并且对上层协议几乎没有任何影响
`01:33` 也就是具有很好的透明性
`01:37` 由于链路上传送的各种协议
`01:40` 数据单元的手部和数据载荷都被加密了
`01:44` 协议数据单元的截
`01:46` 或者无法从协议数据单元中
`01:49` 提取原地址和目的地址
`01:52` 因此可以防止各种形式的流量分析
`01:57` 由于信道加密机完全使用硬件加密技术
`02:01` 因此加密和解密速度快
`02:05` 并且无需传送额外的数据
`02:08` 采用该技术不会减少网络的有效带宽
`02:13` 然而在网络互联的情况下
`02:17` 信道加密
`02:21` 通信安全的技术
`02:23` 不能确保端到端通信的安全
`02:27` 在实际应用中
`02:28` 通常只在容易被窃听的无线链路
`02:32` 例如卫星链路上
`02:34` 或军用网络等专用网络的通信链路
`02:38` 所在不安全区域的部分
`02:40` 使用信道加密技术
`02:43` 接下来我们介绍数据链路层安全实力
`02:48` 随着802.11无线局域网的广泛应用
`02:52` 其安全问题越来越受到关注
`02:57` 在无线通信方式下
`02:59` 电磁波在自由空间辐射传播
`03:04` 任何无线终端都可
`03:06` 在无线接入点的信号覆盖范围内
`03:08` 接收器无线信号若不采取相应安全措施
`03:14` 任何无线终端都可以接入到网络中
`03:18` 进而窃听网络通信或非法使用网络资源
`03:23` 因此802011
`03:25` 无线局域网的安全问题就显得尤为重要
`03:30` 802011无线局域网
`03:32` 主要在数据链路层为用户提供安全服务
`03:38` 早期802011
`03:39` 无线局域网所采取的安全机制比较简单
`03:44` 分别是ssd匹配机制
`03:49` mac地址过滤机制有限
`03:52` 等效保密机制如图所示
`03:58` 网络管理员必须为无线接入点ap分配一个
`04:02` 不超过32字节的服务及标识符
`04:06` s s i d
`04:07` 也就是无线网络的名称
`04:11` 若用户给无线终端配置的s s i d
`04:14` 与接入点ap的相同
`04:17` 则这些无线终端可以加入该app形成的网络
`04:23` 若用户给无线终端配置的ssd
`04:27` 与接入点ap的不同
`04:29` 则这些无线终端不能加入该app形成的网络
`04:35` 这种ssd匹配机制
`04:38` 仅是一种简单的不加密的口令鉴别
`04:42` 不能防止窃听和冒充
`04:45` 它仅仅提供了一种非常弱的访问控制功能
`04:51` 更进一步的可以采取mac地址过滤机制
`04:57` 网络管理员可以为接入点ap配置一个
`05:01` 允许接入网络的mac地址列表
`05:04` 只有mac地址在该列表中的无线终端
`05:08` 所发送的帧才能被ap接收和转发
`05:13` 然而攻击者可以通过无线网络中传输的
`05:18` 数据流来截获有效的mac地址
`05:21` 并将自己的无线局域网网卡的mac地址
`05:25` 也设置为截获的有效的mac地址
`05:30` 这样攻击者就可以接入到网络中
`05:35` 很显然mac地址过滤机制与ssd匹配机制一样
`05:40` 也只能提供非常弱的访问控制功能
`05:45` 再进一步可以采取有限等效保密机制
`05:50` 有限等效保密机制的英文缩写词为w p
`05:56` 网络管理员在接入点ap上配置
`05:59` 由ap和用户的无线终端共享的we p密钥
`06:05` 很显然
`06:10` 网络管理员需要告知用户该wp密钥
`06:15` 以便由用户在自己的无线终端上设置之后
`06:20` 无线终端与接入点ap之间使用wp加密算法
`06:26` 完成实体鉴别和数据通信
`06:30` wp并没有密钥分发机制
`06:34` 在鉴别过程和其后的数据通信过程中
`06:38` 的所有通信都使用同一个共享密钥
`06:43` 并且所有接入到同一网络的无线终端
`06:47` 都使用这同一个密钥
`06:51` 另外wp采用的加密算法的强度较低
`06:56` 国内外众多研究人员
`06:58` 已从理论和实际上都证明了
`07:02` wep加密存在严重的安全隐患
`07:07` 1999年发布的hp 1802011标准
`07:11` 仅具有我们刚刚介绍的
`07:13` 三种较弱的安全机制
`07:17` 2004年获得批准的i v e
`07:20` 802.11i标准
`07:22` 则具有更强的安全机制
`07:30` 更强的加密算法以及一种密钥分发机制
`07:36` 在802011i正式发布前
`07:39` 无线局域网受保护的接入
`07:42` 曾作为无线局域网安全的过度标准替代
`07:46` w e p
`07:47` 为802.11
`07:49` 无线局域网提供更强的安全性
`07:53` 无线局域网受保护的接入
`07:55` 的英文缩写词为w p a wpa 2
`08:00` 是wpa的第二个版本
`08:03` 他也是hp 1802011 i的商业名称
`08:09` 目前大多数802.11
`08:12` 无线局域网都支持wpa和wpa 2
`08:16` 但建议用户尽量使用wpa 2
`08:21` 接下来我们举例说明
`08:23` 802.11i的安全框架
`08:27` 除了接入点ap和无线终端外
`08:31` 802.11i还定义了一种鉴别服务器
`08:35` 其英文缩写词为a s ap和与a s进行通信
`08:42` 将a从ap分离出来的目的在于
`08:46` 可使一台a s服务于多台ap
`08:50` 集中在一台as中处理鉴别和接入
`08:53` 可以降低ap的复杂性和成本
`08:59` 在802011i中
`09:02` 无线终端与ap之间建立安全通信的过程
`09:09` 第一阶段是安全性能力
`09:11` 发现ap会向其信号覆盖范围内的无线终端
`09:17` 通告其所能提供的鉴别和加密方式
`09:22` 各无线终端收到后和请求
`09:25` 各自所期望的特定的鉴别和加密方式
`09:31` 尽管在该阶段
`09:33` 无线终端已经与ap交换了豹纹
`09:37` 但此阶段无线终端还没有被鉴别
`09:40` 也没有获得用于数据通信的加密密钥
`09:45` 第二阶段是无线终端和as双向鉴别
`09:50` 共同生成主密钥mk
`09:54` 在该阶段中
`09:55` ap仅在无线终端和a之间转发报文
`10:01` 鉴别过程
`10:02` 使用由扩展的鉴别协议所定义的一种
`10:06` 端到端的报文格式
`10:09` 扩展的鉴别协议的英文缩写词为e a p
`10:14` 它只是一个鉴别框架
`10:16` 并未指定具体的鉴别协议
`10:20` 在ap中
`10:21` a可选择多种鉴别方式中的任何一种
`10:26` 来执行鉴别
`10:28` 主要利用公钥加密技术
`10:34` 在某个无线终端和as之间进行相互鉴别
`10:39` 并生成双方共享的一个主密钥mk
`10:45` 第三阶段是无线终端与a s生成成对
`10:49` 主力要pmk
`10:53` 除了在上一个阶段所生成的
`10:56` 由某个无线终端和as之间共享的主密钥mk
`11:02` 该无线终端与as s
`11:04` 彼此之间还要再生成一个密钥
`11:07` 称为成对主密钥
`11:09` 其英文缩写词为p m k pmk
`11:14` 由无线终端和ap共同使用
`11:19` a s将p mk发送给ap
`11:23` 这时该无线终端和ap
`11:26` 就具有了一个共享的密钥
`11:28` pmk
`11:30` 他们使用pmk相互鉴别
`11:34` 第四阶段是ap和无线终端各自用p mk
`11:40` 生成临时密钥
`11:41` tk是要上一阶段获得的pmk
`11:47` 该无线终端和ap可以生成
`11:50` 用于加密通信的临时密钥
`11:53` 其英文缩写词为tk
`11:56` tk被用于该无线终端与ap之间
`12:00` 链路级的数据加密
`12:04` rov 1802011 i
`12:06` 提供了多种加密形式
`12:12` 临时密钥完整性协议
`12:15` 写英文缩写词为t k i p
`12:19` 以及基于高级加密标准a的加密方案
`12:25` 考虑到不同用户和不同应用的安全需要
`12:29` 例如企业用户需要很高的企业级安全保护
`12:34` 而家庭用户往往只是使用网络来浏览网页
`12:39` 收发电子邮件等
`12:40` 对安全保护的要求相对较低
`12:44` 为了满足不同用户的需求
`12:47` i roe 802011 i
`12:49` 规定了以下两种应用模式
`12:53` 节目是是用鉴别服务器a和复杂的安全
`12:59` 鉴别机制来保护无线网络的通信
`13:02` 安全家庭模式也称个人模式
`13:10` 在ap或无线路由器上以及无线终端上配置
`13:16` 预设了共享密钥
`13:17` pk来保护无线网络的安全通信
`13:22` 本节课到这里就结束了
`13:25` 请同学们快速回忆一下本节课的主要内容
`13:57` 同学们
`13:58` 我们下节课再见