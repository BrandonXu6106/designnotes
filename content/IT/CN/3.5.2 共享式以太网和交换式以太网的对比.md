---
title: 3.5.2 共享式以太网和交换式以太网的对比
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=37
bvid: BV18g411D7Fm
cid: "817189064"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817189064&page=37&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:09` 本节课我们对比共享式以太网和交换式以太网
`00:16` 共享式以太网与交换式以太网的对比
`00:19` 主要是对比集线器与交换机
`00:23` 如图所示
`00:24` 这是使用集线器将四台主机互联而成的小型共享式以太网
`00:31` 这是使用交换机将四台主机互联而成的小型交换式以太网
`00:38` 假设交换机已经通过自学习算法逐步建立了完整的转发表
`00:45` 也就是学习到了网络中所有主机与自己各接口的对应关系
`00:52` 首先来看主机发送单波真的情况
`00:56` 假设主机a给b发送一个单波真
`01:00` 对于使用集线器的共享式以太网
`01:03` 单播真会通过极限器传播到网络中的其他各主机
`01:09` 其他各主机中的网卡会根据单波真的目的mark地址决定接受或丢弃该帧
`01:17` 对于使用交换机的交换式以太网交换机收到单波真号
`01:23` 根据真的目的mac地址和自身的转发表
`01:27` 将真明确的转发给目的主机
`01:30` 而不是网络中的其他各主机
`01:35` 目的主机中的网卡根据丹波真的穆里巴克地址
`01:39` 决定接受改正
`01:42` 来看主机发送广播真的情况
`01:45` 假设主机a发送了一个广播帧
`01:49` 不知同学们是否还记得广播帧首部中
`01:52` 目里马克地址字段的值应填入广播地址
`01:56` 即48比克
`01:58` 全16进制形式为全f的广播地址
`02:03` 对于使用极限期的共享式以太网广播
`02:07` 真会通过集线器传播到网络中的其他各主机
`02:12` 其他各主机中的网卡检测到真的mod mark地址是广播地址
`02:17` 就接受改正
`02:20` 对于使用交换机的交换式以太网交换机收到广播帧号
`02:25` 检测到真的目的mark地址是广播地址
`02:29` 于是从除该针进入交换机的接口的其他所有接口
`02:34` 转发该针网络中其他各主机中的网卡
`02:38` 收到该帧后
`02:39` 检测到真的movie mac地址是广播地址
`02:43` 就接受该真
`02:46` 从本地可以看出
`02:48` 是用极限期的共享式以太网中的各主机属于同一个广播域
`02:54` 而使用交换机的交换式以太网中的各主机也属于同一个广播域
`03:01` 尽管集线器和交换机对广播真的转发情况从效果上看是相同的
`03:09` 但它们的基本原理并不相同
`03:13` 集线器只工作在物理层
`03:15` 它并不能识别真守不中的目的mac地址
`03:19` 因为这属于数据链路层的范畴
`03:23` 不管极限器收到的是单波真还是广播真
`03:28` 集线器仅仅是将表示真的信号从其他所有接口转发出去
`03:34` 而交换机工作在数据链路层
`03:37` 它可以识别帧的结构
`03:40` 从真首部中提取出茉里mark地址
`03:43` 然后查找自己的转发表
`03:46` 根据查表结果对帧进行明确转发或盲目的转发或丢弃
`03:54` 再来看多对主机间同时通信的情况
`03:58` 假设主机a给b发送单波真的
`04:01` 同时主机c给d也发送单波真
`04:05` 对于使用集线器的共享式以太网
`04:08` 当多对主机同时通信时
`04:11` 必然会产生碰撞
`04:14` 遭遇碰撞的针会传播到网络中的各主机
`04:18` 各主机中的网卡检测到遭遇碰撞的真号
`04:22` 将其丢弃
`04:25` 对于使用交换机的交换式以太网
`04:28` 由于交换机对收到的帧进行存储转发
`04:32` 并且能够实现多对接口的高速并行交换
`04:37` 因此不会产生碰撞
`04:40` a发送给b的单波真
`04:42` 以及c发送给d的单播真
`04:44` 都会被交换机正确转发
`04:48` 主机b和d中的网卡
`04:50` 接受各自收到的单播真
`04:53` 通过上述个例子中的对比情况
`04:56` 可以加深我们理解集线器与交换机的区别
`05:01` 另外我们还可以得出使用集线器和交换机扩展共享式以太网的区别
`05:10` 如图所示
`05:11` 两个独立的共享是以太网
`05:14` 他们各自既是一个独立的广播域
`05:17` 也是一个独立的碰撞域
`05:20` 若用集线器将这两个独立的共享式以太网连接起来
`05:25` 则会形成一个具有更大广播域和碰撞域的共享式以太网
`05:32` 换句话说
`05:33` 使用集线器扩展共享式以太网
`05:36` 既扩大了广播域
`05:38` 也扩大了碰撞域
`05:41` 若用交换机将这两个独立的共享式以太网连接起来
`05:46` 则会形成一个具有更大广播域
`05:50` 但原本独立的两个碰撞域仍被交换机隔离的以太网
`05:55` 换句话说
`05:57` 使用交换机扩展共享时
`05:59` 以太网扩大了广播域
`06:01` 但隔离了碰撞域
`06:04` 嗯嗯接下来请同学们做几道与本节课各知识点相关的
`06:11` 408考研真题
`06:13` 首先来看这道题
`06:16` 请同学们暂停播放视频思考一下
`06:20` 然后给出你的答案
`06:27` 本题的答案是选项a
`06:29` 大家都选对了吗
`06:32` 我们来一起分析一下网桥工作在数据链路层
`06:37` 因此网桥具备属于数据链路层范畴的相关能力
`06:43` 网桥的接口数量很少
`06:45` 通常只有2~4个
`06:47` 一般只用来连接不同的网段
`06:50` 1990年面试的交换式集线器
`06:54` 实质上是具有多个接口的网桥
`06:57` 常称为以太网交换机或二层交换机
`07:01` 综上所述
`07:03` 选项a的叙述正确
`07:06` 如图所示
`07:07` 这是我们之前刚刚介绍过的
`07:10` 在使用交换机互联而成的交换式以太网中
`07:14` 多段主机之间同时通信的情况
`07:18` 由于交换机对收到的帧进行存储转发
`07:22` 并且能够实现多个接口的高速并行交换
`07:27` 因此不会产生碰撞
`07:29` 所以选项b叙述错误
`07:32` 如图所示
`07:34` 这是我们之前刚刚介绍过的
`07:37` 用交换机将两个独立的共享式以太网连接起来
`07:42` 会形成一个具有更大广播域
`07:45` 但原本独立的两个碰撞域仍被交换机隔离的以太网
`07:50` 因此选项c的叙述错误
`07:54` 以太网交换机工作在数据链路层
`07:57` 包含旗下的物理层
`07:59` 因此以太网交换机具备了属于数据链路层范畴的相关能力
`08:05` 但不同网络层协议的网络互联
`08:08` 属于网络层及其上层的范畴
`08:11` 以太网交换机无法处理
`08:13` 因此选项d的叙述错误
`08:18` 再来看这道题
`08:20` 请同学们暂停播放视频
`08:23` 动手练习一下
`08:25` 然后给出你的答案
`08:31` 本题的答案是选项d
`08:34` 大家都选对了吗
`08:35` 我们来一起分析一下
`08:38` 根据提议
`08:40` 主机h2 给h4 发送一个数据帧
`08:44` 交换机收到后进行自学习和转发真的工作
`08:49` 所谓自学习就是登记真的原mac地址以及捐进入交换机的接口号
`08:56` 完成自学习
`08:58` 好在交换机自身的转发表中查找真的目的mac地址
`09:04` 根据查表结果对帧进行明确转发或盲目转发或丢弃
`09:11` 由于本题并未给出交换机的转发表的具体内容
`09:16` 因此根据提议
`09:18` 该帧或被明确转发或被盲目转发
`09:22` 为了简单起见
`09:24` 假设该针被明确转发给极限器
`09:28` 集线器收到该针后
`09:30` 将其从自己的其他接口转发出去
`09:34` 这样主机h3 和h4 都会收到该增
`09:39` 根据真守不中的目的mac地址
`09:42` h3 会丢弃该帧
`09:44` 而h4 会接受改真
`09:48` 根据题意
`09:49` 主机h4 收到h2 发来的数据帧后
`09:53` 会给h2 发送一个确认帧
`09:56` 集线器收到该帧后
`09:58` 将其从自己的其他接口转发出去
`10:02` 这样主机h3 和交换机都会收到该帧
`10:07` 根据真守不中的目的mac地址
`10:10` h3 会丢弃该帧
`10:13` 交换机收到该帧后
`10:16` 首先登记该针的原mac地址
`10:18` 以及该针进入交换机的接口号
`10:22` 然后在交换机自身的转发表中查找真的目的mac地址
`10:28` 也就是主机h2 的mac地址可以找到
`10:33` 这是因为该mark地址在之前主界h2 给h4 发送数据
`10:39` 真实已被交换机学习到了
`10:42` 因此按照该mac地址所在转发条目中记录的接口号
`10:47` 将该针从这个接口明确转发出去
`10:53` 综上所述
`10:54` 能够收到主机h4 发给h2 的确认帧的主机有h2 和h3
`11:03` 再来看这道题
`11:05` 由于到目前为止
`11:07` 同学们还未学习过路由器的相关知识
`11:11` 因此我们将本题所需要的路由器的相关知识告诉大家
`11:18` 路由器既隔离冲突域
`11:20` 要隔离广播域
`11:23` 请同学们暂停播放视频
`11:26` 动手练习一下
`11:28` 然后给出你的答案
`11:34` 本题的答案是选项c
`11:37` 大家都选对了吗
`11:39` 我们来一起分析一下集线器既不隔离冲突域
`11:45` 也不隔离广播域
`11:48` 交换机隔离冲突域
`11:50` 但不隔离广播域
`11:53` 因此这是四个独立的冲突域
`11:58` 这是一个广播域
`12:01` 这也是一个广播域
`12:04` 本节课到这里就结束了
`12:07` 请同学们快速回忆一下本节课的主要内容
`12:32` 同学们
`12:33` 我们下节课再见