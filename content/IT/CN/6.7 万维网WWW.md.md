---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=96"
bvid: "BV18g411D7Fm"
cid: "817207968"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817207968&page=96&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍万维网
`00:13` 万维网并非某种特殊的计算机网络
`00:16` 它是一个大规模的联机式的信息储藏所
`00:21` 是运行在英特网上的一个分布式应用
`00:25` 他利用网页之间的超链接
`00:27` 将不同网站的网页链接成一张逻辑上的信息网
`00:32` 万维网是欧洲粒子物理实验室的蒂姆伯纳斯里
`00:36` 最初于1989年3月提出的
`00:40` 1993年2月诞生了世界上第一个图形界面的浏览器mozc
`00:47` 1995年
`00:48` 著名的网景浏览器上市
`00:52` 目前比较流行的浏览器有chrome浏览器
`00:57` 火狐浏览器
`00:59` sufferi浏览器
`01:01` 欧朋浏览器
`01:03` ie浏览器
`01:05` 浏览器最重要的部分是渲染引擎
`01:09` 也就是浏览器内核负责对网页内容进行解析和显示
`01:15` 这是上述各浏览器使用的内核
`01:19` 不同的浏览器内核对网页内容的解析也有所不同
`01:23` 因此同一网页在不同内核的浏览器里的显示效果可能不同
`01:30` 网页编写者需要在不同内核的浏览器中测试网页显示效果
`01:36` 接下来我们举例说明万维网应用
`01:40` 我们在用户主机中使用浏览器来访问湖南科技大学的万维网服务器
`01:47` 也就是访问湖南科技大学的官方网站
`01:51` 我们在浏览器的地址栏中输入湖南科技大学官方网站的域名
`01:56` 并按下回车键后
`01:58` 浏览器将发送请求报文给服务器
`02:02` 服务器收到请求报文后执行相应操作
`02:06` 然后给浏览器发回响应报文
`02:10` 浏览器解析并渲染响应报文中的内容
`02:14` 这样我们就可以看到网站首页了
`02:18` 为了方便地访问在世界范围的文档
`02:21` 万维网使用统一资源定位符来指明英特网上任何种类资源的位置
`02:28` 统一资源定位符的一般形式由以下四个部分组成
`02:34` 分别是协议主机端口路径
`02:39` 我们之前在浏览器地址栏中输入的是湖南科技大学官方网站的域名
`02:45` 目的是获取网站首页的内容
`02:48` 其对应的统一资源定位符如图所示
`02:54` 当我们点击网页中的某个超链接时
`02:57` 将跳转到另一个网页
`03:00` 可以看到这是该网页相应的统一资源定位符
`03:04` 其中协议主机和端口与网站首页相同
`03:09` 不同的是路径和网页文件
`03:13` 接下来我们介绍万维网文档
`03:17` 这是我们之前访问的湖南科技大学官方网站的首页
`03:21` 我们可以将其另存为文件
`03:25` 可以看到有一个扩展名为html的文件和一个文件夹
`03:31` 我们打开该文件夹看看其内容
`03:36` 这三个扩展名为htm的文件是html文档
`03:41` 这五个扩展名为js的文件是javascript文档
`03:46` 这两个扩展名为css的文件是css文档
`03:51` 其他的jpg文件和png文件是图片文件
`03:57` html是超文本标记语言的英文缩写词
`04:02` 它使用多种标签来描述网页的结构和内容
`04:07` css是层叠样式表的英文缩写词
`04:11` 它从审美的角度来描述网页的样式
`04:16` javascript是一种脚本语言
`04:19` 用来控制网页的行为
`04:22` 由html css javascript编写的万维网文档
`04:28` 由浏览器内核负责解释和渲染
`04:32` 我们来举例说明
`04:34` 这是用html编写的最简单的html文档
`04:39` 用浏览器打开该html文档
`04:43` 可以看到浏览器渲染出了一个非常简单的网页
`04:48` 在html文档中使用两个html标签来定义html文档的范围
`04:56` 在其内部是用两个head的标签定义html文档的手部
`05:02` 使用两个body标签定义html文档的主体
`05:08` 首部中两个title标签之间的内容被渲染为网页的标题
`05:14` 两个p标签之间的内容被渲染为一个文本段落
`05:19` 通过本例可以看出
`05:21` html使用多种标签来描述网页的结构和内容
`05:27` 但是所呈现出来的内容样式过于简单
`05:31` 或者说不够美观
`05:34` 我们可以在css文档中定义一些所需要的样式
`05:38` 对网页显示内容进行美化
`05:42` 例如我们编写了这样一个css文档
`05:46` 在其中定义了一种样式
`05:49` 即颜色为深粉色
`05:51` 字体大小为36个像素
`05:54` 然后我们在之前编写好的html文档的手部使用link标签
`06:01` 将该css文档引入
`06:04` 将样式名称指定给主体中
`06:07` 需要更改样式的那个p标签
`06:10` 现在我们在浏览器中进行刷新
`06:13` 就可以看到浏览器重新渲染出了网页内容
`06:18` 可以看到hello word段落的颜色和字体大小都发生了相应的变化
`06:24` 接下来我们再给该网页添加一个按钮
`06:29` 在之前的html文档的主体中使用button标签来添加一个按钮
`06:36` 我们为该按钮指定一个发生单击事件时应该调用的处理函数
`06:42` 然后用javascript脚本语言来编写一个js文档
`06:47` 在该文档中编写单击事件处理函数的具体实现代码
`06:54` 在html文档的首部
`06:56` 使用script标签
`06:58` 将该js文档引入在事件处理函数中
`07:03` 通过元素的id来找到相应的元素
`07:07` 也就是显示hello world的p标签
`07:10` 然后更改其显示内容
`07:13` 现在我们在浏览器中进行刷新
`07:17` 就可以看到我们所添加的按钮
`07:20` 当我们用鼠标点击该按钮时
`07:23` hello world变成了
`07:25` 谢谢你的赞
`07:27` 通过上述举例
`07:29` 相信同学们对html文档
`07:31` css文档
`07:33` javascript文档已经有了初步的认识
`07:37` 至于这些文档的具体编写属于web前端开发的基础
`07:42` 并不属于本课程的教学大纲
`07:45` 我们就不再深入介绍了
`07:48` 需要大家注意的是
`07:50` 这些文档都部署在服务器端
`07:54` 有一些是web前端人员开发设计好的静态页面
`07:58` 有一些是服务器后端程序根据用户需求自动生成的动态页面
`08:04` 他们都需要从服务器传送给用户的浏览器进行解析和渲染
`08:11` 这就不得不提到tcp ip体系应用层中的一个非常重要的协议
`08:17` 即超文本传输协议
`08:19` http http定义了浏览器及万维网客户进程
`08:26` 怎样向万维网服务器请求万维网文档
`08:30` 以及万维网服务器怎样把万维网文档传送给浏览器
`08:36` 我们来举例说明
`08:38` 我们使用用户主机来访问湖南科技大学的万维网服务器
`08:44` 可以看成是用户主机中的浏览器进程及客户进程
`08:49` 与服务器中的服务器进程
`08:51` 基于英特网的通信浏览器进程
`08:55` 首先发起与服务器进程的tcp连接
`08:59` 使用熟知端口号80
`09:03` 基于这条已建立好的tcp连接
`09:06` 浏览器进程向服务器进程发送http请求报文
`09:11` 服务器进程收到后执行相应操作
`09:15` 然后给浏览器进程发回http响应报文
`09:20` http一点零采用非持续连接方式
`09:24` 在该方式下
`09:26` 每次浏览器要请求一个文件
`09:29` 都要与服务器建立tcp连接
`09:32` 当收到响应后就立即关闭连接
`09:36` 我们来举例说明
`09:40` 这是客户与服务器之间通过三报文握手进行tcp连接
`09:46` 在这三个报文中的最后一个报文的数据载荷部分携带有http请求
`09:53` 报文
`09:55` 服务器收到后给客户发回http响应报文
`10:00` 这是一次请求和响应所耗费的时间
`10:04` 即为往返时间
`10:06` rtt这又是一次请求和响应所耗费的时间
`10:11` 而tt这是文档的传输实验
`10:16` 可以看到
`10:17` 请求一个万维网文档所需的时间为两倍的rtt加文档的传输时延
`10:25` 每请求一个文档就要有两倍rtt的开销
`10:29` 若一个网页上有很多引用对象
`10:32` 例如图片等
`10:34` 那么请求每一个对象都需要额外花费两倍的rtt的时间
`10:40` 为了减少食盐浏览器通常会建立多个并行的tcp连接
`10:46` 同时请求多个对象
`10:49` 但是这会大量占用万维网服务器的资源
`10:53` 特别是万维网服务器往往同时服务于大量客户的请求
`10:59` 这会使其负担很重
`11:02` http一点一采用持续连接方式
`11:06` 在该方式下
`11:08` 万维网服务器在发送响应后
`11:11` 仍然保持这条连接是同一个客户
`11:15` 和该服务器可以继续在这条链接上传送后续的http请求
`11:22` 报文和响应报文
`11:24` 这并不局限于传送同一个页面上引用的对象
`11:29` 而是只要这些文档都在同一个服务器上就行
`11:34` 为了进一步提高效率
`11:37` http一点一的持续连接
`11:39` 还可以使用流水线方式工作
`11:42` 解浏览器在收到http响应报文之前
`11:46` 就能够连续发送多个请求报文
`11:50` 这样的一个接一个的请求报文
`11:53` 到达服务器号
`11:54` 服务器就发回一个接一个的响应报文
`11:59` 这样就节省了很多个rtt时间
`12:02` 是tcp连接中的空闲时间减少
`12:05` 提高了下载文档的效率
`12:09` 接下来我们介绍http的报文格式
`12:14` http是面向文本的
`12:17` 其报文中的每一个字段都是一些阿斯克码串
`12:21` 并且每个字段的长度都是不确定的
`12:25` 首先来看http请求报文的格式
`12:29` 我们用很窄的小格子表示空格
`12:32` 用标有cr lf的格子表示回车换行
`12:37` http请求报文的第一行是请求行
`12:42` 由方法字段开始
`12:44` 旗号跟一个空格后
`12:47` 跟统一资源定位符字段
`12:50` 其后跟一个空格后跟版本字段
`12:55` 最后是回车换行
`12:58` 从第二行开始
`12:59` 就是首部行
`13:01` 每一个手部行由首部字段名开始
`13:04` 旗号跟一个冒号
`13:07` 再跟一个空格
`13:09` 然后是该字段的取值
`13:12` 最后是回车换行
`13:15` 可以有多个手不行
`13:17` 如图所示
`13:24` 在最后的首部行下面是一个空行
`13:29` 在空行下面是实体主体
`13:32` 通常不使用
`13:34` 我们来举例说明http请求报文的格式
`13:38` 这是浏览器发送的http请求报文的具体内容
`13:43` 第一行为请求行指明了方法
`13:47` get统一资源定位符以及http的版本
`13:52` 这是第一个首部行
`13:55` 首部字段名为host
`13:57` 其职为湖南科技大学官方网站的域名
`14:01` 这是第二个首部行
`14:03` 首部字段名为connection
`14:06` 其职为close
`14:08` 这是告诉服务器发送完请求的文档后
`14:12` 就可以释放连接
`14:14` 这是第三个首部行
`14:16` 首度自办名为user agent
`14:19` 其实是浏览器的类型及版本
`14:24` 这是第四个手部行
`14:26` 首部字段名为accept language
`14:29` 其职为cn
`14:30` 这是告诉服务器用户希望优先得到中文版本的文档
`14:36` 在最后一个手不行的
`14:38` 下面是一个空行
`14:41` 该http请求报文没有实体主体
`14:46` http请求报文支持以下方法我们就不再逐一介绍了
`14:53` 再来看http响应报文的格式
`14:57` http响应报文的第一行是状态行
`15:01` 由版本字段开始
`15:04` 旗号跟一个空格后跟状态码字段旗号跟一个空格后跟短语字段
`15:13` 最后是回车换行
`15:16` 除状态行外
`15:18` http响应报文的其他部分与http请求报文格式的对应部分是相似的
`15:25` 我们就不再赘述了
`15:28` 状态行中的状态码共分为五大类
`15:32` 共33种
`15:34` 我们举例说明响应报文中常见的状态行
`15:38` 该状态还表示服务器接受了请求
`15:43` http一点一表示版本202是状态吗
`15:49` accepted是短语
`15:51` 也就是对状态码的简单描述
`15:55` 该状态行表示请求错误
`15:58` 而该状态行表示找不到所请求的页面
`16:03` 一般来说
`16:05` 浏览器并不会直接显示出服务器发来的这些状态行信息
`16:10` 而是以更友好的形式向用户告知服务器所返回的状态信息
`16:17` 例如当我们访问某些网站时
`16:20` 浏览器可能会显示类似该图所示的提示信息
`16:25` 其背后的本质是浏览器收到了包含这条状态行的响应报文
`16:33` 在我们访问网站时
`16:35` 浏览器通常会使用cookie在服务器上记录用户信息
`16:41` 早期的万维网应用非常简单
`16:44` 仅仅是用户查看存放在不同服务器上的各种静态的文档
`16:50` 因此http被设计成为一种无状态的协议
`16:55` 这样可以简化服务器的设计
`16:59` 现在用户可以通过万维网进行各种复杂的应用
`17:04` 如网上购物
`17:06` 电子商务等
`17:08` 这些应用往往需要万维网服务器能够识别用户
`17:13` 例如我们使用浏览器在某个网站上已经注册了自己的账号
`17:20` 当在该网站上登录
`17:22` 自己的账号时
`17:24` 除了输入用户名和密码外
`17:26` 我们还可以选择记住选项
`17:30` 这样当我们下一次使用该浏览器再次访问该网站时
`17:36` 网站可以自动识别出我们
`17:38` 而不用我们再次输入账号信息
`17:42` cookie提供了一种机制
`17:44` 使得万维网服务器能够记住用户
`17:48` 而无需用户主动提供用户标识信息
`17:52` 也就是说cookie是一种对无状态的http进行状态化的技术
`17:59` 我们来举例说明cookie的工作原理
`18:03` 用户主机中的浏览器进程
`18:05` 首先与万维网服务器中的服务器进程建立tcp连接
`18:10` 当用户的浏览器进程初次向服务器进程发送http请求
`18:16` 报文时
`18:17` 服务器进程就会为其产生一个唯一的cookie识别码
`18:22` 并以此为索引
`18:24` 在服务器的后端数据库中创建一个项目
`18:28` 用来记录该用户访问该网站的各种信息
`18:32` 接着就会给浏览器进程发回http响应报文
`18:37` 在响应报文中包含有一个首部字段为set cookie的手部行
`18:44` 该字段的取值就是cookie识别嘛
`18:48` 当浏览器进程收到该响应报文之后
`18:51` 就在一个特定的cookie文件中添加一行记录该服务器的域名和cookie的识别码
`19:00` 当用户再次使用浏览器访问这个网站时
`19:04` 每发送一个http请求报文
`19:07` 浏览器都会从cookie文件中取出该网
`19:11` 站的cookie识别码并放到http请求报文的cookie首部行中
`19:17` 服务器根据cookie识别码就可以识别出该用户
`19:22` 并返回该用户的个性化网页
`19:26` 接下来我们介绍万维网缓存与代理服务器
`19:32` 在万维网中还可以使用缓存机制
`19:36` 以提高万维网的效率
`19:38` 万维网缓存又称为web缓存
`19:42` 可位于客户机
`19:44` 也可位于中间系统上
`19:47` 位于中间系统上的web缓存
`19:49` 又称为代理服务器
`19:52` web缓存
`19:53` 把最近的一些请求和响应占存在本地磁盘中
`19:59` 当新请求到达时
`20:01` 若发现这个请求与暂时存放的请求相同
`20:05` 就返回暂存的响应
`20:08` 而不需要按统一资源定
`20:10` 位符的地址再次去英特网访问该资源
`20:14` 我们来举例说明
`20:16` 假设这是英特网上的某台万维网服务器
`20:20` 为了与外围网代理服务器的名称区分
`20:24` 我们简称该服务器为原始服务器
`20:28` 这是校园网中的某台万维网代理服务器
`20:32` 我们简称其为代理服务器
`20:35` 当校园网中的某台主机要访问英特网上的原始服务器时
`20:41` 他首先会向校园网中的代理服务器发送请求
`20:46` 若代理服务器中存放有所请求的对象
`20:50` 则代理服务器会向该主机发回包含所请求对象的响应
`20:56` 若代理服务器中没有所请求的对象
`20:59` 则代理服务
`21:01` 器会向英特网上的原始服务器发送请求
`21:05` 原始服务器将包含有所请求对象的响应发回给代理服务器
`21:12` 代理服务器将该响应存入web缓存
`21:15` 然后给主机发回该响应
`21:19` 可以想象
`21:20` 如果外部缓存的命中率比较高
`21:23` 则路由器r一和r之间的链路上的通信量将大大减少
`21:29` 因而可以减少校园网各主机访问因特网的时延
`21:35` 有的同学可能会有这样的疑问
`21:37` 这是原始服务器中的某个文档
`21:41` 这是该文档在代理服务器中的副本
`21:45` 假设原始服务器中的该文档已被更改之后
`21:49` 校园网中的某台主机要请求该文档
`21:53` 他首先向校园网中的代理服务器发送请求
`21:58` 代理服务器找到该文档后
`22:01` 将其封装在响应报文中发回给主机
`22:06` 这样主机所请求到的文档与原始服务器中的文档就不一致了
`22:13` 实际上
`22:14` 原始服务器通常会为每个响应的对象设定一个修改时间字段
`22:19` 和一个有效日期字段
`22:22` 当校园网中的某台主机要请求原始服务器中的该文档时
`22:28` 他首先向校园网中的代理服务器发送请求
`22:33` 若代理服务器中的该文档未过期
`22:36` 则代理服务器将其封装在响应报文中发回给主机
`22:42` 若代理服务器中的该文档已过期
`22:46` 则
`22:46` 代理服务器会向英特网上的原始服务器发送请求
`22:52` 在请求报文中包含有一个首部字段为if modified since的首部行
`22:59` 该字段的取值就是该文档的修改日期
`23:03` 原始服务器根据该文档的修改日期
`23:06` 就可判断出代理服务器中存储的该文档是否与自己存储的该文档一致
`23:13` 如果一致
`23:14` 则给代理服务器发送不包含实体主体的响应状态码为304
`23:21` 短语为not modified
`23:24` 代理服务器重新更新该文档的有效日期
`23:27` 然后将该文档封装在响应报文中发回给主机
`23:33` 如果不一致
`23:35` 则给代理服务器发送封装有该文档的响应报
`23:39` 文如图所示
`23:43` 这样代理服务器就更新了该文档
`23:47` 然后将更新后的该文档封装在响应报文中发回给主机
`23:54` 最后我们来做两个相关的练习题
`23:59` 这是计算机专业考研全国统考计算机网络部分2015年的题40
`24:07` 请同学们暂停播放视频
`24:09` 思考一下
`24:11` 给出你的答案
`24:15` 答案是选项c
`24:17` 大家都选对了吗
`24:19` 我们来一起分析一下请求报文的第一行为请求行指明了方法
`24:27` get所请求对象的统一资源定位符以及http的版本
`24:34` 因此选项a的描述正确
`24:37` 这是请求行下面的第一个首部行
`24:41` 首部字段名为host
`24:43` 其职为网站的域名
`24:45` 因此选项b的描述正确
`24:49` 这是第二个手不行
`24:51` 首部字段名为connection
`24:54` 其职为close
`24:56` 这是告诉服务器发送完请求的文档后
`25:00` 就可以释放连接
`25:02` 即非持续连接
`25:05` 若是持续连接方式取值
`25:08` 因为keep alive
`25:09` 而不是close
`25:11` 因此选项c的描述错误
`25:15` 这是第三个首部行
`25:17` 首部字段名为cookie
`25:19` 其
`25:20` 实是服务器给浏览器生成的qq识别码
`25:24` 表明该浏览器曾经访问过该网站
`25:29` 因此选项d的描述正确
`25:33` 再来做这道练习题
`25:35` 请同学们暂停播放视频
`25:38` 思考一下
`25:39` 给出你的答案
`25:46` 本题稍有难度
`25:47` 我们来一起分析一下
`25:50` 这是浏览器与服务器之间通过三报文握手进行tcp连接
`25:57` 在这三个报文中的最后一个报文的数据载荷部分携带有http请求
`26:04` 报文请求的对象是rfc.html页面
`26:10` 服务器收到后给客户发回包含有该对象的http响应报文
`26:18` 由于rfc.html页面中引用了另外两个jpg小图像
`26:25` 则浏览器会继续依次分别请求这两个对象
`26:30` 很显然浏览器从开始建立tcp连接到收到全部内容为止
`26:37` 需要四个rtt
`26:40` 本题的解题关键在于对http中持续连接方式的理解
`26:46` 以及非流水线方式的理解
`26:50` 由于这些概念刚刚在本节课中介绍过
`26:54` 因此就不再赘述了
`26:57` 本节课到这里就结束了
`27:00` 我们将本节课的内容小结如下
`27:06` 同学们
`27:07` 我们下节课再见