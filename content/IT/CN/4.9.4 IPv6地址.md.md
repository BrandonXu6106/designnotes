---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=74"
bvid: "BV18g411D7Fm"
cid: "817199392"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817199392&page=74&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:09` 本节课我们介绍ipv 6地址的相关内容
`00:14` 首先介绍ipv 6地址空间大小
`00:18` 在ipv 6中
`00:19` 每个地址占128个比特
`00:22` 因此ipv 6地址空间大小为
`00:25` 二的128次方
`00:27` 该数值大于3.4乘以十的38次方
`00:33` 如果整个地球表面
`00:36` 都覆盖着需要ipv 6地址的通信设备
`00:40` 那么ipv 6允许每平方米拥有7x10的
`00:45` 23次方个ipv 6地址
`00:48` 如果ipv 6地址分配速率是
`00:51` 每微秒分配100万个ipv 6地址
`00:54` 则需要十的19次方
`00:57` 年这么长的时间才能
`00:59` 将所有可能的地址分配完毕
`01:02` 很显然这样巨大的地址空间
`01:05` 在采用合理编制方法的情况下
`01:08` 在可预见的未来是不会用完的
`01:12` 接下来我们介绍ipv 6地址的表示方法
`01:16` 如图所示
`01:19` 这是128比克的某个ipv 6地址
`01:23` 很显然这种表示方法非常不方便
`01:27` 我们可将每16个比特分为一组
`01:30` 共八组
`01:32` 如图所示
`01:35` 每组之间使用冒号分割
`01:37` 如图所示
`01:41` 将每组中的每四个比特
`01:43` 转换为一个16进制数
`01:45` 如图所示
`01:50` 这样就可以得到该ipv 6地址的冒号
`01:53` 16进制技法如图所示
`01:58` 请同学们注意
`01:59` 这种技法不区分大小
`02:02` 写
`02:03` 在ipv 6地址的冒号
`02:05` 16进制技法的基础上
`02:07` 在使用左侧零省略和连续零压缩
`02:11` 可以使ipv 6地址的表示更加简洁
`02:15` 左侧零省略是指两个冒号间的16进制
`02:19` 树桩
`02:20` 最前面的一串零可以省略
`02:22` 不写
`02:23` 连续零压缩是指一连串连续的零
`02:26` 可以用一对冒号取代
`02:29` 我们来举例说明
`02:31` 这是某个采用冒号16进制技法的
`02:34` ipv 6地址
`02:35` 这里的一个零
`02:37` 这里的两个零
`02:39` 还有这里的一个零
`02:41` 可以进行左侧零省略
`02:44` 而这里两个冒号之间的一连串零
`02:47` 可以进行连续零压缩
`02:50` 这样可以得
`02:51` 到更简化的形式
`02:54` 需要注意的是
`02:55` 在一个ipv 6地址中
`02:58` 只能使用一次连续零压缩
`03:00` 否则会导致歧义
`03:03` 我们来举例说明
`03:06` 这是四个不同的ipv 6地址
`03:09` 如果对他们中的每一个地址
`03:12` 都进行多次连续零压缩
`03:14` 则会得到同一个有歧义的地址
`03:20` 这是只使用一次连续零压缩
`03:22` 并使用左侧零省略的简化结果
`03:27` 可以看到得到了四个不同的ipv 6地址
`03:31` 并且他们各自都可以没有歧义的变换回
`03:35` 各自的标准形式
`03:38` 冒号16进制技法
`03:40` 还可结合点分十进制的后缀
`03:43` 这在i p v4 向ipv 6过渡阶段非常有用
`03:48` 我们来举例说明
`03:50` 这是某个ipv 6地址
`03:53` 该地址的这部分采用冒号16进制表示
`03:57` 而这部分采用点分十进制表示
`04:01` 对冒号16进制部分进行连续零压缩
`04:04` 可以得到更简化的表示形式
`04:09` 塞der的斜线表示法在ip v6 中仍然可用
`04:14` 我们来举例说明
`04:16` 这是某个采用冒号16进制和斜线技法的
`04:20` ipv 6地址
`04:22` 斜线后面的数值60
`04:25` 指明了网络前缀为60比特
`04:29` 对其进行左侧零省略
`04:31` 连续零压缩
`04:32` 可以得到更简化的表示形式
`04:36` 接下来我们介绍ipv 6地址的分类
`04:40` ipv 6数据报的目的地址有三种
`04:43` 基本类型
`04:44` 分别是单波多播
`04:48` 任播
`04:50` 单波是指传统的点对点通信
`04:58` 发送到一组计算机中的
`05:00` 每一个ipv 6没有采用广播的数据
`05:04` 而将广播看作多波的一个特例
`05:08` 任播是ipv 6新增的一种类型
`05:11` 任播的终点是一组计算机
`05:14` 但数据报只交付其中的一个
`05:18` 通常是按照路由算法得出的
`05:21` 距离最近的一个rfc 4291文档
`05:25` 对ipv 6地址进行了分类
`05:28` 分别是未指名地址
`05:32` 环回地址
`05:34` 多播地址
`05:35` 本地链路
`05:36` 单播地址
`05:38` 全球单播地址
`05:40` 首先来看未指名地址
`05:43` 未指名地址是128个比特
`05:46` 为全民的地址
`05:48` 可缩写为两个冒号
`05:51` 该地址不能用作目的地址
`05:53` 只能用于还没有配置到一个标准
`05:56` ipv 6地址的主机作为原地址
`06:00` 未指明地址仅有一个
`06:03` 来看环回地址
`06:05` 环回地址是最低
`06:07` 比特为
`06:08` 一其余127个比特为全零的地址
`06:12` 可缩写为两个冒号
`06:15` 后面跟一个一
`06:17` 该地址的作用与ipv 4的环回地址相同
`06:21` 但ipv 6的环回地址只有一个
`06:25` 再来看多播地址
`06:27` 多播地址是最高巴比特为权一的地址和
`06:32` 即为f f00 冒号冒号斜线八
`06:36` ipv 6要多播地址的功能
`06:38` 与ipv 4多播地址相同
`06:40` 这类地址占ipv 6
`06:42` 地址空间的1/256
`06:46` 再来看本地链路单播地址
`06:49` 本地链路单播地址是最高
`06:52` 十比特为1111111010的地址和
`06:57` g为f180 冒号冒号斜线时
`07:02` 即使用户网络没有连接到
`07:04` 特网
`07:05` 但仍然可以使用tcp ip协议连接
`07:09` 在这种网络上的主机
`07:11` 都可以使用本地链路单播地址进行通信
`07:15` 但不能和英格网上的其他主机通信
`07:19` 这类地址占ipv 6
`07:21` 地址空间的1/1024
`07:25` 再来看全球单播地址
`07:28` 全球单播地址是使用的最多的一类地址
`07:32` ipv 6全球单播地址采用三级结构
`07:36` 这是为了使路由器可以更快地查找路由
`07:41` 如图所示
`07:43` 这是三级结构的ipv 6全球单播地址
`07:48` 它由48比特的全球路由
`07:51` 选择前缀16比特的子网标识符
`07:54` 64比特的接口标识符
`07:57` 这三部分构成全球路由
`08:00` 选择前缀分配给公司和机构
`08:03` 用于英特网装路由器的路由
`08:06` 选择相当于ipv 4分类地址中的网络号
`08:12` 子网标识符
`08:13` 用于各公司和机构构建自己的子网接口
`08:18` 标识符
`08:23` 相当于ipv 4分类地址中的主机号
`08:28` 有64个比特
`08:29` 足以将各种接口的硬件地址直接进行编码
`08:34` 这样就不需要使用地址解析协议arp了
`08:39` 本节课到这里就结束了
`08:42` 请同学们快速回忆一下本节课的主要内容
`09:14` 同学们
`09:15` 我们下节课再见