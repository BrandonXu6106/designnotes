---
title: 3.4.2 （2）共享式以太网的争用期、最小帧长和最大帧长（有片头 有字幕 有底部logo）
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=29
bvid: BV18g411D7Fm
cid: "817186671"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817186671&page=29&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍共享式以太网的征用期
`00:14` 最小帧长和最大增长的相关知识
`00:18` 首先介绍共享是以太网的征用期
`00:24` 我们知道
`00:25` 使用c s m a c d协议的共享总线
`00:28` 以太网上的任意站点
`00:30` 在发送帧的过程中都可能会遭遇碰撞
`00:35` 如图所示
`00:36` 这是一个共享总线
`00:38` 以太网
`00:39` 该总线上的任意站点
`00:41` 在发送针的过程中都可能会遭遇碰撞
`00:46` 请同学们思考一下
`00:48` 某个站点从发送帧开始
`00:51` 最长要经过多长时间
`00:54` 才能检测出自己发送的帧
`00:57` 与其他站点发送的真产生了碰撞
`01:03` 很显然
`01:04` 我们应当考虑
`01:06` 位于共享总线以太网两端的两个站点
`01:09` 发送的针产生碰撞的情况
`01:13` 例如本例中的站点a和d
`01:16` 它们分别位于共享总线以太网的两端
`01:20` a与d之间的信号传播时延
`01:23` 就是该以太网单程端到端传播时延即为套
`01:28` 假设纵坐标为时间
`01:31` 我们来看站点a和d都发送帧
`01:34` 并产生碰撞的情况
`01:37` 在t等于零时刻
`01:39` 主机a要发送帧
`01:42` 当检测到总线空闲96比特时间后
`01:45` 立即发动针
`01:47` 在t等于t减去德尔塔时刻
`01:51` 站点a发送了针还未到达站点d
`01:55` 而占点地也要发动针
`01:58` 尽管总线现在已经被站点a占用
`02:01` 但是站点a发送的信号还未到达站点地
`02:06` 因此站点地检测不到总线盲
`02:10` 站点地检测到总线空闲96比特时间后
`02:13` 立即发动针
`02:15` 这必然会产生碰撞
`02:18` 碰撞时刻为t等于t
`02:21` 减去二分之德尔塔时刻
`02:23` 但此时双方都检测不到碰撞
`02:27` 碰撞信号沿总线传播到总线的两端
`02:33` t等于套时刻
`02:35` 站点d首先检测到碰撞
`02:38` 于是立即停止发动针
`02:41` 在t等于二套减去德尔塔时刻
`02:44` 站点a也检测到碰撞
`02:47` 于是立即停止发送帧
`02:51` 站点a和d发送帧都失败了
`02:54` 他们各自要退避一段随机时
`02:56` 间后再重新发送
`02:59` 如果有的同学对该图各时刻点的计算
`03:03` 没有搞懂
`03:04` 可以看看这幅等效的图
`03:07` 图中各时刻点的计算其实是非常简单的
`03:12` 同学们不要想复杂了
`03:14` 早在大家读小学时
`03:16` 就做过类似的相向行径的应用题
`03:21` 从本例可以看出
`03:23` 站点从发送帧开始
`03:25` 最多经过时长二套
`03:28` 即德尔塔趋近于零
`03:30` 就可检测出所发送的针是否遭遇了碰撞
`03:35` 因此共享总线以太网的端到端往返时间
`03:39` 二套被称为征用期或碰撞窗口
`03:44` 它是一个非常重要的参数
`03:47` 站点从发送帧开始
`03:49` 经过征用期
`03:51` 二套这段时间还没有检测到碰
`03:53` 撞就可以肯定这次发送不会产生碰撞
`03:59` 从征用期的概念可以看出
`04:01` 共享总线以太网上的每一个站点
`04:04` 从发送帧开始到之后的一小段时间内
`04:08` 都有可能遭遇碰撞
`04:11` 而这一小段时间的长短是不确定的
`04:15` 它取决于另一个发送帧的站点
`04:18` 与本站点的距离
`04:20` 但不会超过总线的端到端往返传播时延
`04:24` 即一个征用期二套
`04:27` 很显然总线的长度越长
`04:30` 相应的单程端到端传播时延越大
`04:34` 网络中占点数量越多
`04:36` 发生碰撞的概率就越大
`04:40` 因此共享以太网的总线长度不能太长
`04:44` 接入的站点数量也不能太多
`04:48` 十兆比特
`04:49` 每秒共享总线
`04:50` 以太网规定征用期二套的值为512
`04:54` 比特发送时间即51.2微秒
`04:58` 计算如下
`05:02` 单程端到端传播时延套计算如下
`05:08` 假设信号的传播速率为2x10的八次方
`05:12` 米每秒
`05:13` 则总线长度计算如下
`05:19` 需要说明的是
`05:21` 十兆比特每秒共享总线
`05:23` 以太网
`05:24` 将征用期二套的值规定为512比特
`05:28` 发送时间
`05:29` 除考虑了信号传播时延外
`05:32` 还考虑到网络中可能存在
`05:35` 转发器所带来的时延以及产生碰撞时
`05:40` 继续发送32比特或48比特
`05:43` 人为干扰信号所持续的时间
`05:47` 因此单程端到端传播实验套
`05:50` 其实会小于我们的计算值
`05:53` 25.6微秒
`05:59` 以太网规定总线长度不能超过2500百米
`06:07` 与以太网征用期这个知识点相关的
`06:10` 408考研真题
`06:13` 请同学们暂停播放视频
`06:23` 我们来一起分析一下
`06:25` 根据题意和画出如下所示的示意图
`06:30` 题目要求主机甲和乙均检测到冲突
`06:35` 这是本题的一个关键点
`06:38` 主机甲和乙同时发送数据
`06:41` 这种情况就是从开始发送数据时刻起
`06:44` 到两台主机均检测到冲突时
`06:47` 刻纸所需时间最短的情况
`06:51` 信号沿总线传播到总线的中间
`06:55` 会产生碰撞
`06:57` 碰撞信号再传播回主机
`07:00` 很显然
`07:01` 最短需要经过的时长为两主机之间
`07:05` 信号的单程传播时延即为套
`07:09` 再来看这种情况
`07:11` 主机甲首先发送数据
`07:14` 当表示数据的信号
`07:16` 沿总线传播到无限接近主机以时
`07:20` 主机椅也要
`07:21` 发送数据尽管总线并不空闲
`07:25` 但主机已检测到总线空闲
`07:28` 于是发送数据
`07:30` 这必然会产生碰撞
`07:33` 碰撞信号沿总线传播
`07:36` 先到达主机意义之后到达主机甲
`07:40` 这种情况就是从开始发送数据时刻起
`07:45` 到两台主机均检测到冲突时刻
`07:48` 纸所需时间最长的情况
`07:52` 信号传播这段距离所耗费的时间
`07:55` 无限接近于总线端到端单程的传播实验套
`08:00` 同理
`08:01` 碰撞信号传回到主机甲的时间
`08:04` 也无限接近于总线端到端
`08:07` 单程的传播实验套
`08:10` 因此最长需要经过的时长为两主机之间
`08:15` 信号的往返传播时延即为二套使用题目给
`08:20` 定的已知量可计算出套
`08:25` 接下来我们举例说明
`08:27` 共享
`08:28` 是以太网的最小增长和最大增长的问题
`08:32` 假设站点a给b发送一个很短的针
`08:37` 边发送边检测碰撞
`08:40` 由于该针很短
`08:42` 因此很快就发送完毕
`08:45` 站点a不再检测碰撞
`08:49` 假设该针在传送过程中
`08:51` 站点c也要发送帧
`08:54` 站点c在检测到信道空闲
`08:57` 96比特时间后发送帧
`09:00` 这必然会产生碰撞
`09:03` 占点地
`09:04` 最终收到遭遇碰撞的针丢弃即可
`09:08` 然而站点a并不知道已发送完毕的针
`09:12` 遭遇的碰撞
`09:13` 而不会重发该针
`09:16` 这样占点地就无法正确收到该真
`09:21` 为了确保共享总线
`09:23` 以太网上的每一个站点
`09:25` 在发送完一个完整的针之前
`09:28` 能够检测出是否产生了碰撞
`09:31` 真的发送时延就不能少于共享总线
`09:35` 以太网端到端的往返时间
`09:38` 借一个征用期二套
`09:41` 对于十兆比特每秒的共享总线
`09:43` 以太网
`09:44` 其征用期二套的值规定为51.2微秒
`09:49` 因此其最小帧长为512比特
`09:53` 即64字节
`09:55` 计算如下
`09:59` 当某个站点在发送帧时
`10:02` 如果真的前64字节没有遭遇碰撞
`10:06` 那么真的后续部分也就不会遭遇碰撞
`10:11` 也就是说如果遭遇碰撞
`10:14` 就一定是在真的前64字节之内
`10:19` 由于发动针的站点边发动针边检测碰撞
`10:23` 一旦检测到碰撞
`10:25` 就立即终止针的发送
`10:27` 此时已发送的数据量一定小于64字节
`10:32` 因此接收站点收到长度
`10:35` 小于64字节的针
`10:37` 就可判定
`10:38` 这是一个遭遇了碰撞而异常终止的无效针
`10:42` 将其丢弃即可
`10:45` 有的同学可能想到了
`10:48` 既然有最小增长的规定
`10:50` 那是否有最大增长的规定呢
`10:54` 一般来说
`10:55` 真的数据载荷的长度
`10:57` 应远大于真手部和尾部的总长度
`11:02` 这样可以提高帧的传输效率
`11:05` 然而如果不限制数据载荷的长度上限
`11:10` 就可能使得真的长度太长
`11:13` 这会带来一些问题
`11:15` 我们来举例说明
`11:17` 如图所示
`11:19` 站点a给b发送一个很长的针
`11:23` 这会使得占点a长时间占用总线
`11:27` 而总线上的其他站点
`11:29` 迟迟拿不到总线的使用权
`11:33` 另外由于针很长
`11:36` 还可能导致站点地的接收缓冲区无法装下
`11:40` 该针而产生溢出
`11:43` 因此以太网版本二的马可
`11:46` 真的数据载荷的最大长度
`11:48` 被限制为1500字节
`11:51` 加上手部和尾部
`11:53` 以太网版本二的马克
`11:55` 真的最大长度为1518字节
`11:59` 而数据载荷的最小长度规定为46字节
`12:03` 以满足最小增长为64字节的要求
`12:11` 与以太网最小帧长
`12:13` 这个知识点相关的408考研真题
`12:18` 请同学们暂停播放视频
`12:22` 然后给出你的答案
`12:28` 本题的答案是选项d
`12:31` 大家都选对了吗
`12:33` 我们来一起分析一下
`12:36` 根据本节课的知识可知
`12:38` 最小帧长等于数据传输速率乘以征用
`12:43` 其二套要减小最小帧长
`12:46` 在数据传输速率不变的情况下
`12:49` 就要减少征用期二套
`12:52` 这相当于减少最远的两个站点之间的距离
`12:56` 即减少总线长度
`12:59` 因此选项a和b被首先排除
`13:04` 设最远两个站点之间的距离为d
`13:07` 最小增长为l
`13:09` 与题目给定的相关已知量一起带入上市
`13:13` 如下所示
`13:16` 简化该式可得距离d与最小增长l的关系
`13:22` 显然若最小增长减少800比特
`13:27` 最远的两个站点之间的距离
`13:29` 至少需要减少80米
`13:32` 本节课到这里就结束了
`13:35` 请同学们快速回忆一下本节课的主要内容
`14:03` 同学们
`14:04` 我们下节课再见