---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=70"
bvid: "BV18g411D7Fm"
cid: "817198022"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817198022&page=70&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:09` 本节课我们介绍
`00:11` 多播路由选择协议的相关知识
`00:14` 首先介绍多波路由选择协议
`00:18` 多播路由选择协议的主要任务是
`00:20` 在多播路由器之间
`00:22` 为每个多波组建立一个多波转发数
`00:27` 多波转发数
`00:28` 连接多波源
`00:29` 和所拥有该多播组成员的路由器
`00:34` 目前有以下两种方法来构建多波转发数
`00:38` 一种是基于元素多播路由选择
`00:42` 另一种是组共享数多播路由选择
`00:46` 首先来看基于元素的多播路由选择
`00:51` 基于元素
`00:52` 多播路由选择的最典型算法是反向路径
`00:55` 多波算法
`00:57` 其英文缩写词为rpm
`01:00` rpm算法包含以下两个步骤
`01:05` 利用反向路径广播算法
`01:07` 建立一个广播转发数
`01:09` 反向路径广播的英文缩写词为r p b
`01:15` 利用剪枝算法
`01:17` 剪除广播转发数中的下游非成员路由器
`01:21` 获得一个多波转发数
`01:24` 要建立广播转发数
`01:26` 可以使用红算法
`01:28` 我们来举例说明
`01:31` 如图所示
`01:33` 路由器r1
`01:35` 象棋邻居路由器r3 发送广播分组
`01:39` r3 收到后复制该广播分组
`01:42` 并向自己的所有下游邻居路由器转发
`01:47` 就像除r e外的所有民居路由器转发
`01:51` 以此类推
`01:53` 这种方法
`01:54` 最终可将广播分组的副本
`01:56` 传送到网络中的所有节点
`01:59` 这是红算法在五环网络中的使用情况
`02:04` 然而如果网络中存在环路使用红犯法
`02:09` 会产生严重的问题
`02:12` 借广播分组的一个或多个副本
`02:15` 将在环路中永久兜圈
`02:18` 如图所示
`02:28` 这种无休止的广播分组的复制和转发
`02:32` 最终将导致该网络中产生大量的广播分组
`02:36` 使得网络带宽被完全占用
`02:40` 利用反向路径广播
`02:42` rpb算法生成的广播转发数不会存在环路
`02:47` 因此可以避免广播分组在环路中兜圈
`02:51` 而pp算法的要点是
`02:53` 每一台路由器在收到一个广播分组时
`02:57` 先检查该广播分组是否是从原点
`03:01` 经最短路径传送过来的
`03:04` 若是本路由器
`03:06` 就从自己除刚才接收该广播分组的接口
`03:10` 的所有其他接口转发该广播分组
`03:14` 否则丢弃该广播分组
`03:17` 如果本路由器有好几个邻居
`03:20` 路由器都处在到原点的最短路径上
`03:24` 也就是存在好几条同样长度的最短路径
`03:28` 那么只能选取一条最短路径
`03:32` 选取的规则是这几条最短路径中的邻居
`03:36` 路由器的ip地址最小的那条最短路径
`03:40` rpp中
`03:41` 反向路径的意思是在计算最短路径时
`03:45` 把原点当做终点
`03:48` 我们来举例说明
`03:51` 如图所示
`03:54` 为了简单起见
`03:56` 假设各路由器之间的路径距离为一
`04:00` 原点向路由器r一发送了一个广播分组
`04:04` r一收到后
`04:05` 向其邻居路由器r2 和r3 转发
`04:10` r收到r一转发来的广播分组后
`04:13` 发现r一就在r2
`04:15` 自己到原点的最短路径上
`04:18` 因此r象棋邻居路由器r3 和r4
`04:22` 转发该广播分组
`04:24` 同理r3 收到r一转发来的广播分组号
`04:28` 向其邻居路由器r2 和r5 转发
`04:32` r收到r3 转发来的广播分组号
`04:36` 发现r3 不再r自己到原点的最短路径上
`04:41` 因此丢弃r3 转发来的广播分组
`04:45` 同理r3 丢弃r2 转发来的广播分组
`04:51` r4 收到r2 转发来的广播分组号
`04:54` 发现r2 就在r4
`04:56` 自己到原点的最短路径上
`04:59` 因此r4 象棋邻居路由器r5
`05:02` r6 和r7 转发该广播分组
`05:06` 同理r5 收到r3 转发来的广播分组号
`05:11` 象棋邻居路由器r4
`05:13` r7 和r8 转发该广播分组
`05:17` r4 收到r5 转发来的广播分组号
`05:20` 发现r5 不在r4
`05:22` 自己到原点的最短路径上
`05:25` 因此丢弃而无转发来的广播分组
`05:29` 同理r5 丢弃r4 转发来的广播分组
`05:34` 从图中可以看出
`05:36` r7 到原点有两条最短路径
`05:40` 一条是经
`05:41` 过r4 r2 r一到达原点
`05:44` 另一条是经过r5 r3 r一到达原点
`05:50` 假设r4 的ip地址小于r5 的ip地址
`05:54` 因此选择经过r4 的那条最短路径
`05:59` 即r7 只转发r4 转发来的广播分组
`06:03` 而丢弃r5 转发来的广播分组
`06:07` 经过上述过程
`06:09` 最终可以得出转发广播分组的广播转发数
`06:14` 以后
`06:15` 就按该广播转发数转发广播分组
`06:18` 这样就避免了广播分组兜圈子
`06:22` 同时每一个路由器也不会收到
`06:25` 重复的广播分组
`06:27` rpb虽然很好地解决了转发环路的问题
`06:31` 但只是实现了广播
`06:33` 要实现真正的多波
`06:35` 还要将像r8 这样
`06:37` 的没有多播组成员的非成员节点
`06:40` 从广播转发数上减除
`06:43` r8
`06:44` 通过rgmp协议发现自己没有多播组成员
`06:48` 若r8 也没有下游路由器
`06:51` 即r8 是业界电
`06:53` 则r8 象棋上游路由器r5
`06:56` 发送一个减脂报文
`06:58` 将自己从多多转发数上减除
`07:03` 如果被减至的路由器
`07:04` 通过i g m p又发现了新的多播组成员
`07:09` 则会向上游路由器发送一个嫁接报文
`07:13` 并重新加入到多波转发数中
`07:17` 需要说明的是
`07:18` 尽管r通过i g m p
`07:21` 发现自己没有多播组成员
`07:24` 但也要保留r2
`07:26` 以确保多波转发数的连通性
`07:29` 再来看组共享数多播路
`07:32` 由选择组共享数
`07:35` 多播路由
`07:35` 选择采用基于核心的分布式生成树算法
`07:39` 来建立共享数
`07:41` 该方法在每个多播组中
`07:44` 指定一个核心路由器
`07:47` 以该路由器为根
`07:48` 建立一棵连接多播组的所有成员
`07:52` 路由器的生成数作为多波转发数
`07:56` 每个多波组装除了核心路由器
`07:59` 其他所有成员路由器
`08:01` 都会像自己多播组中的核心路由器
`08:04` 单播加入报文
`08:06` 加入报文
`08:07` 通过单播朝着核心路由器转发
`08:10` 直到它到达已经属于该多波生成树的
`08:14` 某个节点
`08:15` 或者直接到达核心路由器
`08:18` 加入报文所经过的路径
`08:20` 就确定了一条从单播该报
`08:23` 文的边缘节点到核心路由器之间的分支
`08:27` 而这个新分支
`08:29` 就被嫁接到现有的多波转发数上
`08:32` 我们来举例说明
`08:35` 如图所示
`08:37` 假设这些主机都是同一个多播组的成员
`08:41` r5
`08:42` 被选择作为该多波转发数的核心路由器
`08:47` 而是首先加入该多波转发数
`08:51` 而四象r5 单播加入报文链路
`08:55` r4 到r5
`08:56` 就要成为该多波转发数的初始生成数
`09:01` r3 通过r6 像r5 单播加入报文
`09:05` 已加入该生成数
`09:07` 单播路径从r3 经过r6 到达r5
`09:12` 这会使路径r3 r6
`09:14` r5 被嫁接到该生成树上
`09:19` r2 通过像r5 直接单播加入
`09:21` 报文一加入该生成数是链路r2
`09:26` r5 被嫁接到该生成树上
`09:30` r7 通过像r5 单播加入报文
`09:33` 来加入该生成数
`09:35` 由于r7 到r5 的单播路径要经过r6
`09:39` 而r6 已经加入了该生成数
`09:43` 因此r7 的加入报文到达r6
`09:46` 就会使链路r7
`09:48` r6 立即被嫁接到该生成树上
`09:52` 经过上述过程
`09:54` 就形成了以r5 为核心
`09:56` 路由器的多波转发数
`09:59` 由于r一没有该多播组的成员
`10:02` 因此r一不会像r5 单播加入报文
`10:07` 所以r一不在该生成树上
`10:10` 假设原主机给该多播组
`10:13` 发送了一个多波分组
`10:15` 当r一收到后
`10:17` 会将该多
`10:18` 波分组封装到目的地址
`10:21` 为核心路由器r5 的单播分组装
`10:24` 将该单部分组发送给二五
`10:27` r5
`10:28` 收到后
`10:29` 将封装在单波分组中的多波分组解封出来
`10:33` 在多播转发数上红泛转发
`10:37` 最后我们简单介绍一下
`10:40` 英特网的多播路由选择协议
`10:43` 目前还没有
`10:44` 在整个因特网范围使用的
`10:46` 多波路由选择协议
`10:49` 下面是一些建议使用的多播路由选择协议
`10:52` 如图所示
`10:57` 尽管英特网工程任务组i t f
`11:00` 努力推动着英特网上的全球
`11:03` 多波主干网的建设
`11:05` 但至今在英特网上的ip多波
`11:08` 还没有得到大规模的应用
`11:11` 主要原因是
`11:13` 改变一个已成功运行
`11:15` 且广泛部署的网络层协议
`11:18` 是一件极其困难的事情
`11:21` 目前ip多播主要应用在一些局部的园区网
`11:25` 络专用网络或者虚拟专用网中
`11:29` 需要说明的是
`11:31` p to p技术的广泛应用
`11:33` 推动了应用层多播技术的发展
`11:36` 许多视频流公司和内容分发公司
`11:40` 通过构建自己的应用层多播覆盖网络
`11:44` 来分发他们的内容
`11:46` 但上述多播路由选择协议的算法思想
`11:50` 在应用层多波中依然适用
`11:54` 本节课到这里就结束了
`11:57` 请同学们快速回忆一下本节课的主要内容
`12:19` 同学们
`12:19` 我们下节课再见