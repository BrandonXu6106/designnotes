---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=110"
bvid: "BV18g411D7Fm"
cid: "817212744"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817212744&page=110&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍防火墙
`00:12` 访问控制与入侵检测系统的相关内容
`00:17` 尽管之前几节课介绍的
`00:22` 网络提供保密性
`00:23` 报文完整性
`00:25` 实体鉴别和不可否认性的基本的安全服务
`00:30` 但仍无法应对以下安全问题
`00:33` 非法用户利用系统漏洞进行未授权的登录
`00:38` 授权用户非法获取更高级别的权限
`00:42` 通过网络传播病毒蠕虫和托洛伊木马
`00:47` 阻止合法用户正常使用
`00:49` 服务的拒绝服务攻击
`00:52` 为了降低上述安全威胁所带来的安全风险
`00:57` 可以使用防火墙来严格控制
`01:00` 出入网络边界的分组
`01:02` 禁止任何不必要的通信
`01:04` 从而减少入侵的发生
`01:08` 由于防火墙不可能阻止所有入侵行为
`01:12` 可使用入侵检测系统作为第二道防线
`01:16` 通过对进入网络的分组
`01:18` 进行检测与深度分析
`01:21` 来发现疑似入侵行为的网络活动
`01:24` 并进行报警
`01:26` 以便进一步采取相应措施
`01:30` 我们首先介绍防火墙的相关知识
`01:34` 防火墙属于一种访问控制技术
`01:38` 具体实现为一种可编程的特殊路由器
`01:42` 防火墙
`01:43` 把一个单位的内部网络
`01:45` 与其他网络进行安全隔离
`01:49` 这里的单位是指机构或公司
`01:52` 其他网络一般指英特网
`01:55` 例如下图所示
`01:57` 根据防火墙装配置的访问控制策略
`02:00` 某些分组允许通过防火墙
`02:03` 而某些分组则被禁止通过
`02:06` 如图所示
`02:13` 访问控制策略
`02:14` 由使用防火墙的单位根据自己的安全需要
`02:18` 自行制定
`02:20` 实现防火墙技术的设备一般有两种
`02:23` 分别是分组过滤路由器
`02:26` 应用网关
`02:28` 首先来看分组过滤路由器
`02:32` 分组过滤路由器
`02:33` 是一种具有分组过滤功能的路由器
`02:37` 他根据所配置的分组过滤规则
`02:40` 对出入内部网络的分组执行转发或丢弃
`02:44` 即分组过滤
`02:46` 分组过滤规则
`02:48` 所采用的各参数
`02:50` 来自分组的网际层和运输层
`02:53` 手部中的某些字段的值
`02:56` 例如网际层的原ip地址和目的ip地址
`03:01` 运输层的原端口和目的端口
`03:05` 以及协议类型等
`03:09` 我们来举例说明
`03:11` 如图所示
`03:14` 如果给分组过滤路由器配置一条
`03:17` 将目的端口号为二幺的初分组
`03:21` 进行阻拦的分组过滤规则
`03:24` 则本单位可信网络中的用户
`03:27` 将无法从不可信的英特网中的所有
`03:30` ftp服务器下载文件
`03:33` 这是因为tcp ip应用层中的ftp协议
`03:37` 使用运输层tcp的端口号为二一
`03:41` 用于控制连接
`03:43` 不知道同学们是否还记得该知识点
`03:48` 通常分组过滤规则
`03:51` 以访问控制列表的形式
`03:53` 存储在分组过滤路由器中
`03:56` 访问控制列表的英文缩写词为a c l
`04:00` 网络管理人员可以通过命令行和图形界面
`04:05` 来配置acl中的规则
`04:08` 下面给出一个简单的sl例子
`04:13` 规则
`04:13` 一和二
`04:14` 使得内网中的主机
`04:16` 可以访问英特网中的各种web服务器规则
`04:21` 三和四
`04:22` 使得英特网中的主机可以访问内网中
`04:25` ip地址为要求2.16
`04:28` 8.1.252的电子邮件服务器
`04:32` 规则五则
`04:33` 禁止英特网中ip地址为211.6
`04:37` 7.2
`04:38` 30.1的主机访问内网中的任何主机
`04:43` 需要说明的是
`04:45` 该sl所进行的分组过滤是无状态的
`04:53` 更强的分组过滤
`04:54` 路由器还支持有状态的分组过滤
`04:58` 也就是要跟踪每个连接或绘画的通信状态
`05:03` 并根据这些状态信息来决定是否转发分组
`05:09` 从上述介绍可以看出
`05:11` 分组过滤路由器简单高效
`05:14` 对用户是透明的
`05:17` 也就是用户感觉不到
`05:19` 三组过滤路由器的存在
`05:22` 然而分组过滤路由器
`05:25` 不能对应用层数据进行过滤
`05:28` 例如不能禁止某个用户
`05:31` 对某个特定网络应用进行某个特定的操作
`05:35` 也不支持应用层用户鉴别
`05:39` 这些功能需要使用应用网关来实现
`05:44` 接下来我们就来看应用网关
`05:48` 因为网关又称为代理服务器
`05:51` 它可以实现应用层数据的过滤
`05:55` 鉴别
`05:56` 当用户通过应用网关
`06:01` 应用网关可以要求用户进行身份鉴别
`06:06` 然后根据用户身份
`06:08` 对用户做出相应的访问控制
`06:11` 我们来举例说明
`06:14` 假设图中的应用网关具有邮件网关的功能
`06:18` 该邮件网关收到邮件时
`06:21` 除了检查邮件地址
`06:28` 最终决定该邮件是否能通过邮件网关
`06:34` 因为网关也有一些局限
`06:36` 例如每种网络应用
`06:39` 都需要一个专用的应用网关
`06:42` 当然多个不同的应用网关
`06:45` 可以运行在同一台主机上
`06:48` 在应用层处理和转发报文处理负
`06:52` 担角状应用网关对应用程序是不透明的
`06:58` 用户需要在应用程序客户端
`07:01` 指明应用网关接代理服务器的地址
`07:06` 通常可将分组过滤路由器和应用网关
`07:10` 这两种防火墙技术结合使用
`07:13` 如图所示
`07:18` 和两个分组过滤路由器
`07:21` 他们通过两个局域网连接起来
`07:29` 其任务是保护内部网络的安全
`07:33` 主要由网络管理员进行配置和使用
`07:36` 普通的网络用户较少接触
`07:40` 对于普通网络用户使用
`07:43` 更多的是个人防火墙
`07:46` 个人防火墙是一种
`07:48` 安装在用户计算机中的应用程序
`07:51` 它对用户计算机的网络通信行为进行监控
`07:56` 很显然
`08:00` 个人防火墙仅保护单台计算机
`08:04` 并且由用户为其配置分组过滤规则
`08:09` 由于比较简单
`08:10` 一
`08:11` 般无需网络管理员配置
`08:13` 一般操作系统都自带功能较弱的防火墙
`08:19` 尽管防火墙
`08:20` 提高了内部网络的安全防护程度
`08:23` 但仍存在以下局限
`08:27` 对防火墙的配置是否正确和完善
`08:30` 在很大程度上
`08:32` 决定了防火墙可以发挥的安全防护作用
`08:37` 防火墙对恶意代码
`08:39` 例如病毒木马等的查杀能力非常有限
`08:44` 因此不能有效地防止恶意代码
`08:47` 通过网络的传播
`08:49` 由于查杀恶意代码的计算开销非常大
`08:53` 若提高防火墙的查杀力度
`08:55` 则会降低防火墙的处理速度
`08:58` 进而降低用户的网络带宽
`09:02` 防火墙对于一些利用系统漏洞或网
`09:06` 络协议漏洞进行的攻击是无法防范的
`09:10` 攻击者通过分组过滤规则中允许的端口
`09:14` 对某个服务器的漏洞进行攻击
`09:17` 这对于一般的分组过滤路由器
`09:20` 是无法防护的
`09:22` 即使使用应用网关
`09:24` 也必须具有
`09:25` 能够识别该特定漏洞的应用网关
`09:28` 才能阻断攻击
`09:31` 防火墙技术自身也存在不足
`09:35` 例如分组过滤路由器
`09:37` 不能防止ip地址和端口号欺骗应用软件
`09:42` 自身
`09:43` 也可能有软件漏洞
`09:48` 最后我们简单介绍一下入侵检测系统
`09:54` 通过之前的介绍可知
`09:56` 防火墙并不能阻止所有的入侵行为
`10:01` 那么在
`10:02` 入侵已经开始
`10:03` 但还未造成危害
`10:05` 或在造成更大危害之前
`10:07` 及时检测到入侵
`10:09` 并尽快阻止入侵
`10:11` 尽量把危害降到最小
`10:14` 就是非常有必要的
`10:17` 入侵检测系统正是这样一种技术
`10:20` 其英文缩写词为i d s i d
`10:24` 对初入网络的分组执行深度检查
`10:28` 当检查到可疑分组时
`10:31` 会及时向网络管理员发出警报或进行阻断
`10:36` 由于d的误报率较高
`10:39` 因此一般情况下
`10:41` 不建议对可疑分组进行自动阻断
`10:45` ibs能够检测多种网络攻击
`10:49` 例如端口扫描
`10:52` 拒绝服务攻击
`10:55` 网络映射恶意代码系统漏洞
`11:03` 入侵检测系统一般分为
`11:05` 基于特征的入侵检测系统
`11:07` 和基于异常的入侵检测系统
`11:11` 基于特征的入侵检测系统
`11:14` 需要维护一个已知各类攻击的
`11:17` 标志性特征的数据库
`11:19` 检测到与某种攻击特征匹配的分组或
`11:23` 分组序列时
`11:25` 就判断可能出现了某种入侵行为
`11:30` 标志性特征必须具有很好的区分度
`11:34` 标志性特征一般由网络安全专家提供
`11:38` 由单位的网络管理员定制
`11:41` 并将其加入到数据库中
`11:45` 很显然
`11:46` 基于特征的入侵检测系统
`11:48` 只能检测已知攻击
`11:51` 对于位置攻击则无法防范
`11:55` 基于异常的入侵检测系统
`11:58` 通过观
`11:59` 察正常运行的网络流量
`12:06` 检测到网络流量的某种统计规律
`12:09` 不符合正常情况时
`12:11` 则判断可能发生了入侵行为
`12:16` 区分正常流量和统计异常流量
`12:18` 是非常困难的
`12:21` 现在很多研究
`12:28` 减少对网络安全专家的依赖
`12:31` 衡量入侵检测系统的重要依据
`12:37` 若漏报率比较高
`12:39` 则只能检测到少量入侵
`12:42` 给人以安全的假象
`12:45` 对于特定的入侵检测系统
`12:48` 可以通过调整某些参数或阈值
`12:52` 来降低漏报率
`12:54` 但这同时会增大误
`12:56` 报率若误报率太高
`12:59` 会导致大量的虚假警报
`13:02` 网络管理员需要花费大量时间
`13:07` 甚至会因为虚假警报太多
`13:09` 而对警报视而不见
`13:16` 本节课到这里就结束了
`13:19` 请同学们快速回忆一下本节课的主要内容
`14:03` 同学们
`14:04` 我们下节课再见