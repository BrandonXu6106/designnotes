---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=111"
bvid: "BV18g411D7Fm"
cid: "817213221"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817213221&page=111&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍常见的网络攻击及其防范
`00:15` 首先介绍网络扫描的相关知识
`00:19` 网络扫描是获取攻击目标信息的一种重要技术
`00:24` 攻击目标信息包括目的主机的ip地址
`00:28` 操作系统运行的程序及存在的漏洞等
`00:33` 在进行网络攻击之前
`00:35` 对攻击目标的信息掌握的越全面和具体
`00:39` 就越能有效合理地制定出攻击策略和攻击方法
`00:44` 进而提高网络攻击的成功率
`00:48` 网络扫描主要有四种类型
`00:51` 分别是主机发现端口扫描
`00:56` 操作系统检测漏洞扫描
`01:00` 我们首先来看主机发现主机发现是指搜索要攻击的主机
`01:08` 这是对其进行攻击的前提
`01:11` 搜索要攻击的主机
`01:13` 实际上是要确定该目标主机的ip地址
`01:18` 进行主机发现的主要方法是利用网际控制报文协议samp
`01:25` 如图所示
`01:28` 攻击者向目标主机发送s m p查询报文
`01:33` 目标主机收到后会发送s mp应答报文作为响应
`01:39` 攻击者收到后就知道了该目标主机正在运行
`01:45` 基于该原理
`01:46` 攻击者可以使用ping命令
`01:48` 对某个ip地址范围内的所有ip地址进行连通性测试
`01:54` 来发现正在运行的目标主机
`01:58` 那么为了防范聘扫描
`02:01` 可以配置主机或服务器对s m p查询报文不进行响应
`02:08` 也可以配置网络防火墙
`02:10` 不允许通过i c a m p查询报文
`02:14` 我们来举例说明
`02:16` 如图所示
`02:18` 这是我在家中自己的主机上使用浏览器访问湖南科技大学官网的情况
`02:25` 可以正常访问
`02:28` 然而当我在windows命令行中使用拼命令测试
`02:33` 与湖南科技大学官网的联通信时
`02:36` 却迟迟收不到应答
`02:39` 这有可能是拼命令使用的s m p查询报文被学校的防火墙阻断了
`02:46` 也有可能是网站服务器被设置成不对s m p查询报文进行响应
`02:54` 针对防火墙对s m p查询报文进行阻断的情况
`03:00` 攻击者可能会向目标主机故意发送手部有错误的ip数据报
`03:06` 当目标主机收到这种ip数据报时
`03:10` 会向攻击者发送i c a m p差错报告报文
`03:15` 一般情况下
`03:17` 防火墙不会阻断c m p差错报告报文
`03:22` 否则目标主机将失去进行s m p差错报告的功能
`03:28` 而影响正常的网络通信
`03:32` s m p插座报告报文穿过防火墙到达攻击者
`03:37` 攻击者就知道了
`03:38` 目标主机正在运行
`03:41` 下面来看端口扫描
`03:45` 在进行主机发现确定了要攻击的目标主机后
`03:50` 攻击者可以进一步通过端口扫描来获取目标主机所有端口的工作状态
`03:57` 进而得出目标主机上开放了哪些网络服务
`04:02` 这是tcp ip体系结构的应用层常用协议
`04:07` 各自使用的运输层端口号
`04:10` 攻击者可以作为tcp客户尝试与tcp服务器的某个端口建立tcp连接
`04:18` 来检测该tcp端口是否处于工作状态
`04:24` 对于udp攻击者可以向某个udp目的端口发送udp用户数据报
`04:31` 若目标主机上的该目的端口处于关闭状态
`04:36` 目标主机就会发回s m p差错报告报文进行响应
`04:42` 报告的错误类型为端口不可达
`04:46` 攻击者收到s m p差错报告报文号
`04:50` 就可推断出该udp端口处于关闭状态
`04:55` 若目标主机上的该目的端口处于打开状态
`05:00` 由于该udp用户数据报是攻击者发送的
`05:04` 其内容一般不可能满足接收方的要求
`05:08` 因此接收方不会做出任何响应
`05:13` 这样攻击者就不会收到来自目标主机的s m p差错报告报文
`05:20` 因此可以推断出该udp端口处于打开状态
`05:26` 下面来看操作系统
`05:28` 检测不同的操作系统所存在的安全漏洞可能有很多不同
`05:35` 要利用这些安全漏洞进行攻击
`05:38` 首先必须检测远程目标主机所使用的操作系统类型
`05:44` 通常有三种方法
`05:46` 一种是获取操作系统其标信息
`05:51` 在客户机与服务器建立连接的过程中
`05:54` 服务器往往会返回各种独特的欢迎信息
`06:00` 攻击者可根据这些信息来推断出服务器的操作系统类型
`06:06` 另一种操作系统检测方法是获取主机端口状态信息
`06:13` 操作系统通常会默认开启一些常用的网络服务
`06:18` 这些服务会打开各自特定的端口进行网络监听
`06:24` 攻击者可对目标主机进行端口扫描
`06:28` 根据端口扫描结果来推断目标主机的操作系统类型
`06:35` 还有一种操作系统检测方法是获取tcp ip协议栈指纹
`06:41` 尽管rfc文档严格规定了各种协议的三要素及语法
`06:47` 语义和同步
`06:49` 但并没有规定各种协议的具体实现
`06:54` 同一个协议在不同操作系统中的实现细节可能会有所不同
`07:00` 攻击者可据此推断目标主机的操作系统类型
`07:05` 例如这是不同操作系统实现ip协议时
`07:10` ip数据包生存时间ttl的默认值
`07:15` 这是不同操作系统实现tcp协议时
`07:18` tcp窗口默认初始值
`07:23` 再来看漏洞扫描
`07:25` 利用操作系统检测
`07:27` 可以获知目标主机的操作系统类型
`07:31` 利用端口扫描
`07:33` 可以获知目标主机提供了哪些网络应用服务
`07:38` 根据操作系统类型和网络应用服务
`07:41` 和在网络安全机构提供的漏洞库中查找匹配的漏洞
`07:47` 攻击者根据基于网络的漏洞扫描结果
`07:51` 进一步采取攻击措施
`07:54` 当然了
`07:55` 用户自己也可以进行基于主机的漏洞扫描
`08:00` 以便及时修补漏洞
`08:03` 防范上述各种类型的网络扫描
`08:06` 可以采取以下措施
`08:09` 仅打开确实需要使用的端口
`08:12` 关闭闲置和危险端口
`08:15` 限制英特网中的主机主动与内部网络中的主机进行通信
`08:22` 设置防火墙
`08:23` 根据安全要求设置分组过滤策略
`08:27` 例如过滤不必要的s m p报文
`08:32` 使用入侵检测系统
`08:34` 及时发现网络扫描行为和攻击者ip地址
`08:39` 配置防火墙
`08:40` 对来自该地址的分组进行阻断
`08:45` 网络扫描的行为特征是比较明显的
`08:48` 例如在短时间内对某一ip地址范围内的每个地址和端口发起连接等
`08:57` 目前大部分防火墙都具有识别简单的网络扫描行为的功能
`09:04` 然而很多攻击者也在研究如何隐蔽自己的网络攻击行为
`09:10` 例如利用虚假原地址减缓扫描速度
`09:16` 动态调整扫描顺序
`09:18` 分布式扫描等
`09:21` 网络管理员或个人用户
`09:24` 可利用网络扫描工具对系统或个人计算机进行定期检查
`09:30` 以便及时关闭危险端口
`09:33` 发现漏洞
`09:35` 并安装相应的安全漏洞补丁
`09:39` 网络扫描工具实际上是网络安全的双刃剑
`09:43` 网络管理员可以使用它们来检测自己系统的安全漏洞
`09:49` 然而网络攻击者可以使用它们来发现攻击目标
`09:56` 接下来我们介绍网络监听的相关知识
`10:01` 网络中传输的数据大部分都是明文形式
`10:06` 如果攻击者对网络进行监听
`10:09` 并截获了包含有大量明文信息的一系列分组
`10:13` 则可从这些分组中直接分析出账号
`10:17` 密码等敏感信息
`10:20` 常见的网络监听类型有分组嗅探器
`10:25` 交换机
`10:26` 毒化攻击
`10:28` arp欺骗
`10:31` 我们首先来看分组溴氮气分组嗅探器是一种网络监听工具软件
`10:38` 它运行在网络中的某台主机上
`10:41` 被动接收所有出入该主机的网络适配器的数据
`10:46` 链路层协议数据单元
`10:49` 如图所示
`10:51` 这是某个内网主机访问湖南科技大学官网的过程中
`10:55` 主机中的分组嗅探器软件where shark
`10:59` 捕获到的输入该主机网络适配器的一系列相关分组
`11:05` 如果将网络适配器配置为混杂方式
`11:09` 网络适配器就会接收所有进入自己接口的马克真
`11:15` 而不管这些真的目的mac地址是否指向该网络适配器
`11:20` 我们来举例说明
`11:22` 如图所示
`11:24` 对于这个使用极限器互联而成的共享是以太网主机a给b发送单波真
`11:32` 由于极限期的广播特性
`11:35` 该单播真会传送到主机b和c主机b的网卡
`11:41` 根据该单波真的目的mac地址会接受该针
`11:45` 而主机c的网卡会丢弃该帧
`11:49` 然而如果将主机c的网卡配置为混杂方式
`11:54` 则网卡会接受该增802011
`11:59` 无线局域网的情况与共享是以太网类似
`12:02` 这里就不再赘述了
`12:05` 然而对于使用交换机互联而成的交换式以太网
`12:10` 情况就不同了
`12:13` 假设交换机已经完成了自学习
`12:17` 主机a给b发送单波真
`12:20` 该张伯桢会被交换机明确转发给主机b而不会转发给主机c
`12:28` 这样即使将主机c的网卡配置为混杂方式
`12:33` 主机c也无法收到该真
`12:36` 为了让主机c能够捕获到网络中其他主机发送的帧
`12:41` 必须采用一些特殊手段
`12:44` 例如交换机毒化攻击
`12:48` 如图所示
`12:50` 攻击者向以太网交换机发送大量的伪造不同源mac地址的
`12:55` 真以太网交换机
`12:58` 收到这样的帧进行自学习
`13:01` 就把真守不装伪造的原market地址写入自己的转发表中
`13:07` 由于这种伪造原mac地址的帧数量巨大
`13:11` 因此很快就会使以太网交换机的转发表填满
`13:17` 使得真正需要保存的mac地址被更新淘汰
`13:22` 此时如果交换机收到正常的单波真
`13:26` 由于在交换机的转发表中找不到该单波真的物理mac地址
`13:32` 交换机就不得不广播该单播真
`13:36` 该张柏芝到达目的主机后
`13:39` 目的主机根据单波真的目的mark地址接受该单波真
`13:45` 然而网络中的攻击者也会收到该担保真
`13:50` 如果攻击者的网卡被配置为混杂方式
`13:53` 也会接受该单波真
`13:57` 按上述方法
`13:58` 攻击者主机中的分组嗅探器就能监听到网络中其他主机的通信了
`14:06` 除交换机毒化工机外
`14:09` 也可以利用arp欺骗来进行网络监听
`14:14` 如图所示
`14:15` 攻击者c希望监听主机a和b之间的通信
`14:20` 于是c向a发送a arp报文
`14:23` 声称自己是b a收到该报文后
`14:28` 误认为c就是b c还会向b发送arp报文
`14:33` 声称自己是a b收到该报文号
`14:37` 误认为c就是a这会造成a给b发送分组会错误地发送给c
`14:45` c收到后再转发给b
`14:49` b给a发送分组也会错误地发送给c
`14:53` c收到后再转发给a
`14:56` 就这样主机a和b之间的通信完全被攻击者c监听
`15:03` 而a和b却不知道c的存在
`15:06` 实际上a arp欺骗是之前曾介绍过的中间人攻击
`15:13` 设想一下
`15:14` 如果网络中的所有主机都被攻击者进行了a arp欺骗
`15:20` 则攻击者就将自己伪装成了路由器
`15:24` 从而可以监听到网络中的所有通信
`15:28` 这对网络安全造成了极大的危害
`15:32` 防范上述各种类型的网络监听
`15:35` 可采取以下措施
`15:38` 使用交换机替代极限器
`15:41` 这不但可以提高网络性能
`15:44` 还能使攻击者在交换机环境中更难实时监听
`15:49` 禁用交换机的自学习功能
`15:52` 将ip地址mac地址与交换机的接口进行静态绑定
`15:58` 这样可以限制非法主机的接入
`16:01` 使攻击者无法实施交换机毒化攻击
`16:06` 也使a arp欺骗难以实施
`16:10` 对于lp欺骗主机或路由器
`16:13` 可以仅使用静态lp表
`16:16` 而不再依据a arp请求报文或响应报文
`16:20` 动态更新划分微战可以限制攻击者的监听范围
`16:26` 因为分组嗅探器只能在单个局域网范围内进行防范
`16:33` 网络监听的最有效方法是进行数据加密和实体鉴别
`16:40` 最后我们简单介绍一下拒绝服务攻击
`16:44` 拒绝服务攻击是最容易实现却又最难防范的攻击手段
`16:50` 拒绝服务攻击的类型有
`16:53` 基于漏洞的拒绝服务工期
`16:57` 基于资源消耗的拒绝服务攻击
`17:01` 分布式拒绝服务攻击
`17:04` 首先来看基于漏洞的拒绝服务攻击
`17:08` 这种攻击方式主要利用网络协议漏洞或操作系统漏洞
`17:14` 攻击者向目标系统发送一些特殊分组
`17:19` 使目标系统在处理这些分组时出现异常甚至崩溃
`17:24` 这种攻击又称为剧毒包或杀手包攻击
`17:30` 著名的死亡之拼攻击就属于基于漏洞的拒绝服务攻击
`17:36` 如图所示
`17:38` 攻击者向目标主机发送超长的s m p回送请求报文
`17:44` 该报文会被封装成ip数据报进行发送
`17:49` 然而由于该报文的长度太长
`17:52` 这会导致ip数据报的长度超过其最大长度65535字节
`17:59` 目标主机收到这种ip数据报时
`18:02` 会发生内存分配错误
`18:05` 导致堆栈崩溃
`18:06` 系统死机
`18:09` 再来看基于资源消耗的拒绝服务攻击
`18:13` 这种攻击是拒绝服务攻击中采用最多的一种攻击
`18:19` 攻击者通过向目标系统发送大量的分组
`18:23` 从而耗尽目标系统的资源
`18:26` 致使目标系统崩溃而无法向正常用户提供服务
`18:32` tcp s y n洪泛攻击就是一种典型的基于资源消耗的拒绝服务攻击
`18:39` 如图所示
`18:41` 攻击者作为tcp客户
`18:44` 伪造大量不同的ip地址
`18:47` 并以这些ip地址给tcp服务器发送大量tcp连接请求
`18:53` 这是攻击者使用伪造的某个ip地址
`18:57` 向tcp服务器发送的第一个tcp连接请求
`19:03` 由于tcp服务器不能区分正常的tcp连接请求报文段和恶意的tcp连接请求
`19:11` 报文段只能为每个tcp连接请求建立tcp连接
`19:17` 为其分配缓存等资源
`19:20` 并发回相应的tcp连接请求
`19:23` 确认报文段
`19:26` 然而攻击者并不会对tcp连接请求确认报文段
`19:30` 发送普通的确认报文段进行响应
`19:34` 这样就无法完成tcp建立连接的三报文握手过程
`19:41` 导致出现tcp半连接状态
`19:44` 如果攻击者通过大量伪造的ip地址
`19:48` 按上述方法导致tcp服务器产生大量tcp半连接
`19:54` 当半连接的数量超过了tcp服务器允许的上限时
`20:00` tcp服务器就不会再接受任何tcp连接请求
`20:06` 这将导致tcp服务器无法为正常客户提供服务
`20:12` 再来看看与tcp s y n洪泛攻击思想类似的另一种攻击方法
`20:19` 如图所示
`20:21` 攻击者构建大量不同ip数据报的分片
`20:25` 将它们发送给目标主机
`20:28` 但是攻击者不会发送完构成任何一个ip数据报的所有分片
`20:35` 这将导致目标主机一直缓存已收到的部分分片
`20:41` 并一直等待收集一个ip数据报的所有分片
`20:46` 很显然
`20:47` 这将耗费目标主机越来越多的系统缓存
`20:51` 直到系统崩溃
`20:55` 还有一种被称为反射攻击的间接攻击方法
`20:59` 例如smart攻击
`21:02` 如图所示
`21:04` 攻击者a向其所在网络发送大量的i c a m p回送请求报文
`21:11` 这里我们仅以发送一个报文为例
`21:16` s m p回送请求报文被封装在ip数据报中
`21:20` 发送ip数据报的目的ip地址为攻击者a所在网络的广播地址
`21:27` 原ip地址被伪造成主机c的地址
`21:32` 该ip数据报被交换机转发给网络中的其他各主机
`21:38` 这将导致网络中的其他各主机作为反射节点
`21:42` 将封装有s m p回送回答报文的ip数据报
`21:47` 作为响应都发网被攻击的目标主机
`21:52` 这可能导致目标主机崩溃而无法对外提供服务
`21:57` 还有可能导致网络拥塞
`22:00` 很显然
`22:01` 反射攻击具有放大攻击流量的功能
`22:05` 网络中的主机越多
`22:07` 这种放大效果越好
`22:10` 基于资源消耗的拒绝服务攻击
`22:13` 需要攻击者向目标主机发送大量的分组
`22:18` 然而仅靠一个攻击员一般很难达到效果
`22:23` 可以使用分布式拒绝服务攻击来实现大量攻击源
`22:28` 同时发起攻击
`22:30` 如图所示
`22:32` 攻击者首先通过嗅探口令漏洞
`22:35` 渗透木马等非法入侵手段来控制英特网上的大量主机
`22:42` 使这些主机成为僵尸主机
`22:46` 当僵尸主机达到一定数量时
`22:49` 攻击者就要控制这些僵尸主机
`22:52` 同时向某个目标服务器发送大量恶意请求
`22:57` 这会导致该服务器资源耗尽而瘫痪
`23:01` 当其他主机给该服务器发送正常请求时
`23:05` 就会出现拒绝服务的情况
`23:10` 防范上述各种类型的拒绝服务攻击
`23:13` 可以采取以下措施
`23:17` 利用防火墙对恶意分组进行过滤
`23:21` 例如过滤掉所有s m p回送请求报文
`23:25` 可以防范什么攻击
`23:28` 然而对于像tcp s i n红范这类攻击
`23:32` 一般很难区分哪些tcp连接请求报文是恶意的
`23:39` 使用支持原端过滤的路由器
`23:42` 尽量减少ip地址伪造的现象
`23:46` 如果要通过原端过滤来防范dos攻击
`23:50` 则必须使英特网上所有的路由器都具有远端过滤功能
`23:56` 然而目前原端过滤并不是路由器的强制功能
`24:01` 支持原端过滤的路由器还是少数
`24:06` 追溯dos攻击源
`24:08` 路由器对通过自己的ip数据报的手部进行标记
`24:13` 通过该标记可追溯到dos攻击源
`24:17` 这个过程通常很慢
`24:20` 并且需要人工干预
`24:23` 因此目前主要用于事后追查
`24:26` 以及为采取相应的法律手段提供依据
`24:31` 进行dos攻击检测
`24:34` 通过分析分组手部特征和流量特征来检测正在发生的d o s攻击
`24:40` 并发出警报
`24:43` 上述措施并不能彻底防范dos攻击
`24:47` dos攻击是目前最容易实现却又最难防范的攻击手段
`24:54` 本节课到这里就结束了
`24:57` 请同学们快速回忆一下本节课的主要内容
`26:42` 本课程到这里就全部结束了
`26:45` 希望同学们学习了本课程后能够有所收获
`26:49` 同学们
`26:50` 我们有缘再见