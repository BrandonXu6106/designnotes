---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=40"
bvid: "BV18g411D7Fm"
cid: "817189728"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817189728&page=40&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍虚拟局域网微难的实现机制
`00:15` 虚拟局域网微软技术是在交换机上实现的
`00:19` 需要交换机能够实现以下两大功能
`00:23` 一个是能够处理带有微量标记的针
`00:27` 也就是h v e802.1q真
`00:31` 另一个是交换机的各端口可以支持不同的端口类型
`00:36` 不同端口类型的端口对针的处理方式有所不同
`00:41` 我们首先来看i rob 1802点eq真
`00:46` i trible e802.1q针
`00:49` 也称为dot one q真
`00:51` 他对以太网的mac真格式进行了扩展
`00:55` 插入了四字节的微软标记
`00:59` 如图所示
`01:00` 这是以太网版本二的马克真格式
`01:05` 这是插入微量标记后的802.1q帧的格式
`01:10` 微软标记的最后12个比特称为微量标识符vid
`01:16` 它唯一的标识了以太网帧属于哪一个
`01:19` 微辣vid的取值范围是0~4095
`01:25` 其中零和4095都不用来表示微辣
`01:29` 因此用于表示微软的vid的有效取值范围是1~4094
`01:36` 需要注意的是
`01:38` 802点eq真是由交换机来处理的
`01:42` 而不是用户主机来处理的
`01:45` 当交换机收到普通的以太网帧时
`01:49` 会将其插入四字节的微量标记
`01:52` 将其转变为802.1q帧
`01:55` 简称为打标签
`01:58` 当交换机转发802.1q真实
`02:01` 可能会删除其四字节危难标记
`02:05` 将其转变为普通以太网帧
`02:08` 简称为续标签
`02:11` 未按标记字段的其他内容
`02:14` 对我们理解未来划分机制来说并没有什么帮助
`02:18` 因此就不再赘述了
`02:21` 接下来我们介绍交换机的端口类型
`02:26` 交换机的端口类型一般有以下三种
`02:29` 它们分别是access trunk
`02:35` hybrid
`02:37` 需要说明的是
`02:38` 思科交换机没有hybrid端口
`02:42` 在介绍这三种交换机端口类型之前
`02:46` 我们首先需要了解一下端口的缺省
`02:49` vd这个概念在刺客交换机上将其称为本征危难
`02:56` 例如思科交换机在用户未配置危难时
`03:01` 所有端口都默认属于微难一
`03:05` 即所有端口的本征微辣都是微量一
`03:09` 而在华为交换机上将其称为端口微辣
`03:14` 简介为p v i d
`03:17` 在我们接下来的介绍中
`03:19` 为了描述方便
`03:21` 我们采用pvd
`03:23` 而不是本身微难
`03:25` 需要注意的是
`03:27` 交换机的每个端口有且仅有一个pvd
`03:32` 接下来我们首先介绍access端口
`03:36` access端口一般用于连接用户计算机
`03:40` 只能属于一个围栏
`03:44` 因此access端口的pv i d值与端口所属微量的id相同
`03:51` 如图所示
`03:52` 主机abcd分别连接在交换机的一个端口上
`03:58` 交换机首次上电时
`04:00` 默认配置各端口属于微难一
`04:04` 也就是各端口的pvid值等于一
`04:08` 默认配置各端口的类型为access
`04:11` 我们用大写字母a来表示access端口的接收处理方法是
`04:18` 一般只接受未打标签的普通以太网
`04:21` mark真根据接收针的端口的pvid给针打标签
`04:27` 即插入四字节危难标记字段
`04:31` 字段中的vid取值与端口的pv i d取值相等
`04:37` 我们来举例说明
`04:40` 假设主机a发送了一个广播帧
`04:43` 该针从交换机的端口一进入交换机
`04:48` 由于端口一的类型是access
`04:51` 他会对接收到的未打标签的普通以太网mark真打标签
`04:57` 也就是插入四字节的微软标记字段
`05:00` 如图所示
`05:03` 由于端口一的pv i d值等于一
`05:07` 因此所插入的四字节未按标记字段中的v d的值也等于一
`05:14` access端口的发送处理方法是若真中的vip与端口的pv i d相等
`05:21` 则去标签后转发该针
`05:24` 否则不转发
`05:27` 对于本地广播真中的vid的取值与端口234的pv i d取值都等于一
`05:36` 因此交换机会从这三个端口对帧进行去标签转发
`05:43` 再来看这个例子
`05:45` 我们的应用需求是将主机a和b划归到危难二
`05:50` 将主题c和d划归到危难三
`05:54` 这样微蓝二中的广播针不会传送到微难三
`05:59` 未来三中的广播帧也不会传送到微蓝二
`06:04` 为了实现这种应用
`06:06` 可以在交换机上创建微纳二和微量三
`06:10` 然后将交换机的端口一和二划归到微量二
`06:15` 因此端口一和二的pvd值等于二
`06:20` 将交换机的端口三和四划归到微软三
`06:25` 因此端口三和四的pvp值等于三
`06:30` 我们来看主机a发送广播帧的情况
`06:34` 该针从交换机的端口一进入交换机
`06:38` 由于端口一的类型是access
`06:41` 他会对接收到的未打标签的普通以太网mark真打标签
`06:46` 也就是插入四字节的微量标记字段
`06:50` 如图所示
`06:53` 由于端口一的pvp值等于二
`06:57` 因此所插入的四字节未按标记字段中的vid的值也等于二
`07:04` 广播真中的vid的取值与端口二的pvd取值都等于二
`07:10` 因此交换机会从端口二对帧进行去标签转发
`07:17` 我们再来看主机c发送广播帧的情况
`07:21` 该针从交换机的端口三进入交换机
`07:26` 由于张口三的类型是access
`07:29` 他会对接收到的未打标签的普通以太网mark真打标签
`07:34` 也就是插入四字节的微软标记字段
`07:38` 如图所示
`07:41` 由于端口三的pid值等于三
`07:45` 因此所插入的四字节未按标记字段中的vid的值也等于三
`07:52` 广播真中的vid的取值与端口四的pvd取值都等于三
`07:58` 因此交换机会从端口四对帧进行去标签转发
`08:05` 接下来我们介绍串个端口
`08:09` 窗口端口一般用于交换机之间或交换机与路由器之间的互联
`08:16` 创个端口可以属于多个围栏
`08:19` 也就是说创口端口可以接收和发送多个微软的帧
`08:25` 用户可以设置窗口端口的pvd值
`08:30` 默认情况下串个端口的pvd值为一
`08:34` 我们来举例说明创个端口的功能
`08:38` 如图所示
`08:39` 两台交换机互联而成的一个交换式以太网
`08:45` 我们的应用需求是将主机ab ef划归到微软一
`08:51` 将主机cd g h划归到微蓝二
`08:56` 由于交换机首次上电时默认配置各端口属于危难一
`09:01` 其相应的pid值等于一
`09:05` 并且端口的类型为access
`09:08` 因此我们需要对交换机进行相应的配置
`09:12` 才能满足应用需求
`09:15` 分别在两个交换机上创建微难二
`09:19` 并将他们的端口三和四都划归到围栏二
`09:23` 其相应的pv i d值等于二
`09:27` 而两个交换机的端口一和二保持默认配置即可
`09:32` 也就是属于微难一
`09:34` 其相应的pvd值等于一
`09:38` 特别需要注意的是
`09:40` 两个交换机互联的端口五
`09:43` 需要将它们的类型更改为trunk类型
`09:46` 而他们的pvid值保持默认的一即可
`09:52` 串个端口的发送处理方法是对vid等于pvd的真去标签再转发
`10:01` 假设主题a发送了一个广播
`10:04` 真该针从交换机一的端口一进入交换机
`10:10` 由于端口一的类型是access
`10:13` 他会对接收到的未打标签的普通以太网mark真打标签
`10:18` 也就是插入四字节的微软标题字段
`10:21` 如图所示
`10:25` 由于端口一的pv i d值等于一
`10:28` 因此所插入的四字节为了标记字段中的v d的值也等于一
`10:35` 该广播帧中的vid的取值与端口二的pvd值都等于一
`10:41` 端口二的类型是access
`10:44` 因此交换机一会从端口二对帧进行去标签转发
`10:51` 该广播帧中的vid的取值与端口五的pv i d值都等于一
`10:57` 端口五的类型是创客
`11:00` 因此交换机一会从端口五对帧进行去标签转发
`11:06` 很显然该广播帧会从交换机二的端口五进入交换机二
`11:13` 串口端口的接收处理方法是接收未打标签的针
`11:18` 根据接收针的端口的pvid给针打标签
`11:23` 即插入四字节未来标记字段
`11:27` 字段中的vid取值与端口的pvd取值相等
`11:40` 也就是插入四字节的微量标记字段
`11:43` 如图所示
`11:47` 由于端口五的pvid值等于一
`11:50` 因此所插入的四字节危难标记字段中的vid的值也等于一
`11:57` 该广播帧中的vid的取值与端口一和二的pv i d值都等于一
`12:04` 端口一和二的类型都是access
`12:14` 再来看主机c发送广播帧的情况
`12:18` 该针从交换机一的端口三进入交换机
`12:22` 由于端口三的类型是access
`12:25` 他会对接收到的未打标签的普通以太网mark真打标签
`12:31` 也就是插入四字节的危难标记字段
`12:35` 如图所示
`12:38` 由于端口三的pvd值等于二
`12:41` 因此所插入的四字节微量标记字段中的v i d的值也等于二
`12:48` 该广播帧中的vid的取值与端口四的pv i d值都等于二
`12:55` 端口四的类型是access
`12:57` 因此交换机一会从端口四对帧进行去标签转发
`13:04` 该广播帧中的vid的取值与端口五的pvd值不相等
`13:10` 由于串口端口对v i d不等于p v i d的真是直接转发的
`13:16` 因此交换机一会从端口五对帧直接转发
`13:22` 也就是不去掉标签
`13:23` 而带着标签直接转发
`13:27` 很显然
`13:28` 该802.1q广播真会从交换机二的端口五进入交换机二串口端口
`13:36` 接收已打标签的802.1q帧
`13:40` 该广播帧中的vid的取值与端口三和四的pv i d值都等于二
`13:47` 端口三和四的类型都是access类型
`13:59` 通过本币可以看出
`14:01` 再由多个交换机互联而成的交换式以太网中划分危难时
`14:07` 连接主机的交换机端口应设置为access类型
`14:12` 交换机之间互联的端口应设置为trunk类型
`14:21` 以加深对access端口和串口端口功能的理解
`14:26` 请大家暂停播放视频
`14:29` 思考一下
`14:30` 给出你的答案
`14:33` 本题的答案如下所示
`14:36` 希望大家都能解答正确
`14:47` hybrid端口即可用于交换机之间或交换机与路由器之间的互联
`14:53` 这一点与串口端口相同
`14:56` 也可用于交换机与用户计算机之间的互联
`15:01` 这一点又与access端口相同
`15:05` 除此之外
`15:06` hybrid端口的绝大部分功能与串口端口相同
`15:11` 不同点在于hybrid端口的发送处理方法
`15:16` hybrid端口会查看真的vd是否在端口的去标签列表中
`15:23` 若存在则去标签号再转发
`15:26` 若不存在则直接转发
`15:30` 接下来我们通过一个简单的应用实例来说明hybrid端口的功能
`15:38` 主机abc连接在同一个交换机的不同接口上
`15:43` 请利用hybrid端口的功能实现以下应用需求
`15:49` a和b都能与c相互通信
`15:52` 但a与b不能相互通信
`15:56` 如图所示
`15:58` 我们可以将这三台主机所连接的交换机的三个端口划分到不同的微辣
`16:05` 并且端口类型设置为hybrid
`16:09` 假设这三个微烂分别是微量十
`16:13` 微蓝20
`16:14` 微量30
`16:16` 相应的pvid分别为12 13 十
`16:22` 在端口一的去标签列表中配置危难时和微量30
`16:29` 在端口二的区标签列表中配置微量20和微量30
`16:34` 在端口三的区标签列表中配置危难时
`16:38` 微蓝20
`16:39` 微量30
`16:41` 主机a给c发送数据帧
`16:45` 该针从交换机的端口一进入交换机
`16:49` 由于端口一的类型是hybrid
`16:52` 他会对接收到的未打标签的普通以太网mark真打标签
`16:57` 也就是插入四字节的微软标题字段
`17:01` 如图所示
`17:05` 由于端口一的pv i d值等于十
`17:08` 因此所插入的四字节未按标记字段中的vid的值也等于十
`17:15` 该针将从端口三转发
`17:19` 由于真正的vd的取值在端口三的区标签列表中
`17:24` 因此该针会被区标签转发
`17:28` 这样主机c就可以收到主机a发送的数据帧
`17:33` 同理主机c给a发送数据帧
`17:37` 主机a同样可以收到
`17:39` 我们就不再演示了
`17:42` 那么主机b给c发送数据帧的具体过程又如何呢
`17:47` 如图所示
`17:56` 同理主机c给b发送数据帧
`18:00` 主机b同样可以收到
`18:02` 我们就不再演示了
`18:06` 再来看看主机a给b发送数据帧的情况
`18:10` 该针从交换机的端口一进入交换机
`18:14` 给该针打标签
`18:16` 其中vd取值为十
`18:20` 该针将从端口二转发
`18:23` 由于真正的vd的取值不在端口二的去标签列表中
`18:29` 因此该征被直接转发
`18:33` 这样主机b就会收到一个带有微量标记的802.1q帧
`18:39` 但是主机b可以识别普通以太网mark真
`18:43` 而不能识别802.1q真
`18:46` 只能丢弃该帧
`18:49` 同理
`18:50` 主机b给a发送的普通以太网mac主机a收到的却是802.1q帧
`18:57` 无法识别而丢弃
`19:01` 本节课到这里就结束了
`19:03` 我们将本节课的内容小结如下
`19:10` 同学们
`19:11` 我们下节课再见