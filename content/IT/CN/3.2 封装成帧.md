---
title: 3.2 封装成帧
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=19
bvid: BV18g411D7Fm
cid: "817183520"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817183520&page=19&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍封装成真的相关知识
`00:15` 所谓封装成真就是指数据链路层给上层交付下来的协议数据单元
`00:21` 添加针头和针尾
`00:23` 使之成为真
`00:25` 例
`00:26` 如下图所示
`00:33` 在针头和真伪中包含有重要的控制信息
`00:38` 例如这是以太网版本二的mark真格式
`00:43` 这是上层交付下来的协议数据单元
`00:47` 这是针头
`00:48` 这是真伪
`00:51` 这是点对点协议ppp的真格式
`00:55` 这是上层交付下来的协议数据单元
`00:59` 这是针头
`01:00` 这是真伪
`01:03` 发送方的数据链路层
`01:05` 将上层交付下来的协议数据单元封装成真后
`01:09` 还要通过物理层将构成真的个比特转换成电信号发送到传输媒体
`01:17` 那么接收方的数据链路层
`01:19` 如何从物理层交付的比特流中提取出一个个的针呢
`01:25` 例如这一串比特流是一个真吗
`01:29` 那这一串比特流是一个真吗
`01:33` 实际上针头和针尾的作用之一就是帧定界
`01:39` 例如这是ppp真的格式
`01:42` 在其针头和针尾中各包含有一个长度为一字节的标志字段
`01:49` 其作用就是真定键
`01:53` 假设发送方发送的是pp真比特流装红色部分是真定界标志
`02:01` 那么接收方的数据链路层就可以依据真定价标志
`02:05` 从物理层交付的比特流中提取出一个个的真
`02:10` 需要说明的是
`02:12` 并不是每一种数据链路层协议的真都包含有帧定界标志
`02:18` 例如这是以太网版本二的mac格式
`02:22` 在其针头和真伪中并没有包含帧定界标志
`02:28` 那么接收方又是如何从物理层交付的比特流中
`02:32` 提取出一个个的以太网帧呢
`02:35` 实际上以太网的数据链路层封装好mark真后
`02:40` 将其交付给物理层
`02:43` 物理层会在mac真前面添加八字节的前导码
`02:47` 然后再将比特流转换成电信号
`02:50` 发送前导码中的前七个字节为前同步码
`02:56` 作用是使接收方的时钟同步之后的一字节为真
`03:01` 开始定界符
`03:03` 表明其后面紧跟着的就是mac真
`03:08` 另外以太网还规定了针尖间隔时间为96比特的发送时间
`03:15` 因此mark真并不需要真结束定界符
`03:20` 需要说明的是
`03:21` 针尖间隔还有其他作用
`03:24` 我们在后续课程中再进行介绍
`03:28` 接下来我们介绍透明传输的问题
`03:33` 透明传输是指数据链路层对上层交付的传输数据并没有任何限制
`03:40` 就好像数据链路层不存在一样
`03:44` 我们来举例说明
`03:46` 这是发送方数据链路层收到其上层交付的协议数据单元
`03:52` 给其添加针头和真伪
`03:55` 使其成为真
`03:58` 为了简单起见
`03:59` 我们只画出了针头和真伪中的帧定界标志
`04:05` 真定价标志也就是一个特定数值
`04:09` 请大家思考一下
`04:11` 如果在上层交付的协议数据单元中
`04:14` 恰好也包含了这个特定数值
`04:17` 接收方还能正确接收该帧吗
`04:22` 回答是否定的
`04:24` 如图所示
`04:26` 接收方在收到第一个帧定界标志时
`04:29` 认为这是真的开始
`04:32` 这并没有错误
`04:35` 当接收方再次接收到真定价标志时
`04:38` 会误认为真结束了
`04:41` 如果数据链路层不采取其他措施来避免接收方对真是否结束的误判
`04:48` 就不能称为透明传输
`04:51` 也就是说
`04:52` 数据链路层对上层交付的协议数据单元有限制
`04:57` 其内容不能包含帧定界符
`05:00` 很显然这样的数据链路层没有什么使用价值
`05:05` 实际上各种数据链路层协议一定会想办法来解决这个问题
`05:11` 例如在发送帧之前对帧的数据部分进行扫描
`05:17` 每出现一个帧定界符
`05:19` 就在其前面插入一个转义字符
`05:23` 接收方数据链路层
`05:25` 在物理层交付的比特流中提取
`05:27` 真遇到第一个帧定界符时
`05:31` 认为这是真的开始
`05:33` 当遇到转义字符时
`05:35` 就知道其后面的一字节内容虽然与帧定界符相同
`05:40` 但它是数据
`05:42` 而不是定界符
`05:44` 剔除转移字符号
`05:46` 将其后面的内容作为数据继续提取
`05:50` 对于这个转移字符也是相同的处理
`05:54` 当再次提取到帧定界符时
`05:57` 表明这是真的结束
`06:01` 请大家再来思考一下
`06:02` 这种情况
`06:05` 在上层交付给数据链路层的协议数据单元中
`06:09` 既包含了真定界符
`06:11` 又包含了转义字符
`06:13` 应该怎么处理呢
`06:16` 方法仍然是在发送帧之前对帧的数据部分进行扫描
`06:22` 每出现一个帧定界符或转义字符
`06:26` 就在其前面插入一个转义字符
`06:30` 需要说明的是
`06:32` 转义字符是一种特殊的控制字符
`06:35` 其长度为一个字节
`06:38` 十进制值为27
`06:40` 而并不是e s以及c这三个字符
`06:46` 我们刚刚介绍的是面向字节的物理链路
`06:50` 使用字节填充或称字符填充的方法来实现透明传输
`06:57` 对于面向比特的链路
`06:59` 应该使用比特填充的方法来实现透明传说
`07:04` 例如这是某个点对点协议的真
`07:08` 这是真的手部
`07:10` 这是帧的数据部分
`07:12` 这是真的尾部
`07:15` 为了简单起见
`07:16` 在真手部和尾部中仅给出了真定界标志
`07:21` 而未给出其他控制字段
`07:25` 而帧的数据部分出现了两个帧定界标志
`07:30` 但它们实际上是数据
`07:32` 而不是真定键
`07:35` 在发送前可以采用零比特填充法对数据部分进行扫描
`07:42` 每五个连续的比特一后面就插入一个比特零
`07:47` 这样就确保了真定界在整个帧中的唯一性
`07:52` 也就可以实现透明传输
`07:56` 接收方的数据链路层
`07:57` 从物理层交付的比特流中提取真实
`08:01` 将帧的数据部分中的每五个连续的比特一
`08:04` 后面的那个比特零剔除即可
`08:08` 接下来我们来做一个有关零比特填充的练习题
`08:13` 这是计算机专业考研全国统考计算机网络部分2013年的题
`08:20` 37
`08:21` 我们一起来做这道题
`08:24` 题目中所给的高级数据链路控制协议
`08:27` hdl c采用针头和真伪中的标志字段作为真定界
`08:34` 其值为零
`08:35` 1111110
`08:38` hdl c为了实现透明传输
`08:41` 采用零比特填充法
`08:43` 也就是每五个连续一后面插入一个比特零
`08:48` 如下所示
`08:51` 因此选项a正确
`08:54` 最后还要提醒大家
`08:57` 为了提高帧的传输效率
`08:59` 应当使帧的数据部分的长度尽可能大一些
`09:04` 如图所示
`09:05` 帧的数据部分的长度应远大于针头和针尾的长度
`09:10` 这样才能提高帧的传输效率
`09:13` 因为仅从数据链路层来看
`09:16` 帧的数据部分才是真正要传输的数据
`09:20` 针头和针尾是为了实现数据链路层功能而额外添加的
`09:27` 当然了
`09:28` 考虑到差错控制等多种因素
`09:31` 每一种数据链路层协议都规定了帧的数据部分的长度上限
`09:36` 称为最大传送单元mt u
`09:41` 本节课到这里就结束了
`09:43` 我们将本节课的内容小结如下
`09:49` 同学们
`09:50` 我们下节课再见