---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=58"
bvid: "BV18g411D7Fm"
cid: "817194705"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817194705&page=58&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍静态路由配置及其可能产生的路由环路问题
`00:17` 静态路由配置是指用户或网络管理员使用路由器的相关命令
`00:23` 给路由器人工配置路由表
`00:26` 这种人工配置方式简单
`00:29` 开销小
`00:30` 但不能及时适应网络状态的变化
`00:33` 一般只在小规模网络中采用
`00:37` 使用静态路由配置
`00:38` 可能出现以下导致产生路由环路的错误
`00:43` 一个是配置错误
`00:45` 另一个是聚合了不存在的网络
`00:48` 还有一个是网络故障
`00:51` 接下来我们首先举例说明静态路由配置
`00:56` 我们采用如图所示的网络拓扑和相应的ip地址配置
`01:02` 这是路由器r一的路由表
`01:05` 路由器r一通过自己的接口零所配置的ip地址和地址源码
`01:11` 可以自动得出接口零所在的网络
`01:15` 由于接口零与该网络直连
`01:19` 则吓一跳
`01:20` 不是路由器地址
`01:21` 而是通过接口零转发ip数据报给该网络中的某个主机
`01:27` 这属于直接交付
`01:30` 这条自动得出的路由条目的类型
`01:33` 属于直连路由
`01:35` 同理r一还可自行得出接口一的直连网络路由条目
`01:43` 这是路由器r2 的路由表
`01:45` 这是其接口零的直连网络路由条目
`01:49` 这是其接口一的直连网络路由条目
`01:54` 假设r一要转发一个ip数据报给该网络中的某个主机
`02:00` 从图中可以看出
`02:02` r一应该将该ip数据报转发给路由器r2 的接口零
`02:08` 但r一的路由表中并没有关于该目的网络的路由条目
`02:14` 换句话说
`02:15` r一并不知道目的网络的存在
`02:18` 因此我们可以使用路由器的相关配置命令
`02:22` 给r一添加一条到达该目的网络的路由条目
`02:27` 这是该目的网络的地址
`02:31` 吓一跳
`02:31` 为路由器r2 的接口零的地址
`02:35` 该路由条目是我们人工配置的静态路由
`02:40` 假设r2 要转发一个ip数据报给该网络中的某个主机
`02:46` 从图中可以看出
`02:48` r2 应该将该ip数据报转发给路由器r一的接口一
`02:55` 但r2 的路由表中并没有关于该目的网络的路由条目
`03:00` 因此我们可以给r2 添加一条到达该目的网络的路由条目
`03:07` 这是该目的网络的地址
`03:10` 吓一跳为路由器r一的接口
`03:13` 一的地址类型为静态
`03:17` 以上就是我们举例说明的静态路由配置
`03:22` 接下来我们举例说明默认路由的概念
`03:27` 假设路由器r2 的接口二连接到了英特网
`03:31` 这是路由器r一的路由表
`03:34` 这是其接口零的直连网络路由条目
`03:38` 这是其接口一的直连网络路由条目
`03:42` 这是我们人工配置的到达该目的网络的静态路由
`03:49` 假设r一要转发一个ip数据报给英特网中某个网络中的某个主机
`03:56` 从图中可以看出
`03:57` r一应该将该ip数据报转发给路由器r2 的接口零
`04:08` 如果我们给r一添加针对这些网络的每一条路由条目
`04:13` 则会给人工配置带来巨大的工作量
`04:17` 并且使r e的路由表变得非常大
`04:21` 降低了查表转发的速度
`04:24` 实际上对于具有相同下一跳的不同目的网络的路由条目
`04:30` 我们可以用一条默认路由条目来替代默认路由条目中的目的
`04:36` 网络地址为零点点零点
`04:39` 地址源码也为零点点零点
`04:43` 其sider形式为零点点零点斜线零
`04:49` 对于本地默认路由条目中的下一跳是路由器r2 的接口
`04:55` 零的地址
`04:56` 由于默认路由也是由我们人工配置的
`05:00` 因此其类型也是静态
`05:04` 在配置了默认路由条目后
`05:07` 我们甚至可以删除这条路由条目
`05:11` 接下来我们举例说明特定主机路由的概念
`05:17` 有时候我们可以给路由器添加针对某个主机的特定主机路由条目
`05:23` 一般用于网络管理人员对网络的管理和测试
`05:29` 另外在需要考虑某种安全问题时
`05:32` 也可以采用特定主机路由
`05:36` 假设这是该网络中的某台特定主机
`05:40` 我们可以在r一的路由表中添加一条到达该主机的特定主机路由条目
`05:48` 特定主机路由条目中的目的网络地址为该特定主机的ip地址
`05:54` 地址源码为255.255.255.255
`06:00` 其塞
`06:00` 这形式为特定主机ip地址斜线32
`06:06` 对于本地特定主机路由条目中的下一跳是路由器r2 的接口
`06:12` 零的地址
`06:14` 由于特定主机路由也是由我们人工配置的
`06:18` 因此其类型也是静态
`06:22` 可以看出特定主机路由的目的网络前缀最长
`06:26` 路由最具体
`06:29` 默认路由的目的网络前缀最短
`06:32` 路由最模糊
`06:34` 当路由器查表转发ip数据报时
`06:37` 若有多条路由条目可选
`06:40` 则采用最长前缀匹配的原则
`06:43` 选用目的网络前缀最长的那个路由条目进行转发
`06:49` 接下来我们举例说明静态路由配置错误可能导致的路由环路问题
`06:56` 如图所示
`06:57` 这是各路由器自动得出的直连网络
`07:02` 这是我们给各路由器人工配置的静态路由
`07:06` 我们来看看路由器r中的这条人工配置的静态路由条目
`07:12` 它表明二要转发ip数据报到该网络吓一跳
`07:18` 应转发给r一的接口一
`07:22` 假设我们将下一条错误的配置成了该地址
`07:26` 也就是错误地指向了r3 的接口
`07:30` 零
`07:31` 则当r2 要转发ip数据报到该网络
`07:34` 实吓一跳
`07:36` 会错误地转发给路由器r3 的接口零
`07:40` r3 收到该ip数据报后进行查表转发
`07:45` 找到了匹配的路由条目
`07:48` 吓一跳
`07:49` 应该转发给该地址
`07:51` 也就是转发给r2 的接口一
`07:55` 而收到该ip数据报后进行查表转发
`08:00` 找到了匹配的路由条目
`08:02` 吓一跳
`08:03` 应该转发给该地址
`08:06` 也就是转发给r3 的接口零
`08:09` 很显然
`08:11` 由于我们静态路由配置错误
`08:13` 导致r2 和r3 之间产生了路由环路
`08:19` 为了防止ip数据报在路由环路中永久兜圈
`08:24` 在ip数据报首部设有生存时间
`08:27` ttl字段
`08:29` ip数据报进入路由器后
`08:32` ttl字段的值被路由器减一
`08:36` 若ttl字段的值减1号不等于零
`08:39` 则被路由器转发
`08:41` 否则被丢弃
`08:44` 我们再来举例说明
`08:46` 聚合了不存在的网络可能导致的路由环路问题
`08:51` 这是路由器r一的路由表
`08:54` 这是其自动得出的直连网络
`08:58` 这是我们给其人工配置的默认路由
`09:01` 下一跳指向路由器r的接口零
`09:06` 这是路由器r的路由表
`09:08` 这是其自动得出的直连网络
`09:12` 这是我们给其人工配置的一条聚合路由
`09:16` 它是由这两个网络地址聚合而来的
`09:20` 具体方法就是找这两个网络地址的共同前缀
`09:25` 然后将共同前缀保持不变
`09:28` 将剩余比特全部取零
`09:31` 写成点分十进制形式
`09:33` 在其后面写上斜线
`09:36` 斜线后面写上共同前缀的数量
`09:40` 假设r要转发ip数据报到该网络进行查表转发
`09:47` 找到了匹配的路由条目吓一跳
`09:50` 应该转发给该地址
`09:52` 也就是转发给r一的接口
`09:55` 一而一收到该ip数据报后
`09:59` 进行查表转发
`10:01` 找到了匹配的路由条目
`10:04` 吓一跳
`10:05` 是通过接口二直接交付
`10:08` 我们再来看看这条聚合路由的细节
`10:17` 该网络存在于我们的网络拓扑中
`10:21` 该网络也存在于我们的网络拓扑中
`10:26` 而该网络不存在
`10:29` 该网络也不存在
`10:31` 当r要转发ip数据报到这个不存在的网络时
`10:36` 进行查表转发
`10:39` 找到了匹配的路由条目
`10:42` 吓一跳
`10:42` 应该转发给该地址
`10:45` 也就是转发给r一的接口一
`10:48` 但对于这个不存在的网络路由器
`10:52` r2 应该不易转发
`10:54` 却错把它转发给了路由器r一而一收到该ip数据报后进行查表转发
`11:06` 应该转发给该地址
`11:08` 也就是转发给r2 的接口零
`11:12` 很显然r一和r2 之间产生了路由环路
`11:18` 针对这种情况
`11:19` 我们可以在r的路由表中添加针对所聚合的不存在的网络的黑洞路由
`11:32` 黑洞路由的吓一跳为闹铃
`11:34` 这是路由器内部的虚拟接口
`11:38` 可以把它形象地看成是一个黑洞
`11:41` ip数据报进入它后就有去无回了
`11:45` 也就是路由器丢弃了该ip数据报
`11:49` 而不是转发该ip数据报
`11:52` 现在假设r要转发ip数据报到这个不存在的网络进行查表转发
`12:01` 找到了两条可选的路由条目
`12:04` 根据最长前缀匹配的原则
`12:07` 将会选择这条到达该不存在网络的黑洞路由
`12:13` 吓一跳
`12:13` 为虚拟接口闹铃
`12:16` 因此该ip数据报会进入这个黑洞
`12:21` 接下来我们再来举例说明网络故障可能导致的路由环路问题
`12:27` 假设路由器r一检测到其接口零所直连的网络出现了故障而不可达
`12:35` 就会自动在其路由表中删除该直连网络的路由条目
`12:40` 之后
`12:41` r要转发ip数据报到该网络进行查表转发
`12:47` 找到匹配的路由条目吓一跳
`12:50` 应该转发给该地址
`12:52` 也就是转发给r一的接口
`12:55` 一而一收到该ip数据报后进行查表转发
`13:01` 找不到该ip数据报的目的
`13:03` 网络的相关路由条目只能走默认路由
`13:07` 吓一跳
`13:08` 应该转发给该地址
`13:10` 也就是转发给r2 的接口零
`13:14` 这样就将该ip数据报错误的转发给了r2
`13:19` 很显然r一和r之间产生了路由环路
`13:24` 针对这种情况
`13:26` 我们可以在r一的路由表中添加针对该直连网络的黑洞路由
`13:32` 这样当r2 要转发ip数据报到该网络实进行查表转发
`13:39` 找到匹配的路由条目
`13:42` 吓一跳
`13:43` 转发给r一的接口
`13:44` 一而一收到该ip数据报后
`13:48` 进行查表转发
`13:50` 找到匹配的路由条目
`13:53` 这是一条黑洞路由
`13:55` 吓一跳
`13:56` 为虚拟接口闹铃
`13:58` 因此该ip数据报会进入这个黑洞
`14:03` 假设一段时间后
`14:05` 之前的故障消失了
`14:07` 则r一又自动地得出了其接口零的直连网络的路由条目
`14:13` 并将我们之前人工配置的针对该直连网络的黑洞路由条目
`14:19` 设置为失效状态
`14:22` 假设r一再次检测到其接口零所直连的网络出现了故障
`14:27` 而不可达则会自动在其路由表中删除该直连网络的路由条目
`14:34` 并将我们之前人工配置的针对该直连网络的黑洞路由条目
`14:40` 设置为生效状态
`14:43` 本节课到这里就结束了
`14:46` 我们将本节课的内容小结如下
`14:51` 同学们
`14:52` 我们下节课再见