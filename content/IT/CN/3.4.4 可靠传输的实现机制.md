---
title: 3.4.4 可靠传输的实现机制
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=24
bvid: BV18g411D7Fm
cid: "817185233"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817185233&page=24&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 在上节课中我们介绍了回退n帧协议
`00:15` 回退n帧协议的接收窗口尺寸
`00:17` wr只能等于一
`00:20` 因此接收方只能按序接收
`00:23` 正确到达的数据分组
`00:26` 一个数据分组的密码
`00:27` 就会导致其后续多个数据分组
`00:30` 不能被接收方按序接收而丢弃
`00:34` 尽管他们没有雾吗
`00:36` 这必然会造成发送方
`00:38` 对这些数据分组的超时重传
`00:41` 显然这是对通信资源的极大浪费
`00:45` 为了进一步提高性能
`00:48` 可设法止重船出现误码的数据分组
`00:52` 因此接收窗口的尺寸w r不应再等于一
`00:57` 而应大于一
`00:59` 以便接收方先收下失序到达但无误码
`01:03` 并且序号落在接收窗口内的那些数据分组
`01:08` 等到所均分组收齐后再一并送交上层
`01:13` 这就是本节课所要介绍的选择重传协议
`01:17` 需要注意的是
`01:19` 选择重传协议
`01:21` 为了使发送方仅重传出现差错的数据分组
`01:25` 接收方不能再采用累计确认
`01:29` 而需要对每个正确接收到的数据分组
`01:32` 进行逐一确认
`01:34` 下面我们来举例说明
`01:37` 选择重传协议的工作原理
`01:40` 假设采用三个比特给分组编序号
`01:44` 因此序号的取值范围是0~7
`01:48` 如图所示
`01:50` 这是发送方的分组序号
`01:53` 这是接收方的分组序号
`01:57` 当序号增加到70
`02:00` 下一个序号又从零开始
`02:03` 发送窗口的尺寸
`02:05` w t的取值范围是大于一
`02:08` 小于等于二的3-1次
`02:11` 其中的三是构成分组序号的比特数量
`02:15` 本例取wt的值为四
`02:18` 如图所示
`02:19` 序号落在发送窗口内的这四个数据分组
`02:23` 可以连续发送
`02:25` 而序号落在发送窗口外的数据分组
`02:28` 不允许发送
`02:30` 接收窗口的尺寸
`02:32` w r的取值
`02:33` 一般情况下可与发送窗口的尺寸
`02:36` wt的取值相同
`02:38` 在本例中其值为四
`02:41` 如图所示
`02:42` 序号落在接收窗口内的这四个数据分组
`02:46` 允许接收
`02:47` 而序号落在接收窗口外的数据分组
`02:50` 不允许接收
`02:53` 发送方
`02:54` 将序号落在发送窗口内的这四个数据分组
`02:57` 依次连续发送出去
`03:00` 他们经过互联网的传输陆续到达接收方
`03:05` 但其中的2号数据分组丢失了
`03:08` 只要序号落入接收窗口内
`03:13` 接收方都会接收
`03:16` 接收方接收0号和1号数据分组
`03:20` 并发送0号和1号确认分组
`03:24` 接收窗口向前滑动两个位置
`03:27` 这样就有四和五这两个新的序号
`03:31` 落入接收窗口
`03:33` 接收方接收3号数据分组
`03:35` 并发送3号确认分组
`03:38` 但接收窗口不能向前滑动
`03:41` 因为3号数据分组
`03:43` 是为按序到达的数据分组
`03:47` 这些确认分组经过互联网的传输陆续到达
`03:51` 发送方
`03:53` 发送方每按序收到一个确认分组
`03:56` 发送窗口就向前滑动一个位置
`04:00` 发送方接收0号和1号确认分组
`04:04` 发送窗口向前滑动两个位置
`04:08` 这样就有四和五
`04:10` 这两个新的序号落入发送窗口
`04:14` 发送方将序号落入发送窗口的
`04:17` 4号和5号数据分组发送出去
`04:22` 发送方现在可以将已经收到确认的
`04:25` 0号和1号数据分组从发送缓存中删除了
`04:30` 而接收方可择机
`04:32` 将以按序接收的0号和1号数据分组
`04:35` 交付上层处理
`04:39` 发送方接收3号确认分组
`04:42` 但发送窗口不能向前滑动
`04:45` 因为这是一个为按序到达的确认分组
`04:49` 发送方还未收到他之前的2号确认分组
`04:54` 不过需要记录3号数据分组已收到确认
`04:59` 这样该数据分组就不会超时重发
`05:04` 4号和5号数据分组到达接收方
`05:08` 接收方接收他们
`05:10` 并发送4号和5号确认分组
`05:14` 但接收窗口不能向前滑动
`05:17` 因为它们是为按序到达的数据分组
`05:20` 接收方还未收到他们之前的2号数据分组
`05:26` 假设在4号和5号确认分组的传输过程中
`05:30` 发送方针对2号数据分组的重传
`05:34` 计时器超时了
`05:36` 发送方重传2号数据分组
`05:40` 4号和5号确认分组陆续到达
`05:43` 发送方
`05:45` 发送方接收他们
`05:47` 但发送窗口不能向前滑动
`05:51` 因为他们是为按序到达的确认分组
`05:54` 发送方还未收到他们之前的2号确认分组
`05:59` 不过需要记录
`06:01` 4号和5号数据分组已收到确认
`06:04` 这样他们就不会超时重发
`06:08` 发送方之前重传的2号数据分组
`06:11` 到达接收方
`06:14` 接收方接收该数据分组
`06:16` 并发送2号确认分组
`06:20` 接收窗口现在可以向前滑动四个位置
`06:24` 这样就有6701
`06:28` 这四个新的序号落入接收窗口
`06:33` 2号确认分组经过互联网的传输
`06:35` 到达发送方
`06:38` 发送方接收该确认分组
`06:41` 发送窗口现在可以向前滑动四个位置
`06:45` 这样就有6701
`06:49` 这四个新的序号落入发送窗口
`06:53` 发送方现在就可以继续
`06:56` 将这四个序号的数据分组依次发送出去了
`07:01` 接下来我们再来讨论一下
`07:04` 选择重传协议的
`07:06` 发送窗口和接收窗口的尺寸问题
`07:10` 发送方的发送窗口尺寸
`07:12` w t必须满足大于一
`07:16` 小于等于二的n减一次
`07:19` 其中n是构成分组序号的比特数量
`07:23` 若wt等于一
`07:26` 则与停止等待协议相同
`07:29` 若wt大于二的n减一次
`07:32` 则会造成接收方
`07:34` 无法分辨新旧数据分组的问题
`07:38` 接收方的接收窗口尺寸
`07:40` w r必须满足大于一
`07:43` 小于等于w t
`07:46` 若wr等于一
`07:48` 则与回退n帧协议相同
`07:52` 若wr大于wt则没有意义
`07:56` 下面我们就来看看
`07:58` 如果发送窗口和接收窗口的尺寸
`08:02` 超过了它们的取值范围
`08:04` 会出现什么样的情况
`08:07` 我们还是采用三个比特给分组编序号
`08:11` 记序号0~7
`08:14` 发送窗口的尺寸
`08:15` wt取最大值
`08:18` 接收窗口的尺寸w r也取最大值
`08:21` 也就是wt等于wr等于二的三减
`08:26` 一次等于四
`08:29` 假设我们故意将发送窗口尺寸
`08:31` wt设置为五
`08:33` 相应的将接收窗口尺寸wr也设置为五
`08:38` 看看会出现什么情况
`08:42` 发送方将序号落入发送窗口内的0~4号
`08:46` 这五个数据分组依次发送出去
`08:50` 他们经过互联网的传输
`08:53` 依次到达接收方
`08:55` 接收方接收他们并发送0~4号确认分组
`09:02` 接收窗口向前滑动五个位置
`09:06` 这样就有56701
`09:10` 这五个新的序号落入接收窗口
`09:14` 这些确认分组经过互联网的传输
`09:17` 陆续到达发送方
`09:20` 但其中的0号确认分组丢失了
`09:24` 发送方接收1~4号确认分组
`09:27` 并记录1~4号数据分组已收到确认
`09:32` 发动窗口不能向前移动
`09:35` 一段时间后
`09:37` 0号数据分组的重传计时器超时了
`09:41` 发送方重传0号数据分组
`09:45` 该数据分组经过互联网的传输到达接收方
`09:50` 其序号零落在接收窗口内
`09:54` 接收方会接收它
`09:57` 但是接收方先前已经正确接收过
`10:01` 该数据分组了
`10:03` 如果现在还要接收
`10:05` 那就会出现分组重复这种传输差错
`10:10` 也就是说
`10:11` 如果发送窗口和接收窗口的尺寸
`10:15` 超过了取值范围
`10:17` 就会使接收方无法分辨新旧数据分组
`10:21` 进而出现分组重复这种传输差错
`10:26` 接下来我们对选择重传协议的工作原理
`10:30` 进行一下小结
`10:33` 这是
`10:33` 我们刚刚举例说明过的
`10:35` 发送窗口和接收窗口的尺寸问题
`10:39` 就不再赘述了
`10:41` 发送方可在未收到接收方
`10:44` 确认分组的情况下
`10:45` 将序号落在发送窗口内的多个数据分组
`10:49` 全部发送出去
`10:52` 接收方可接收为按序到达
`10:55` 但没有误码
`10:57` 并且序号落在接收窗口内的数据分组
`11:01` 为了使发送方仅从传出现差错的分组
`11:06` 接收方不能再采用累计确认
`11:09` 而需要对每个正确接收到的数据分组
`11:12` 进行逐一确认
`11:15` 接收方只有在按序接收数据分组号
`11:19` 接收窗口才能向前相应滑动
`11:22` 发送方只有按序收到
`11:25` 对已发送数据分组的确
`11:27` 认分组时
`11:28` 发送窗口才能向前相应滑动
`11:31` 若收到未按序到达的确认分组
`11:34` 应对其进行记录
`11:37` 以防止其相应数据分组的超时重发
`11:41` 但发送窗口不能向前滑动
`11:45` 接下来我们来做一个有关选择
`11:50` 这是计算机专业考研全国统考
`11:54` 计算机网络部分2011年的题
`11:57` 35
`11:59` 请大家暂停播放视频
`12:01` 思考一下
`12:02` 给出你的答案
`12:07` 答案是选项b
`12:09` 大家都选对了吗
`12:11` 我们一起来分析一下
`12:15` 与回退n帧协议不同
`12:17` 选择重传协议不支持累计确认
`12:21` 接收方每接收一个数据帧
`12:24` 就会发挥相应的确认帧
`12:28` 题目所给收到1号帧的确认
`12:30` 而0号和2号针依次超时
`12:35` 因此需要重传0号和2号针
`12:39` 至于发送方已发送的3号数据帧
`12:43` 题目并未给出他的任何其他线索
`12:46` 因此无需考虑3号针
`12:50` 我们再来画个示意图
`12:52` 以便更容易理解该题
`12:56` 假设这是真可用的序号
`13:00` 这是发送窗口
`13:03` 发送方将序号落在发送窗口内的
`13:06` 0~3号数据帧依次发送出去之后
`13:12` 收到了1号帧的确认
`13:14` 而0号和2号针的重传计时器超时
`13:19` 因此需要重传的就是0号和2号这两个真
`13:26` 本节课到这里就结束了
`13:29` 我们将本节课的内容小结如下
`13:34` 同学们
`13:35` 我们下节课再见