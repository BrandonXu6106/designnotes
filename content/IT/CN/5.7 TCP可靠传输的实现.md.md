---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=87"
bvid: "BV18g411D7Fm"
cid: "817206035"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817206035&page=87&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍tcp可靠传输的实现
`00:14` tcp基于以字节为单位的滑动窗口
`00:18` 来实现可靠传输
`00:20` 我们来举例说明
`00:23` 这是英特网上的两台主机
`00:25` 它们之间已经建立了一个tcp连接
`00:29` 为了简单起见
`00:30` 我们假定数据传输只在一个方向进行
`00:34` 换句话说
`00:36` 发送方给接收方发送tcp数据报文段
`00:40` 接收方给发送方发送相应的tcp确认报文段
`00:45` 这样的好处是使讨论仅限于两个窗口
`00:49` 也就是发送方的发送窗口
`00:52` 和接收方的接收窗口
`00:55` tcp的滑动窗口是以字节为单位的
`00:59` 如图所示
`01:00` 这是发送方待发送数据字节的序号
`01:04` 为了方便讲解和在有限的屏幕上
`01:07` 显示更多的字节序号
`01:10` 字节序号的值都取得很小
`01:13` 现在假设发送方收到了一个
`01:16` 来自接收方的确认报文段
`01:19` 在报文段首部中的窗口字段的值为20
`01:23` 也就是接收方
`01:24` 表明自己的接收窗口的尺寸为20
`01:27` 字节确认号字段的值为31
`01:31` 这表明接收方希望收到下一个数据的
`01:34` 序号是31
`01:37` 而序号30为止的数据
`01:39` 已经全部正确接收了
`01:42` 因此发送方根据这两个字段的值
`01:45` 构造出自己的发送窗口
`01:48` 如图所示
`01:50` 为了简单起见
`01:51` 我们假定网络不存在拥塞问题
`01:55` 也就是发送方在构造自己的发送窗口时
`01:58` 仅考虑接收方的接收窗口
`02:01` 而不考虑拥塞窗口
`02:04` 由于本例中接收方告诉发送方
`02:07` 自己的接收窗口尺寸为20
`02:10` 因此发送方将自己的发送窗口
`02:13` 尺寸也设置为20
`02:16` 发送方在没有收到接收方确认的情况下
`02:19` 可以把发送窗口内的数据
`02:22` 依次全部发送出去
`02:24` 凡是已经发送过的数据
`02:27` 在未收到确认之前都必须暂时保留
`02:31` 以便在超时重传时使用
`02:34` 这是发送窗口的后沿
`02:37` 这是发送窗口的前沿
`02:40` 发送窗口后沿的后面部分
`02:42` 是已发送并已收到确认的数据字节的序号
`02:47` 这些数据字节显然不需要再保存在
`02:50` 发送缓存中了
`02:52` 可以将它们删除
`02:54` 发送窗口前沿的前面部分
`02:57` 是当前不允许发送的数据字节的序号
`03:02` 我们来看看发动窗口后沿的移动情况
`03:06` 有以下两种可能
`03:08` 一种是不动
`03:10` 也就是没有收到新的确认
`03:13` 发送窗口的后沿不会移动
`03:16` 另一种是向前移动
`03:19` 也就是收到了新的确认
`03:21` 发送窗口的后沿向前移动
`03:25` 发送窗口的后沿不可能向后移动
`03:29` 因为不能撤销掉已收到的确认
`03:33` 再来看发送窗口前沿的移动情况
`03:36` 有以下三种可能
`03:39` 通常情况下
`03:40` 发动窗口的前沿是不断向前移动的
`03:44` 但也有可能不动
`03:46` 一种是由于没有收到新的确认
`03:49` 接收方通知的窗口大小也没有改变
`03:54` 另一种是收到了新的确认
`03:56` 可向前移动相应位置
`03:59` 但接收方通知的窗口缩小了
`04:02` 前沿应该向后回缩
`04:05` 如果向前移动和向后回缩的尺寸恰好相等
`04:09` 就会使得发动窗口的前沿不动
`04:14` 发送窗口的前沿还可能向后收缩
`04:17` 这发生在接收方通知的窗口变小了
`04:21` 但tcp标准强烈不赞成这样做
`04:25` 因为很可能发送方在收到这个通知之前
`04:29` 就已经发送了窗口中的许多数据
`04:33` 现在又要收缩窗口
`04:35` 不让发送这些数据显然就会产生错误
`04:40` 现在假定发送方将发送窗口内
`04:43` 序号31~41的数据
`04:46` 封装在几个不同的报文段中发送出去
`04:51` 此时发送窗口的位置并没有改变
`04:55` 发送窗口内
`04:56` 序号31~41的数据已经发送
`05:00` 但未收到确认
`05:02` 而序号42~50的数据是允许发送
`05:06` 但还未发送的
`05:09` 请同学们思考一下
`05:11` 我们如何描述发送窗口的状态呢
`05:15` 换句话说
`05:16` 如果我们要编程实现滑动窗口机制
`05:20` 那么对于发送窗口的状态
`05:22` 应该如何标记和维护呢
`05:26` 如图所示
`05:27` 可以使用三个指针
`05:29` p1 p2 p3
`05:31` 分别指向相应的字节序号
`05:34` 这样小于p一的
`05:36` 就是已发送并已收到确认的部分
`05:42` 大于等于p3 的是不允许发送的部分
`05:47` p3 到p一可以得出当前发送窗口的尺寸
`05:53` p2 减p1
`05:55` 可以得出已发送
`05:57` 但尚未收到确认的字节数量
`06:01` p3 减p2 可以得出允许发送
`06:05` 但当前尚未发送的字节数量
`06:10` 我们再来看看接收方的接收窗口
`06:13` 它的尺寸为20
`06:16` 在接收窗口外面
`06:17` 到30号为止的数据
`06:19` 是已经发送过相应确认
`06:22` 并已交付给应用进程的数据
`06:25` 因此无需在保留这些数据
`06:28` 可将他们从接收缓存中删除了
`06:32` 接收窗口内
`06:33` 31~50号数据是允许接收的数据
`06:39` 接收窗口外
`06:40` 51号及其后续数据目前不允许接收
`06:46` 假设发送方之前发送的封装
`06:49` 有32和33号数据的报文段
`06:52` 到达的接收方
`06:54` 由于数据序号落在接收窗口内
`06:57` 所以接收方接受它们
`07:00` 并将它们存入接收缓存
`07:03` 但是他们是为按序到达的数据
`07:07` 因为31号数据还没有到达
`07:11` 这有可能是丢了
`07:13` 也有可能是滞留在网络中的某处
`07:16` 请注意
`07:17` 接收方只能对按序收到的数据
`07:20` 中的最高序号给出确认
`07:23` 因此接收方发出的确认
`07:26` 报文段中的确认序号仍然是31
`07:29` 也就是希望收到31号数据
`07:33` 窗口字段的值仍是20
`07:36` 表明接收方没有改变自己接收窗口的大小
`07:41` 发送方收到该确认报文段后
`07:43` 发现
`07:44` 这是一个针对31号数据的重复确认
`07:48` 就知道接收方收到了为按序到达的数据
`07:52` 由于这是针对31号数据的
`07:55` 第一个重复确认
`07:57` 因此这并不会引起发送方
`08:00` 针对该数据的快重传
`08:03` 另外接收方通知的窗口尺寸仍是20
`08:08` 因此发送方保持自己的发送窗口
`08:11` 尺寸为20
`08:14` 现在假设封装有31号数据的报文段
`08:17` 到达了接收方
`08:19` 接收方接受该报文段
`08:22` 将其封装的31号数据存入接收缓存
`08:28` 接收方现在可将接收到的
`08:30` 31~33号数据交付给应用进程
`08:35` 然后将接收窗口向前移动三个序号
`08:39` 并给发送方发送确认报文段
`08:43` 该确认报文段中窗口字段的值仍为20
`08:47` 表明接收方没有改变自己接收窗口的大小
`08:52` 确认号字段的值为34
`08:55` 这表明接收方已经收到了序号
`08:58` 33为止的全部数据
`09:02` 现在假设又有几个数据
`09:04` 报文段到达了接收方
`09:07` 他们封装有37
`09:09` 38以及40号数据
`09:12` 这些数据的序号虽然落在接收窗口内
`09:16` 但他们都是为按序到达的数据
`09:19` 只能先暂存在接收缓存中
`09:23` 假设接收方先前发送的确认报文段
`09:26` 到达了发送方
`09:28` 发送方接收后
`09:29` 将发送窗口向前滑动三个序号
`09:34` 发送窗口的尺寸保持不变
`09:37` 这样就有新序号
`09:39` 51~53落入发送窗口内
`09:43` 而序号31~33移出了发送窗口
`09:48` 现在可将31~33号数据
`09:52` 从发送缓存中删除了
`09:54` 因为已经收到了接收方针对他们的确认
`10:00` 发送方继续将发送窗口内
`10:02` 序号42~53的数据
`10:05` 封装在几个不同的报文段中发送出去
`10:09` 现在发送窗口内的序号已经用完了
`10:13` 发送方在未收到接收方发来确认的情况下
`10:17` 不能再发送新的数据
`10:21` 序号落在发送窗口内的已发送数据
`10:24` 如果迟迟收不到接收方的确认
`10:27` 则会产生超时重传
`10:31` 接下来我们还要对tcp可靠传输的实现
`10:35` 做几点补充说明
`10:38` 第一点
`10:39` 虽然发送方的发送窗口
`10:41` 是根据接收方的接收窗口设置的
`10:45` 但在同一时刻
`10:47` 发送方的发送窗口
`10:49` 并不总是和接收方的接收窗口一样大
`10:53` 这是因为
`10:55` 网络传送窗口值需要经历一定的时间滞后
`10:59` 并且这个时间还是不确定的
`11:03` 另外发送方
`11:05` 还可能根据网络当时的拥塞情况
`11:08` 适当减小自己的发送窗口尺寸
`11:12` 第二点
`11:14` 对于不按序到达的数据应如何处理
`11:17` tcp并无明确规定
`11:21` 如果接收方把不按序到达的数据一律丢弃
`11:26` 那么接收窗口的管理将会比较简单
`11:30` 但这样做对网络资源的利用不利
`11:34` 因为发送方会重复传送较多的数据
`11:39` tcp通常对不按序到达的数据
`11:42` 是先临时存放在接收窗口中
`11:45` 等到字节流中所缺少的字节收到后
`11:49` 再按序交付上层的应用进程
`11:53` 第三点
`11:55` tcp要求接收方
`11:57` 必须有累计确认和捎带确认机制
`12:01` 这样可以减小传输开销
`12:04` 接收方可以在合适的时候发送确认
`12:08` 也可以在自己有数据要发送时
`12:11` 把确认信息顺便捎带上
`12:15` 但是接收方不应过分推迟发送确认
`12:20` 否则会导致发送方不必要的超时重传
`12:24` 这反而浪费了网络资源
`12:27` tcp标准规定
`12:29` 确认推迟的时间不应超过0.5秒
`12:33` 若收到一连串具有最大长度的报文段
`12:37` 则必须每隔一个报文段就发送一个确认
`12:42` 另外捎带确认实际上并不经常发生
`12:46` 因为大多数应用程序很少
`12:49` 同时在两个方向上发送数据
`12:52` 第四点
`12:53` tcp的通信是全双工通信
`12:57` 通信中的每一方都在发送和接收报文段
`13:02` 因此每一方都有自己的
`13:04` 发送窗口和接收窗口
`13:07` 在谈到这些窗口时
`13:09` 一定要弄清楚是哪一方的窗口
`13:14` 最后我们来做两个相关的习题
`13:17` 这是计算机专业考研全国统考
`13:21` 计算机网络部分2009年的题
`13:24` 38
`13:25` 请同学们暂停播放视频
`13:28` 思考一下
`13:30` 给出你的答案
`13:34` 答案是选项d
`13:36` 大家都选对了吗
`13:38` 我们来一起分析一下
`13:41` 根据题意可知
`13:43` 这是主机甲给主机已发送的
`13:47` 第一个数据报文段
`13:49` 其首部中序号字段的值为200
`13:53` 也就是说
`13:54` 其数据载荷的第一个字节的序号为200
`13:58` 由于数据载荷共有300个字节
`14:02` 因此数据载荷的最后一个字节的序号为
`14:05` 499
`14:07` 这是主机甲给主机已发送的第二个数据报
`14:12` 文段的数据载荷部分
`14:15` 由于前一个报文段
`14:16` 数据载荷的
`14:17` 最后一个字节的序号是499
`14:21` 因此本报文段
`14:23` 数据载荷的第一个字节的序号
`14:25` 因为500
`14:27` 相应的其首部中序号字段的值因为500
`14:32` 由于数据载荷共有500个字节
`14:36` 因此数据载荷的最后一个字节的序号为九
`14:40` 百99
`14:42` 这是主机椅给主机甲发送的
`14:45` 确认报文段的手部
`14:48` 其中确认序号字段的值为1000
`14:51` 这是因为主机以目前已经按序正确接收了
`14:56` 序号到999为止的全部数据
`14:59` 现在希望接收序号为1000及其后续的数据
`15:05` 该确认报文段
`15:07` 实际上是对已正确接收的两个
`15:10` tcp数据报文段的累计确认
`15:14` 再来看2011年的题
`15:17` 40
`15:18` 请同学们暂停播放视频
`15:21` 思考一下
`15:22` 给出你的答案
`15:27` 答案是选项b
`15:29` 大家都选对了吗
`15:31` 我们来一起分析一下
`15:35` 根据题意
`15:36` 我们可以画出如下所示的示意图
`15:48` 题目要求我们回答
`15:50` 主机已发送给主机甲的确认序号是多少
`15:54` 我们从已知的第三个数据报文段首部中
`15:58` 序号字段的值为900开始分析
`16:02` 这就表明其数据载荷的第一个字节的序号
`16:06` 为900
`16:07` 由于数据载荷共有500个字节
`16:11` 因此数据载荷的最后一个字节的序号为
`16:15` 1399
`16:18` 根据第三个数据报文段
`16:20` 数据载荷的第一个字节的序号为900
`16:23` 可以得出第二个数据报文段
`16:26` 数据载荷的最后一个字节的序号为800
`16:29` 99
`16:31` 由于数据载荷共有400个字节
`16:35` 因此数据载荷的第一个字节的序号为500
`16:40` 相应的第二个数据报文段
`16:43` 首部中序号字段的值
`16:44` 因为500
`16:47` 根据第二个数据报文段
`16:49` 数据载荷的第一个字节的序号为500
`16:52` 可以得出第一个数据报文段
`16:55` 数据载荷的最后一个字节的序号为400
`16:58` 99
`17:00` 由于数据载荷共有300个字节
`17:04` 因此数据载荷的第一个字节的序号为200
`17:08` 相应的第一个数据报文段
`17:11` 首部中序号字段的值
`17:12` 因为200
`17:15` 由于tcp规定
`17:17` 只能对按序到达的最高序号进行确认
`17:21` 因此主机发送的确认报文段
`17:25` 实际上是对第一个数据报文段的确认
`17:29` 由于第一个数据报文段
`17:31` 数据载荷的
`17:32` 最后一个字节的序号为499
`17:35` 因此针对该序号的确认序号
`17:38` 因为500表明序号499为止
`17:42` 的全部数据已经收到了
`17:45` 现在希望接收500号及其后续数据
`17:51` 本节课到这里就结束了
`17:53` 我们将本节课的内容小结如下
`18:00` 同学们
`18:01` 我们下节课再见