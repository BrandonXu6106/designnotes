---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=89"
bvid: "BV18g411D7Fm"
cid: "817206495"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817206495&page=89&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:06` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们简单介绍一下tcp的选择确认
`00:17` 在之前介绍tcp的快重传和可靠传输时
`00:22` tcp
`00:23` 接收方只能对按序收到的数据
`00:26` 中的最高序号给出确认
`00:28` 当发送方超时重传时
`00:31` 接收方之前已收到的未按序到达的数据
`00:35` 也会被重传
`00:37` 那么能否设法只传送缺少的数据
`00:41` 而不重船已经正确到达
`00:44` 只是为按序到达的数据呢
`00:47` 回答是肯定的
`00:49` tcp可以使用选择确认来解决该问题
`00:54` 我们来举例说明
`00:56` 如图所示
`00:58` 假设这是tcp接收方收到的
`01:01` 对方发送过来的序号不连续的字节流序号
`01:06` 1~1000的连续字节流号缺少了序号
`01:10` 1001~1500的字节流序号1000
`01:15` 501~3000的连续字节流
`01:18` 由于前面缺少了部分字节流
`01:21` 为了后续描述方便
`01:23` 我们称其为第一个字节块
`01:27` 在第一个字节块后面
`01:29` 又缺少了序号
`01:30` 3001~3500的字节流
`01:35` 序号3000
`01:36` 501~4500的连续字节流
`01:39` 由于前面也缺少了部分字节流
`01:42` 我们称其为第二个字节块
`01:46` 综上所述
`01:47` 对于本地接收方
`01:49` 收到了和前面的字节流
`01:51` 不连续的两个字节块
`01:54` 假设这些字节的序号都在接收窗口内
`01:58` 那么接收方就要先收下这些数据
`02:02` 但要把这些信息准确地告诉发送方
`02:06` 是发送方不再重复发送
`02:08` 这些数据可以使用两个指针
`02:12` 来分别指向字节块的左边界和右边界
`02:17` 左边界指出字节块的第一个字节的序号
`02:21` 右边界指出字节块后面第一个字节的序号
`02:27` 这是分别指向第二个字节块
`02:29` 左边界和右边界的指针
`02:33` 如图所示
`02:34` 这是tcp报文段的首部格式
`02:38` 在tcp报文段的固定首部中
`02:41` 并没有为不连续字节块的边界信息
`02:44` 提供专用的字段
`02:47` rfc 2018文档规定
`02:50` 如果要使用选择确认功能
`02:52` 需要在扩展手部中的选项字段来实现
`02:56` 其中使用一个字节来指明是否使用选择
`03:01` 确认选项
`03:02` 还要使用一个字节来指
`03:04` 明这个选项要占用多少字节
`03:08` 指明一个边界需要四字节
`03:11` 因为tcp对每个字节数据采用四字节的序号
`03:16` 而一个字节块需要两个边界解八字节
`03:21` 因此最大长度为40字节的选项
`03:24` 字段只能包含四个字节块的信息
`03:29` 需要说明的是
`03:30` tcp选择确认的相关文档
`03:33` 并没有指明发送方应当怎样响应
`03:36` 选择确认报文段
`03:38` 因此大多数的tcp实现还是重传
`03:42` 所有未被确认的数据块
`03:46` 本节课到这里就结束了
`03:49` 请同学们快速回忆一下本节课的主要内容
`04:00` 同学们
`04:01` 我们下节课再见