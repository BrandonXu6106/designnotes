---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=77"
bvid: "BV18g411D7Fm"
cid: "817200435"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817200435&page=77&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 本节课我们介绍408考研计算机网络部分
`00:11` 2022年新增知识点
`00:13` 软件定义网络sdn的相关内容
`00:18` 根据考纲要求
`00:19` 我们仅介绍sdn的相关基本概念
`00:23` 预祝各位同学成功上岸
`00:27` 软件定义网络
`00:28` sdn的概念最早由斯坦福大学的尼克macun教授于2009年提出
`00:36` sdn最初只是学术界讨论的一种新型网络体系结构
`00:42` 近些年来
`00:43` sdn发展快速
`00:45` 被不少企业相继采用
`00:53` 建立的数据中心网络b4
`00:57` sdn是当前网络领域最热门和最具发展前途的技
`01:02` 术之一
`01:03` 成为近年来的研究热点
`01:06` 我想这也是其新增为考研知识点的原因
`01:10` 我们首先来回忆一下传统路由器的功能
`01:15` 一个功能是为主机间的通信提供转发服务
`01:19` 如图所示
`01:22` 原主机给目的主机发送分组
`01:25` 该分组需要经过多个路由器的转发
`01:28` 最终到达目的主机
`01:32` 另一个功能是路由
`01:33` 选择从原主机到达目的主机的路径可能不止一条
`01:40` 例如这条路径
`01:43` 还有这条路径
`01:50` 他会选择经过路由器数量少的这条路径
`01:55` 因此我们将路由器之间传送的信息种类总结如下
`02:02` 一类是主机之间的分组
`02:04` 如图所示
`02:09` 另一类是路由器之间的路由信息
`02:12` 如图所示
`02:16` 路由器之间交互路由信息
`02:19` 并利用路由算法得出路由表
`02:22` 再根据路由表得出转发表
`02:26` 我们把路由器的网络层抽象的划分为两个层面
`02:31` 一个层面是控制层面
`02:34` 另一个层面是数据层面
`02:37` 或称为转发层面
`02:40` 这里所谓的层面与计算机网络体系结构中的分层是类似的
`02:46` 都是抽象的概念
`02:50` 在控制层面中
`02:51` 每一个路由器无法独自创建出自己的路由表
`02:56` 路由器必须和相邻的路由器周期性的交换路由信息
`03:01` 然后才能够创建出自己的路由表
`03:05` 根据路由选择协议所使用的路由算法
`03:09` 计算路由需要由软件来完
`03:12` 成这需要花费较多的时间
`03:15` 一般是秒数量级
`03:19` 在数据层面中
`03:21` 每一个路由器基于自己生成的转发表来转发分组
`03:26` 为了提高转发速率
`03:28` 现在的路由器一般都基于硬件进行转发
`03:33` 单个分组的转发时间为纳秒数量级
`03:38` 在sdn体系结构中
`03:40` 路由器中的路由软件都不存在了
`03:44` 因此路由器之间不再相互交换路由信息
`03:49` 在控制层面中
`03:51` 有一个在逻辑上集中的远程控制器
`03:54` 如图所示
`03:58` 逻辑上集中的远程控制器在物理上可由不同地点的多个服务器组成
`04:05` 如图所示
`04:09` 远程控制器掌握各主机和整个网络的状态
`04:13` 它能够为每一个分组计算出最佳的路由
`04:17` 并为每一个路由器生成其正确的转发表
`04:22` 这样路由器的工作就变得非常单纯了
`04:26` 也就是对收到的分组进行查表转发即可
`04:32` sdn这种新型网络体系结构的核心思想是
`04:36` 把网络的控制层面和数据层面分离
`04:41` 而让控制层面利用软件来控制数据层面中的许多设备
`04:48` 下面介绍openflow协议与sdn体系结构的关系
`04:54` openflow协议是一个得到高度认可的标准
`04:58` 在讨论sdn时
`05:00` 往往与open flow一起讨论
`05:03` openflow协议可被看成是sdn体系结构中控
`05:08` 制层面与数据层面之间的通信接口
`05:12` 如图所示
`05:13` 这是sdn体系结构中的控制层面和数据层面
`05:19` openflow协议是控制层面与数据层面之间的通信接口
`05:25` openflow协议使得控制层面的控制器
`05:29` 可以对数据层面中的物理设备进行直接访问和控制
`05:35` 如图所示
`05:37` 这是控制层面中的控制器
`05:40` 这是数据层面中的物理设备
`05:44` 控制器通过openflow协议直接访问和控制物理设备
`05:51` open flow协议的技术规范
`05:53` 由非盈利性的产业联盟开放网络基金会nf负责制定
`06:00` omnf的任务是致力于sdn的发展和标准化
`06:05` 但并未规定必须使用open floor
`06:09` 只不过大部分sdn产品采用了open flow
`06:13` 作为其控制层面与数据层面的通信接口
`06:18` open flow从2009年底发表的一点版开始
`06:23` 每年都被更新
`06:25` 历经12次更新
`06:27` 到2015年3月发布了1.5.1版
`06:31` 目前较为成熟的是1.3版本
`06:36` 我们知道传统意义上的数据层面的任务是根据转发表转发分组
`06:44` 转发分组分为以下两个步骤
`06:48` 步骤一是进行匹配
`06:51` 即查找转发表中的网络前缀
`06:54` 进行最长前缀匹配
`06:58` 步骤二是执行动作
`07:01` 也就是把分组从匹配结果指明的接口转发出去
`07:07` 我们来举例说明
`07:10` 这是路由器r一的路由表
`07:13` 当r一收到某个去网该网络的分组时
`07:17` 首先进行匹配
`07:20` 也就是查找与该分组目的地址相匹配的路由条目
`07:25` 然后执行动作
`07:27` 也就是按照匹配的路由条目中的吓一跳的指示进行转发
`07:35` 上述这种匹配加动作的转发方式在sdn中得到了扩充
`07:41` 增加了新的内容
`07:42` 变成了广义的转发
`07:46` sn的广义转发分为以下两个步骤
`07:50` 步骤一是进行匹配
`07:53` 与传统匹配不同的是
`07:56` 能够对网络体系结构中各层手部中的字段进行匹配
`08:01` 例如数据链路层
`08:03` 网络层
`08:04` 运输层
`08:06` 步骤二是执行动作与传统动作不同的是
`08:10` 不仅可以转发分组
`08:12` 还可以负载均衡重写ip首部人为的阻挡或丢弃一些分组
`08:21` 在sdn的广义转发中完成匹配加动作的设备并不局限在网络层工作
`08:28` 因此不再称为路由器
`08:32` 而称为open flow交换机或分组交换机
`08:37` 或更简单的称为交换机
`08:41` 相应的在sdn中取代传统路由器中转发表的是刘表
`08:48` 一个流就是通过网络的一种分组序列
`08:53` 而在此序列中的每个分组都共享分组首部某些字段的值
`09:00` 例如某个流可以是具有相同原ip地址和目的ip地址的一连串分组
`09:08` openflow交换机中的流表是由sdn远程控制器来管理的
`09:15` sdn远程控制器通过一个安全信道
`09:18` 使用openflow协议来管理open flow交换机中的流表
`09:24` 如图所示
`09:25` 这是sdn远程控制器和openflow交换机
`09:30` 这是openflow交换机中的安全信道和刘表
`09:35` sdn远程控制器使用openflow协议
`09:39` 通过安全性道来管理openflow交换机中的流表
`09:45` 需要注意的是
`09:47` 尽管网络设备可以由不同厂商来生产
`09:50` 并可使用在不同类型的网络中
`09:54` 但是从sdn远程控制器看到的则是统一的逻辑交换功能
`10:01` 每个open flow交换机必须有一个或多个流表
`10:06` 如图所示
`10:08` 这是open flow一点零版本的流表
`10:22` 它们是首部字段值
`10:24` 字段计数器
`10:26` 字段动作字段
`10:34` 用来使入分组的对应手部与之匹配
`10:38` 因此又称为匹配字段
`10:42` 匹配不上的分组就被丢弃或被发送到sdn远程控制器做更多的处理
`10:50` 如图所示
`10:58` 涉及数据链路层
`11:01` 网际层
`11:04` 运输层
`11:06` 三个层次的手部
`11:08` 很显然
`11:09` open flow中的匹配与以前介绍过的计算机网络体系结构
`11:15` 分层处理的原则完全不同
`11:18` 在openflow交换机中
`11:20` 既可以处理数据链路层的真
`11:23` 也可以处理网际层的ip数据报
`11:27` 还可以处理运输层的tcp或udp报文
`11:33` 再来看计数器字段
`11:37` 计数器字段是一组计数器
`11:40` 一个是记录已经与该流表相匹配的分组数量的计数器
`11:46` 另一个是记录该流表象上次更新到现在经历时间的计数器
`11:54` 再来看动作字段
`11:57` 动作字段是一组动作
`12:00` 当分组匹配某个流表象时
`12:03` 执行该流表象中动作字段指明的以下某个或多个动作
`12:09` 即把分组转发到指明的端口
`12:15` 丢弃分组
`12:17` 把分组进行复制后
`12:19` 再从多个端口转发出去
`12:29` 网际层以及运输层的手部
`12:34` 下面我们举例说明
`12:36` 基于刘表的匹配加动作
`12:40` 如图所示
`12:44` 在这个简单的网络中
`12:46` 有h一到h6 共六台主机
`12:51` 其ip地址标注在各自的旁边
`12:54` 还有s一到s3 共三台openflow交换机
`12:59` 每台交换机都有四个端口
`13:02` 其端口号标注在各自端口的旁边
`13:06` 另外还有一台sdn远程控制器来控制这些openflow交换机的匹配加动作
`13:15` 首先来看简单转发的例子
`13:19` 假设设定的转发规则是h一或h2 发往h3 和h4 的分组
`13:26` 其转发路径因为s一到s3
`13:30` s3 到s2
`13:32` 如图所示
`13:37` 根据这条转发规则
`13:39` sdn远程控制器应该给openflow交换机s一设置这样的流表象
`13:49` 在匹配中的原ip地址等于一零点点一点星
`13:53` 表明左起前三个十进制数分别为一零点点一的任何ip地址
`14:03` 主机h一和h2 的ip地址都是其中的地址
`14:08` 同理目的ip地址等于一零点点二点新
`14:13` 表明左起前三个十进制数分别为一零点点二的任何ip地址
`14:21` 主机h3 和h4 的ip地址都是其中的地址
`14:27` 在动作中的转发三表明
`14:30` open flow交换机s一应从自己的端口三转发所匹配的分组
`14:38` 这是sdn远程控制器给openflow交换机s3 设置的流表象
`14:45` 与s一的流表相相比
`14:48` s3 的流表象中多了入端口等于一的匹配条件
`14:53` 表明
`14:54` 只有从s3 自己端口一进入的分组
`14:57` 才能与该流表项的后续项目进行匹配
`15:03` 这是sdn远程控制器给openflow交换机s2 设置的流表象
`15:09` 这里就不再赘述了
`15:12` 再来看负载均衡的例子
`15:15` 为了均衡链路s2 到s3 和链路s一到s3 的通信量
`15:22` 制定了以下规则
`15:24` 凡是从h4 发往h5 和h6 的分组
`15:28` 其转发路静音为s2 到s3
`15:33` 如图所示
`15:38` 凡是从h3 发往h5 和h6 的分组
`15:42` 请转发路径
`15:43` 因为s2 到s1
`15:46` s一到s3
`15:48` 如图所示
`15:54` 很显然
`15:55` 采用基于目的ip地址的传统转发方法是不能实现这种负载均衡的
`16:03` 但在本例中
`16:04` 只要在openflow交换机s2 的流表项中设置好合适的匹配项目即可
`16:13` 再来看防火墙的例子
`16:16` 假设在openflow交换机s2 中设置防火墙
`16:21` 此防火墙的作用是仅仅接收来自于open flow交换机
`16:26` s一相连的主机所发送的分组
`16:30` 而不管这些分组是从s2 自己的哪个端口进来的
`16:36` 根据这样的规定
`16:38` 可以得出s2 的流表象
`16:44` 需要说明的是
`16:46` 为了简单起见
`16:48` 在上述粒子的流表项中省略了计数器字段
`16:53` 尽管上述例子非常简单
`16:56` 但我们已经可以看出sdn中的这种广义转发的多样性和灵活性
`17:03` 下面我们来总结一下sdn体系结构及其四个关键特征
`17:09` 如图所示
`17:14` 特征一是基于流的转发
`17:17` 由sdn控制器控制的分组交换机分布在数据层面中
`17:23` 而分组的转发可以基于数据链路层
`17:28` 网络层和运输层协议数据单元pdu中的首部字段的值进行
`17:35` 这一传统路由器仅根据ip数据报的目的ip地址进行转发
`17:41` 有着明显的区别
`17:44` 各分组交换机的流表中的流表象
`17:47` 都是由sdn控制器进行计算管理和安装的
`17:52` 这样sd的转发规则都详细规定在各分组交换机的流表中
`18:00` 特征二是数据层面与控制层面分离
`18:05` 在传统的转发设备路由器中
`18:09` 数据层面与控制层面都位于同一个设备
`18:13` 中
`18:14` 但在sdn中
`18:16` 数据层面与控制层面是分离的
`18:19` 也就是二者不在同一个设备中
`18:24` 数据层面有许多相对简单而快速的分组交换机
`18:29` 这些分组交换机基于各自的流表执行匹配加动作的规则
`18:37` 控制层面则由若干服务器和相应的软件组成
`18:42` 这些服务器和软件计算并管理这些分组交换集中的流表
`18:50` 特征三是位于数据层面
`18:57` sd中的控制层面是用各类软件实现的
`19:01` 而且这些软件还可以处于不同的机器上
`19:06` 并且还可能远离位于数据层面的分组交换机
`19:15` 一个构建是sdn控制器
`19:19` 也就是网络操作系统
`19:27` sdn控制器维护准确的网络状态信息
`19:31` 例如远程链路分组交换机和主机的状态
`19:37` 并把这些信息提供给运行在控制层面的各种控制应用程序
`19:44` 另外还会提供一些方法
`19:47` 使得这些控制层面的应用程序
`19:49` 能够对数据层面中的分组交换机进行监视
`19:53` 编程和控制特征
`19:57` 四是可编程的网络
`20:04` 使得整个sd网络成为可编程的网络
`20:09` sdn控制器
`20:21` 能够确定原点和终点之间的端到端的路径
`20:33` 必须被阻挡
`20:35` 也就是进行接入控制
`20:42` 置分组交换机在转发分组时执行负载均衡的措施
`20:52` 从sdn体系结构的四个关键特征可以看出
`20:57` sdn把网络的许多功能都分离开了
`21:01` 数据层面的交换机
`21:09` 这些都是可以分开的实体
`21:12` 并且可由不同厂商和机构来提供
`21:17` 这与传统网络截然不同
`21:20` 在传统网络中
`21:22` 路由器或交换机是由单独的厂商提供的
`21:26` 其控制层面和数据层面以及协议的实现都是集成在一个设备里面的
`21:34` 需要注意的是
`21:36` sdn控制器与其下面的数据层面中的受控设备的通信接口称为南向api
`21:50` 称为北向api
`21:53` 在s d n体系结构中
`21:55` s d控制器是最复杂的
`21:59` 我们对其进行简要介绍
`22:03` sdn控制器可划分成三个层次
`22:07` 分别是通信层
`22:19` 通信层是sdn控制器中的底层
`22:23` 其任务是通过南向api接口
`22:26` 完成sdn控制器与位于数据层面中的受控的网络设备之间的通信
`22:34` 网络范围的状态管理层是sdn控制器的中间层
`22:40` sdn控制层面如果要做出任何最终的控制决策
`22:45` 例如在所有的分组交换机中配置流表
`22:49` 以便进行端到端的转发或实现负载均衡
`22:54` 或实现某种特殊的防火墙能力
`22:58` 就需要让控制器掌握全网的主机信息
`23:03` 链路信息
`23:04` 分组交换机信息
`23:06` 以及其他受s n控制器控制的设备的信息
`23:31` 管理层中的网络状态和流表进行读写操作
`23:36` 目前已经出现了一些开源的sdn控制器
`23:41` 最有代表性的就是open daylight和unlo
`23:45` 有兴趣的同学可访问相关的官方网站
`23:49` 这里就不再介绍了
`23:53` 本节课到这里就结束了
`23:55` 我们将本节课的内容小结如下
`24:05` 最后请同学们趁热打铁