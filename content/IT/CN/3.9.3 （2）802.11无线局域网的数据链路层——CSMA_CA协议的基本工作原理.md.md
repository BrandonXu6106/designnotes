---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=45"
bvid: "BV18g411D7Fm"
cid: "817190905"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817190905&page=45&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍cs m a c a协议的基本工作原理
`00:16` 如图所示
`00:18` 横坐标为时间
`00:20` 纵坐标表示802.11
`00:22` 无线局域网中的各站点
`00:25` 假设信道是空闲的
`00:28` 原站要发送它的第一个数据帧
`00:32` 若检测到信道空闲
`00:34` 则在等待dfs间隔后才能发送
`00:39` d i f s是指d c f针间间隔长度为128微秒
`00:46` 在dcf方式中
`00:48` d i f s被用来发送数据帧和管理针
`00:53` dcf是分布式协调功能的英文缩写词
`00:58` 在d c f方式下没有中心控制站点
`01:02` 每个站点使用c s m a c a协议
`01:06` 通过征用信道来获取发送权
`01:10` dcf方式是802011定义的默认方式
`01:14` 必须实现等待d i s s间隔是考虑到可能有其他的站
`01:21` 有高优先级的真要发送本地
`01:25` 假设现在没有其他高优先级真要发送
`01:29` 因而原战就发送了自己的数据帧
`01:34` 另外针手部中的持续时间字段的值指出了原站要占用信道的时间
`01:42` 包括墓地战发挥确认真所需的时间
`01:46` 目力战若正确收到该帧
`01:49` 则经过s i f s间隔后向原站发送确认真a c k
`01:56` 若原站在重传计时器设置的超时时间内没有收到a c k
`02:02` 就必须重传之前已
`02:04` 发送的数据帧直到收到a c k为止
`02:08` 或者经过若干次的重传失败后放弃发送
`02:13` 当某个站检测到正在信道中传送的真首部中的持续时间字段时
`02:20` 就要调整自己的网络分配向量
`02:23` 其英文缩写词为n a v n a v
`02:27` 指出了完成这次真的传送
`02:30` 且信道转入空闲状态所需的时间
`02:34` 这就是所谓的虚拟载波监听机制
`02:38` s i f s是短针间间隔的英文缩写词
`02:43` 长度为28微秒
`02:46` 它是最短的间接间隔
`02:48` 用来分割开属于一次对话的个针
`02:52` 一个站点应当能够在这段时间内从发送方式切换到接收方式
`03:00` 使用sfs
`03:02` 的针类型有a c k j c t s真的目地站收到正确的数据帧号
`03:09` 给原站发送确认真的原因是由于无线信道的误码率较高
`03:15` c s m a c协议需要使用停止等待的确认机制来实现可靠传输
`03:23` 这与使用cs ma cd协议的共享式以太网是不同的
`03:29` 如果同学们记住了这个知识点
`03:31` 则这到408考研真题可以秒杀本题的答案是选项d
`03:40` 在n a v这段时间内
`03:42` 若其他站也有真要发送
`03:45` 就必须推迟发送
`03:48` 在nvv这段时间结束后
`03:50` 在经过一个d f s间隔
`03:53` 然后还要退避一段随机时间后才能发送帧
`03:59` 还要退避
`04:00` 一段随机时间的原因是
`04:02` 当某个站在发送帧时
`04:05` 很可能有多个站都在监听信道并等待发送帧
`04:10` 一旦信道空闲
`04:12` 这些战几乎同时发送针而产生碰撞
`04:17` 为了避免上述情况
`04:19` 所有要发动针的站检测到信道从忙转为空闲号
`04:25` 都要执行退避算法
`04:28` 这样不仅可以减少发生碰撞的概率
`04:32` 还可以避免某个站长时间占用无线信道
`04:37` 当某个站要发送数据真实
`04:40` 仅在这种情况下
`04:42` 才不使用退避算法
`04:45` 检测到新的空弦
`04:47` 并且该数据帧不是成功发送完上一个数据帧之后
`04:52` 立即连续发送的数据帧
`04:55` 除此之外的以下情况
`04:57` 都必须使用推理算法在发送针之前检测到信道
`05:04` 出于盲派
`05:07` 在每一次重传一个真实
`05:12` 在每一次成功发送帧后
`05:14` 要连续发送下一个针
`05:18` 接下来我们介绍cs mc协议的推理算法
`05:24` 在执行退避算法时
`05:26` 站点为退避计时器设置一个随机的退避时间
`05:31` 当退避计时器的时间减小到零时
`05:35` 就开始发送数据
`05:38` 当退b计时器的时间还未减小到零时
`05:41` 而信道又转变为盲状态
`05:45` 这时就要冻结蜕皮计时器的数值
`05:48` 重新等待信道变为空弦
`05:51` 在经过中间间隔d f s后
`05:55` 继续启动退避计时器
`05:58` 在进行第二次退避时
`06:01` 退比时间在实习编号011直到2~2加x方减一中随机选择一个
`06:09` 然后乘以基本退避时间
`06:12` 就可以得到随机的退避时间
`06:15` 当17编号达到250 50就不再增加了
`06:21` 我们来举例说明csm a c a协议的推理算法
`06:26` 如图所示
`06:28` 横坐标为时间
`06:29` 纵坐标表示802011无线局域网中的各站点
`06:35` 假设a正在占用无线信道发送帧
`06:39` 在a发送帧的过程中
`06:41` bcd也有真要发送
`06:45` 我们用向上的箭头来表示b c d于是进行载波监听
`06:53` 发现新道盟需要退避
`06:56` 根据退避算法选择出各自的随机退避时间
`07:01` 并在每个时期对信道进行一次检测
`07:06` 当检测到信道由盲状态转为空闲状态
`07:11` 且经过真天间隔
`07:12` d f s号退避计时器开始倒计时
`07:18` 假设c的推理时间最短
`07:20` 当c的退b计时器到时候
`07:23` c立即开始发动针
`07:27` 此时信道由空闲状态转换为盲状态
`07:32` 当b和d检测到信道门后
`07:35` 就要冻结各自剩余的推理时间
`07:39` 假设在c占用无线信道发送帧的过程中
`07:43` e也要发送帧
`07:45` e于是进行再不监听
`07:48` 发现信道忙需要退避
`07:52` 根据退避算法选择出一个随机的推理时间
`07:57` 并在每个时期对信道进行一次检测
`08:01` 当检测到信道由盲状态转为空闲状态
`08:05` 且经过针尖间隔d f s后
`08:09` 退避计时器开始倒计时
`08:13` 当b和d检测到信道由盲状态转换为空闲状态
`08:18` 且经过中间间隔d i f s后
`08:22` 他们各自的退避计时器从新开始
`08:25` 从上次冻结的退避剩余时间倒计时
`08:30` d的退避计时器会首先到十
`08:34` d立即开始发动针
`08:37` 此时信道由空闲状态转为盲状态
`08:42` 当b和e检测到信道忙后
`08:45` 就冻结各自剩余的推理时间
`08:49` 当地发送完之后
`08:51` 信道将转为空闲状态
`08:54` 当b和e检测到信道由盲状态转换为空闲状态
`09:00` 且经过中间间隔d i f s号
`09:03` 他们各自的退避计时器重新开始从上次冻结的退避剩余时间倒计时
`09:12` e的退避计时器会首先到11立即开始发动针
`09:18` 此时信道由空闲状态转为盲状态
`09:23` 当b检测到信道门后
`09:26` 就要冻结自己剩余的推理时间
`09:30` 当e发送完之后
`09:32` 信道将转为空闲状态
`09:35` 当b检测到信道由盲状态转换为空闲状态
`09:40` 且经过针尖间隔d f s后
`09:44` 它的退避计时器重新开始从上次冻结的退避剩余时间倒计时
`09:51` 当b的退避计时器到10号必立即开始发送针
`09:58` 若b发送完这一针号还有真要发送
`10:02` 则在检测到信道空闲
`10:04` 且经过针尖间隔d i f s后
`10:08` 还必须在退避一段随机时间后才能发送
`10:14` 为了进一步降低发生碰撞的概率
`10:17` 802011无线局域网允许原战对信道进行预约
`10:23` 如图所示
`10:25` 假设信道是空闲的
`10:27` 原站要发送它的第一个数据帧
`10:31` 若检测到信道空闲
`10:33` 则在等待d i f s的针尖间隔号
`10:37` 先发送一个rtx针
`10:40` 而tx真是短的控制针
`10:43` 它包括原地址
`10:45` 目的地址和本次通信所需的持续时间
`10:50` 其中包含了目的战发挥确认真所需的时间
`10:56` 若目地站正确收到原站发来的rts
`10:59` 真在检测到信道空闲
`11:03` 且经过中间间隔s i f s后
`11:07` 就向原站发送一个cs帧作为响应
`11:12` c t s真是短的响应控制针
`11:16` 它也包括本次通信所需的持续时间
`11:20` 这是从rts帧中将此持续时间复制到cs帧中的
`11:27` 原站正确收到cts真后
`11:29` 在等待一个针尖间隔s i f s号就可发送第一个数据帧了
`11:37` 目的战若正确收到该真
`11:40` 则经过s r f s间隔后向原站发送确认真a c k
`11:47` 除原站和目的站的其他各站在收到cs帧或数据证号就推迟访问信道
`11:56` 这样就确保了原站和目的站之间的通信不会受到其他站的干扰
`12:03` 若rts真发生碰撞
`12:06` 原战就不可能收到cs真
`12:10` 原战会执行退避算法重传rtx真
`12:15` 使用rtx针和cs帧进行信道预约
`12:20` 会带来额外的开销
`12:22` 但由于rts真和cs帧都很短
`12:27` 发生碰撞的概率
`12:28` 碰撞产生的开销以及本身的开销都很小
`12:34` 对于一般的数据帧
`12:36` 其发送时延往往远大于传播时延
`12:40` 碰撞的概率很大
`12:42` 且一旦发生碰撞而导致数据间重发
`12:46` 则浪费的时间就很多
`12:49` 因此用很小的代价对信道进行预约往往是值得的
`12:55` 尽管如此
`12:56` 802.11无线局域网仍为用户提供了以下三种选择
`13:02` 一种是使用rtx针和cs真
`13:07` 另一种是只有当数据
`13:09` 帧的长度超过某个数值时才使用rtx针和c t s真
`13:16` 还有一种是不使用rtx针和c t s真
`13:21` 由于rts帧和cts针都会携带通信
`13:26` 需要持续的时间
`13:28` 这与之前介绍过的数据帧可以携带通信
`13:32` 所需持续时间的虚拟赛博监听机制是一样的
`13:36` 因此使用rt s真和c t s真进行信道预约
`13:42` 也属于虚拟在博监听机制
`13:46` 利用虚拟在播监听机制
`13:48` 站点只要监听到数据帧rtx针或c t s帧中的任何一个
`13:55` 就能知道信道将被占用的持续时间
`13:59` 而不需要真正监听到信道上的信号
`14:03` 因此虚拟赛博监听机制能减少隐蔽战
`14:07` 带来的碰撞问题如图所示
`14:11` a和b互为隐蔽战
`14:13` a给ap发送rtx真
`14:16` 进行信道预约
`14:18` 虽然b监听不到该帧
`14:21` 但却能监听到ap硬打点a的cp真
`14:25` b根据cs帧中的持续时间
`14:29` 修改自己的网络分配向量n a v在nv指示的时间内
`14:35` 虽然b监听不到a发送给ap的真
`14:39` 但b也不会发动针干扰a和ap的通信
`14:44` 接下来请同学们练习两个与本节课知识点相关的408考研真题
`14:52` 请同学们暂停播放视频思考一下
`14:56` 然后给出你的答案
`15:02` 本题的答案是选项d
`15:05` 大家都选对了吗
`15:07` 我们来一起分析一下
`15:10` 本题考察的就是我们刚刚才介绍过的c s m a c a协议
`15:16` 通过交换rt s真和c t s真进行信道预约
`15:22` 再来看这道408考研真题
`15:26` 请同学们暂停播放视频思考一下
`15:30` 然后给出你的答案
`15:35` 本题的答案是选项a
`15:38` 大家都选对了吗
`15:40` 我们来一起分析一下
`15:43` 本题考察的是dcf针尖间隔
`15:46` d i f与短针间间隔s r f s的作用及各自的长度
`15:54` 这也是本节课介绍过的内容
`15:56` 就不再赘述了
`16:00` 本节课到这里就结束了
`16:03` 请同学们快速回忆一下本节课的主要内容
`16:15` 同学们
`16:16` 我们下节课再见