---
title: 3.5 点对点协议PPP
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=25
bvid: BV18g411D7Fm
cid: "817185463"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817185463&page=25&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍点对点协议
`00:13` pp点对点协议
`00:15` ppp是目前使用最广泛的点对点数据链路层协议
`00:21` 请大家想想看一般的英特网用户是如何接入到英特网的呢
`00:28` 通常都是要通过连接到某个英特网服务提供者sp
`00:33` 例如中国电信
`00:34` 中国联通
`00:36` 中国移动这三大运营商才能接入因特网
`00:40` 这些sp已经从英特网管理机构申请到了一批ip地址
`00:46` 用户计算机只有获取到sp所分配的合法ip地址后
`00:51` 才能成为英特网上的主机用户
`00:54` 计算机与sp进行通信时所使用的数据链路层协议
`01:00` 通常就是ppp协议
`01:03` 这里需要说明的是
`01:05` 在1999年公布的在以太网上运行的ppp协议
`01:10` 即p p p over isn't net
`01:12` 简称为p p p o e
`01:15` 它使得sp可以通过dsl电路调制解调器
`01:20` 以太网等宽带接入技术
`01:22` 以以太网接口的形式为用户提供接入服务
`01:28` 另外点对点协议ppp也广泛应用于广域网路由器之间的专用线路
`01:36` ppp协议是英特网工程任务组i etf在1992年制定的
`01:43` 经过1993年和1994年的修订
`01:47` 现在的ppp协议已成为英特网的正式标准
`01:51` 我们
`01:52` 可以在tf官方网站的相关页面搜索并查看ppp协议的相关rfc文档
`02:02` ppp协议为在点对点链路传输各种协议数据报提供了一个标准方法
`02:08` 主要由以下三部分构成
`02:11` 第一对各种协议数据报的封装方法
`02:16` 也就是我们之前课程中介绍的封装成真
`02:20` 第二链路控制协议lcp用于建立配置以及测试数据链路的连接
`02:33` 其中的每一个协议支持不同的网络层协议
`02:39` 从网络体系结构的角度看
`02:42` p p p是数据链路层的协议
`02:45` 他将上层交付下来的协议数据单元封装成p p p真
`02:50` 为了支持不同的网络层协议
`02:57` 其中的每一个协议支持不同的网络层协议
`03:02` 例如tcp ip中的ip nowhere network
`03:07` 网络操作系统中的ips
`03:10` 苹果公司的apple talk等链路控制协议
`03:14` lcp用于建立配置以及测试数据链路的连接
`03:21` ppp协议能够在多种类型的点对点链路上运行
`03:26` 例如面向字节的异步电路
`03:29` 面向比特的同步链路
`03:32` 接下来我们来看看ppp协议的真格式
`03:37` 真手部由四个字段构成
`03:40` 针尾部由两个字段构成
`03:44` 各字段的长度以及帧的数据部分的最大长度
`03:48` 如图所示
`03:50` 真守和真伪不中的标志字段是pp真的定界符
`03:56` 取值为16进制的7亿
`03:59` 真守不中的地址字段取值为16进制的f f控制字段
`04:06` 取值为16进制的零三
`04:09` 最初曾考虑以后再对这两个字段的值进行其他定义
`04:14` 但至今也没有给出
`04:17` 可见这两个字段实际上并没有携带pp真的信息
`04:23` 真守不中的协议字段指明了帧的数据部分应送交哪个协议处理
`04:30` 当协议字段取值为16进制的0021时
`04:34` ppp帧的数据部分就是ip数据报
`04:38` 如图所示
`04:41` 当协议字段取值为16进制的c021 时
`04:45` ppp帧的数据部分就是链路控制协议lcp的分组
`04:51` 如图所示
`04:54` 当协议字段取值为16进制的八
`05:03` 如图所示
`05:06` 真伪不中的帧检验序列
`05:08` fcs字段其实是使用循环冗余校验cr c计算出的校验位
`05:15` 用于检查pp真是否存在误吗
`05:20` 接下来我们来看看ppp协议是如何解决透明传输问题的
`05:27` 当ppp帧的数据部分出现真守和真伪中的标志字段时
`05:33` 如果不采取措施
`05:35` 则会造成接收方对pp真是否结束的误判
`05:40` 因为标志字段是pp真的定界符
`05:44` 取值为16进制的7亿
`05:46` 也就是二进制的01111110
`05:51` ppp协议实现透明传输的方法
`05:54` 取决于所使用的链路类型
`05:58` 如果是面向字节的异步链路
`06:00` 则采用字节填充法
`06:03` 也就是插入
`06:05` 转义字符如果是面向比特的同步电路
`06:09` 则采用比特填充法
`06:12` 也就是插入比特零
`06:15` 我们来举例说明字节填充法
`06:19` 也就是以字节7亿作为ppp真的定界符
`06:24` 以字节为单位来讨论问题
`06:28` 来看发送方对ppp帧数据部分的处理
`06:32` 将出现的每一个7亿字节转变成两字节序列7d5 e如图所示
`06:41` 这相当于在7亿字节前插入了转移字节7d
`06:46` 并将7亿字节减去16进制的二零
`06:51` 将出现的每一个7d字节转变成两字节序列7d5 d如图所示
`07:00` 在出现的每一个阿斯克码控制字符前插入一个7d字节
`07:06` 同时将该字符的编码加上16进制的二零
`07:11` 例如图中所示
`07:14` 对于ppp真的接收方只要进行反变换即可恢复出原来的ppp帧的数据部分
`07:24` 我们再来举例说明
`07:25` 比特填充法
`07:27` 也就是以01111110作为ppp真的定界符
`07:33` 以比特为单位来讨论问题
`07:37` 假设ppt帧的数据部分如图所示
`07:45` 来看发送方对ppp帧数据部分的处理
`07:50` 对帧的数据部分进行扫描
`07:53` 一般由硬件实现
`07:56` 只要发现五个连续的比特一则立即填充一个比特零
`08:02` 如图所示
`08:06` 再来看接收方对ppp帧数据部分的处理
`08:11` 对帧的数据部分进行扫描
`08:14` 一般由硬件实现
`08:17` 只要发现五个连续的比特一
`08:19` 就把其后的一个比特零删除
`08:23` 如图所示
`08:27` 接下来我们来看看ppp协议是如何进行差错检测的
`08:39` fcs字段使用循环冗余校验cr c来计算该字段的取值
`08:47` 采用的生成多项式如下所示
`08:52` rfc 1662文档的附录部分给出了fcs的计算方法的c语言实现
`08:59` 为了减少对cpu的占用
`09:02` 采用查表法来实现fcs的计算范围
`09:07` 如图所示
`09:10` 接收方每收到一个pvp真就进行cr c检验
`09:16` 若crc检验正确
`09:18` 就收下这个针
`09:20` 反之就丢弃这个针
`09:23` 使用ppp的数据链路层向上不提供可靠传输服务
`09:29` 最后我们以拨号接入为例
`09:33` 简单介绍一下ppp协议的工作状态
`09:37` ppt链路的开始和结束状态都是静止状态
`09:42` 这时并不存在物理层的连接
`09:46` 当检测到调制解调器的载波信号
`09:49` 并建立物理层连接后
`09:51` ppp就进入链路的建立状态
`09:55` 这时链路控制协议lcp开始协商一些配置选项
`10:02` 若协商成功
`10:04` 则进入鉴别状态
`10:06` 若协商失败则退回到静止状态
`10:17` 可以不使用鉴别
`10:20` 也可以使用口令鉴别协议paa p或挑战握手鉴别协议c h a p
`10:27` 若通信双方无需鉴别或鉴别身份成功
`10:32` 则进入网络状态
`10:35` 若鉴别失败则进入终止状态
`10:44` 配置完成后就进入打开状态
`10:48` p p p链路的两端
`10:57` 如果在ppt链路上运行的是ip协议
`11:08` 例如分配ip地址
`11:11` 只要链路处于打开状态
`11:14` 就可以进行数据通信
`11:17` 当出现故障或链路的异端发出终止请求时
`11:22` 就进入终止状态
`11:25` 当载波停止后则回到静止状态
`11:30` 本节课到这里就结束了
`11:33` 我们将本节课的内容小结如下
`11:38` 同学们
`11:39` 我们下节课再见