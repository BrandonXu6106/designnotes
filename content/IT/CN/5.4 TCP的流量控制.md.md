---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=85"
bvid: "BV18g411D7Fm"
cid: "817204961"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817204961&page=85&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍tcp的流量控制
`00:14` 一般来说我们总是希望数据传输的更快一些
`00:19` 但如果发送方把数据发送的过快
`00:22` 接收方就可能来不及接收
`00:25` 这就会造成数据的丢失
`00:28` 所谓流量控制就是让发送方的发送速率不要太快
`00:33` 要让接收方来得及接收
`00:36` 利用滑动窗口机制可以很方便地在tcp连接上实现对发送方的流量控制
`00:44` 我们来举例说明
`00:47` 假设主机a和b是英特网上的两台主机
`00:52` 他们之间已经建立了tcp连接
`00:55` a给b发送数据
`00:57` b对a进行流量控制
`01:00` 这是主机a装待发送数据的字节序号
`01:05` 假设主机a发送的每个tcp报文段可携带100字节数据
`01:11` 因此图中每个小格子表示100个字节数据的序号
`01:16` 在主机a和b建立tcp连接时
`01:19` b告诉a我的接收窗口为400
`01:23` 因此主机a将自己的发送窗口也设置为400
`01:29` 这意味着主机a在未收到主机b发来的确认时
`01:33` 可将序号落入发送窗口中的全部数据发送出去
`01:39` 接下来我们举例说明主机b对a的流量控制
`01:45` 主机a将发送窗口内序号1~100的数据封装成一个tcp报文段
`01:51` 发送出去
`01:52` 发送窗口内还有300字节可以发送
`01:57` 这里的s e q是tcp报文段首部中的序号
`02:01` 字段取值一表示tcp报文段数据载荷的第一个字节的序号是一
`02:08` 这里的data表示这是tcp数据报文段
`02:13` 主机a将发送窗口内序号101~200的数据封装成一个tcp报文段
`02:20` 发送出去
`02:22` 发送窗口内还有200字节可以发送
`02:26` 主机a将发送窗口内序号201~300的数据封装成一个tcp报文段
`02:34` 发送出去
`02:35` 但该报文段在传输过程中丢失了
`02:39` 主机a发送窗口内还有100字节可以发送
`02:44` 主机b对主机a所发送的201号以前的数据进行累计确认
`02:51` 并在该累计确认中将窗口字段的值调整为300
`02:55` 也就是对主机a进行流量控制
`03:00` 这里的大写a c k是tcp报文段首部中的标志位
`03:05` 取值一表示这是一个tcp确认报文段
`03:10` 小写a c k是tcp报文段首部中的确认号字段取值201
`03:17` 表示序号201之前的数据已全部正确接收
`03:22` 现在希望收到序号201及其后续数据
`03:27` rw n d是tcp报文段首部中的窗口字段
`03:32` 取值300
`03:33` 表示自己的接收窗口大小为300
`03:38` 主机a收到该累计确认后
`03:40` 将发送窗口向前滑动
`03:43` 使已发送并收到确认的这些数据的序号移出发送窗口
`03:50` 由于主机b在该累计确认中将自己的接收窗口调整为了300
`03:56` 因此主机a相应地将自己的发送窗口调整为300
`04:02` 目前主机a发送窗口内的序号为201~500
`04:07` 也就是主机a还可以发送这300字节
`04:12` 其中201~300号字节是已发送的数据
`04:16` 若重传计时器超时
`04:19` 它们会被重传
`04:21` 301~400号字节
`04:24` 以及401号到5号字节还未被发送
`04:28` 可被分别封装在一个tcp报文段中
`04:31` 发送主机a现在可将发送缓存中序号1~200的字节数据全部删除了
`04:40` 因为已经收到了主机币对他们的累计确认
`04:45` 主机a将发送窗口内序号301~400的数据封装成一个tcp报文段
`04:52` 发送出去
`04:53` 发送窗口内还有100字节可以发送
`04:58` 主机a将发送窗口内序号401~500的数据封装成一个tcp报文段
`05:05` 发送出去
`05:06` 至此序号落在发送窗口内的数据已经全部发送出去了
`05:12` 不能再发送新数据了
`05:15` 现在发送窗口内序号201~300
`05:19` 这100个字节数据的重传计时器超时了
`05:23` 主机a将它们重新封装成一个tcp报文段发送出去
`05:28` 暂时不能发送其他数据
`05:32` 主机b收到该重传的tcp报文段后
`05:35` 对主机a所发送的501号以前的数据进行累计确认
`05:41` 并在该累计确认中将窗口字段的值调整为100
`05:47` 这是主机b对主机a进行的第二次流量控制
`05:52` 主机a收到该累计确认后
`05:55` 将发送窗口向前滑动
`05:58` 使已发送并收到确认的这些数据的序号移出发送窗口
`06:04` 由于主题b在该累计确认中将自己的接收窗口调整为了100
`06:10` 因此主机a相应地将自己的发送窗口调整为100
`06:16` 目前主机a发动窗口那个序号为501~600
`06:21` 也就是主机a还可以发送这100字节
`06:26` 主机a现在可将发送缓存中
`06:28` 序号201~500的字节数据全部删除了
`06:33` 因为已经收到了主机币对他们的累计确认
`06:38` 主机a将发送窗口内序号501~600的数据封装成一个tcp报文段
`06:45` 发送出去
`06:47` 至此序号落在发送窗口内的数据已经全部发送出去了
`06:52` 不能再发送新数据了
`06:55` 主机b对主机a所发送的601号以前的数据进行累计确认
`07:01` 并在该领域确认中将窗口字段的值调整为零
`07:06` 这是主机b对主机a进行的第三次流量控制
`07:11` 主机a收到该累计确认后
`07:14` 将发送窗口向前滑动
`07:17` 使已发送并收到确认的这些数据的序号移出发送窗口
`07:23` 由于主机b在该累计确认中将自己的接收窗口调整为了零
`07:29` 因此主机a相应的将自己的发送窗口调整为零
`07:35` 目前主机a不能再发送一般的tcp报文段了
`07:40` 主机a现在可将发送缓存中序号501~600的字节
`07:45` 数据全部删除了
`07:47` 因为已经收到了主机币对他们的累计确认
`07:53` 假设主机b向主机a发送了零窗口的报文段
`07:56` 后不久
`07:57` 主机b的接收缓存又有了一些存储空间
`08:02` 于是主机b向主机a发送了接收窗口等于300的报文段
`08:08` 然而这个报文段在传输过程中丢失了
`08:12` 主机a一直等待主机b发送的非零窗口的通知
`08:17` 而主机b也一直等待主机a发送的数据
`08:22` 如果不采取措施
`08:24` 这种互相等待而形成的思索局面将一直持续下去
`08:30` 为了解决这个问题
`08:32` tcp为每一个连接设有一个持续计时器
`08:37` 只要tcp连接了一方收到对方的零窗口通知
`08:42` 就要启动持续计时器
`08:45` 若持续计时器超时
`08:47` 就要发送一个零窗口探测报文
`08:50` 仅携带一字节的数据
`08:53` 而对方在确认这个探测报文段时
`08:57` 给出自己现在的接收窗口值
`09:00` 如果接收窗口仍然是零
`09:02` 那么收到这个报文段的一方就要重新启动持续计时器
`09:07` 如果接收窗口不是零
`09:09` 那么死锁的局面就可以被打破了
`09:13` 在本地中
`09:14` 主机a收到零窗口通知时
`09:17` 就启动一个持续计时器
`09:20` 当直接计时器超时
`09:22` 主机a立刻发送一个仅携带一字节数据的零窗口探测报文段
`09:28` 假设主机b此时的接收窗口又为零了
`09:32` 主机币就在确认这个零窗口探测报文段时
`09:36` 给出自己现在的接收窗口值为零
`09:40` 主机a再次收到零窗口通知
`09:43` 就再次启动一个持续计时器
`09:46` 当直接计时器超时
`09:48` 主机a立刻发送一个零窗口探测报文段
`09:53` 假设主机b此时的接收缓存又有了一些存储空间
`09:58` 于是将自己的接收窗口调整为了300
`10:02` 主机b就在确认这个零窗口探测报文段时
`10:06` 给出自己现在的接收窗口值为300
`10:09` 这样就打破了死锁的局面
`10:13` 同学们可能会有这样的疑问
`10:15` 主机a所发送的零窗口探测报文段到达主机b时
`10:20` 如果主机b此时的接收窗口仍然为零
`10:24` 那么主机b根本就无法接受该报文段
`10:28` 又怎么会针对该报文段给主机a发挥确认呢
`10:33` 实际上tcp规定
`10:35` 即使接收窗口为零
`10:37` 也必须接受零窗口探测报文段
`10:41` 确认报文段以及携带有紧急数据的报文段
`10:47` 请大家再来思考一下这个问题
`10:50` 如果零窗口探测报文段丢失了
`10:53` 会出现怎样的问题呢
`10:56` 还能否打破死锁的局面呢
`11:00` 回答是肯定的
`11:02` 因为零窗口探测报文段也有重传计时器
`11:07` 当重传计时器超时后
`11:09` 零窗口探测报文段会被重传
`11:13` 接下来我们来做一个相关的练习题
`11:18` 这是计算机专业考研全国统考计算机网络部分2010年的题39
`11:27` 由于该题涉及到了我们还未学习的拥塞控制的内容
`11:32` 因此我们需要先做一些说明
`11:34` 然后再请同学们来完成
`11:38` tcp发送方的发送窗口
`11:40` 实际上是在自身的拥塞窗口和tcp接收方的接收窗口中取小者
`11:47` 而在我们本节课的举例中
`11:50` 为了简单起见
`11:51` 我们忽略了拥塞控制
`11:54` 也就是认为tcp发送方的发送窗口等于接收方的接收窗口
`12:01` 本题未给出tcp发送方的发送窗口的初始值
`12:05` 则我们取拥塞窗口值作为发送窗口值
`12:11` 现在请大家暂停播放视频
`12:14` 思考一下
`12:15` 给出你的答案
`12:21` 答案是选项a
`12:23` 大家都选对了吗
`12:26` 我们来一起画图分析一下
`12:29` 这是主机甲中带发送数据的序号
`12:34` 根据题意可知
`12:35` 主机甲的发送窗口为4000字节
`12:39` 主机甲向主机已连续发送两个最大段
`12:44` 共2000字节
`12:46` 也就是将发送窗口内序号0~1999的字节数据发送出去
`12:54` 主机乙给主机甲发送针对第一个段的确认
`12:59` 并在该确认中给出自己当前的接收窗口大小为2000字节
`13:06` 主机甲受到该确认后
`13:08` 将发动窗口向前滑动
`13:11` 使已发送并收到确认的第一个段的序号移出发送窗口
`13:18` 由于主机已在确认中给出的自己的接收窗口大小为2000字节
`13:25` 因此主机甲相应地将自己的发送窗口调整为2000字节
`13:32` 很显然主机甲还可以向主机已发送2000~2999号字节数据
`13:39` 共1000个字节
`13:42` 本节课到这里就结束了
`13:45` 我们将本节课的内容小结如下
`13:52` 同学们
`13:53` 我们下节课再见