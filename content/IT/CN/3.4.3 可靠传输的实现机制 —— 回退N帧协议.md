---
title: 3.4.3 可靠传输的实现机制 —— 回退N帧协议
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=23
bvid: BV18g411D7Fm
cid: "817184380"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817184380&page=23&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 在上节课中我们介绍了停止等待协议
`00:15` 如图所示
`00:16` 发送方每发送完一个数据分组就要停止发送
`00:20` 并等待接收方的确认分组
`00:23` 当收到接收方的确认分组后
`00:26` 才能发送下一个数据分组
`00:28` 如此反复进行
`00:30` 从图中可以看出
`00:32` 发送方每发送完一个数据分组
`00:35` 就至少要等待一个收发双方之间的往返时间
`00:40` 当往返时间较大时
`00:42` 例如卫星链路停止等待协议的信道利用率很低
`00:47` 若出现超时重传
`00:49` 则信道利用率更低
`00:52` 如图所示
`00:54` 如果发送方在收到接收方的确认分组之前
`00:58` 可以连续发送多个数据分组
`01:04` 这是一种流水线式的传说
`01:07` 就本地而言
`01:09` 同等条件下
`01:11` 在相同的时间内使用停止等待协议的发送方只能发送一个数据分组
`01:17` 而采用流水线传输的发送方可以发送五个数据分组
`01:22` 本节课我们介绍回退n帧协议
`01:26` 该协议在流水线传输的基础上
`01:29` 利用发送窗口来限制发送方可连续发送分组的个数
`01:35` 我们来举例说明
`01:38` 假设采用三个比特给分组编序号
`01:41` 因此序号的取值范围是0~7
`01:46` 如图所示
`01:47` 这是收发双方各自的分组序号
`01:51` 当序号增加到70
`01:53` 下一个序号又从零开始
`01:57` 发送方要维持一个发送窗口
`02:00` 序号落在发送窗口内的数据分组可被连续发送
`02:05` 而不必等收到接收方的相应确认分组后再发送
`02:10` 发送窗口的尺寸即为wt
`02:13` 对于本例其取值范围是大于一
`02:17` 小于等于二的三次减一
`02:20` 其中的三是构成分组序号的比特数量
`02:25` 本地取wt的值为五
`02:28` 如果wt的值取为一
`02:30` 则是停止等待协议
`02:33` 如果wt的值超过取值范围的上限
`02:37` 则会造成严重的错误
`02:39` 我们之后会举例详细说明
`02:43` 如图所示
`02:44` 序号落在发送窗口内的这五个数据分组可以连续发送
`02:49` 而序号落在发送窗口外的数据分组不允许发送
`02:55` 接收窗口的尺寸即为wr
`02:58` 对于回退n帧协议
`03:00` 其取值只能为一
`03:02` 这一点与停止等待协议是相同的
`03:06` 如图所示
`03:08` 序号落在接收窗口内的这个数据分组允许接收
`03:12` 而序号落在接收窗口外的数据分组不允许接收
`03:17` 我们首先来看最简单的情况
`03:20` 也就是无差错的情况
`03:23` 发送方将序号落在发送窗口内的0~4号数据分组依次连续发送出去
`03:31` 他们经过互联网的传输
`03:33` 正确到达了接收方
`03:35` 也就是没有出现乱序和物嘛
`03:38` 接收方按序接收
`03:40` 他们每接收一个接收窗口就向前滑动一个位置
`03:45` 并给发送方发送针对所接收分组的确认分组
`03:50` 0~4号确认分组经过互联网的传输
`03:54` 正确到达了发送方
`03:57` 发送方每接收一个发送窗口就向前滑动一个位置
`04:02` 这样就有新的序号落入了发送窗口
`04:06` 发送方可以将收到确认的数据分组从缓存中删除了
`04:11` 而接收方可以择机将已接收的数据分组交付上层处理
`04:18` 接下来我们来看累积确认的概念
`04:22` 使用回退n帧协议的接收方可以采用累积确认的方式
`04:27` 也就是说接收方不一定要对收到的数据分组逐个发送确认
`04:33` 而是可以在收到几个数据分组后
`04:36` 对按序到达的最后一个数据分组发送确认
`04:41` a c k n表示序号为n及以前的所有数据分组都已正确接收了
`04:48` 我们来举例说明
`04:49` 累计确认
`04:51` 发送方将序号落在发送窗口内的0~4号数据分组依次连续发送出去
`04:59` 他们经过互联网的传输
`05:01` 正确到达了接收方
`05:03` 接收方按序接收
`05:05` 他们当接收完0号和1号数据分组后
`05:10` 给发送方发送了一个累计确认a c k1
`05:14` 当接收完2~4号数据分组后
`05:18` 又给发送方发送了一个累计确认ak 4
`05:23` 假设ak一在传输过程中丢失了
`05:26` 而ak 4正确到达了发送方
`05:31` 发送方接收a c k4 后
`05:33` 就知道了序号为四及之前的数据分组已被接收方正确接收了
`05:39` 于是将发送窗口向前滑动五个位置
`05:44` 这样就有新的序号落入了发送窗口
`05:48` 发送方可以将收到确认的数据分组从缓存中删除了
`05:53` 而接收方可以择期将已接收的数据分组交付上层处理
`05:59` 从本地可以看出
`06:01` 使用累计确认的其中一个优点
`06:04` 就是即使确认分组丢失
`06:07` 发送方也可能不必重传
`06:10` 例如本例中ak一丢失了
`06:13` 但并没有造成1号数据分组的超时重传
`06:18` 使用累计确认还有其他好处
`06:21` 例如可以减少接收方的开销
`06:24` 减少对网络资源的占用等
`06:28` 当然了
`06:29` 使用累计确认也有缺点
`06:32` 那就是不能向发送方及时反映出
`06:35` 接收方已经正确接收的数据分组的信息
`06:39` 接下来我们来看出现差错的情况
`06:44` 发送方将序号落在发送窗口内的这五个数据分组依次连续发送出去
`06:51` 他们经过互联网的传输到达了接收方
`06:55` 假设他们在传输过程中受到了干扰
`06:59` 其中5号数据分组出现了误码
`07:02` 接收方通过数据分组中的检错码发现了错误
`07:07` 于是丢弃该数据分组
`07:09` 而后续到达的这四个数据分组的序号与接收窗口中的序号不匹配
`07:16` 接收方同样也不能接受他们将它们丢弃
`07:20` 并对之前按序接收的最后一个数据分组进行确认
`07:25` 也就是发送ak 4
`07:28` 每丢弃一个数据分组
`07:31` 就发送一个ak 4
`07:34` 这四个ak 4经过互联网的传输到达了接收方
`07:39` 发送方之前就接收过ak 4
`07:43` 当收到这些重复的ak 40
`07:46` 就知道了之前所发送的数据分组出现了差错
`07:51` 于是可以不等超时计时器超时就立刻开始重传
`07:57` 至于收到几个重复确认
`07:59` 就立刻重传
`08:01` 由具体实现来决定
`08:04` 在本例中
`08:06` 假设收到这四个重复的确认
`08:08` 并不会触发发送方立刻重传一段时间后
`08:14` 超时计时器出现超时
`08:17` 发送方将发送窗口内已发送过的这些数据分组全部重传
`08:23` 在本例中
`08:25` 尽管序号为6701的数据分组之前已经正确地到达接收方
`08:32` 但由于5号数据分组密码不被接受
`08:36` 他们也受到牵连而不被接受
`08:40` 发送方还要重传这些数据分组
`08:43` 这就是所谓的go back n
`08:46` 也就是回退n帧
`08:48` 可见当通信线路质量不好时
`08:52` 回退n帧
`08:53` 协议的信道利用率并不比停止等待协议高
`08:57` 接下来我们来看看如果发送窗口的尺寸wt超过其取值范围的上限
`09:04` 会出现什么情况
`09:06` 对于本例采用三个比特给分组编序号
`09:11` wt的最大值为二的三次减一
`09:14` 也就是七
`09:15` 我们故意超过该上限
`09:18` 将wt取值为八
`09:21` 发送方将序号落在发送窗口内的零至7号
`09:25` 这八个数据分组依次连续发送出去
`09:29` 他们经过互联网的传输
`09:31` 正确到达了接收方
`09:34` 接收方按序正确接收他们号给发送方
`09:38` 发回累计确认ak 7
`09:41` 假设ak 7在传输过程中丢失了
`09:45` 这将导致发送方的超时重传
`09:49` 重传的零至7号数据分组到达接收方
`09:54` 现在问题来了
`09:56` 接收方根据当前接收窗口内的序号
`10:00` 会对这八个数据分组按序接收
`10:04` 但是接收方之前已经接收过这八个数据分组了
`10:09` 现在是在重复介绍
`10:12` 也就是说
`10:13` 接收方无法分辨新旧分组
`10:16` 进而会产生分组重复这种传输差错
`10:20` 因此发送窗口的尺寸不能超过其上限
`10:25` 接下来我们对回退n帧协议的工作原理进行一下小结
`10:33` 发送窗口尺寸wt的取值范围是大于一
`10:37` 小于等于二的n次减一
`10:40` 其中n是构成分组序号的比特数量
`10:45` 若wt等于一
`10:47` 则变回了停止等待协议
`10:50` 若wt大于二的n次减一
`10:53` 则会造成接收方无法分辨新旧数据分组的问题
`10:59` 接收方的接收窗口尺寸w r只能等于一
`11:04` 因此接收方只能按序接收数据分组
`11:09` 发送方可在未收到接收方确认分组的情况下
`11:13` 将序号落在发送窗口内的多个数据分组全部发送出去
`11:19` 接收方只接受序号落在接收窗口内且无误码的数据分组
`11:25` 并且将接收窗口向前滑动一个位置
`11:29` 与此同时给发送方发回相应的确认分组
`11:34` 为了减少开销
`11:36` 接收方不一定每收到一个按序到达且五码的数据分组
`11:41` 就给发送方发回一个确认分组
`11:50` 才针对最后一个数据分组发送确认分组
`11:54` 这要成为累计确认
`11:57` 或者可以在自己有数据分组要发送时
`12:00` 才对之前按序接收且无误码的数据分组进行捎带确认
`12:07` 接收方收到未按序到达的数据分组
`12:10` 除丢弃外
`12:11` 还要对最近按序接收的数据分组进行确认
`12:16` 发送方只有收到对已发送数据分组的确认时
`12:20` 发送窗口才能向前相应滑动
`12:24` 发送方收到多个重复确认时
`12:27` 可在重传计时器超时前尽早开始重传
`12:31` 由具体实现决定
`12:34` 发送方发送窗口内某个已发送的数据分组产生超时重发时
`12:40` 其后续在发送窗口内
`12:42` 且已发送的数据分组也必须全部重传
`12:47` 这就是回归恩真协议名称的由来
`12:56` 这是计算机专业考研全国统考计算机网络部分2009年的题
`13:03` 35
`13:05` 请大家暂停播放视频
`13:07` 思考一下
`13:09` 给出你的答案
`13:12` 答案是选项c大家都选对了吗
`13:17` 我们一起来分析一下题目所给发送方只收到023号帧的确认
`13:26` 这就表明接收方正确接收了0~3号针
`13:30` 并针对他们中的每一个发送了确认针
`13:35` 只不过针对1号帧的确认帧丢失了
`13:39` 这是题目中的陷阱
`13:41` 但又没有相应的选项
`13:43` 所以迷惑性并不是很大
`13:47` 截止到计时器超时
`13:49` 发送方只收到了针对023号帧的确认
`13:54` 而发送方之前已经发送了0~7号针
`13:59` 因此应该从4号针开始重传
`14:02` 即重传之前已经发送过的4567号针
`14:07` 共计重传四个针
`14:11` 我们再来画个示意图
`14:13` 以便更容易理解该题
`14:16` 假设这是真可用的序号
`14:19` 这是发送窗口
`14:21` 发送方将序号落在发送窗口内的0~7号数据帧依次发送出去
`14:29` 当收到针对0号数据帧的确认帧
`14:32` ak 0时
`14:34` 发送窗口向前移动一个位置
`14:38` 若收到针对1号数据帧的确认帧
`14:40` a c k一时发送窗口也会向前移动一个位置
`14:46` 只不过ak一在传输过程中丢失了
`14:51` 当收到针对2号数据帧的确认帧
`14:53` ak 20发送窗口向前移动两个位置
`14:58` 将序号一和二全部移出发送窗口
`15:03` 当收到针对3号数据帧的确认帧
`15:06` a c k30 发送窗口向前移动一个位置之后
`15:12` 发送方出现了超时
`15:14` 将发送窗口内已发送但未收到确认的4567号数据帧依次重传
`15:24` 本节课到这里就结束了
`15:27` 我们将本节课的内容小结如下
`15:32` 回退n帧协议在流水线传输的基础上
`15:36` 利用发送窗口来限制发送方连续发送数据分组的数量
`15:41` 是一种连续a arq协议
`15:44` 在协议的工作过程中
`15:46` 发送窗口和接收窗口不断向前滑动
`15:50` 因此这类协议又称为滑动窗口协议
`15:56` 由于回退n帧协议的特性
`15:58` 当通信线路质量不好时
`16:01` 其信道利用率并不比停止等待协议高
`16:06` 下节课我们将介绍选择重传协议
`16:10` 它是对回退n帧协议的改进
`16:13` 同学们
`16:14` 我们下节课再见