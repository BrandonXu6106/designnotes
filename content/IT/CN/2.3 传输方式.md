---
title: 2.3 传输方式
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=13
bvid: BV18g411D7Fm
cid: "817182109"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817182109&page=13&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍几种传输方式
`00:14` 他们是串行传输和并行传输
`00:18` 同步传输和异步传输
`00:21` 单工半双工以及全双工通信
`00:26` 我们首先来看串行传输和并行传输
`00:30` 串行传输是指数据是一个比特
`00:34` 一个比特依次发送的
`00:36` 因此在发送端和接收端之间只需要一条数据传输线路即可
`00:43` 并行传输是指一次发送n个比特
`00:47` 而不是一个比特
`00:49` 为此在发送端和接收端之间需要有n条传输线路
`00:56` 并行传输的优点是速度为串行传输的n倍
`01:00` 但也存在一个严重的缺点及成本高
`01:05` 请同学们思考一下
`01:07` 在计算机网络中
`01:09` 数据在传输线路上的传输采用的是串行传输还是并行传输
`01:17` 答案是串行传说
`01:20` 计算机内部的数据传输常采用并行传输方式
`01:25` 例如cpu与内存之间通过总线进行数据传输
`01:31` 常见的数据总线宽度有八位
`01:34` 16位
`01:35` 32位和64位
`01:39` 再来看同步传输和异步传输
`01:43` 采用同步传输方式
`01:45` 数据块以稳定的比特流的形式传输
`01:49` 字节之间没有间隔
`01:52` 接收端在每个比特信号的中间时刻进行检测
`01:56` 以判别接收到的是比特零还是比特一
`02:01` 由于不同设备的时钟频率存在一定差异
`02:05` 不可能做到完全相同
`02:08` 在传输大量数据的过程中
`02:10` 所产生的判别时刻的累积误差
`02:13` 会导致接收端对比特信号的判别错位
`02:18` 因此需要采取方法是收发双方的时装保持同步
`02:25` 实现收发双方时钟同步的方法主要有两种
`02:28` 一种是外同步
`02:31` 也就是在收发双方之间添加一条单独的时钟信号线
`02:36` 发送端在发送数据信号的同时
`02:40` 另外发送一路时钟同步信号
`02:43` 接收端按照时钟同步信号的节奏来接收数据
`02:49` 另一种是内同步
`02:51` 也就是发送端将时钟同步信号编码到发送数据中一起传输
`02:57` 例如传统以太网所采用的就是曼彻斯特编码
`03:03` 这部分内容我们将在下节课详细讨论
`03:07` 采用异步传输方
`03:09` 式时以字节为独立的传输单位
`03:13` 字节之间的时间间隔不是固定的
`03:16` 接收端仅在每个字节的起始处对字节内的比特实现同步
`03:22` 为此通常要在每个字节前后分别加上起始位和结束位
`03:30` 这里异步是指字节之间一步
`03:33` 也就是字节之间的时间间隔不固定
`03:37` 但字节中的每个比特仍然要同步
`03:40` 也就是个比特的持续时间是相同的
`03:45` 再来看单工半双工和全双工
`03:50` 单工通信又称为单向通信
`03:53` 通信双方只有一个数据传输方向
`03:57` 例如无线电广播采用的就是这种通信方式
`04:03` 半双工又称为双向交替通信通信
`04:07` 双
`04:08` 方可以相互传输数据
`04:10` 但不能同时进行
`04:12` 例如对讲机采用的就是这种通信方式
`04:17` 全双工通信又称为双向同时通信
`04:22` 通信双方可以同时发送和接收信息
`04:27` 例如电话采用的就是这种通信方式
`04:32` 单向通信只需要一条清道
`04:35` 而双向交替通信或双向同时通信则都需要两条信道
`04:41` 每个方向各一条
`04:44` 本节课到这里就结束了
`04:47` 在本节课中
`04:48` 我们介绍了串行传输和并行传说
`04:53` 同步传说和异步传说
`04:57` 单工半双工以及全双工通信
`05:02` 本节课的内容比较容易理解
`05:05` 重点是弄清楚同步传输中的同步
`05:09` 异步传输中的异步
`05:10` 它们各自的含义是什么
`05:13` 同学们
`05:14` 我们下节课再见