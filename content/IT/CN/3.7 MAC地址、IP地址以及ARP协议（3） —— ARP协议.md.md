---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=55"
bvid: "BV18g411D7Fm"
cid: "817193583"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817193583&page=55&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家来到我的计算机网络微课堂
`00:10` 在之前的课程中
`00:11` 我们分别介绍了属于数据链路层范畴的mac地址
`00:16` 和属于网络层范畴的ip地址
`00:19` 以及数据包在传输过程中
`00:22` ip地址和mac地址的变化情况之后
`00:26` 我们提出了这样一个问题
`00:29` 那就是如何通过ip地址找到其相应的mac地址
`00:35` 这就是本节课我们将要介绍的地址解析协议arp所要实现的主要功能
`00:42` 下面我们就来举例说明arp协议的工作原理
`00:48` 这是一个共享总线型的以太网
`00:51` 为了简单起见
`00:53` 我们只画出了该网络中的三台主机
`00:56` 各主机所配置的ip地址和其网卡上固化的mac地址
`01:02` 如图所示
`01:04` 假设主机b要给主机c发送数据包
`01:08` 主机b知道主机c的ip地址
`01:11` 但不知道他的mac地址
`01:14` 因此主机b的数据链路层在封装mark真时就无法填写目的market址字段
`01:22` 进而也就无法构建出要发送的mark真
`01:26` 实际上每台主机都会有一个arp高速缓存表
`01:31` 例如这是主机b的arp高速缓存表
`01:36` arp高速缓存表中记录有ip地址和mac地址的对应关系
`01:42` 例如这是主机b之前获取到的主机a的ip地址与mac地址的对应关系
`01:50` 在本例中
`01:52` 当主机b要给主机c发送数据包时
`01:56` 会首先在自己的a arp高速缓存表中查找主题c的ip地址
`02:01` 所对应的mac地址
`02:03` 但未找到
`02:05` 因此主机b需要发送arp请求报文来获取主机c的mac地址
`02:13` arp请求报文的内容是
`02:15` 我的ip地址为192.168点点二
`02:20` 我的mac地址为0010f9 a34377
`02:27` 我想知道ip地址为192.168点点三的主机的mac地址
`02:34` 需要说明的是
`02:35` 为了简单起见
`02:37` 这里我们以比较通俗的语言来描述a arp请求报文的内容
`02:42` 但实际上lp请求报文有其具体的格式
`02:48` 另外需要大家注意的是
`02:51` arp请求报文被封装在mac帧中
`02:54` 发送目的地址为广播地址
`02:58` 主机b发送封装有arp请求报文的广播
`03:02` 真总线上的其他主机都能收到该广播帧
`03:08` 主机a的网卡收到该广播帧后
`03:11` 将其送交上层处理
`03:13` 上层的arp进程解析a arp请求报文
`03:18` 发现所询问的ip地址不是自己的ip地址
`03:22` 因此不予理会
`03:25` 主机c的网卡收到该广播帧后
`03:27` 将其送交上层处理
`03:30` 上层的a r p进程解析a r p请求报文
`03:34` 发现所询问的ip地址正是自己的ip地址
`03:38` 需要进行响应
`03:41` 主机c首先将a arp请求报文中所携带的主机b的ip地址与mac地址
`03:48` 记录到自己的arp高速缓存表中
`03:52` 然后给主机b发送lp响应报文
`03:56` 已告知自己的mac地址
`03:59` aarp响应报文的内容是
`04:02` 我的ip地址是192.168点点三
`04:07` 我的mac地址为000c c f b84 a82
`04:13` 需要注意的是
`04:15` a arp响应报文被封装在mark真中
`04:18` 发送目的地址为主机b的mac地址
`04:23` 主机c给主机b发送封装有a arp响应报文的单播真
`04:29` 总线上的其他主机都能收到该单波真
`04:33` 主角a的网卡收到该单波真后
`04:36` 发现其目的mac地址与自己的mac地址不匹配
`04:41` 直接丢弃该帧主机b的网卡
`04:45` 收到该单播帧号
`04:47` 发现其目的mac地址就是自己的mac地址
`04:50` 将其交付上层处理
`04:53` 上层的arp进程解析a arp响应报文
`04:58` 将其所包含的主机c的ip地址与mac地址
`05:02` 记录到自己的a arp高速缓存表中
`05:05` 如图所示
`05:13` 主机b现在可以给主机c发送之前想发送的数据包了
`05:19` arp高速缓存表中的每一条记录都有其类型
`05:24` 类型分为动态和静态两种
`05:27` 动态类型是指记录是主机自动获取到的
`05:32` 其生命周期默认为两分钟
`05:36` 当生命周期结束时
`05:38` 该记录将自动删除
`05:41` 这样做的原因是ip地址与mac地址的对应关系并不是永久性的
`05:48` 例如当主机的网卡坏了
`05:51` 更换新的网卡后
`05:53` 主机的ip地址并没有改变
`05:55` 但主机的mac地址改变了
`05:59` 静态类型是指记录是用户或网络维护人员手工配置的
`06:05` 不同操作系统下的生命周期不同
`06:08` 例如系统重启后不存在
`06:12` 或在系统重启后依然有效
`06:16` 接下来请大家思考一下
`06:19` 在下图所示的网络拓扑中
`06:21` 主机h一是否可以使用arp协议获取到主机h2 的mac地址
`06:29` 回答是否定的
`06:31` a arp协议只能在一段链路或一个网络上使用
`06:36` 而不能跨网络使用
`06:39` 对于本地arp协议的使用是逐段链路进行的
`06:46` 本节课到这里就结束了
`06:48` 我们将之前课程中曾介绍过的mac地址
`06:52` ip地址以及本节课介绍的地址解析协议
`06:57` arp的内容小结如下
`07:02` 需要说明的是
`07:03` 除a arp请求和响应报文外
`07:06` a r p还有其他类型的报文
`07:09` 例如用于检查ip地址冲突的无故a r p或称免费a r p
`07:16` 另外aarp协议没有安全验证机制
`07:20` 存在ap欺骗或攻击等问题
`07:24` 同学们
`07:25` 我们下节课再见