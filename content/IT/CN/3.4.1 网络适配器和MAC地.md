---
title: 3.4.1 网络适配器和MAC地
url: https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=27
bvid: BV18g411D7Fm
cid: "817186023"
author: 五味666
upload_date: 2022-08-28
created: 2026-06-08
tags:
  - status/seed
  - ori/bili
  - IT/CN
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817186023&page=27&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:10` 本节课我们介绍网络适配器和mac地址的相关知识
`00:15` 首先介绍网络适配器的相关知识
`00:19` 要将计算机连接到以太网
`00:22` 需要使用相应的网络适配器
`00:25` 网络适配器一般简称为网卡
`00:28` 如图所示
`00:30` 不管是使用共享总线的共享式以太网
`00:34` 还是使用集线器的共享式以太网
`00:37` 要将计算机接入以太网
`00:40` 都需要使用网卡
`00:42` 如图所示
`00:43` 这是一款千兆以太网卡
`00:47` 这是核心芯片
`00:49` 例如real tec公司的8169sc
`00:53` 该芯片实现了以太网的数据链路层和物理层
`00:59` 这是1p room
`01:01` 例如93c46 芯片
`01:04` 它是用来存储mac地址和网卡相关信息的
`01:08` 电可擦可编程只读存储器
`01:12` 这是bot room插槽
`01:14` 可以安装用于网络无盘工作站启动的bt芯片
`01:21` 这是网络隔离变压器
`01:23` 它将核心芯片与连接外部局域网的rj 45网络接口进行隔离
`01:30` 以便提高抗干扰能力
`01:33` 并对核心芯片进行防雷击保护
`01:38` 这是rj 45网络接口
`01:40` 用于连接双绞线电缆
`01:44` 这是pci接口
`01:46` 它是网卡与计算机主板的接口
`01:50` 如图所示
`01:52` 在计算机内部
`01:53` 网卡与cpu之间的通信
`01:56` 一般是通过计算机主板上的i o总线以并行传输方式进行
`02:03` 网卡与外部以太网之间的通信
`02:06` 一般是通过传输媒体
`02:09` 例如同轴电缆
`02:11` 双绞线
`02:12` 电缆光纤以串行方式进行的
`02:16` 网卡除了要实现物理层和数据链路层功能
`02:21` 其另外一个重要功能就是要进行并行传输和串行传输的转换
`02:28` 由于网络的传输速率和计算机内部总线上的传输速率并不相同
`02:41` 在确保网卡硬件正确的情况下
`02:44` 为了使网卡正常工作
`02:46` 还必须要在计算机的操作系统中为网卡安装相应的设备驱动程序
`02:53` 驱动程序负责驱动网卡发送和接收真
`02:58` 接下来我们介绍mac地址的相关知识
`03:02` 对于点对点信道
`03:04` 由于只有两个站点分别连接在信道的两端
`03:09` 因此其数据链路层不需要使用地址
`03:13` 然而对于连接有多个站点的广播信道
`03:17` 情况就不一样了
`03:19` 我们来举例说明总线上的某台主机要给另一台主机发送帧
`03:27` 由于广播信道天然的广播特性
`03:30` 表示真的信号会通过总线传播到总线上的其他所有主机
`03:37` 那么这些主机中的网卡如何判断收到的真是否是发送给自己的呢
`03:44` 很显然是用广播信道的数据链路层
`03:48` 必须使用地址来区分各主机
`03:52` 当多个主机连接在同一个广播信道上
`03:56` 要想实现两个主机之间的通信
`03:59` 则每个主机都必须有一个唯一的标识
`04:03` 记一个数据链路层地址
`04:07` 假设总线上各主机的数据链路层地址
`04:10` 分别用一个不同的大写字母来表示
`04:14` 在每个主机发送的真的首部中
`04:17` 都携带有发送主机和接收主机的地址
`04:22` 由于这类地址是用于媒体接入控制的
`04:26` 其英文缩写词为m a c
`04:29` 因此被称为mark地址
`04:32` 假设要主机b给主机c发送真
`04:36` 则在真守簿中的目的地址字段应填入主机c的mark地址c
`04:42` 而在原地址字段应填入主机b的mark地址b
`04:47` 这样总先生的其他各主机中的网卡收到该称号
`04:52` 就可以根据真守不中的目的地址字段的值是否与自己的mac地址匹配
`04:58` 决定丢弃或接受改正
`05:02` mac地址一般被固化在网卡的电可擦
`05:06` 可编程只读存储器e e p rom中
`05:10` 因此mac地址也被称为硬件地址
`05:15` mark地址有时也被称为物理地址
`05:18` 如图所示
`05:20` 在windows系统的命令行
`05:22` 通过ip config命令和参数二可以查看tcp ip配置信息
`05:29` 其中mark地址被称作物理地址
`05:33` 请同学们注意
`05:35` 不要被物理地址中的物理二字误导
`05:38` 误认为物理地址属于网络体系结构中物理层的范畴
`05:44` 实际上物理地址属于数据链路层范畴
`05:49` 2018年就曾出现过与该知识点相关的408考研真题
`05:55` 请同学们暂停播放视频思考一下
`05:59` 给出你的答案
`06:05` 本题的答案是选项c
`06:07` 大家都做对了吗
`06:10` 我们来一起分析一下
`06:13` 如果同学们还记得第二章中有关物理层接口特性的相关知识
`06:18` 则用排除法可知答案是选项c
`06:22` 因为选项a中的接口形状属于物理层接口的机械特性
`06:28` 选项b中的引脚功能属于物理层接口的功能特性
`06:34` 选项d中的信号电瓶属于物理层接口的电气特性
`06:40` 但如果同学们记得物理地址是数据链路层使用的地址
`06:45` 又称为硬件地址或mac地址
`06:48` 这个知识点则本题可以秒杀
`06:52` 然而如果同学们对上述两个知识点都概念模糊
`06:57` 那么很可能最先排除掉的也是选项c
`07:02` 因为对于本题物理地址中的物理二字最容易误导大家
`07:09` 一般情况下
`07:14` 一块是用于接入有线局域网的以太网卡
`07:19` 另一块是用于接入无线局域网的wifi网卡
`07:24` 每块网卡都有一个全球唯一的mac地址
`07:29` 交换机和路由器往往具有更多的网络接口
`07:33` 所以会拥有更多的mac地址
`07:37` 综上所述
`07:38` 严格来说
`07:40` mac地址是对网络上各接口的唯一标识
`07:44` 而不是对网络上各设备的唯一标识
`07:48` 接下来我们介绍h分e802 局域网的marke地址格式
`07:53` 如图所示
`07:56` hp 1802标准为局域网规定了一种由48比特构成的marc地址
`08:03` 每八个比特为一个字节
`08:06` 从左至右依次为第一字节到第六字节
`08:11` 前三个字节是组织唯一标识符
`08:14` o u i生产网络设备的厂商
`08:18` 需要向i h e的注册管理机构申请一个或多个o u i
`08:24` 后三个字节是获得o u i的厂商可自行随意分配的网络接口标识符
`08:32` 只要保证生产出的网络设备没有重复地址
`08:36` 即可将每四个比特写成一个16进制的字符
`08:41` 共12个字符
`08:44` 将每两个字符分为一组
`08:46` 共六组
`08:48` 各组之间用短线连接就可以得到mac地址的标准表示方法
`08:55` windows系统采用的就是这种表示方法
`08:59` 例如这是某个采用标准表示方法的mac地址
`09:05` 除标准表示方法外
`09:07` 还可将短线改为冒号linux系统
`09:11` 苹果系统和安卓系统采用的就是这种表示方法
`09:17` 例如这个马克地址
`09:20` 另外还可将每个字符分为一组
`09:23` 共三组
`09:25` 各组之间用点连接
`09:27` packet tracer仿真软件采用的就是这种表示方法
`09:32` 例如这个mac地址需要说明的是
`09:36` market中的组织唯一标识符o u i也称为公司标识符
`09:42` 我们可在h e的官方网站查询公司标识符的分配情况
`09:49` 可以看到这个oi已被分配给了华为科技有限公司
`09:55` 这个oi也被分配给了华为科技有限公司
`10:00` 而这个o u i已被分配给了小米通信科技有限公司
`10:06` 如果我们知道设备的mac地址
`10:09` 而不知道该设备的厂商信息
`10:12` 可以通过设备的mac地址来查询
`10:16` 有很多网站都免费提供这样的查询服务
`10:21` 例如在下图所示的网站输入设备的mac地址后
`10:25` 就可以查出设备的厂商信息
`10:30` i trl e规定marc地址的第一字节的b0 位为i机位是单独的意思
`10:38` 即是组的意思
`10:41` 到g位为零时
`10:43` 表示mark地址是单播地址
`10:47` 当i g位为一时
`10:49` 表示mark地址是组播地址
`10:52` 现在称为多播地址
`10:56` i top 1
`10:57` 还考虑到不愿向i roe的注册管理机构购买组织唯一标识符
`11:02` o u i的情况
`11:04` 为此而rop一规定
`11:06` mark地址的第一次结的第一位为g l a g是全球的意思
`11:14` l是本地的意思
`11:16` 当gl位为零时
`11:18` mark地址是全球管理的
`11:21` 这样可确保在全球没有相同的mac地址
`11:27` 网络设备厂商
`11:28` 像h o p e购买的o u i都属于全球管理
`11:33` 当glv为一时
`11:35` market地址是本地管理的
`11:38` 用户可任意分配mark地址
`11:42` 由于ig v和glv的取值共有四种组合
`11:47` 因此可将48比特的mac地址划分为全球单波
`11:53` 全球多播
`11:54` 本地单播以及本地多播四个类型
`11:59` 如下表所示
`12:02` 第一次结的第一位取零表明market地址是全球管理的第一字节的
`12:10` 第一位取一
`12:11` 表明market地址是本地管理的第一字节的
`12:16` b0 y取零表明mac地址是单播地址
`12:21` 第一字节的b0 y取一表明mark地址是多播地址
`12:27` 全球单播地址是由厂商生产网络设备时固化在设备中的
`12:34` 全球多播地址是交换机和路由器等标准网络设备所支持的多播地址
`12:41` 用于特定功能
`12:43` 例如交换机的生成树协议
`12:46` s t p使用的多播地址为0180c2000000
`12:53` 这是一个全球多播地址
`12:57` 本地单播地址由网络管理员分配
`13:01` 优先级高于网络接口的全球单播地址
`13:05` 换句话说
`13:06` 可以覆盖网络接口的全球单播地址
`13:11` 本地多播地址可由用户对网卡编程实践一表明其属于哪些多播组
`13:19` 若将本地多播地址
`13:21` 除第一字节的b0 和b12 位外的剩余46比特置为全一时
`13:28` 就是广播地址
`13:30` 其标准表示形式为f f f f f f f f f f f f
`13:38` 也就是16进制全f形式
`13:43` 48比特的mac地址共有二的48次方个
`13:48` 大约为280多万亿个全球单播地址
`13:53` 全球多播地址
`13:54` 本地单播地址以及本
`13:57` 地多播地址各占其1/4
`14:00` 约70万亿个
`14:02` 有兴趣的同学可以统计一下自己拥有多少个mac地址
`14:08` 并且思考一下48比特的全球唯一的单播mac地址会耗尽吗
`14:18` 对于台式计算机至少会有一个以太网接口
`14:23` 对于笔记本电脑
`14:25` 平板电脑
`14:38` 对于上述接口
`14:40` 每个都需要分配一个全球单播地址来唯一标识带接口
`14:47` 对于使用48比特mark地址的应用程序
`14:51` i trible e的目标寿命为100年
`14:54` 也就是一直到2080年
`14:57` 但鼓励采用64比特作为替代
`15:02` 接下来我们介绍波1802局域网的mark地址发送顺序
`15:09` 字节发送顺序为第一字节到第六字节字节内的比特发送顺序
`15:15` 为b0 位到b7 位
`15:18` 如图所示
`15:24` 接下来我们举例说明单播mark地址的作用
`15:28` 如图所示
`15:34` 各主机网卡上固化的全球单播mac地址
`15:38` 如图所示
`15:41` 假设主机b要给主机c发送一个单波真
`15:46` 主机b首先要构建该单波真在真首部中的目的地址字段
`15:52` 填入主机c的mac地址
`15:55` 原地址字段填入自己的mac地址
`15:59` 主机b将该单播真发送出去
`16:08` 发现该单波真的目的mac地址与自己的mac地址不匹配
`16:13` 于是丢弃白针
`16:16` 需要说明的是
`16:17` 这里使用不匹配这个词
`16:20` 而没有使用不相同这个词的原因是考虑到目的地址是广播地址的情况
`16:28` 主机c的网卡发现该单伯真的目的market地址与自己的market地址匹配
`16:34` 于是接受该针
`16:36` 并将该针的数据载荷交付上层处理
`16:41` 请同学们注意接收与接受这两个词的含义
`16:47` 接收是指网卡从网络中收到某个真
`16:52` 而接受是指网卡接收某个帧后
`16:56` 还要判断这个针是否符合要求
`16:59` 例如目的mac地址是否与网卡的mac地址匹配
`17:04` 真州是否有雾霾等
`17:06` 决定接受还是丢弃这个真
`17:11` 再来看广播mac地址的作用
`17:14` 假设主机b要发送一个广播帧
`17:18` 主机b首先要构建该广播帧在真首部中的目的地址字段
`17:25` 填入广播marc地址
`17:27` 也就是48比特为全16进制
`17:31` 形式为全f的mac地址
`17:34` 原地址字段填入自己的mac地址
`17:38` 主机b将该广播帧发送出去
`17:41` 主机a和c都会收到该广播帧
`17:45` 主机a和b各自的网卡
`17:48` 发现该针的目的mac地址是广播mac地址
`17:52` 于是接受该广播帧
`17:54` 并将该针的数据载荷交付上层处理
`18:00` 多波马克地址举例如图所示
`18:04` 四个交换机连接形成了一个环路
`18:08` 这样做的目的是为了提高网络的可靠性
`18:13` 环路会造成广播真在环路中永久兜圈的严重问题
`18:19` 为了解决该问题
`18:21` 交换机之间会交互一种特殊的针
`18:24` 即bpd
`18:26` 也就是网桥协议数据单元
`18:30` 各交换机根据bp d u中的一些参数
`18:33` 按照生成树协议所使用的生成树算法
`18:37` 最终某些交换机会阻塞自己的某些接口
`18:42` 这样就破除了环落
`18:45` 而dp d有手部中的目的地址
`18:48` 就是生成树协议专用的多波market地址
`18:52` 0180c2000000
`18:57` 有关生成树协议的相关知识
`18:59` 我们将在后续课程中介绍
`19:03` 另外除了这种专用的全球多播market之外
`19:13` 这类地址由ip多播地址映射而来
`19:17` 我们将在后续课程中介绍
`19:25` 就检查真守不中的目的market地址按以下情况处理
`19:32` 如果目的mac地址是广播地址
`19:35` 则接受该真
`19:39` 如果目的mac地址与网卡上固化的全球单播mac地址相同
`19:44` 则接受该真
`19:47` 如果穆里mark地址是网卡支持的多播地址
`19:51` 则接受该真四
`19:54` 除上述情况外
`19:56` 丢弃该针网卡还可被设置为一种特殊的工作方式
`20:02` 即混杂方式工作
`20:05` 在混杂方式的网卡
`20:07` 只要收到共享媒体上传来的真就会收下
`20:12` 而不管真的目的mac地址是什么
`20:15` 对于网络维护和管理人员
`20:18` 这种方式可以监视和分析局域网上的流量
`20:23` 以便找出提高网络性能的具体措施
`20:28` 嗅探器就是一种工作
`20:30` 在混杂方式的网卡
`20:32` 再配合相应的工具软件
`20:44` 就像一把双刃剑
`20:46` 黑客常利用这种方式非法获取网络用户的口令
`20:52` 请同学们注意
`20:53` 全球单播marc地址就如同身份证上的身份证号码具有唯一性
`21:01` 它往往与用户个人信息绑定在一起
`21:05` 因此用户应尽量确保自己拥有的全球单播market地址不被泄露
`21:13` 为了避免用户设备连接wifi热点时
`21:16` mac地址泄露的安全问题
`21:19` 目前大多数移动设备都已经采用了随机mac地址技术
`21:26` 本节课到这里就结束了
`21:29` 请同学们快速回忆一下本节课的主要内容
`22:00` 同学们
`22:01` 我们下节课再见