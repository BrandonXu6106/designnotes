---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=56"
bvid: "BV18g411D7Fm"
cid: "817194013"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817194013&page=56&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍ip数据报的发送和转发过程
`00:15` ip数据报的发送和转发过程包含以下两部分
`00:20` 一部分是主机发送ip数据报
`00:23` 另一部分是路由器转发ip数据报
`00:27` 需要说明的是
`00:29` 为了将重点放在tcp ip协议站的网际层发送和转发ip数据报的过程上
`00:36` 在本节课的举例中
`00:38` 我们将忽略
`00:39` 使用arp协议来获取目的主机或路由器接口的mac地址的过程
`00:45` 以及以太网交换机自学习和转发真的过程
`00:51` 下面我们就来举例说明ip数据报的发送和转发过程
`00:57` 在下图所示的小型互联网中
`01:00` 路由器的接口零直连了一个交换式以太网
`01:04` 接口一也直连了一个交换式以太网
`01:08` 我们给该网络分配了这样的网络地址和子网掩码
`01:13` 给网络中的各主机和路由器的接口配置了相应的ip地址和子网掩码
`01:20` 如图所示
`01:27` 我们给另一个网络分配了这样的网络地址和子网掩码
`01:32` 给网络中的各主机和路由器的接口配置了相应的ip地址和子网掩码
`01:38` 如图所示
`01:45` 我们知道同一个网络中的主机之间可以直接通信
`01:50` 这属于直接交付
`01:52` 不同网络中的主机之间的通信需要通过路由器来中转
`01:57` 这属于间接交付
`02:00` 那么原主机如何判断出目的主机是否与自己在同一个网络中呢
`02:07` 我们来举例说明
`02:09` 假设主机c要给主机f发送ip数据报
`02:14` 主机c将自己的ip地址和子网掩码相与
`02:18` 就可以得到主机c所在网络的网络地址
`02:23` 既然主机c要给主机f发送ip数据报
`02:27` 那主机c肯定知道主机f的ip地址
`02:31` 否则就没法发送了
`02:33` 主机c将主机f的ip地址与自己的子网掩码
`02:38` 相与就可得到目的网络地址
`02:41` 该地址与主机c的网络地址不相等
`02:44` 因此主机c就知道了主机f与自己不在同一个网络
`02:50` 它们之间的通信属于间接交付
`02:54` 主机c需要将ip数据报传输给路由器
`02:58` 由路由器将ip数据报转发给主机f
`03:03` 那么主机c又是如何知道应该把ip数据报交给哪个路由器进行转发
`03:11` 实际上
`03:12` 用户为了让本网络中的主机能和其他网络中的主机进行通信
`03:18` 就必须给其指定本网络中的一个路由器
`03:22` 由该路由器帮忙进行转发
`03:25` 所指定的路由器也被称为默认网关
`03:30` 对于本列我们
`03:31` 可以将路由器接口零的ip地址指定给该接口所直连网络中的各个主机
`03:38` 作为默认网关
`03:42` 同理可将路由器接口一的ip地址
`03:46` 指定给该接口所直连网络中的各个主机作为默认网关
`03:54` 这样当本网络中的主机要和其他网络中的主机进行通信时
`04:00` 会将ip数据报传输给默认网关
`04:03` 由默认网关帮主机将ip数据报转发出去
`04:08` 假设本例中的主机a要给主基地发送ip数据报
`04:14` 这属于间接交付
`04:16` 主机a会将该ip数据报传输给自己的默认网关
`04:21` 也就是图中所示的路由器
`04:25` 当路由器收到ip数据报后
`04:28` 又是如何转发的
`04:31` 路由器首先会检查ip数据报的首部是否出错
`04:36` 若出错则丢弃大ip数据报
`04:39` 并通告原主机
`04:42` 若没有出错
`04:43` 则进行转发
`04:46` 路由器
`04:46` 根据ip数据报首部中的目的地址
`04:49` 在自己的路由表中查找匹配的路由条目
`04:54` 若找到匹配的路由条目
`04:56` 则转发给路由条目中指示的吓一跳
`05:00` 若找不到则丢弃该ip数据报
`05:03` 并通告原主机
`05:06` 为了简单起见
`05:08` 我们假设本例中的ip数据报首部没有出现差错
`05:13` 路由器取出ip数据报
`05:15` 首部各地址字段的值
`05:18` 原地址字段的值为主机a的ip地址
`05:21` 目的地址字段值为主机d的ip地址
`05:26` 接下来路由器就要对该ip数据报进行查表转发了
`05:32` 这是路由器的路由表
`05:34` 当我们给路由器的接口配置ip地址和子网掩码时
`05:39` 路
`05:39` 由器就知道了自己的该接口与哪个网络是直连的
`05:45` 例如在本例中
`05:47` 接口零所直连的网络是192.168点点零
`05:52` 相应的地址掩码为255.255.255.128
`05:58` 不需要下一跳路由器
`06:00` 因为接口零与该网络是直连的
`06:04` 接口一所直连的网络是192.168点点128
`06:10` 相应的地址掩码为255.255.25.128
`06:16` 不需要下一跳路由器
`06:18` 因为接口一与该网络是直连的
`06:23` 需要说明的是
`06:24` 路由表中可能还会有其他路由条目
`06:28` 这可以是用户或网络管理员手工配置的静态
`06:32` 路由也可以是路由器使用路由协议自动获取到的动态路由
`06:39` 我们将在后续课程中详细介绍这部分内容
`06:43` 本节课就不再赘述了
`06:47` 接下来路由器根据ip数据报的目的地址
`06:51` 在自己的路由表中查找匹配的路由条目
`06:55` 逐条检查路由条目
`06:58` 将目的地址与路由条目中的地址掩码相与
`07:02` 得到目的网络地址
`07:05` 该目的网络地址与路由条目中的目的网络地址不相同
`07:10` 则这条路由条目不匹配
`07:12` 再检查下一条路由条目
`07:16` 将目的地址与下一条路由条目中的地址掩码相与
`07:20` 得到目的网络地址
`07:23` 该目的网络地址与
`07:25` 路由条目中的目的网络地址相同
`07:28` 则这条路由条目就是匹配的路由条目
`07:32` 按照他的下一跳指示
`07:34` 也就是从接口一转发该ip数据报
`07:39` 这样主基地就可以收到路由器转发来的该ip数据报
`07:45` 需要说明的是
`07:47` 我们这里所介绍的路由器查表转发ip数据报的过程
`07:52` 只是为了让同学们理解其最基本的工作原理
`07:57` 在路由器的实际研发过程中
`07:59` 需要设计很好的数据结构
`08:02` 以便提高查找速度
`08:05` 再来看这种情况
`08:08` 假设主机a给本网络上的各设备发送了一个广播ip数据报
`08:15` 在数据报首部中的目的地址字段可
`08:19` 以填写的目的地址为192.168点点127
`08:24` 这是本网络的广播地址
`08:27` 也可以填写255.25
`08:29` 5.25
`08:30` 5.255
`08:31` 这是受限的广播地址
`08:34` 该网络中的各设备都会收到该广播ip数据报
`08:39` 但是路由器收到后并不会转发该数据报
`08:44` 也就是说路由器是隔离广播域的
`08:49` 这是很有必要的
`08:51` 试想一下
`08:52` 如果英特网中数量巨大的路由器收到广播ip数据报后都进行转发
`08:59` 则会造成巨大的广播风暴
`09:02` 严重浪费英特网资源
`09:05` 同理主机a给另一个网络发送广播ip数据报
`09:11` 在数据报首部中的目的地
`09:13` 址字段填写的目的地址为192.168点点255
`09:19` 这是网络192.168点点128的广播地址
`09:25` 主机a将该广播ip数据报传输给路由器
`09:29` 希望由路由器当其转发
`09:32` 但路由器判断出这是广播ip数据报
`09:36` 不会转发
`09:38` 接下来我们来做几个相关的练习题
`09:43` 这是计算机专业考研全国统考计算机网络部分2010年的题
`09:50` 38
`09:51` 请大家暂停播放视频
`09:54` 思考一下
`09:55` 给出你的答案
`09:58` 答案是选项d
`10:00` 大家都选对了吗
`10:02` 我们来一起分析一下
`10:06` 中继器和集线器工作在物理层既不隔离冲突域
`10:11` 也不隔离广播域
`10:14` 网桥和交换机工作
`10:16` 在数据链路层可以隔离冲突域
`10:20` 不能隔离广播域
`10:22` 路由器工作在网络层
`10:25` 即隔离冲突域
`10:26` 也隔离广播域
`10:29` 因此本题的答案是选项d
`10:32` 再来看2012年的题
`10:35` 37
`10:36` 请大家暂停播放视频
`10:38` 思考一下
`10:40` 给出你的答案
`10:43` 答案是选项c
`10:45` 大家都选对了吗
`10:46` 我们来一起分析一下ip路由器工作在tcp ip体系结构的网际层
`10:54` tcp ip体系结构的网际层并不负责可靠传输
`10:59` 也就是不能确保传输的ip分组不丢失
`11:05` ip路由器对收到的ip分组头进行插错校验
`11:10` 当发现错误时
`11:12` 会丢弃该ip分组
`11:14` 并向原主机发送s a m p差错报告报文
`11:19` 综上所述
`11:21` 题目中的描述三是错误的
`11:24` 利用排除法
`11:25` 可知选项c正确
`11:28` 题目中的其他描述项都是正确的
`11:32` 再来看2015年的题
`11:34` 47的第三问
`11:37` 请大家暂停播放视频
`11:39` 思考一下
`11:41` 给出你的答案
`11:45` 我们来一起分析一下
`11:48` 我们将题目所给的主机一的配置信息标注在它的旁边
`11:53` 从子网掩码和主机一的ip地址的sider表示方法都可以看出
`11:59` 网络前缀为24个比特
`12:02` 而3w服务器的ip地址的网络前缀也是24个比特
`12:08` 并且与主机一的网络前缀相同
`12:12` 因此主机一可以访问3w服务器
`12:16` 因为从ip地址配置以及物理连接方面看
`12:20` 主机一和3w服务器在同一网络中
`12:25` 但是主机一不能访问internet
`12:29` 因为它的默认网关错误的配置成了dh cp服务器的ip地址
`12:35` dh cp服务器不具备路由器的功能
`12:38` 无法将
`12:39` ip分组转发到internet
`12:43` 如图所示
`12:44` 主机一的默认网关错误的指定为了dh cp服务器的ip地址
`12:50` 正确的配置应该指定为路由器
`12:53` 该接口的ip地址
`12:56` 本节课到这里就结束了
`12:59` 我们将本节课的内容小结如下
`13:05` 同学们
`13:06` 我们下节课再见