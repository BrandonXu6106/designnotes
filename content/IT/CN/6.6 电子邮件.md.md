---
title: "湖南科技大学 - 计算机网络"
url: "https://www.bilibili.com/video/BV18g411D7Fm?spm_id_from=333.788.videopod.episodes&vd_source=b7647c28b9310a1aceb6a792c0f7fe07&p=95"
bvid: "BV18g411D7Fm"
cid: "817207646"
author: "五味666"
upload_date: "2022-08-28"
created: "2026-06-08"
tags: ["status/seed", "ori/bili", "IT/CN"]
enableToc: true
excalidraw-open-md: true
---

<iframe src="https://player.bilibili.com/player.html?aid=514946641&bvid=BV18g411D7Fm&cid=817207646&page=95&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

应出版社的要求，此视频再次发布一遍~
对应书籍动态发过：https://t.bilibili.com/698207939796664353

本书为B站口碑爆棚的“计算机网络微课堂”升级版，由B站知名up主@湖科大教书匠  衔打造，深入浅出地讲解计算机网络知识，注重分析各种技术背后的原理和方法，注重内容的正确性、准确性和新颖性。

本书配套100+个讲解视频，扫码随时观看；精美、高清的思维导图，帮你厘清知识架构，配套习题，有讲解有答案，及时巩固与练习；专业术语中英对照，提供图、表两种形式，540+幅插图用心绘制，动态过程跃然纸上，原理、概念细致拆解，真正做到“深入浅出”。

内容全面涵盖计算机考研知识点，非常适合高等院校电子信息工程、通信工程、物联网工程、信息工程、电气工程自动化、自动化、计算机科学与技术等相关专业的同学们学习参考。

## 字幕

`00:05` 同学们好
`00:06` 欢迎大家学习计算机网络这门课程
`00:10` 本节课我们介绍电子邮件
`00:14` 电子邮件是英特网上最早流行的一种应用
`00:18` 并且仍然是当今英特网上最重要最实用的应用之一
`00:25` 传统的电话通信属于实时通信
`00:28` 存在以下两个缺点
`00:31` 第一电话通信的主教和被教双方必须同时在场
`00:37` 第二一些不是十分紧迫的变化
`00:41` 也常常不必要的打断人们的工作和休息
`00:45` 而电子邮件与邮政系统的寄信相似
`00:50` 发件人将邮件发送到自己使用的邮件服务器
`00:56` 发件人的邮件服务器将收到的邮件按其目的地址转发到收件人
`01:02` 邮件服务器中的收件人邮箱
`01:05` 收件人在方便的时候访问收件人邮件服务器
`01:10` 装自己的邮箱获取收到的电子邮件
`01:15` 电子邮件使用方便
`01:17` 传递迅速
`01:18` 而且费用低廉
`01:20` 它不仅可以传送文字信息
`01:23` 而且还可以附上声音和图像
`01:27` 由于电子邮件的广泛使用
`01:30` 现在许多国家已经正式取消了电报业务
`01:34` 在我国电信局的电报业务也因电子邮件的普及而濒临消失
`01:42` 电子邮件系统采用客户服务器方式
`01:46` 其三个主要组成构件是用户代理
`01:50` 邮件服务器以及电子邮件所需的协议
`01:55` 我们来举例说明
`01:58` 假设这是邮件的发送方
`02:01` 这是邮件的接收方
`02:04` 在发送方的计算机中需要使用用户代理来发送邮件
`02:10` 在接收方的计算机中同样也需要使用用户代理来接收邮件
`02:16` 用户代理是用户与电子邮件系统的接口
`02:21` 又称为电子邮件客户端软件
`02:25` 这是发送方使用的邮件服务器
`02:28` 例如qq邮件服务器
`02:31` 这是接收方使用的邮件服务器
`02:34` 例如谷歌邮件服务器
`02:37` 邮件服务器是电子邮件系统的基础设施
`02:42` 英特网上所有的服务提供商都有邮件服务器
`02:47` 其功能是发送和接收邮件
`02:51` 同时还要负责维护用户的邮箱
`02:55` 如图所示
`02:56` 我们可以简单地认为邮件服务器装有很多邮箱
`03:02` 还有用来缓存带转发邮件的缓存
`03:07` 发送方使用用户代理通过邮件发送协议
`03:12` 例如sm tp将邮件发送给发送方邮件服务器
`03:18` 发送方邮件服务器同样通过邮件发送协议将该邮件发送给接收方
`03:25` 邮件服务器
`03:27` 接收方在方便的时候需要用户代理通过邮件读取协议
`03:33` 例如pop 3
`03:35` 从接收方邮件服务器读取邮件
`03:39` 也就是说电子邮件所签的协议包括邮件发送协议和邮件读取协议两类
`03:48` 接下来我们更进一步说明上述的邮件发送和接收过程
`03:55` 发送方的用户代理
`03:57` 作为sm tp客户与发送方邮件服务器中的sm tp服务器进行tcp连接
`04:06` 然后基于这条连接使用sm tp协议来发送邮件给发送方邮件服务器
`04:15` 发送方邮件服务器中的sm tp客户
`04:18` 与接收方邮件服务器中的sm tp服务器进行tcp连接
`04:24` 然后基于这条连接
`04:26` 使用s mtp协议来发送已收到的待转发邮件给接收方邮件服务器
`04:35` 接收方的用户代理
`04:36` 作为top 3客户与接收方邮件服务器中的pop 3服务器进行tcp连接
`04:44` 然后基于这条连接
`04:46` 使用pop 3协议从接收方邮件服务器读取邮件
`04:52` 可以看到这是邮件发送协议的使用范围
`04:57` 包含发送方用户代理到发送方邮件服务器
`05:02` 以及发送方邮件服务器到接收方邮件服务器这两部分
`05:08` 这是邮件读取协议的使用范围
`05:12` 只有接收方用户代理到接收方邮件服务器这一部分
`05:18` 接下来我们介绍属于邮件发送协议的简单邮件传送协议
`05:24` sm tp的基本工作原理
`05:27` 我们以发送方邮件服务器使用s mtp协议给接收方邮件服务器
`05:33` 发送带转发的邮件为例
`05:37` 发送方邮件服务器周期性的扫描邮件缓存
`05:41` 如果发现有待转发的邮件
`05:44` 则
`05:45` 发送方邮件服务器中的sm tp客户
`05:48` 会与接收方邮件服务器中的sm tp服务器进行tcp连接
`05:54` 端口号为25之后
`05:58` sm tp客户就可以基于这条tcp连接给sm tp服务器发送s mtp命令
`06:06` 共14条
`06:09` s m tp服务器也会给s m tp客户发送相应的应答
`06:14` 共21种
`06:17` sm tp客户与服务器之间通过命令与应答的交互方式
`06:23` 最终实现sm tp客户发送邮件给sm tp服务器
`06:29` 接下来我们就要简单介绍一下该过程
`06:34` 当tcp连接建立成功后
`06:37` s m tp服务器会主动推送服务
`06:41` 就去应答给sm tp客户
`06:44` 应答代码二零
`06:46` 后面可能更有描述信息
`06:49` 例如这是网易邮件服务器给出的描述信息
`06:55` s m tp客户收到该应答后
`06:58` 向服务器表明身份
`07:00` 告知自己s mtp服务器的域名
`07:03` 具体命令为hello
`07:06` 其号为命令参数
`07:09` s m tp服务器若认为身份有效
`07:12` 则发回应答代码250
`07:16` 否则发回其他代码
`07:18` 例如421表示服务不可用
`07:23` s m tp客户收到该应答后
`07:25` 使用命令mail from来告诉服务器邮件来自何方
`07:32` sm tp服务器若认为合理
`07:35` 则发回应答代码250
`07:38` 否则发回其他错误代码
`07:42` s m tp客户收到该应答后
`07:45` 使用命令r c p t two来告诉服务器邮件去往何方
`07:50` 也就是收件人邮箱
`07:53` s m tp服务器中如果有该收件人邮箱
`07:57` 则发回应答代码250
`08:01` 否则发回其他错误代码
`08:04` s m tp客户收到该应答后
`08:07` 使用data命令来告诉服务器自己准备发送邮件内容了
`08:15` sm tp服务器如果准备好接收
`08:18` 发回应答代码354
`08:21` 否则发回其他错误代码
`08:25` sm tp客户收到该应答后
`08:29` 就向服务器发送邮件内容
`08:33` s m tp客户发送完邮件内容后
`08:37` 还要发送结束服
`08:41` s m tp服务器若收件成功
`08:44` 则发回应答代码250
`08:48` 否则发回其他错误代码
`08:52` s m tp客户收到该应答后
`08:55` 使用命令quit向服务器请求断开连接
`09:01` s m tp服务器发回应答代码
`09:04` 二一表示接受请求并主动断开连接
`09:09` 需要说明的是
`09:11` 为了简单起见
`09:13` 我们省略了可能需要的认证过程
`09:16` 还省略了应答代码
`09:18` 后面一般都跟随的简单描述信息
`09:22` 不同的sm tp服务器给出的相同应答代码
`09:27` 其后面跟随的描述信息可能不同
`09:31` 接下来我们介绍电子邮件的信息格式
`09:36` 电子邮件的信息格式并不是由sm tp协议定义的
`09:41` 而是在rfc 82文档中单独定义
`09:45` 该文档已在2008年更新为rfc 5322
`09:51` 一个电子邮件有信封和内容两部分
`09:55` 而内容又有手部和主体两部分构成
`10:00` 例如这是电子邮件的信封
`10:04` 这是电子邮件的内容
`10:07` 它有手部和主体两部分构成
`10:13` 手部和主体的信息都需要由用户来填写
`10:17` 手部中包含有一些关键字
`10:20` 后面加上冒号
`10:23` 例如关键字from后面填入发件人的电子邮件地址
`10:30` 一般由邮件系统自动填入关键字to
`10:36` 后面填入一个或多个收件人的电子邮件
`10:41` 关键字cc后面填入一个或多个收件人以外的抄送人的电子邮件地址
`10:50` 抄送人收到邮件号可看可不看
`10:54` 邮件可回可不回
`10:56` 邮件关键字subject后面填入邮件的主题
`11:02` 它反映了邮件的主要内容
`11:06` 很显然最重要的关键字是to和subject
`11:11` 它们往往是必填选项
`11:15` 用户写好手部后
`11:17` 邮件系统将自动地将信封所需的信息提取出来
`11:22` 并写在信封上
`11:24` 所以用户不需要填写电子邮件
`11:27` 信封上的信息
`11:29` 在填写完首部各关键字的内容后
`11:33` 用户还需要撰写邮件的主体部分
`11:37` 这才是用户想传递给收件人的核心信息
`11:49` sm tp协议只能传送阿斯克码文本数据
`11:54` 不能传送可执行文件或其他的二进制对象
`11:58` 也就是说
`12:00` sm tp不能传送带有图片
`12:03` 音频或视频数据的多媒体邮件
`12:07` 并且许多其他非英语国家的文字
`12:11` 例如中文
`12:12` 俄文
`12:13` 甚至带有重音符号的法文或德文
`12:17` 也无法用sm tp传送
`12:21` 为解决s m tp传送非阿斯克码文本的问题
`12:25` 提出了多用途英特网邮件扩展one
`12:30` 如图所示
`12:31` s m tp协议只能传送阿斯克码文本数据
`12:36` 这是电子邮件的收发双方
`12:39` 假设发送方发送的电子邮件中包含有非阿斯克码数据
`12:45` 则不能直接使用sm tp进行传送
`12:49` 需要通过mam进行转换
`12:52` 将非阿斯克码数据转换为阿斯克码数据
`12:56` 然后就可以使用sm tp进行传送了
`13:01` 接收方也要使用慢对接收到的阿斯克码数据进行逆转换
`13:07` 这样就可以得到包含有非阿斯克码数据的电子邮件
`13:13` 为了实现这种转换
`13:15` man增加了五个新的邮件首部字段
`13:19` 这些字段提供了有关邮件主体的信息
`13:23` 定义了许多邮件内容的格式
`13:26` 对多媒体电子邮件的表示方法进行了标准化
`13:31` 定义了传送编码
`13:33` 可对任何内容格式进行转换
`13:36` 而不会被邮件系统改变
`13:40` 实际上mm不仅仅用于sm tp
`13:44` 也用于后来的同样面向阿斯码字符的超文本传送协议
`13:50` http
`13:51` 到目前为止
`13:53` 我们已经介绍完了涉及邮件发送的相关内容
`14:00` 接下来我们介绍涉及邮件读取的相关内容
`14:06` 常用的邮件读取协议有以下两个
`14:10` 一个是邮局协议pop top 3是其第三个版本是英特网正式标准
`14:19` 另一个是英特网邮件访问协议i m a p mp 4是其第四个版本
`14:27` 目前还只是英特网建议标准
`14:30` top 3是非常简单
`14:33` 功能有限的邮件读取协议
`14:36` 用户只能以下载并删除方式或下载并保留方式
`14:42` 从邮件服务器下载邮件到用户计算机
`14:47` 不允许用户在邮件服务器上管理自己的邮件
`14:52` mp 4是功能比top 3强大的邮件读取协议
`14:58` 用户在自己的计算机上就可以操控邮件服务器中的邮箱
`15:04` 就像在本地操控一样
`15:06` 因此map是一个联机协议
`15:11` top 3和mp 4都采用基于tcp连接的客户服务器方式
`15:19` top 3需要熟知端口110
`15:22` mp 4使用熟知端口143
`15:26` 有关top 3协议和mp 4协议的具体内容
`15:31` 有兴趣的同学可自行查阅相关资料
`15:35` 我们就不再深入介绍了
`15:38` 实际上本节课涵盖了电子邮件系统涉及到的各方面基本内容
`15:45` 对于了解电子邮件系统的基本工作原理来说已经足够了
`15:51` 如果同学们将来要进行电子邮件系统相关程序设计
`15:56` 例如编写一个电子邮件客户端软件
`16:00` 则还需要对相关协议的fc文档进行深入阅读
`16:06` 现在越来越多的用户使用基于万维网的电子邮件
`16:12` 通过浏览器登录邮件服务器
`16:15` 万维网网站就可以撰写收发阅读和管理电子邮件
`16:22` 这种工作方式与map很类似
`16:26` 不同的是用户计算机无需安装专门的用户代理程序
`16:31` 也就是电子邮件客户端软件
`16:34` 只需要使用通用的万维网浏览器即可
`16:39` 邮件服务器网站通常都提供非常强大和方便的邮件管理功能
`16:45` 用户可以在邮件服务器网站上管理和处理自己的邮件
`16:51` 而不需要将邮件下载到本地进行管理
`16:56` 我们来举例说明基于万维网的电子邮件应用
`17:01` 假设用户a和b都使用网易邮件服务器
`17:07` 这是用户a的电子邮件地址
`17:10` 这是用户b的电子邮件地址
`17:14` 用户a要给用户b发送邮件
`17:18` 用户a使用浏览器登录邮件服务器网站撰写并发送邮件给用户b
`17:27` 用户b也使用浏览器登录邮件服务器网站读取收到的邮件
`17:34` 用户a和b在发送和接收邮件时
`17:38` 与服务器之间使用的都是http协议
`17:43` 而不需要使用我们之前介绍过的sm tp和pop 3协议
`17:50` http协议是超文本传送协议
`17:53` 我们将在后续课程中介绍
`17:57` 再来看另一种情况
`17:59` 假设用户a需要网易邮件服务器
`18:03` 这是它的电子邮件地址
`18:05` 用户c使用谷歌邮件服务器
`18:08` 这是它的电子邮件地址
`18:11` 用户a要给用户c发送邮件
`18:15` 用户a使用浏览器登录自己的邮件服务器网站撰写并发送邮件给用户
`18:22` c使用的是http协议
`18:27` 用户a的邮件服务器使用sm tp将邮件发送给用户c的邮件服务器
`18:35` 用户c也使用浏览器登录自己的邮件服务器网站读取收到的邮件
`18:42` 使用的也是http协议
`18:46` 最后我们来做几个相关的练习题
`18:50` 这是计算机专业考研全国统考计算机网络部分
`18:56` 2012年的题40
`18:59` 请大家暂停播放视频
`19:02` 思考一下
`19:03` 给出你的答案
`19:09` 答案是选项d
`19:11` 大家都选对了吗
`19:13` 我们来一起分析一下
`19:18` 题目所给的四个选项中只出现了两种协议
`19:23` 一种是用于邮件发送的简单邮件传送协议sm tp
`19:29` 另一种是用于邮件读取的邮局协议
`19:32` top 3阶段
`19:35` 一是用户一的用户代理程序向用户一的邮件服务器发送邮件
`19:41` 应该使用sm tp协议阶段
`19:45` 二是用户一的邮件服务器给用户二的邮件服务器发送邮件
`19:51` 也应该使用s mtp协议阶段
`19:56` 三是用户二的用户代理程序从用户二的邮件服务器读取邮件
`20:02` 应该需要pop 3协议
`20:06` 再来看2013年的题
`20:08` 40
`20:10` 请大家暂停播放视频
`20:12` 思考一下
`20:14` 给出你的答案
`20:18` 答案是选项a
`20:20` 大家都选对了吗
`20:22` 我们来一起分析一下
`20:25` 如图所示
`20:26` 这是我们之前介绍过的s m tp协议
`20:31` 只支持传送阿斯玛文本数据
`20:34` 如果电子邮件中包含有非阿斯克码数据
`20:39` 需要经过mam转换变成阿斯克码数据后
`20:43` 才能使用s m tp协议进行传输
`20:47` 因此叙述一正确
`20:50` 再来看我们之前介绍过的这幅图
`20:54` 邮件服务器之间发送邮件采用的是sm tp协议
`21:00` 因此叙述二正确
`21:04` 用户代理给邮件服务器发送邮件
`21:08` 也使用s m tp协议
`21:11` 因此叙述三也正确
`21:15` 用户代理从邮件服务器读取邮件时使用的是pop 3协议
`21:22` 还可以使用功能更强的mvp协议
`21:26` 因此叙述四错误
`21:30` 再来看2018年的题
`21:32` 40
`21:33` 请大家暂停播放视频
`21:36` 思考一下
`21:37` 给出你的答案
`21:41` 答案是选项d
`21:44` 相信大家都能选对
`21:46` 我们就不再赘述了
`21:49` 本节课到这里就结束了
`21:52` 我们将本节课的内容小结如下
`21:58` 同学们
`21:59` 我们下节课再见